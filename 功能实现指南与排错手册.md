# MapleBot 功能实现指南与排错手册

> 本文档记录MapleBot各核心功能的实现原理、操作流程、注意事项、常见问题及解决方案。下次功能损坏时，参考本文档可快速修复或重新实现。

---

## 一、热键跑马灯功能

### 1.1 功能说明
在小地图下方、平台按钮上方，有一条从右到左滚动的绿色文字跑马灯，显示各热键功能说明。

### 1.2 实现原理
- **位置**：`_hk_x=UI_MAP_X(29)`, `_hk_y=412`, `_hk_w=UI_MAP_W(403)`, `_hk_h=36`
- **滚动逻辑**：每帧 `_hotkey_scroll_x -= 4`，文字完全移出左边界后重置到右边缘
- **无缝循环**：文字尾部进入红框后，在右边补一份相同文字
- **字体**：simhei.ttf，24号，深绿色(0,128,0)

### 1.3 关键代码位置
- 初始化：`__init__` 中预加载字体和文字尺寸（约789-805行）
- 绘制：`_render_minimap` 方法中（约3501-3528行）

### 1.4 性能优化（重要！）
**问题**：每帧都做 `textbbox` 计算、颜色空间转换、PIL图像创建，导致卡顿。
**解决方案**：
1. 初始化时预计算文字宽度高度，不用每帧 `textbbox`
2. 预加载字体，不用每帧加载
3. 滚动速度从每帧8像素降到4像素，更平滑
4. 文字内容预定义为成员变量，不用每帧重复定义

### 1.5 常见问题
| 问题 | 原因 | 解决方案 |
|------|------|----------|
| 跑马灯卡顿 | 每帧textbbox计算+颜色转换开销大 | 初始化时预计算文字尺寸，降低滚动速度 |
| 文字不显示 | 字体加载失败 | 检查simhei.ttf是否存在，失败时用默认字体 |
| 滚动不连贯 | 无缝循环逻辑有问题 | 检查 `_dx + _tw < _hk_w` 条件，确保补一份文字 |

---

## 二、游戏蒙板三点校准功能（F11自动校准）

### 2.1 功能说明
按F11启动五步自动校准流程，在游戏蒙板上显示三个基点（红/绿/蓝），用于计算小地图到游戏屏幕的缩放倍率。

### 2.2 五步流程
| 步骤 | 按键次数 | stage变化 | 功能 |
|------|----------|-----------|------|
| 第1步 | 第1次按F11 | 0→1 | 显示基点（红绿蓝三点），蒙板出绿蓝圆，可拖动到特色背景位置 |
| 第2步 | 第2次按F11 | 1→2 | 截图绿蓝圆位置的背景图保存为模板，开始模板匹配跟踪，蒙板画绿/蓝空心圆 |
| 第3步 | 第3次按F11 | 2→3 | 人物走到绿色圆上，记录绿点小地图坐标，绿圈消失 |
| 第4步 | 第4次按F11 | 3→4 | 人物走到蓝色圆上，记录蓝点小地图坐标，蓝圈消失 |
| 第5步 | 第5次按F11 | 4→0 | 计算倍率（X/Y分开），关闭所有显示，清空点位，保存倍率 |

### 2.3 三点含义
- **红点（基点）**：人物当前位置，与游戏自带黄色光点重合
- **绿点**：X方向参考点，用于计算scale_x（小地图X距离/屏幕X距离）
- **蓝点**：Y方向参考点，用于计算scale_y（小地图Y距离/屏幕Y距离）

### 2.4 关键代码位置
- 校准入口：`_start_auto_calibration` 方法（约2207行）
- 模板截图：`_capture_calib_template` 方法（约2454行）
- 模板匹配：`_match_calib_templates` 方法（约2493行）
- 倍率计算：`_finish_auto_calibration` 方法（约2410行）
- 蒙板绘制：WM_PAINT消息处理（约4340行开始）

### 2.5 配置参数
```python
_calib_template_size = 54      # 模板截图大小（54x54像素，half=27，27*2=54）
_calib_match_threshold = 0.7   # 模板匹配置信度阈值
```

### 2.6 走过的弯路与解决方案（重要！）

#### 弯路1：模板尺寸55导致截图一直失败
**现象**：第二次按F11进不了第二步，一直卡在第一步。
**根因**：`_calib_template_size = 55`，但 `half = 55 // 2 = 27`，截取区域 `x2-x1 = (sx+27)-(sx-27) = 54`，实际模板是54x54，而代码检查要求55x55，所以截图一直失败。
**解决方案**：把 `_calib_template_size` 从55改成54。
**排查方法**：添加调试日志输出模板尺寸，发现返回True但尺寸54x54≠55x55。

#### 弯路2：绿点蓝点不能同时匹配到（有绿就没有蓝）
**现象**：模板匹配时，绿点和蓝点不能同时匹配到，有绿就没有蓝。
**根因**：每帧同时匹配绿点和蓝点，两个模板匹配互相干扰。
**解决方案**：改成奇偶帧交替匹配，奇数帧只匹配绿点，偶数帧只匹配蓝点，一次只匹配一个。
**附加优化**：匹配阈值从0.75降到0.7，兼顾准确率和召回率。

#### 弯路3：第三步第四步记录后圈不消失
**现象**：第三步记录绿点后，绿圈应该消失但没有消失；第四步记录蓝点后，蓝圈应该消失但没有消失。
**根因**：蒙板绘制逻辑是 `auto_stage >= 2` 时都显示绿圈蓝圈，没有根据stage判断。
**解决方案**：改成 `auto_stage == 2 or auto_stage == 3` 时显示，且stage=3时只显示蓝圈（绿圈已记录消失），stage=4时都不显示。

#### 弯路4：切换方案后倍率不显示
**现象**：校准成功后倍率显示正常，但切换方案后倍率不显示。
**根因**：切换方案时加载了校准数据（calib_left_pt等），但没有重新计算 `_calibrated_scale_x/y`。
**解决方案**：在切换方案加载校准数据后，根据calib_left_pt、calib_right_pt、calib_top_pt重新计算倍率。

#### 弯路5：调试日志不显示
**现象**：添加了很多调试日志但UI上看不到。
**根因**：用了 `_add_log` 方法，这个方法只是把消息append到列表，不渲染到UI。
**解决方案**：改用 `_rlog` 方法，这才是带时间戳/颜色/自动滚动的UI运行日志。

### 2.7 失败重试逻辑
- **第二步（截图保存）**：失败提示"截图保存失败 请重新按 F11"，失败3次后直接关闭整个过程
- **第三步（绿点记录）**：失败提示"绿光圈位置记录失败 请重按 F11"，失败3次后直接关闭
- **第四步（蓝点记录）**：失败提示"蓝光圈位置记录失败 请重按 F11"，失败3次后直接关闭
- **注意**：失败后不回第一步重新调整位置，保持当前stage

### 2.8 注意事项
1. 模板截图时会隐藏蒙板，截图完成后恢复蒙板显示
2. 绿点和蓝点模板截图间隔1秒，确保画面稳定
3. 模板匹配使用ROI局部检测，减少算力消耗
4. 校准成功后倍率保存在 `data/route_XX_calib.json` 文件中
5. 切换方案时会自动加载对应方案的校准数据并重新计算倍率

### 2.9 蒙板窗口构造说明（后期维护补充 · 2026-09-01）

> 本节记录蒙板（Overlay）窗口的完整构造链路，后期维护改蒙板前必须通读本节，禁止只看CreateWindowEx一行就改样式。

#### 2.9.1 窗口创建
```python
# 扩展样式：透明分层 + 永远置顶（不要加WS_EX_TOOLWINDOW！会导致人物框绘制异常）
dwExStyle = WS_EX_LAYERED | WS_EX_TOPMOST
# 窗口样式：弹出式 + 可见（不要改WS_OVERLAPPED）
style = WS_POPUP | WS_VISIBLE
hwnd = CreateWindowExW(dwExStyle, className, "Overlay", style,
                       0, 0, 100, 100, None, None, hinst, None)
```

#### 2.9.2 透明设置
```python
# 用品红(COLOR_MAGENTA)做透明色键，画面中品红区域完全透明
SetLayeredWindowAttributes(hwnd, COLOR_MAGENTA, 0, LWA_COLORKEY)
```

#### 2.9.3 WNDPROC回调（防GC回收）
```python
# 必须用列表保留引用，否则Python GC回收回调对象后窗口崩溃
self._wnd_proc_ref = [WNDPROC(self._overlay_wnd_proc)]
SetWindowLongPtrW(hwnd, GWLP_WNDPROC, self._wnd_proc_ref[0])
```

#### 2.9.4 WM_PAINT绘制
```python
case WM_PAINT:
    hdc = BeginPaint(hwnd, &ps)
    # GDI绘制（人物框/怪物框/血条/校准点）
    # 画笔/画刷创建后必须DeleteObject释放，否则GDI句柄泄漏
    EndPaint(hwnd, &ps)
```

#### 2.9.5 窗口定位（SetWindowPos几何去重）
```python
# 只在游戏窗口位置/大小变化时才调用SetWindowPos
# 不要每帧/每50ms都置顶——频繁调用会导致任务栏Overlay按钮反复跳
last_geom = None
def _update_overlay_pos():
    global last_geom
    geom = (win_x, win_y, win_w, win_h)
    if geom != last_geom:
        SetWindowPos(hwnd, HWND_TOPMOST, *geom, SWP_NOACTIVATE)
        last_geom = geom
```

#### 2.9.6 蒙板线程架构
- 独立线程 `_win32_overlay_loop`，主线程写数据、蒙板线程读（注意数据竞争）
- 消息循环：`PeekMessage` + `time.sleep(0.05)`（约20fps重绘）
- 启停标志：`_monster_overlay_running`
- **异常处理**：线程异常必须打印详细日志，不要静默退出；主循环自动重启要有冷却/次数限制，否则异常风暴导致Overlay反复出现消失

#### 2.9.7 样式禁忌（踩过的坑）
| 禁忌 | 后果 |
|------|------|
| 加WS_EX_TOOLWINDOW | 人物框/蒙板绘制异常（v93实测人物框消失） |
| 改WS_POPUP为WS_OVERLAPPED | 窗口边框/标题栏异常 |
| 每帧SetWindowPos置顶 | 任务栏Overlay按钮反复跳 |
| WNDPROC不用列表保留引用 | GC回收后窗口崩溃 |
| WM_PAINT不DeleteObject | GDI句柄泄漏，长时间运行后绘制失败 |

#### 2.9.8 任务栏Overlay不停地跳的根因
- **不是窗口样式问题**：29号版本也是LAYERED|TOPMOST+POPUP，不跳
- **根因1**：蒙板线程异常退出→finally重置running=False→主循环自动重启→反复创建窗口→任务栏按钮消失又出现
- **根因2**：模型缺失时每帧print→控制台窗口频繁刷新→任务栏两窗口交替跳
- **正确修法**：修线程异常+模型打包，不要改窗口样式

---

## 三、小地图显示功能

### 3.1 功能说明
在UI面板上显示游戏小地图的实时截图，并叠加平台、梯子、标点等信息。

### 3.2 三点判定边界法
小地图截图使用三点判定边界法：
1. **Y下端**：扫描蓝色条条（小地图底部蓝色条），定位蓝色条条图片，识别后向上移一定距离作为底部边界
2. **X左端**：按小地图图片判定左边界
3. **X右端**：按大地图图片（"大地图"三个字标志）判定右边界，定好后向下移一定距离开始截取小地图

### 3.3 配置参数
```python
UI_MAP_X = 29      # 小地图UI显示区域X
UI_MAP_Y = 131     # 小地图UI显示区域Y
UI_MAP_W = 403     # 小地图UI显示区域宽度
UI_MAP_H = 279     # 小地图UI显示区域高度
FIXED_W = 340      # 内部小地图渲染宽度
MAP_H = 250        # 内部小地图渲染高度
```

### 3.4 走过的弯路与解决方案

#### 弯路1：小地图底部和右边有灰色边框
**现象**：小地图底部和右边有多余的灰色边框。
**根因**：背景图（data/ui_bg_blank.png）本身带灰色边框。
**解决方案**：替换背景图为用户提供的新背景图（461x900），同时调整截取区域：底部边界改到模板顶部（去6px灰边），右边界左移5px。

#### 弯路2：大地图标志X识别不到
**现象**：小地图右边X识别不到。
**根因**：大地图标志X搜索范围太小（mini_x+200）。
**解决方案**：把大地图标志X搜索范围从mini_x+200加大到mini_x+400。

---

## 四、倍率显示功能

### 4.1 功能说明
在小地图左下角显示校准后的倍率值（X:0.xxxx Y:0.xxxx）。

### 4.2 实现原理
- 位置：小地图左下角 `(4, MAP_H - 10)`
- 字体：OpenCV默认字体，大小1.0，线宽2，黑色
- 显示条件：`_calibrated_scale_x > 0 and _calibrated_scale_y > 0`

### 4.3 注意事项
1. 必须先完成F11五步校准，才会显示真实倍率值
2. 切换方案时会自动加载对应方案的校准数据并重新计算倍率
3. 没有校准数据时不显示任何文字（不要显示"no calib"等调试提示）

---

## 五、打包流程

### 5.1 打包命令
```powershell
cd C:\Users\PC\Doubao\chats\2026-08-15\new-chat-4\maple_bot
Remove-Item dist\MapleBot.exe -Force -ErrorAction SilentlyContinue
pyinstaller --noconfirm MapleBot.spec
```

### 5.2 打包配置（MapleBot.spec）
- 入口：maple_route_ui.py
- datas：含config和data目录
- hiddenimports：含ultralytics、cv2、mss、sklearn
- console=False（无控制台窗口）
- upx=True（启用UPX压缩）

### 5.3 输出
- exe路径：`dist\MapleBot.exe`（约72-73MB）

### 5.4 注意事项
1. 打包前必须关闭正在运行的MapleBot.exe，否则会报"拒绝访问"错误
2. 打包时exit code=1但显示"Build complete!"是正常的，不影响使用
3. 调试阶段建议直接用Python运行（运行调试.bat），不用每次打包
4. 确认功能完全正常后再打包成exe最终交付

---

## 六、调试运行方法

### 6.1 直接运行Python脚本
双击 `运行调试.bat`，会自动以管理员权限启动Python脚本。

### 6.2 注意事项
1. 必须以管理员权限运行，否则模拟输入无法发送到游戏
2. 脚本会自动检测非管理员权限并重启到管理员权限
3. 如果启动没反应，检查Python是否安装、依赖库是否齐全
4. 调试时可以在代码中添加 `_rlog()` 输出日志到UI，方便排查问题

---

## 七、通用排错思路

### 7.1 分层排查
遇到问题时，按以下层级排查：
1. **输入源层**：截图画面是否正确，是否拿到预期画面
2. **识别输出层**：匹配分数、坐标是否符合预期
3. **坐标计算层**：ROI区域、坐标转换是否异常
4. **动作执行层**：点击位置、发送消息是否生效
5. **业务逻辑层**：状态流转、条件判断是否正确

### 7.2 调试日志
- 用 `_rlog()` 方法输出日志到UI运行日志区域
- 不要用 `_add_log()`，这个方法不渲染到UI
- 关键节点都要加日志：入口、状态变化、成功/失败分支

### 7.3 备份规范
1. 每完成一个小功能就备份到GIT
2. 备份信息要详细，说明修改了什么、解决了什么问题
3. 遇到问题时可以从GIT恢复到正常版本
4. 备份前确保代码语法正确、功能正常

---

## 八、代码规范

### 8.1 注释规范
- 每段关键代码都要添加注释，说明设计思想
- 配置参数要说明作用、调大/调小的影响
- 复杂逻辑要分步注释
- 修改代码时要更新注释

### 8.2 配置参数
- 阈值、缓冲偏移、重试次数、超时时间全部提取到脚本顶部配置区
- 禁止业务逻辑内硬编码数值
- 修改参数时要同步更新注释

### 8.3 异常处理
- 截图失败、窗口丢失、目标找不到、坐标越界都要有异常捕获
- 不能直接程序崩溃，要有降级、重试、状态回退逻辑
- 异常时要输出日志，方便排查

---

> 本文档持续更新，每次解决新问题或实现新功能后都要记录到本文档中。
