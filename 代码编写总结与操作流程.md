# 代码编写总结与操作流程

> 本文件记录每一步功能开发的代码编写总结、操作流程、关键信息和采坑记录。
> 每完成一个功能，都要在此文件中添加对应的总结。

---

## 1. 倍率差弹窗功能

### 功能描述
在路线页右上角添加倍率差按钮，点击弹出弹窗，可输入X偏差和Y偏差（支持负数和小数点），确认后保存到配置文件。

### 操作流程
1. 点击右上角"倍率差"按钮，打开弹窗
2. 在X偏差输入框中输入数值（支持负数和小数点）
3. 在Y偏差输入框中输入数值（支持负数和小数点）
4. 点击"确认"按钮保存，或点击"取消"/右上角X/点击弹窗外部关闭（不保存）

### 关键实现
1. **弹窗样式**：灰底白字，标题栏可拖拽移动，右上角X关闭按钮
2. **输入框**：黑底白字，聚焦时橙色边框，支持正负号、小数点、数字
3. **按钮**：确认按钮（绿色）保存，取消按钮（红色）不保存
4. **交互逻辑**：
   - 打开弹窗时备份原始值，取消/关闭时恢复
   - 点击弹窗外部自动关闭（不保存）
   - 5秒无操作自动丢失焦点
   - 确认才保存到配置文件

### 代码位置
- 初始化：`__init__`方法中添加`_show_scale_dialog`等状态变量
- 位置计算：`_update_scale_dialog_positions()`方法
- 绘制：`draw()`方法最后添加弹窗绘制代码
- 点击检测：`_on_mouse()`方法中添加弹窗点击检测
- 输入处理：`_handle_input_key()`方法中添加负数和小数点支持
- 保存：`_save_input_config()`方法中添加`scale_x_offset`和`scale_y_offset`到保存列表

### 采坑记录
1. **输入框不支持负数和小数点**：数字输入是通过`_poll_num_input`方法处理的，而不是`_handle_input_key`方法。最初`_poll_num_input`方法中：
   - 没有轮询小数点按键(0xBE, 0x6E)
   - 负号处理只允许`char_x_offset`和`char_y_offset`，不允许`scale_x_offset`和`scale_y_offset`
   - 没有小数点处理逻辑
   需要修改`_poll_num_input`方法：
   - 在轮询的按键列表中添加小数点(0xBE, 0x6E)
   - 在负号处理逻辑中，把`scale_x_offset`和`scale_y_offset`也加进去
   - 添加小数点处理逻辑（只允许倍率差字段输入小数点）
2. **配置文件不保存倍率差**：最初`_save_input_config`方法中`known_ids`只包含`FIGHT_FIELDS + POTION_FIELDS + ROUTE_FIELDS`，需要手动添加`scale_x_offset`和`scale_y_offset`到`known_ids`中。

---

## 2. 准星拖拽功能（pygame透明置顶窗口）

### 功能描述
窗口绑定时，准星可以拖拽到屏幕任意位置，不再受UI窗口限制。使用pygame创建透明置顶窗口显示准星。

### 操作流程
1. 点击"窗口绑定"按钮，进入准星拖拽模式
2. 按住鼠标左键拖拽准星，可拖到屏幕任意位置（包括UI窗口外面）
3. 释放鼠标左键，绑定鼠标指向的顶层窗口
4. 准星窗口自动销毁，恢复UI窗口上的准星显示

### 关键实现
1. **pygame窗口创建**：`_create_crosshair_window()`方法
   - 使用`pygame.display.set_mode((cs, cs), pygame.NOFRAME)`创建无边框窗口
   - 使用`win32gui.SetWindowPos`设置窗口置顶
   - 使用`win32gui.SetWindowLong`和`SetLayeredWindowAttributes`设置白色透明
2. **窗口位置更新**：`_update_crosshair_window(screen_x, screen_y)`方法
   - 使用`win32gui.SetWindowPos`移动窗口到准星中心位置
   - 调用`_draw_crosshair_on_pygame()`绘制准星
3. **准星绘制**：`_draw_crosshair_on_pygame()`方法
   - 白色背景（会被透明化）
   - 红色圆形准星 + 十字线
4. **窗口销毁**：`_destroy_crosshair_window()`方法
   - 调用`pygame.display.quit()`退出pygame显示
5. **拖拽逻辑**：主循环中`_drag_crosshair`为True时
   - 调用`pygame.event.pump()`处理事件，避免窗口无响应
   - 首次拖拽时调用`_create_crosshair_window()`创建窗口
   - 调用`_update_crosshair_window(cursor.x, cursor.y)`更新窗口位置
   - 拖拽结束时调用`_destroy_crosshair_window()`销毁窗口

### 代码位置
- 导入：文件开头添加`import pygame`、`import win32gui`、`import win32con`、`import win32api`
- 初始化：`__init__`方法中添加`_crosshair_pygame_window`等状态变量
- 创建窗口：`_create_crosshair_window()`方法
- 更新位置：`_update_crosshair_window(screen_x, screen_y)`方法
- 绘制准星：`_draw_crosshair_on_pygame()`方法
- 销毁窗口：`_destroy_crosshair_window()`方法
- 拖拽逻辑：主循环中`_drag_crosshair`判断部分

### 采坑记录
1. **tkinter无法使用**：最初尝试用tkinter创建透明置顶窗口，但是沙箱环境中Tcl没有正确安装，报错`Can't find a usable init.tcl`。
2. **pygame未安装**：尝试用pygame时，发现pygame没有安装，需要先`pip install pygame`。
3. **pywin32未安装**：pygame安装后，发现`win32gui`模块不存在，需要先`pip install pywin32`。
4. **pygame窗口无响应**：创建pygame窗口后，如果不调用`pygame.event.pump()`处理事件，窗口会无响应。需要在主循环中拖拽时调用`pygame.event.pump()`。
5. **pygame.quit()导致问题**：最初尝试用`pygame.quit()`完全退出pygame，但是这样会导致下次创建窗口时需要重新初始化。改用`pygame.display.quit()`只退出显示部分，保留pygame初始化状态。

---

## 3. 待添加...

> 每完成一个新功能，在此处添加对应的总结。
