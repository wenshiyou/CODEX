# 【冒险岛世界】打怪区域安全框 说明书

> 最后更新：2026-09-11　对应源码：`maple_route_ui.py`（相关函数统一搜 `_bound_`）　对应提交：6fbfeed（功能）、7da73d7（登记）
> 本文件是该功能的**唯一详细文档**；`PROJECT_STATE.md` 总纲只保留一行索引，正文都在本文件。

---

## 0. 一句话总览

在**控制面板 UI 小地图**上限定人物活动范围，替代已删除的旧"屏幕边缘 A+B"：

- **X 左右**：两条可拖动的**竖线**，人物黄光点越线就硬压朝内方向键 1000~1500ms 拉回；
- **Y 上下**：**不用手划横线（真机证明防不住）**，改成用鼠标点**录制好的台子绿线**、回车合并成"界线"，靠"人物当前站在哪号台"判定能不能再往上/往下。

---

## 1. 为什么 X、Y 要用两套机制（最重要的设计结论）

- **X 是连续量**：人物持续左右走，适合"竖线 X 坐标 vs 光点 X"实时比较 + 越线拉回。
- **Y 是离散的"层/台"，且平台有坡度**。手划水平直线有三个死穴（真机实锤，见第 7 节踩坑）：
  1. 人在框内发起爬梯、等爬过横线才判，但"已在梯上不拦"（中途拦会把人挂梯上）→ 拦晚了，人已出去；
  2. "离下线固定 18 块就禁下跳"在平行台还行，**坡地只防一边**，且框内有二层时连正常"下到框内下层"都误杀；
  3. "踩线才拦"挡不住**一跳跨线**——主动下跳发起那一刻人还在线内（没踩线、拦不住），一跳完才到线外；预测落点/跳完补救是"补"不是"防"。
- **最终解法：Y 认录制绿线的"台子身份"**。绿线本身贴合坡度；以整台为单位——界台是**允许的落脚点**，跳到界台合法，只有"人已站在界台上、还想继续往界外发起动作"才拦。由此坡台准、框内层间不误伤、也不存在跨线。

---

## 2. 操作步骤（UI 小地图）

按钮「打怪区」在小地图"梯删除"正下方，60×20 与梯删除等宽对齐；**灰=常态守护生效，绿=编辑（显示"调整中"）**。

1. 点「打怪区」变绿进入编辑：
   - **定左右**：直接用鼠标拖动两条竖线；
   - **定上下**：用鼠标点录制好的平台绿线（点中折线距离 ≤ `BOUND_PICK_TOL=8` 块即选中，可连点多个**相连台子**），选完按 **回车** 合并成"一条界线"（同层多段可合成一条、染同色）。
2. 再点一批相连台子、回车成第二条；两条齐后，系统按各自"整体平均 Y"**自动定上下**：Y 小（靠上）= **上限组染红**，Y 大（靠下）= **下限组染蓝**。只成形一条时染**紫**（待第二条配对），编辑暂存中的台子染**黄**。
3. 再点「打怪区」变灰 = 退出编辑并写盘 `data/bound_lines.json`，守护立即生效。
4. 编辑中的小操作：两条界线已满再按回车 = 清空重选、当本次是第一条；点已在暂存区的台 = 取消该台；退出时没按回车成形的暂存台不计入。

> 只设一侧也可以：只设上限就只防向上、只设下限就只防向下，另一侧放行。

---

## 3. X 左右：竖线硬拉回（用户已验收"X 好了"）

### 3.1 绘制与拖拽
- 竖线照平台绿线/梯子蓝线，用 `scale_x/scale_y` 画在控制面板 UI 小地图 `map_display` 上（**不要画到游戏透明蒙板——蒙板上看不到**）。编辑=橙 `(0,165,255)`、常态=青 `(255,255,0)`，线宽 = `BOUND_LINE_W×0.8 = 2`。
- 拖拽终版（三易其稿，见踩坑 3）：cv2 鼠标回调 `_on_mouse` **最顶部无条件**记录 `_ui_mouse_x/_ui_mouse_y/_ui_mouse_flags`（与绘制严格同空间、零 DPI 偏移）；主循环 `_bound_drag_tick` 每帧用这份坐标推进"按下命中 / 按住拖 / 松开清"。
- 块坐标一律用绘制同帧存的**实际贴放矩形** `_map_disp_x/y/w/h` 逆运算（画线的严格逆运算，治旧横线 Y 上偏 30，见踩坑 4）。
- 双重硬边界：光标先钳到 `[0,w-1]/[0,h-1]`，再 `_clamp_bound_lines` 保证 `l<r` 且间隔 ≥10，线跑不出小地图。

### 3.2 检测（独立守护线程，只置令、不发键）
- `_bound_guard_loop` 每 `BOUND_POLL_MS=60ms` 跑一次 `_bound_check_once`。
- 直接读**现成的** `_player_map_pos`（主循环每帧 `find_player_dot` 算好、倍率三点校准也在用它；**不另裁帧、不另搞识别**，见踩坑 6）。
- `cx<=l` 置 `'left'`、`cx>=r` 置 `'right'`；**只置令，绝不直接发物理键**（两个脑子抢键是碎步病根，物理键统一由主线发）。
- 一旦置令中途不翻转，交给主线压满固定时长，压完清令再重判。

### 3.3 执行（主线帧首 `_bound_pull_tick`，三段式，百分百照用户铁令）
1. **阶段 0 停主线 + 清场**：返回 True 占 `_aux_busy`（`_random_step`/`_combat_tick`/`_converge_movement` 全跳过）；松攻击键、松战斗套与巡路套的左右键；先**纯清场** `BOUND_RELEASE_MS=50ms`，这期间不压键（松/压分开，游戏才不会同帧吞键相抵）。
2. **阶段 1 硬压朝内键**：清场结束那一帧才定 `dur=rand(1000,1500)`（`BOUND_PULL_MIN/MAX_MS`），用 `_hold_combat_key` 硬压——越左竖线→`VK_RIGHT`、越右竖线→`VK_LEFT`。
3. **阶段 2 保持**：每帧幂等压住，**不重判、不翻转、不看平台边**（旧 `_combat_at_locked_edge` 边判边松是碎步主因，已删）。
4. **阶段 3 到点放手**：`now>=pull_until` 立马 `_release_combat_move`、清拉回令与 side，返回 False 恢复主线。
- 爬梯/锁冻结（`_climb_state!='none'`、`_is_lock_frozen()`）中不做水平拉回（梯子上水平无意义）。

### 3.4 辅助线优先级
掉台归位 `_fall_return_tick` ＞ 解卡 `_unblock_tick` ＞ 本左右拉回。前两者进行时清掉进行中的拉回并松键，避免两套辅助线抢键。

---

## 4. Y 上下：台子绿线界（2026-09-11 晚最终定稿）

### 4.1 数据结构
- 一条界线 = 多个相连台子的 id 列表；`_bound_grpA/_bound_grpB` 两条。
- `_bound_reorder` 按 `_bound_group_y_avg`（组内各台 `_platform_y_avg` 再取均值）自动定：`_bound_top_grp`（上限，Y 小/靠上）、`_bound_bot_grp`（下限，Y 大/靠下）。

### 4.2 判定（关键：认台号，不认 Y 坐标、不预测落点）
- `_bound_current_pf_id` 复用 `_get_current_manual_platform`：光点距某条绿线 ≤15 块 = 正站在该台；腾空/梯子上判不到 → 返回 None → 放行。
- `_bound_block_up()`：当前站的台属于上限组 → 禁再向上。
- `_bound_block_down()`：当前站的台属于下限组 → 禁再向下。
- 未选该向界线 / 编辑态 / 判不到当前台 → 一律放行（不冤枉）。

### 4.3 主动动作闸门（同步即时判）
只在 `_climb_state=='none'`（尚未进爬梯状态机）时拦"新发起"；已在 `descend/to_ladder/climbing` 进行中不拦（中途打断会把人挂梯子上）。共接入这些入口：
1. **`_move_to` 开头**：`dy<0`（向上）且 `_bound_block_up()` → return False；`dy>0`（向下）且 `_bound_block_down()` → return False。覆盖所有经 `_move_to` 的走梯 / descend 下跳 / 走台跨层。
2. **战斗竖直瞬移**：`_dyv<0` 向上 + block_up、`_dyv>0` 向下 + block_down 时不闪。
3. **战斗"怪在正下方按 ↓+跳落层"**：block_down 时松净 `VK_DOWN`、清 `_below_down_since`，只水平朝怪走或站定，绝不发下跳。
4. **combat cross 入口**走 `_bound_blocked_vertical('up'/'down')`（即上面两函数的方向封装）。
5. 斜下方"沿坡走"是水平移动、靠地形被动下落，不算主动下跳，不在拦截内。

> **跳高打（high_slope）明确不拦**：它在原图层原地起跳、人不换层、不出界，所以不加 Y 闸门。

### 4.4 持久化
- 文件 `data/bound_lines.json` = `{l, r, top_grp:[id...], bot_grp:[id...]}`。
- 加载用 `_valid_pf_group` 逐 id 校验当前平台表仍有效（重录/换方案导致平台数变化后，失效旧 id 自动剔除，防误判到别的台）；兼容更早的单值字段 `top_pf/bot_pf`。

---

## 5. 常量（源码约 427~435 行，改前先想影响面）

| 常量 | 值 | 含义 |
|---|---|---|
| BOUND_POLL_MS | 60 | 守护线程轮询周期 ms（约 16 次/秒） |
| BOUND_PULL_MIN_MS / MAX_MS | 1000 / 1500 | X 越线后硬压朝内键的随机时长 |
| BOUND_RELEASE_MS | 50 | X 拉回前"停主线+松键"的纯清场间隙 |
| BOUND_DRAG_HIT | 12 | 编辑态点中竖线的命中半径（块 px） |
| BOUND_PICK_TOL | 8 | 编辑态点选平台绿线的命中容差（到折线最近距离） |
| BOUND_LINE_W | 3 | 竖线粗细基准，UI 实际画 ×0.8=2px |
| BOUND_DEFAULT_INSET | 6 | 未存盘时竖线距小地图左右边的默认内缩 |
| BOUND_FILE | data/bound_lines.json | 竖线 + Y 界线组持久化文件 |

---

## 6. 关键函数清单（行号会偏移，一律用函数名 Grep）

- 守护/拉回：`_bound_guard_loop`、`_bound_check_once`（只置 X 令）、`_bound_pull_tick`（X 三段式硬拉回）
- 竖线拖拽：`_bound_drag_tick`、`_bound_hit_line`、`_bound_drag_update`、`_get/_clamp/_save_bound_lines`、`_bound_toggle_edit`（按钮灰↔绿）、`_start/_stop_bound_guard`
- Y 选界线：`_bound_pick_at`（点绿线入暂存）、`_bound_commit_group`（回车成界线）、`_bound_reorder`（按 Y 定上下）、`_valid_pf_id/_valid_pf_group`（存盘 id 校验）、`_bound_pf_by_id`、`_bound_group_y_avg`
- Y 闸门：`_bound_block_up`、`_bound_block_down`、`_bound_blocked_vertical`、`_bound_current_pf_id`（人当前站哪台，复用 `_get_current_manual_platform`）
- 字段初始化位置：搜 `_bound_top_grp`

---

## 7. 踩过的坑 → 根因 → 解决（按演进顺序，勿走回头路）

1. **旧屏幕边缘 A+B（已整删）**：边缘硬闸 return True 暂停 `_combat_tick`，而它自己移动又靠被暂停的 combat_tick 发键 →"想拉却不动 + 战斗被暂停"近乎永久卡死；旧 `_combat_at_locked_edge` 边判边松方向键 = 左右碎步。→ 删整套，X 改"独立守护只置令 + 主线硬压固定时长"。
2. **线画游戏透明蒙板上看不到**：蒙板 colorkey/层级问题。→ 照平台绿线/梯子蓝线，一律画在控制面板 UI 小地图 `map_display`、同 scale 缩放。
3. **拖线三易其稿**：① cv2 `MOUSEMOVE` 回调——画面每帧 imshow 重绘丢事件、拖不动；② 全局 `GetCursorPos+ScreenToClient`（"倍率 X 范式"）——DPI/窗口缩放下坐标系统性偏移、时灵时不灵；③ 终版 = cv2 回调顶部无条件记坐标 + 主循环轮询，与绘制严格同空间 = 零偏移。**以后 UI 小地图交互一律用 ③，别再用全局 ScreenToClient。**
4. **横线固定向上偏约 30**：反算写死 `UI_MAP_Y=131`，但小地图实际贴放 `map_display_y=143`、显示高还是按 MAP_SCALE 的动态值，基准差叠加缩放比越往下越偏（X 因水平居中碰巧对齐）。→ 统一用绘制同帧存的实际贴放矩形 `_map_disp_x/y/w/h` 逆运算。
5. **X 碎步**：边拉边重判会左右翻转，且松键/压键同帧被游戏吞 = 相抵。→ 停主线 → 50ms 纯清场 → 硬压 1000~1500ms 一口气拉回、中途不翻转，到点才松手重判。
6. **守护线程曾自己 `_raw_frame+find_player_dot` 重复识别人物**：两套识别易不一致。→ 直接读主循环现成 `_player_map_pos`（倍率校准也用它），不另搞识别。
7. **★ Y 手划水平横线三死穴**：见第 1 节，最终弃用两横线、改认台子绿线身份。
8. **早期"越上界主动向下撤" `_bound_vreturn_tick`（复用 enter_descend 下跳/找梯）已整段删除**：台界从发起端拦住后人根本出不去，这套兜底多余；`BOUND_V_SAFE / BOUND_DOWN_STOP_GAP / BOUND_VRETURN_TIMEOUT_MS` 等中间常量一并清干净，**勿恢复**。
9. **只防"主动"，不防"被动"**：本功能拦的是主动跨层/下跳/下瞬移；被怪撞落、自己踩空的**被动掉落**拦不住，归"掉台归位 `_fall_return_tick`"（仅单台锁定、尚未 100% 回位，属待办）。

---

## 8. 排查指引（看日志）

debug.log 过滤前缀 `[打怪区域]`：
- `光点=(x,y) 竖线l=.. r=.. side=.. 上限组=[..] 下限组=[..]`：每秒一次，核对现成光点是否准、当前判成站哪台（验证后可删该临时光点日志）。
- `上闸门:人在上限组..不发起向上跨层` / `下硬防:人在下限组..不发下跳/下爬`：Y 台界拦截命中。
- 行为日志 `打怪区域:已到下限平台,不发下跳(硬防)`：战斗正下方落层被拦。
- 若 Y 完全不拦：先看有没有选界线（上限组/下限组是否为空）、人当前是否被判到界台（光点距绿线 ≤15）、是否已在爬梯状态机中（进行中不拦新发起）。
