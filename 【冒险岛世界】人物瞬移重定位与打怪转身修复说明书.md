# 【冒险岛世界】人物瞬移重定位 + 打怪转身修复 + 按键机制记忆清理 说明书

> 版本：冒险岛世界 MapleStory Worlds（唯一主线；怀旧服仅参考）
> 完成日期：2026-09-07
> 主程序：`maple_route_ui.py`（单体 UI+执行）；纯决策：`combat_logic.py`（本次未改，36 单测全过）
> 关联：瞬移 X/Y 设置见《【冒险岛世界】打怪Y范围面板与瞬移设置改版说明书.md》；强绑窗口 1276×749 见《【冒险岛世界】强绑窗口尺寸与小地图三模板定位说明书.md》

---

## 一、本次总览（三块）

1. **人物特征定位重构**：治好「一瞬移，人物基点就钉在原地约 1 秒、甚至一直找不回」。按用户定稿：找不到立即全图搜（不等）、全图搜到多远都同步、搜不到就停在最后消失位置一直等到重新搜到。
2. **打怪转身修复**：按用户定稿「先松开攻击、再松开旧方向键，然后按住朝怪的新方向键 120~150ms；记录上次方向，下一只怪同方向就不再转身」。
3. **按键机制记忆冲突清理**：把 PROJECT / 代码注释里残留的怀旧服「必须 SendInput、keybd_event 转不动」旧结论，统一改成冒险岛世界实测结论「吃 keybd_event、SendInput 单独发不动」，怀旧服相反机制保留为「仅参考」。

---

## 二、人物特征瞬移重定位（`_match_character` / `_get_player_screen_pos`）

### 2.1 旧逻辑为什么瞬移后丢点（三道关卡叠加，误杀了"瞬移"这种合法大位移）
1. **ROI 只在最后位置 ±200（400×400）内搜**：瞬移后人一下飞出这个范围，ROI 必搜不到。
2. **全图其实搜到了新位置的人物，但旧逻辑有一条"候选离上次位置 >150px 就判为干扰、return None 不采信"**——它把瞬移（合法跳几百 px）当成"乱匹配/跳到怪身上"枪毙了。
3. **输出层 `_get_player_screen_pos` 丢失后用上次位置原地顶 1 秒，超 1 秒才返回 None**；可 1 秒后第 2 条仍在，全图结果继续被 150px 拒绝 → 陷入"ROI 搜不到→全图被拒"的死循环，点一直钉原地。

### 2.2 新逻辑（用户定稿）
- **ROI 优先**：有上次位置且不在瞬移窗口时，先在原地 ±200 小范围匹配，达标就近采信（省算力、稳），**删掉原来的 150px 硬跳变拒绝**（ROI 内本就是原地附近）。
- **ROI 无达标 / 刚瞬移：当帧立即全图搜，不等待**。全图结果**允许离上次很远也同步**（瞬移/走远/切屏都是合法位移）：
  - **≥2 个特征在同一处（彼此 ≤50px）一致命中 → 按置信度加权平均**（真正实现了多特征一致性融合，抗单模板误配）；
  - **只有 1 个特征**：常态远距要求 **≥0.75** 高门槛（防错配到怪/别的玩家）；**瞬移 relocate 窗口内 ≥0.70 即采信**。
- **输出层 `_get_player_screen_pos` 去掉 1 秒上限**：匹配失败就一直返回「最后成功位置」（点停在最后消失处，不清空、不乱飞），后台每帧继续全图搜，**一搜到新位置立刻同步跳过去**；从未定位成功过才返回 None。
- **瞬移瞬间打 relocate 标记 `_char_relocate_until = now + 700`（700ms）**：这期间跳过 ROI 直接全图、单特征 0.70 就认，瞬移后基本当帧跟过去。两个瞬移执行点都打了标：
  - 水平瞬移：`_combat_tick` 追怪段（按住方向 + 瞬移键处）；
  - 垂直瞬移：`_do_teleport`（上/下方向 + 瞬移键处）。

### 2.3 顺带修掉的问题
- 旧 docstring 写着"≥2 特征一致性校验 + 加权平均"，**代码其实根本没实现**（只是挑离上次最近的一个）——本次补成真正的聚类 + 置信度加权。
- 删掉一段**永远不会成立的"低分兜底"死分支**：两个阈值都是 0.70，里面 `best<0.70` 的判断恒为假；并清掉重复的 `_char_feature_matches` 赋值。

### 2.4 代码位置（行号为约值，以 Grep 为准）
- `_match_character`（约 6670）：ROI/全图候选带来源标记 `'roi'/'full'`，决策 1=ROI 就近、决策 2=全图远距同步 + 多特征融合。
- `_get_player_screen_pos`（约 9566）：去掉 1 秒宽限，失败保持最后位置。
- `__init__`（约 738）：`self._char_relocate_until = 0`。
- 水平打点：`_combat_tick`（约 10770）；垂直打点：`_do_teleport`（约 3156）。
- 常量 `CHAR_MATCH_THRESHOLD = CHAR_ROI_THRESHOLD = 0.70`（约 273 / 304）；单特征远距高门槛 0.75、一致性簇半径 50px、relocate 700ms 均在 `_match_character` 内。

---

## 三、打怪转身修复（`_combat_tick` 面向段）

### 3.1 根因（为什么"转不了身"）
- 主攻是**点按**（`_press_game_key` 每次自带 keydown+keyup），所以转身前那句 `_release_attack_key()` 在点按模式下其实是**空操作**（没有持续按住的攻击键可松）。
- 真正的问题：**追怪时左右方向键是 `_hold_combat_key` 持续按住的**，而旧转身分支只松攻击、**没松移动方向键**。怪从一侧换到另一侧要转身时，旧方向键还按着就去按反方向 = **左右相抵，人物僵住转不过来**；同时状态已被记成"朝新方向转好了"，下一帧不再纠正，直接朝反方向打空。

### 3.2 定稿流程（用户 2026-09-07）
1. `needed_facing = 1 if 怪在人物右 else -1`；`_combat_last_face_dir` 记录上次朝向，**两者相同 = 已朝对，不转身、直接打**（同方向连打不多余转身）。
2. 需要转身时，**先彻底松键**：
   - `_release_attack_key()`（松攻击，避免锁在攻击/技能后摇动画里）；
   - `_release_combat_move()`（**松开所有持续按住的旧方向键**，这是转身能转过来的关键）。
3. **按住朝怪的新方向键**：`_send_win_key(vk, keyup=False)`（keybd_event 扫描码，方向键 ext=0x0001），保持 **`random.randint(120,150)` ms（120~150 随机，拟人）**，到点由 `_combat_timed_keys` 自动 keydown→keyup 松开。
4. 记录 `_combat_last_face_dir / _combat_facing = needed_facing`，本帧 return；下一帧同方向不再转身，进入攻击。

### 3.3 代码位置
- `_combat_tick` 面向判断段（约 10707–10727）。
- 定时松键：`_combat_timed_keys` 消费处（约 10451，到期 `_send_win_key(vk,keyup=True)`）。
- 走路追怪同步朝向：`_set_combat_move`（约 9771，朝哪走就把 `_combat_last_face_dir` 记成哪，走到同方向怪不用再转）。
- **3% 随机反向按用户要求原样保留**，以后统一并入"仿人类操作"机制，本轮不动。

---

## 四、按键机制记忆冲突清理（冒险岛世界 vs 怀旧服）

### 4.1 两套游戏机制相反（别再套错）
| | 冒险岛世界 MapleStory Worlds（主线，以此为准） | 冒险岛怀旧服（仅参考） |
|---|---|---|
| 输入模型 | 吃 **`keybd_event`（扫描码，方向键 ext=0x0001）** | DirectInput，只认 **SendInput 扫描码** |
| SendInput 单独发 | **不动（不要用）** | 生效 |
| keybd_event 的 keyup | 生效（发/松都用它） | 攻击键 keyup 收不到 → 松不开/转不动 |
| 统一要求 | **发键/松键同一机制（都 keybd_event）** | 发/松同一机制（都 SendInput） |

### 4.2 两版通用、仍然保留的经验
- 前台切换：`AttachThreadInput` 绑定线程 + 先点一下 Alt 绕过 SetForegroundWindow 限制 + BringWindowToTop，发完恢复原前台并分离线程。
- 方向键必须加扩展键标志 `ext=0x0001`；发键前清理卡住的 Alt/Ctrl/Shift；按键保持 80–180ms 随机。
- PostMessage/SendMessage 后台消息两版都收不到。

### 4.3 本次清理点（只改文档/注释结论，按键逻辑本来就是 keybd_event、未改）
- `PROJECT_STATE.md` 4 处：打怪状态第 2 条、"关键输入机制教训"、用户永久原则里的"★本游戏输入特性"、"键盘发送最终方案"段——主结论全部换成冒险岛世界 keybd_event，怀旧服结论降级为"仅参考·相反"。
- `maple_route_ui.py` 注释 7 处：`_press_game_key` / `_send_win_key` 的 docstring、定时松键、转身段、主攻段、异常日志等，把"SendInput/DirectInput/keybd_event 转不动"的相反措辞统一改成"冒险岛世界吃 keybd_event、怀旧服才只认 SendInput"。
- 发键函数分工：点按 `_press_game_key`；按住/松开 `_send_win_key(vk,keyup=...)`；战斗持续按住方向 `_hold_combat_key` / `_release_combat_key`；持续按住攻击 `_hold_attack_key` / `_release_attack_key`。

---

## 五、坑 / 注意事项
1. **瞬移是合法大位移，绝不能用"离上次位置远=跳变"拒绝**；平时防乱配靠"多特征一致性融合 + 单特征远距 0.75 高门槛"，而不是靠距离一刀切。
2. **转身必须先松开旧的持续移动键**，只松攻击键不够，否则左右方向相抵转不动。
3. 不要再被怀旧服 DirectInput 的旧记录带偏：**冒险岛世界 keybd_event 才生效，SendInput 单独发不动**；遇到"本应生效却不生效"先怀疑是不是套错了游戏机制。
4. 可调参数（真机手感，后续按需微调）：全图单特征远距门槛 0.75、瞬移窗口 0.70、一致性簇半径 50px、relocate 窗口 700ms、转身新方向保持 120~150ms。
5. `data/*.json` 为运行时状态不入库；本次无新增配置字段。

---

## 六、验证记录
- `py_compile maple_route_ui.py` 语法 OK；`test_combat_sim.py` **36/36 PASS**（纯决策层未破坏）。
- 真机待确认项：①瞬移后人物基点是否立刻跟到新位置、不再钉原地；②怪左右横跳时能否顺畅转身；③同方向连打是否不重复转身、平时走路基点不乱飘到怪身上。
- 本轮改动（人物定位重构 + 转身修复 + 记忆/注释清理）**真机确认 OK 后再登记 PROJECT_STATE 总纲并提交可 100% 回滚的 git 快照**。
