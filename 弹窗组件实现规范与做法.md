# 弹窗组件实现规范与做法

## 适用场景
所有需要弹出的窗口（倍率差调整、保存、方案、清除等），统一使用本规范实现。

## 弹窗标准样式
1. **灰底白字**：背景灰色(60,60,60)，文字白色(255,255,255)，简单明了
2. **右上角关闭按钮X**：深灰背景(80,80,80)，白色X文字，点击关闭弹窗
3. **可拖拽移动**：拖拽标题栏（顶部50像素区域）移动弹窗，有边界保护
4. **放在最上层**：弹窗绘制在draw方法最后（return frame之前），确保不被其他元素挡住
5. **自动丢失焦点5秒**：5秒无操作自动丢失焦点，点击聚焦也算操作

## 输入框标准样式
1. **黑底白字**：背景黑色(0,0,0)，文字白色(255,255,255)
2. **白边框**：边框白色(255,255,255)，1像素
3. **聚焦时橙色边框**：聚焦时边框橙色(0,165,255)，2像素
4. **点击输入框内任意位置可聚焦**：点击检测范围是整个输入框，不是只有边框
5. **可输入正负号、小数点、数字**：支持0-9、负号(-)、小数点(.)
6. **退格键可删除字符**：按退格键删除最后一个字符

## 关键实现要点

### 1. 位置变量初始化（必须）
所有弹窗内控件的位置变量必须在`__init__`中初始化为(0,0,0,0)，否则第一次打开弹窗时点击检测会崩溃（None不能unpack）。

```python
# 弹窗内控件位置（必须初始化，否则点击检测时None会崩溃）
self._dlg_scale_x_input = (0, 0, 0, 0)  # X偏差输入框位置
self._dlg_scale_y_input = (0, 0, 0, 0)  # Y偏差输入框位置
self._dlg_scale_ok_btn = (0, 0, 0, 0)  # 确认按钮位置
self._dlg_scale_cancel_btn = (0, 0, 0, 0)  # 取消按钮位置
self._dlg_scale_close_btn = (0, 0, 0, 0)  # 右上角关闭按钮X位置
```

### 2. 打开弹窗时立即计算位置（必须）
在打开弹窗时立即调用`_update_dialog_positions()`方法计算所有控件位置，解决第一次打开弹窗点击没反应的问题。

```python
def _update_scale_dialog_positions(self):
    """立即计算弹窗内所有控件的位置（解决第一次打开弹窗点击没反应的问题）"""
    dlg_x = self._scale_dialog_pos[0]  # 弹窗X坐标
    dlg_y = self._scale_dialog_pos[1]  # 弹窗Y坐标
    dlg_w, dlg_h = 320, 220  # 弹窗宽高
    # 右上角关闭按钮X
    self._dlg_scale_close_btn = (dlg_x+dlg_w-30, dlg_y+5, 25, 25)
    # X偏差输入框
    self._dlg_scale_x_input = (dlg_x+110, dlg_y+65, 160, 35)
    # Y偏差输入框
    self._dlg_scale_y_input = (dlg_x+110, dlg_y+125, 160, 35)
    # 确认按钮
    self._dlg_scale_ok_btn = (dlg_x+60, dlg_y+175, 80, 30)
    # 取消按钮
    self._dlg_scale_cancel_btn = (dlg_x+180, dlg_y+175, 80, 30)
```

### 3. 弹窗绘制放在draw方法最后（必须）
弹窗绘制代码必须放在draw方法最后（return frame之前），确保弹窗永远在最上层，不被其他元素挡住。

### 4. 点击检测优先检测关闭按钮X（必须）
关闭按钮X在标题栏区域，所以点击检测时必须优先检测X按钮，否则会被标题栏拖拽检测覆盖。

### 5. 小数点输入（必须）
轮询按键列表中必须包含小数点（0xBE主键盘.、0x6E小键盘.），否则小数点打不上去。

```python
# 轮询：主键盘0-9 + 小键盘0-9 + 退格 + 回车 + ESC + 负号 + 小数点
poll_vks = list(range(0x30, 0x3A)) + list(range(0x60, 0x6A)) + [0x08, 0x0D, 0x1B, 0xBD, 0x6D, 0xBE, 0x6E]
```

### 6. 自动丢失焦点5秒（必须）
输入框聚焦后，5秒无操作自动丢失焦点。点击聚焦也算操作，需要重置计时器。

```python
# 输入框自动失焦：5秒无变化
if self._focused_field is not None:
    now_ms = time.time() * 1000
    if now_ms - self._last_input_change > 5000:
        self._save_input_config()
        self._focused_field = None
```

### 7. 点击聚焦时重置计时器（必须）
点击输入框聚焦时，必须重置`_last_input_change`，否则点击后5秒内不输入数字就会自动丢失焦点。

```python
# 点击X偏差输入框：聚焦
if sx_x <= x < sx_x + sx_w and sx_y <= y < sx_y + sx_h:
    self._focused_field = "scale_x_offset"
    self._num_field_replace = True
    self._last_input_change = time.time() * 1000  # 点击聚焦也算操作，重置5秒失焦计时器
    return
```

## 弹窗状态变量
```python
self._show_scale_dialog = False  # 是否显示弹窗
self._scale_dialog_pos = [70, 230]  # 弹窗位置（可移动）
self._scale_dialog_dragging = False  # 是否正在拖拽弹窗
self._scale_dialog_drag_offset = [0, 0]  # 拖拽时的偏移量
```

## 常见问题与解决方案

### 问题1：第一次打开弹窗点击没反应
**原因**：位置变量在弹窗绘制时才设置，但第一次打开弹窗时draw还没执行到最后，位置变量还是初始值(0,0,0,0)。
**解决方案**：在打开弹窗时立即调用`_update_dialog_positions()`方法计算所有控件位置。

### 问题2：小数点打不上去
**原因**：轮询按键列表中没有包含小数点（0xBE、0x6E）。
**解决方案**：在轮询按键列表中添加小数点，并添加小数点处理逻辑（只能输入一个小数点）。

### 问题3：弹窗被其他元素挡住
**原因**：弹窗绘制代码不在draw方法最后，后面还有其他绘制代码会覆盖弹窗。
**解决方案**：把弹窗绘制代码移到draw方法最后（return frame之前）。

### 问题4：点击关闭按钮X没反应
**原因**：关闭按钮X在标题栏区域，点击检测时先检测到标题栏拖拽，X按钮的点击被覆盖了。
**解决方案**：点击检测时优先检测X按钮，再检测标题栏拖拽。

### 问题5：点击输入框聚焦后很快自动丢失焦点
**原因**：点击聚焦时没有重置`_last_input_change`，5秒计时器从上次输入开始计算。
**解决方案**：点击输入框聚焦时，重置`_last_input_change = time.time() * 1000`。

### 问题6：位置变量初始值为None导致崩溃
**原因**：位置变量没有在`__init__`中初始化，第一次点击检测时None不能unpack。
**解决方案**：在`__init__`中初始化所有位置变量为(0,0,0,0)。
