# 小地图倍率的成功方案说明书

## 一、问题背景

F11三点校准功能开发过程中遇到多个问题：
1. 小地图上三个点（红、绿、蓝）重叠在一起
2. 人物光点检测不到，一直返回同一个坐标(84,90)
3. 小地图映射窗口被拉伸变形（原始正方形→长方形）
4. 倍率计算不正确
5. 模板匹配失效

---

## 二、实现方法

### 2.1 人物光点检测改进（核心）

**问题根因：**
- `MORPH_OPEN(2x2)` 先腐蚀后膨胀，把2~4px的小光点"吃掉"
- 跟踪逻辑取离上次位置最近的黄色轮廓，结果一直匹配到固定的黄色物体（在(84,90)附近晃动），而不是移动的人物光点
- 面积过滤（1~50像素）也可能过滤掉小光点

**解决方案：**
1. **注释掉 MORPH_OPEN**：避免吃掉2~4px的小光点
2. **收紧黄色范围**：H=20-35（从20-40收紧），S>=100（从80提高），V>=180（从150提高，人物光点很亮）
3. **取最亮黄色区域的质心**：不靠面积过滤，对每个轮廓计算平均亮度（V值），取最亮的轮廓的质心（人物光点最亮）
4. **去掉跟踪逻辑**：避免匹配到固定黄色物体，每帧独立检测

**关键代码：**
```python
# 去掉MORPH_OPEN
# mask = cv2.morphologyEx(mask, cv2.MORPH_OPEN, np.ones((2, 2), np.uint8))

# 对每个轮廓计算平均亮度，取最亮的
for c in cnts:
    contour_mask = np.zeros(mask.shape, dtype=np.uint8)
    cv2.drawContours(contour_mask, [c], -1, 255, -1)
    mean_val = cv2.mean(v_channel, mask=contour_mask)[0]
    candidates.append((cx, cy, area, mean_val))

# 取最亮的轮廓（人物光点最亮）
best = max(candidates, key=lambda p: p[3])
```

---

### 2.2 小地图映射窗口比率修复

**问题根因：**
- 原始小地图约132×119（接近正方形，比率1.1:1）
- 代码固定拉伸到340×250（比率1.36:1），导致变形
- 不同地图小地图尺寸不同，固定放大倍数可能超出窗口

**解决方案：**
1. **动态计算放大倍数**：最大2倍，确保每个地图都能完整显示在UI窗口中
2. **保持原始比率**：不拉伸，渲染尺寸 = 原始尺寸 × 动态放大倍数
3. **边界保护**：左右各留20像素，上下留足够空间给按钮和底部

**关键代码：**
```python
# 动态计算放大倍数，最大2倍
max_scale_x = (UI_W - 40) / float(w)  # 左右各留20像素
max_scale_y = (UI_H - 200) / float(h)  # 上下留足够空间
MAP_SCALE = min(2.0, max_scale_x, max_scale_y)  # 最大2倍
render_w = int(w * MAP_SCALE)
render_h = int(h * MAP_SCALE)
map_display = cv2.resize(display, (render_w, render_h), interpolation=cv2.INTER_NEAREST)
```

---

### 2.3 倍率计算（除以2）

**问题根因：**
- 蒙板是小地图的二倍大小
- 用蒙板上的距离计算倍率时，没有除以2，导致倍率是实际的2倍

**解决方案：**
- 倍率计算时除以2，转换成原始小地图距离

**关键代码：**
```python
# 蒙板是小地图的二倍，除以2转换成原始小地图距离
scale_x = (dx_map / 2.0) / float(dx_screen)
scale_y = (dy_map / 2.0) / float(dy_screen)
```

---

### 2.4 模板匹配（45x45完整截取）

**问题根因：**
- 模板尺寸55太大，匹配不稳定
- 改成45后，因为45是奇数，`half = 45 // 2 = 22`，实际截取的是22×2=44，不是45
- 模板不完整导致匹配失效

**解决方案：**
- 模板尺寸改成45
- 奇数尺寸时+1确保完整截取（45→45x45，不是44x44）

**关键代码：**
```python
self._calib_template_size = 45  # 模板截图大小45x45

half = self._calib_template_size // 2
odd_offset = 1 if self._calib_template_size % 2 else 0  # 奇数尺寸+1确保完整
x1 = sx - half
y1 = sy - half
x2 = sx + half + odd_offset  # 奇数尺寸+1确保完整
y2 = sy + half + odd_offset
```

---

### 2.5 配置文件同步

**问题根因：**
- 修改 `data/minimap_region.json` 后，程序运行时加载的是 `dist/data/minimap_region.json`
- 没有同步到dist目录，修改不生效

**解决方案：**
- 修改配置文件后，必须同步到 `dist/data/` 目录
- 打包时确保data目录下的所有json文件都复制到dist/data/

**关键操作：**
```powershell
Copy-Item data\minimap_region.json dist\data\minimap_region.json -Force
```

---

## 三、关键部分

1. **人物光点检测是核心**：光点检测不到导致三个点重叠，是最关键的问题
2. **小地图映射窗口比率**：保持原始比率不拉伸，确保显示正确
3. **倍率计算除以2**：蒙板是小地图的二倍，必须除以2
4. **模板尺寸奇数问题**：45是奇数，必须+1确保完整截取45x45
5. **配置文件同步**：修改后必须同步到dist/data/目录

---

## 四、重要部分

1. **动态放大倍数**：确保每个地图都能完整显示，不会受窗口限制
2. **取最亮黄色区域质心**：不靠面积过滤，光点多小都能抓到
3. **去掉跟踪逻辑**：避免匹配到固定黄色物体，每帧独立检测
4. **小地图显示位置**：从最下方向上移420像素，再微调19像素，避免被底部按钮挡住

---

## 五、采过的坑与解决办法

### 坑1：人物光点检测一直返回(84,90)，三个点重叠
- **现象**：每次按F11时，`find_player_dot` 都返回(84,90)，完全没有变化
- **根因**：跟踪逻辑取离上次位置最近的黄色轮廓，结果一直匹配到固定的黄色物体
- **解决**：去掉跟踪逻辑，取最亮黄色区域的质心

### 坑2：MORPH_OPEN吃掉小光点
- **现象**：清晰的光点却检测不到
- **根因**：`MORPH_OPEN(2x2)` 先腐蚀后膨胀，2~4px的光点会被腐蚀到几近消失
- **解决**：注释掉MORPH_OPEN

### 坑3：小地图映射窗口被拉伸变形
- **现象**：原始小地图是正方形，投屏过来变成长方形
- **根因**：代码固定拉伸到340×250，比率从1.1:1变成1.36:1
- **解决**：保持原始比率，动态计算放大倍数，最大2倍

### 坑4：倍率计算不正确
- **现象**：倍率值是实际的2倍
- **根因**：蒙板是小地图的二倍，用蒙板上的距离计算倍率时没有除以2
- **解决**：倍率计算除以2

### 坑5：模板尺寸奇数时截取不完整
- **现象**：模板尺寸改成45后，匹配失效
- **根因**：45是奇数，`half = 45 // 2 = 22`，实际截取的是22×2=44，不是45
- **解决**：奇数尺寸时+1确保完整截取

### 坑6：修改配置文件不生效
- **现象**：修改 `minimap_region.json` 后，程序运行时还是旧配置
- **根因**：程序加载的是 `dist/data/minimap_region.json`，修改的是 `data/minimap_region.json`
- **解决**：修改后同步到 `dist/data/` 目录

### 坑7：小地图显示位置太靠下
- **现象**：小地图在最下方，被底部按钮挡住
- **根因**：显示位置计算为 `UI_H - map_h - 80`，太靠下
- **解决**：从最下方向上移420像素，再微调19像素，固定在y=143

---

## 六、总结

小地图倍率功能的成功实现，关键在于：
1. **人物光点检测**：去掉MORPH_OPEN + 收紧黄色范围 + 取最亮质心 + 去掉跟踪逻辑
2. **小地图比率**：动态放大倍数，最大2倍，保持原始比率
3. **倍率计算**：除以2（蒙板是小地图的二倍）
4. **模板匹配**：45x45完整截取，奇数尺寸+1
5. **配置同步**：修改后同步到dist/data/目录

每一个坑都是通过日志分析、分层排查找到根因，然后精准修复。
