# 人物绿框与三点检测框说明书

## 功能说明
人物定位绿框（150x190绿色空心矩形）和镜头死区检测三个背景框，随脚本启动就显示在游戏主窗口蒙板上，不依赖人物特征匹配、不依赖脚本运行状态。

## 组成部分
1. **人物定位绿框**：绿色空心矩形，尺寸150x190，当前用屏幕中心兜底，后续改用小地图光点比例定位
2. **三点检测框**：三个红色/绿色小框，检测游戏主窗口背景变化，判断镜头是否在动
3. **统一蒙板**：所有框都画在同一个Win32透明蒙板窗口上

## 实现细节

### 蒙板绘制的类结构（关键！）
**蒙板绘制代码就在主程序类（MapleRouteUI）里**，不是单独的MonsterOverlay类。
- `_win32_overlay_loop()`：蒙板窗口消息循环，在主程序类里
- WM_PAINT处理：蒙板绘制逻辑，在主程序类里
- 所以绘制代码里的 `self` **就是主程序实例**，可以直接访问 `self.window_rect`、`self._bg_regions` 等属性

### 人物绿框绘制
- 位置：蒙板WM_PAINT里，三个检测框后面，独立绘制（不在 `if char_pos:` 块里）
- 坐标：当前用 `self.window_rect["width"]//2, self.window_rect["height"]//2`（屏幕中心兜底）
- 写法：完全复制三点检测框的写法，CreatePen + SelectObject + Rectangle + SelectObject
- 颜色：绿色（0x00FF00），线宽2
- 尺寸：PLAYER_BOX_W=150, PLAYER_BOX_H=190

### 三点检测框绘制
- 位置：蒙板WM_PAINT里，独立绘制（不在 `if char_pos:` 块里）
- 坐标：`self._bg_regions` 里存的三个区域（x,y,w,h）
- 颜色：3处全动=绿色（镜头在动），否则红色（镜头不动）
- 序号标注：白色数字1/2/3

### 为什么要独立绘制（不依赖char_pos）
- 人物特征匹配需要时间，没匹配出来时char_pos=None
- 如果绿框和检测框放在 `if char_pos:` 块里，没匹配出来时就不显示
- 用户要求"随脚本启动就显示"，所以必须独立绘制，不依赖char_pos

## 踩过的坑

### 大坑：self.app vs self 引用错误（本次最严重的坑）
- **现象**：人物绿框怎么都不显示，三个检测框正常，药品框正常
- **根因**：蒙板绘制代码里错误地写了 `self.app.window_rect`，但蒙板绘制就在主程序类里，`self` 就是主程序实例，**根本没有 `self.app` 这个属性**
- **机制**：`self.app` 不存在 → AttributeError → 被 `except Exception as e` 静默捕获 → 绿框绘制代码跳过 → 绿框不显示
- **为什么难查**：异常被静默吞掉了，控制台没有任何报错，只能靠猜
- **解决**：把 `self.app.window_rect` 改成 `self.window_rect`，直接用主程序实例的属性
- **教训**：
  1. 改任何代码前，先看清楚这段代码在哪个类里，`self` 是什么
  2. 不要凭印象假设类结构（我以为蒙板是单独的类，其实就在主程序类里）
  3. 异常不要静默吞掉，至少打印出来，否则bug查不到
  4. 参考已有代码的写法（三点检测框用的是 `self._bg_regions`，直接用self，就应该想到绿框也用self）

### 坑2：绿框放在if char_pos块里
- **现象**：没匹配人物特征时，绿框和检测框都不显示
- **根因**：绿框和检测框的绘制代码放在 `if char_pos:` 块里
- **解决**：移到if块外面，独立绘制

### 坑3：lock_screen_from_dot返回None
- **现象**：用光点锁定算法算位置时，返回None，绿框不显示
- **根因**：`_target_window_size=(0,0)` 无效（绑定窗口时没正确保存窗口尺寸）
- **临时解决**：先用屏幕中心兜底，确保绿框能显示，后续再解决位置准确问题
- **后续**：需要排查 `_save_target_window_size()` 为什么没正确设置窗口尺寸

## 涉及函数
- `_win32_overlay_loop()`：蒙板窗口消息循环（主程序类里）
- WM_PAINT处理：蒙板绘制逻辑（主程序类里）
- `_detect_camera_motion()`：三点背景差异检测，更新 `_bg_motion_count`
- `_save_target_window_size()`：保存游戏窗口尺寸（后续需要排查）
- `lock_screen_from_dot()`：小地图光点→游戏屏幕坐标算法（后续启用）

## 验证方法
1. 绑定游戏窗口，不启动脚本，确认蒙板上有绿色人物框（屏幕中心）
2. 确认三个检测框正常显示（红色小框，带序号1/2/3）
3. 确认药品检测框（hp_marker/mp_marker）正常显示
4. 匹配人物特征后，确认黄点正常显示，且绿框不跟随黄点（独立位置）
5. 移动角色/镜头，确认三个检测框颜色变化（动=绿，不动=红）

## 关联
- 总纲"蒙板绘制"章节
- 工作记录016号（31号版本完整实现结构）
- 小地图光点锁定人物坐标方案.md（后续绿框位置校准用）
- git commit: f4a6746（角色绿框已现等待调校位置）
