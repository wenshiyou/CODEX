# MapleBot 开发工作记录

> 每解决一个问题，记录问题现象、根因、解决方案、踩过的坑。避免重复踩坑。
> 最后更新：2026-08-28

---

## 恢复操作必备规则（每次回滚/恢复必须遵守）

**回滚或恢复时，必须同时恢复以下三类文件，缺一不可：**
1. **源码**：maple_route_ui.py（及其他 .py 源码）
2. **config**：config/config.json
3. **data（json）**：data/fight_potion_config.json、data/minimap_region.json、data/route_config.json、data/char_templates/ 等

**只恢复源码不恢复配置 = 药/倍率/小地图全部异常。**
**git checkout 只恢复了被跟踪的文件，未跟踪的配置文件需要从备份手动恢复。**

**打包后配置路径（重要）：**
- 打包后程序从 `exe同级目录/data/` 加载配置，不是项目目录的 `data/`
- `DATA_DIR = os.path.join(app_dir(), "data")`，app_dir() 返回 exe 所在目录
- 恢复配置后必须把 `data/` 文件夹复制到 `dist/` 目录下，和 exe 放在一起
- 用户把 exe 复制到别处运行时，必须连同 `data/` 文件夹一起复制

---

## 版本历史（每个打包节点）

| 版本 | git/commit | 日期 | 改动内容 | 状态 |
|------|-----------|------|----------|------|
| v24 | 7919683 | 2026-08-28 | 基点偏移默认400 + 人物特征保存修复(app_dir优先) + 黑屏/按键修复 | 基线正常版 |
| v25 | (未提交) | 2026-08-28 | 主循环全局拖动坐标修复：WM_MOUSEMOVE拖出窗口后用GetAsyncKeyState继续检测，基点可超400 | 测试版 |
| v26 | (未提交) | 2026-08-28 | 蒙板WM_LBUTTONDOWN加SetCapture + 小地图LEFT_OFFSET=3(向右扩) | 光圈拖动修复，小地图右边被切 |
| v27 | (未提交) | 2026-08-28 | 引导文字改屏幕坐标(SetWindowOrgEx下) + LEFT_OFFSET=0(回滚小地图) | 引导文字显示，小地图回滚 |
| v28 | (未提交) | 2026-08-28 | LEFT_OFFSET=0纯净版(回滚v26小地图偏移) | 测试版 |
| v29 | (未提交) | 2026-08-28 | 小地图LEFT_OFFSET=-3(向左扩3px) | 测试版 |
| v30 | (未提交) | 2026-08-28 | 去掉SetWindowOrgEx + 引导文字改客户区坐标 | **错误**：检测框位置对了但光圈拖不动，已回滚 |
| v31 | 7919683回滚 | 2026-08-28 | 回滚到v24纯净版，去掉所有错误修改 | 不行：药有问题、倍率有问题、小地图不行 |
| v34 | 3ca2114+fix | 2026-08-28 | d10787b源码+备份config+map_area_rect None修复 | 配置路径问题：dist/data/缺配置 |
| v35 | 完整备份恢复 | 2026-08-28 | 从backup\完整备份_20260827_154308全部恢复(源码+config+data+素材)+None修复+dist/data配置齐全 | 待测试 |
| v49 | (未提交) | 2026-08-28 | 光点锁定完整版(lock_screen_from_dot+monster_to_map+_get_player_screen_pos接入)+怪物数据记忆(2秒宽限)+PROJECT_STATE核心组件规范+单元测试通过 | 打包完成待测试 |

---

## 记录索引

| 编号 | 日期 | 问题 | 根因 | 状态 |
|------|------|------|------|------|
| 001 | 2026-08-27 | 打包后黑屏 | map_area_rect 初始化位置错误，__init__后部重置覆盖了_detect_minimap的检测结果 | 已解决 |
| 002 | 2026-08-27 | 打包后按键全部失灵 | b3a1673删除_imread3(4转3通道)，load_png返回4通道BGRA，_ui_bg变成4通道，draw函数每帧报broadcast错误被try-except吞掉，窗口消息泵异常导致鼠标回调不触发 | 已解决 |
| 003 | 2026-08-27 | 人物特征保存后重启丢失 | 打包后_load_char_templates从_MEIPASS(只读)加载，但保存写到app_dir(可写)，用户保存的模板下次启动加载不到 | 已解决 |
| 004 | 2026-08-28 | 基点光圈拖动距离被限制在400 | 蒙板WM_LBUTTONDOWN未调用SetCapture，鼠标拖出窗口后WM_MOUSEMOVE停止；主循环全局检测坐标不匹配 | 已解决 |
| 005 | 2026-08-28 | 蒙板所有绘制偏上（加药检测框、人物基点、引导文字） | SetWindowOrgEx(hdc, wr['left'], wr['top'])把所有GDI坐标当屏幕坐标，但实际用的是截图坐标系（窗口左上角为原点），导致所有绘制偏上偏左wr['top']/wr['left'] | 回滚-待正确修复 |
| 006 | 2026-08-28 | 去掉SetWindowOrgEx后光圈拖动失效 | 错误地删除SetWindowOrgEx解决检测框偏上，但光圈拖动WM_MOUSEMOVE坐标逻辑依赖SetWindowOrgEx，删除后点击检测坐标不匹配，光圈拖不动 | 已回滚v24 |
| 007 | 2026-08-28 | v33启动崩溃 NoneType object is not subscriptable | __init__中map_area_rect初始化为None后立即调用_detect_minimap，防抖逻辑直接访问old["left"]未做None检查，首次调用崩溃 | 已修复(v34) |
| 008 | 2026-08-28 | 重新打包一直黑屏(完整备份原包正常) | **入口文件错误**：备份原包用main.py打包(PyQt5程序)，但一直用maple_route_ui.py打包(OpenCV程序)，两个完全不同的程序 | 已确认：用户要的是maple_route_ui.py |
| 009 | 2026-08-28 | maple_route_ui.py打包后主界面灰屏(UI不渲染) | **两个叠加问题**：(1)UI背景图用cv2.imread加载，打包后路径异常导致加载失败用灰色填充；(2)__init__中自动绑定后_detect_minimap设置了map_area_rect，但第894行又重置为None，主循环访问异常导致UI无法渲染 | 已修复(v44)：cv2.imread→load_png+4通道转3通道；删除后部map_area_rect=None重置，改在__init__开头初始化 |

---

## 001 - 打包后黑屏（2026-08-27）

### 现象
- 打包后运行 MapleBot.exe，窗口全黑，UI不渲染
- 源码直接运行正常
- 窗口标题显示"未响应"

### 排查过程
1. 怀疑UPX压缩损坏OpenCV DLL → 关闭UPX，无效
2. 加console=True看启动日志 → 图片资源正常加载
3. imshow处记录frame均值/形状 → frame数据正常
4. 主循环逐步骤写日志 → 定位到map_area_rect相关逻辑异常

### 根因
`__init__`执行顺序：
1. 自动绑定窗口 → 调用`_detect_minimap()` → 设置`self.map_area_rect = {检测结果}`
2. 中间大量初始化代码
3. `self.map_area_rect = None` ← **BUG**：把第1步的检测结果重置为None
4. 主循环`if self.map_area_rect:`永远为False，部分代码路径访问map_area_rect["width"]抛AttributeError
5. 异常导致主循环死循环/窗口消息泵阻塞 → UI无法渲染 → 黑屏

### 解决方案
- `__init__`最开头加`self.map_area_rect = None`（在自动绑定之前）
- 删除后部的`self.map_area_rect = None`重置（会覆盖检测结果）
- 提交：2e8e1e3（首次修复）→ 被b3a1673撤销 → 505f4c8（二次修复）

### 踩过的坑
- 不要在__init__中后部重置已经被前面代码设置过的属性
- 回滚/合并代码后必须diff检查关键修复是否保留
- 黑屏不一定是DLL/资源问题，可能是主循环异常

### 关联文档
- `打包黑屏问题排查与解决方案.md`（详细版）

---

## 002 - 打包后按键全部失灵（2026-08-27）

### 现象
- 打包后UI正常显示，但所有按钮点击无反应
- 标签页切换、刷新、运行、窗口绑定等全部失灵
- 准星拖拽绑定（全局鼠标检测）可以工作，但UI按钮全部失效
- 窗口标题正常（不是未响应），主循环还在运行

### 排查过程
1. 怀疑_on_mouse回调异常 → 加try-except包装器（v21），无效
2. 怀疑_capture_map返回None导致主循环异常 → 加None检查（v22），无效
3. **console=True调试版看控制台输出（关键突破）** → 发现持续报错 `draw error: could not broadcast input array from shape (X,Y,3) into shape (X,Y,4)`
4. 定位到通道不匹配：3通道图赋值给4通道frame

### 根因
b3a1673（对面线程改动）删除了 `_imread3` 函数（作用：4通道BGRA转3通道BGR），改用 `load_png`（用IMREAD_UNCHANGED，保留4通道）。

连锁反应：
1. `load_png` 返回4通道BGRA → `_ui_bgs[tab]` 变成4通道
2. `frame = self._ui_bg.copy()` → frame是4通道
3. draw函数中把3通道图赋值给4通道frame → broadcast错误
4. draw error被try-except捕获 → 只显示静态背景图
5. 通道异常+静态背景 → 窗口消息泵异常 → 鼠标回调不触发 → 所有按钮点不动

### 解决方案
加载UI背景图后统一转3通道：
```python
img = load_png(p)
if img is not None:
    if img.ndim == 3 and img.shape[2] == 4:
        img = cv2.cvtColor(img, cv2.COLOR_BGRA2BGR)
    self._ui_bgs[tab] = cv2.resize(img, (UI_W, UI_H))
```
提交：d10787b

### 踩过的坑
- **console=True是排查打包问题的第一手段**——很多错误被try-except吞了，只有控制台能看到
- 删除工具函数前必须全局搜索调用点，确认通道/返回值预期一致
- 打包后验证不能只看"能启动+UI显示"，必须点按钮测试交互
- 通道不匹配（3 vs 4）是OpenCV常见坑，IMREAD_UNCHANGED会保留alpha
- 不要先怀疑_on_mouse异常，先看控制台有没有被吞的错误

### 关联文档
- `打包黑屏问题排查与解决方案.md` 第六章（详细版）

---

## 003 - 人物特征保存后重启丢失（2026-08-27）

### 现象
- 在程序中框选保存人物特征后，当前会话可以使用
- 关闭程序重新打开后，保存的人物特征消失，变回初始模板
- 用户反馈"人物特征没有保存为最后一次的，关掉后就没有了，变成了初始的那个"

### 排查过程
1. 检查保存路径 `_capture_character_feature` → 保存到 `CHAR_TEMPLATE_DIR = app_dir()/data/char_templates`（可写）
2. 检查加载路径 `_load_char_templates` → 打包后从 `sys._MEIPASS/data/char_templates`（只读临时目录）加载
3. 发现路径不一致：保存到app_dir，加载从_MEIPASS

### 根因
`_load_char_templates` 在打包（frozen）时硬编码从 `_MEIPASS` 加载，而 `_MEIPASS` 是PyInstaller的只读临时目录，只包含打包时内置的默认模板。用户保存的模板在 `app_dir()/data/char_templates`，永远不会被加载。

### 解决方案
修改 `_load_char_templates`，优先从 `CHAR_TEMPLATE_DIR`（app_dir，用户保存的可写目录）加载，若为空再从 `_MEIPASS`（内置默认）加载：
```python
# 1. 优先从 app_dir（用户保存的可写目录）加载
_scan_dir(CHAR_TEMPLATE_DIR)
# 2. 若 app_dir 没有模板，打包时从 _MEIPASS（内置默认）加载
if not self._char_templates and getattr(sys, 'frozen', False):
    _scan_dir(os.path.join(sys._MEIPASS, "data", "char_templates"))
```
提交：0e05b0c

### 踩过的坑
- PyInstaller打包后，`_MEIPASS` 是只读临时目录，不能用于保存用户数据
- 用户数据必须保存到 `app_dir()`（exe所在目录）或 `%APPDATA%`
- 加载和保存路径必须一致，否则保存了也加载不到
- 打包后路径问题是常见bug，必须检查所有 `resource_path()` 和 `sys._MEIPASS` 的使用

---

## 004 - 基点光圈拖动距离被限制在400（2026-08-15）

### 关键词
基点、光圈、自动校准、X倍率、Y倍率、拖动距离、偏移限制、green_offset、blue_offset、蒙板、overlay、_auto_calib、stage=1、绿圈、蓝圈、400码、拉不远

### 现象
- 自动校准X/Y倍率时，stage=1 阶段基点旁的光圈（X=绿圈，Y=蓝圈）可以拖动
- 以前可以拖动很远（超过400像素），现在最多只能拖到约400像素距离
- 默认偏移已从200改为400（v24），但用户手动拖动时仍有上限
- 红色基点（红点+"基"字）显示在角色头顶上方，Y坐标可能偏高
- 用户反馈"以前是可以拉长很远了，现在四百是极限了"

### 排查过程
1. 检查默认偏移值 `_auto_calib_green_offset` / `_auto_calib_blue_offset` → 原为200，已改为400
2. 检查蒙板拖动检测逻辑（WM_MOUSEMOVE）→ 用 GetCursorPos 取屏幕坐标，更新偏移值，无范围限制
3. 检查蒙板窗口大小 → 蒙板窗口=游戏窗口大小，拖出窗口后WM_MOUSEMOVE不再触发
4. 检查主循环全局拖动检测（GetAsyncKeyState）→ **发现坐标不匹配bug**

### 根因
**真正根因：蒙板窗口 WM_LBUTTONDOWN 没有调用 SetCapture**，鼠标拖出绿圈区域/窗口后 WM_MOUSEMOVE 不再触发：
1. 蒙板窗口用颜色键透明（LWA_COLORKEY），洋红色区域透明，鼠标事件穿透到下层窗口
2. 点击绿圈（非透明区域）时 WM_LBUTTONDOWN 能触发，设置 _auto_calib_dragging
3. 拖动时鼠标移出绿圈，进入透明区域，WM_MOUSEMOVE 停止触发
4. 主循环全局检测（GetAsyncKeyState）理论上能接管，但坐标不匹配（v25已修）且不够可靠
5. 游戏窗口宽度约400像素，所以X方向最多拖约400像素

**次要问题（v25已修）**：主循环全局拖动检测中，鼠标坐标被错误地从屏幕坐标转成窗口坐标，导致偏移计算错误。

### 解决方案
1. **v25**：主循环全局拖动检测直接用屏幕坐标，不转窗口坐标
2. **v26（真正修复）**：蒙板窗口 WM_LBUTTONDOWN 时调用 `SetCapture(hwnd)`，WM_LBUTTONUP 时调用 `ReleaseCapture()`。这样拖动全程 WM_MOUSEMOVE 都能收到，不受窗口边界/透明区域影响
3. 小地图左边裁切 LEFT_OFFSET 6→3，把被切掉的地图部分包全

### 踩过的坑
- 默认偏移200和注释"400"不一致，已统一为400
- 屏幕坐标和窗口坐标混用是隐蔽bug
- **Win32透明窗口拖动必须 SetCapture**，否则鼠标移出非透明区域后事件穿透，WM_MOUSEMOVE 停了
- 蒙板窗口大小=游戏窗口大小，但 SetCapture 后可以突破窗口边界接收鼠标消息

### 关联提交
- 7919683（v24：偏移200→400）
- v25（全局拖动坐标修复）
- v26（SetCapture 真正修复 + 小地图左边裁切）
- 关联文件：maple_route_ui.py 蒙板 wnd_proc（WM_LBUTTONDOWN/WM_MOUSEMOVE/WM_LBUTTONUP）

---

## 007 - v33启动崩溃 NoneType object is not subscriptable（2026-08-28）

### 关键词
map_area_rect、NoneType、is not subscriptable、_detect_minimap、防抖、初始化、__init__、启动崩溃、Unhandled exception

### 现象
- v33启动后立即弹出"Unhandled exception in script"
- 报错：TypeError: 'NoneType' object is not subscriptable
- 堆栈：maple_route_ui.py line 8008 → line 7743 run → line 1184 _detect_minimap
- 从完整备份恢复config/data后仍崩溃

### 根因
- `__init__` 第552行：`self.map_area_rect = None`（初始化为None）
- `__init__` 第558行：立即调用 `self._detect_minimap()`
- `_detect_minimap` 第1183行：`old = self.map_area_rect`（old是None）
- 第1184行：`old["left"]` → 对None做下标访问 → 崩溃
- 防抖逻辑未做None检查，首次调用（map_area_rect还没值）就崩溃

### 解决方案
在防抖逻辑前加None检查，首次检测（old为None）跳过防抖直接更新：
```python
old = self.map_area_rect
if old is not None and (abs(old["left"] - new_map["left"]) <= 3 and ...):
    return
```
提交：v34

### 踩过的坑
- 初始化变量为None后，所有访问该变量的地方都要做None检查
- "完整备份"不代表源码没问题，备份的源码可能本身就有bug
- 回滚源码时要检查回滚版本是否有已知问题，不能盲目相信备份
- 启动崩溃优先看__init__的初始化顺序和首次调用路径

### 关联提交
- v33（崩溃版本，d10787b源码+备份config）
- v34（修复None检查）
- 关联文件：maple_route_ui.py _detect_minimap（约1182行）

---

## 工作记录规范

### 每次解决问题后必须记录
1. 问题现象（用户描述+截图）
2. 排查过程（试过哪些方法，哪些无效）
3. 根因（精确到代码行/逻辑）
4. 解决方案（具体改了什么）
5. 踩过的坑（避免下次重复）
6. 关联提交/文档

### 记录格式
- 编号递增
- 日期精确到天
- 状态：已解决/排查中/待验证

---

## 005 - 蒙板所有绘制偏上（加药检测框、人物基点、引导文字）（2026-08-28）

### 关键词
蒙板、overlay、SetWindowOrgEx、坐标原点、屏幕坐标、客户区坐标、截图坐标系、偏上、加药检测框、hp_marker、mp_marker、人物基点、_auto_calib_base、引导文字、校准提示、GDI绘制、坐标转换

### 现象
- 加药检测框（HP/MP血条标记）偏上，不在实际血条位置
- 人物基点（红色圆点+"基"字）偏上，在角色头顶上方而不是角色身上
- 倍率校准引导文字不显示（v27修复后显示了，但位置也偏上）
- 用户反馈"我感觉是你的蒙板位置偏上了，所以药，人物上的基点，都偏上"
- 用户反馈"本来都是对上的，你一调就全部乱套了"

### 排查过程
1. 检查蒙板窗口定位 `SetWindowPos(hwnd, -1, wr['left'], wr['top'], wr['width'], wr['height'])` → 位置和大小正确，和游戏窗口一致
2. 检查 `_update_window_rect` → 用 `GetWindowRect` 获取整个窗口坐标（含标题栏），正确
3. 检查 `_capture_window` → 截图用 `window_rect`，整个窗口坐标系，正确
4. 检查 `FIXED_BAR_Y = 782` → 血条Y固定值，在截图坐标系中正确
5. **关键发现**：蒙板 WM_PAINT 中调用了 `SetWindowOrgEx(hdc, wr['left'], wr['top'], None)`，把GDI逻辑坐标原点设为窗口屏幕左上角
6. SetWindowOrgEx 的作用：逻辑坐标 (x,y) → 设备坐标 (x-wr['left'], y-wr['top'])
7. 但所有绘制坐标（hp_marker、基点、绿圈等）都是**截图坐标系**（以游戏窗口左上角为原点），不是屏幕坐标
8. 所以截图坐标 (hx, hy) 绘制在设备坐标 (hx-wr['left'], hy-wr['top'])，这是负数或很小的值 → 全部偏上偏左

### 根因
`SetWindowOrgEx(hdc, wr['left'], wr['top'])` 假设后续绘制用**屏幕坐标**，但实际所有绘制用的是**截图坐标系**（以游戏窗口左上角为原点 = 蒙板客户区坐标系）。坐标系统不匹配导致所有绘制偏上偏左了 `wr['top']` 和 `wr['left']`。

### 解决方案
去掉 `SetWindowOrgEx` 调用，蒙板客户区坐标 = 截图坐标系，所有绘制直接用截图坐标：
```python
# 删除：
gdi32.SetWindowOrgEx(hdc, _wr['left'], _wr['top'], None)
# 引导文字也改回客户区坐标：
txt_x = max(10, rect.right // 2 - 280)
txt_y = 15
```
提交：v30

### 踩过的坑
- SetWindowOrgEx 改变了整个HDC的坐标映射，不是只影响某一个绘制
- 截图坐标系 ≠ 屏幕坐标系，截图坐标系以窗口左上角为原点，屏幕坐标以屏幕左上角为原点
- 蒙板客户区坐标系 = 截图坐标系（因为蒙板定位在窗口左上角），所以不需要 SetWindowOrgEx 转换
- 改坐标系统时要检查所有绘制点的坐标来源，不能只改一个
- "本来都是好的，一调就乱"通常是坐标系统被改了，要先查坐标映射

### 关联提交
- v30（去掉SetWindowOrgEx，统一用截图坐标系）→ **已回滚**，因为导致光圈拖动失效（见006）
- 正确修复方向：保留SetWindowOrgEx，把hp_marker/mp_marker坐标从截图坐标转屏幕坐标（加wr['left'],wr['top']）
- 关联文件：maple_route_ui.py 蒙板 wnd_proc WM_PAINT（约5497行）

---

## 006 - 去掉SetWindowOrgEx导致光圈拖动失效（2026-08-28）

### 关键词
SetWindowOrgEx、回滚、光圈拖动、WM_MOUSEMOVE、坐标系统、副作用、删除前未评估全局影响、蒙板、overlay、校准基点、auto_calib

### 现象
- v30去掉SetWindowOrgEx后，检测框位置正确了
- 但校准光圈（绿圈/蓝圈）无法拖动，点击没反应
- 用户反馈"你去掉后，位置是对了，但是光圈没有办法移动了"

### 根因
- SetWindowOrgEx不仅影响WM_PAINT绘制，还影响WM_MOUSEMOVE的坐标处理
- 光圈拖动逻辑中，GetCursorPos获取屏幕坐标，结合SetWindowOrgEx的偏移计算点击位置
- 去掉SetWindowOrgEx后，屏幕坐标和客户区坐标不匹配，点击检测落在错误位置，光圈拖不动
- **教训**：删除/修改全局坐标映射前，必须检查所有依赖该映射的代码路径（绘制+鼠标交互+定时器），不能只看绘制

### 解决方案
- 回滚到v24（7919683），恢复SetWindowOrgEx
- 正确修复检测框偏上的方向：保留SetWindowOrgEx，把hp_marker/mp_marker从截图坐标转屏幕坐标（加wr['left'],wr['top']）
- 待用户确认后实施

### 踩过的坑
- "哪里痛治哪里"是大忌：去掉SetWindowOrgEx解决了绘制偏上，但破坏了鼠标交互
- 修改全局状态（坐标映射、窗口样式、定时器）前，必须grep所有引用点
- 删除代码前必须经用户同意，特别是涉及坐标系统、消息处理、全局状态的代码
- 回滚是最安全的止损方式，不要在错误基础上继续修

### 关联提交
- v30（错误修改，已回滚）
- v31（回滚到v24的纯净打包版）
- 回滚目标：7919683（v24）

---

## 009 - 打包后灰屏/UI不渲染（2026-08-28）

### 现象
- 打包后运行，OpenCV窗口显示纯灰色，UI不渲染
- 控制台有正常日志（模板加载、窗口绑定等），无报错
- 源码直接运行正常

### 根因（两个叠加问题）
1. **UI背景图加载失败**：用`cv2.imread(p)`加载，打包后`_MEIPASS`路径异常导致加载失败，fallback用`np.ones*200`灰色填充
2. **map_area_rect被重置**：`__init__`中自动绑定后`_detect_minimap()`设置了`map_area_rect`，但第894行又`self.map_area_rect = None`重置，主循环访问异常导致UI无法渲染

### 解决方案
1. UI背景图加载：`cv2.imread` → `load_png`（`np.fromfile`+`cv2.imdecode`，支持任意路径），加4通道转3通道
2. map_area_rect：`__init__`开头初始化`self.map_area_rect = None`，删除后部的重置（会覆盖自动绑定的检测结果）

### 踩过的坑
- 灰屏不一定是DLL/资源问题，可能是主循环异常
- `cv2.imread`不支持中文路径/特殊路径，`load_png`才是安全的
- 恢复完整备份源码后，仍需检查是否有已知BUG被重新引入（如map_area_rect重置）
- 工作记录是第一参考，遇到问题先搜记录中的关键词

### 验证方法
- 控制台显示`[UI背景] 加载成功: ui_bg_blank.png`说明背景图加载正常
- 控制台显示`Map area: 158 x 9`（而非"未检测到"）说明map_area_rect正常
- 窗口正常显示UI界面

### 关联提交
- v44（修复版，可正常显示）
- 提交：2138657

### 关键词
灰屏、UI不渲染、cv2.imread、load_png、map_area_rect、_detect_minimap、__init__重置、打包黑屏

---

## 010 - 后期维护踩坑总结与回滚29号基线（2026-09-01）

### 关键词
后期维护、另起炉灶、蒙板Overlay、WS_EX_TOOLWINDOW、人物框消失、YOLO模型打包、闪退诊断、回滚29号、数据竞争、SetWindowPos、faulthandler

### 现象
从29号版本（1746d82，稳定基线）到9月1日，约60个提交，新增人物定位框、绿框钳制、人物特征10张、偏移反馈黄点、镜头死区检测、F3偏移面板、合并蒙板、坐标统一、YOLO模型处理、闪退诊断等功能。期间出现：
1. 蒙板Overlay任务栏按钮"不停地跳"（反复出现消失）
2. 加WS_EX_TOOLWINDOW后人物框（绿色定位框）消失
3. 打包后控制台疯狂滚"未找到模型"，任务栏两窗口交替跳
4. v91闪退无任何错误日志
5. 镜头死区检测反复改截图方式十几版仍不稳定

### 排查过程
1. **Overlay跳**：对比29号蒙板样式（WS_EX_LAYERED|WS_EX_TOPMOST）与当前一致，排除样式问题；发现蒙板线程异常退出后finally重置running=False，主循环自动重启→反复创建窗口→任务栏按钮跳
2. **人物框消失**：v93加WS_EX_TOOLWINDOW后人物框不显示，回滚TOOLWINDOW后恢复；确认蒙板绘制依赖原有样式
3. **模型缺失**：检查dist目录发现best.onnx不在，spec未打包模型；模型缺失时每帧检测路径各print一次约50Hz
4. **闪退无日志**：发现主循环except Exception: pass吞异常；加faulthandler.enable()抓native栈
5. **YOLO误判**：Read工具显示ultralytics代码丢了[0]，误判格式不对；300帧真实画面测试确认forward()[0]+NMS正确

### 根因
**核心根因：后期维护中没有沿用既有结构，另起炉灶导致与前面脱节。**
1. 改窗口样式前没通读蒙板创建→WM_PAINT→SetWindowPos完整链路
2. 新增镜头死区检测没通读原有_capture_window和蒙板线程架构
3. 打包后没验证dist目录完整性
4. 用except: pass掩盖问题而非修根因
5. Read工具显示不完整时凭显示下结论，没用脚本完整输出确认

### 解决方案
1. **回滚到29号干净基线**（1746d82），后续功能按用户确认优先级逐个加回
2. **蒙板样式锁定**：WS_EX_LAYERED|WS_EX_TOPMOST+WS_POPUP，不加TOOLWINDOW
3. **SetWindowPos几何去重**：只在窗口位置/大小变化时调用，不每帧置顶
4. **模型打包**：spec加入best.onnx，缺失时只提示一次+短路标志
5. **异常可追溯**：faulthandler.enable()+异常打印日志，禁止静默吞
6. **蒙板线程异常**：打印详细日志，重启加冷却/次数限制

### 踩过的坑
1. **加WS_EX_TOOLWINDOW修任务栏跳，把人物框搞没了**：蒙板绘制依赖原有样式，改样式前必须通读完整链路
2. **YOLO模型没打进包，任务栏疯狂跳**：打包后必须验证dist/data+模型完整
3. **误判YOLO代码有bug**：Read工具可能丢字符，关键代码用脚本完整输出确认
4. **except: pass静默吞异常，闪退无日志**：所有异常必须可追溯
5. **后期新增功能不看前面结构，反复修补越走越远**：新增功能前先Grep同模块代码，理解调用链
6. **蒙板线程异常退出后自动重启，Overlay反复出现**：线程+自动重启必须考虑异常风暴

### 蒙板窗口正确构造（已补充到功能实现指南2.9）
1. 创建：CreateWindowExW(WS_EX_LAYERED|WS_EX_TOPMOST, className, "Overlay", WS_POPUP|WS_VISIBLE, 0,0,100,100, None,None,hinst,None)
2. 透明：SetLayeredWindowAttributes(hwnd, COLOR_MAGENTA, 0, LWA_COLORKEY)
3. WNDPROC：必须用列表保留引用（self._wnd_proc_ref=[WNDPROC(proc)]），防GC回收
4. WM_PAINT：BeginPaint→GDI绘制→EndPaint，画笔/画刷必须DeleteObject释放
5. 定位：SetWindowPos几何去重，只在窗口位置/大小变化时调用
6. 线程：独立线程_win32_overlay_loop，PeekMessage+sleep(0.05)，running标志控制
7. 数据：主线程写成员变量，蒙板线程读，注意数据竞争
8. **样式禁忌**：不加WS_EX_TOOLWINDOW（导致绘制异常），不改WS_POPUP

### 小地图光点锁定补充（已补充到方案文档第十章）
1. 窗口尺寸用整个窗口（1382×807），不是客户区（误用客户区导致基点偏头顶）
2. map_area_rect在__init__开头初始化None，后部不能再重置（覆盖自动检测结果导致灰屏）
3. 光点ffff88纯色，像素数少时不稳，绿框钳制左4右5上3下3
4. YOLO模型必须打进包，缺失时只提示一次+短路

### 经验感想
1. 后期项目最忌另起炉灶——每加功能先花10分钟通读同模块代码
2. 窗口样式/线程模型/坐标体系是全局基础设施，改前必须评估影响
3. 打包后验证三件事：dist/data完整、模型存在、exe无报错
4. 改坏了立即回滚，不要在错误基础上继续补
5. Read工具显示可能不完整，关键代码用脚本完整输出确认

### 验证方法
1. 回滚后git log确认HEAD=1746d82
2. 运行源码确认所有29号原有功能正常（小地图、光点、校准、平台录制）
3. 打包后验证dist/data完整、best.onnx存在
4. 蒙板Overlay不跳、人物框正常显示

### 关联提交
- 29号基线：1746d82（2026-08-29 23:16）
- 回滚TOOLWINDOW：8d4941b
- 各次提交见git log 1746d82..HEAD

---

## 版本迭代明细（v25-v29）

### v25 - 主循环全局拖动坐标修复（2026-08-28）
- **改动**：主循环中检测光圈拖动时，不依赖蒙板WM_MOUSEMOVE，改用GetAsyncKeyState(VK_LBUTTON)全局检测鼠标按下状态，配合GetCursorPos获取屏幕坐标，拖出窗口后仍能继续拖动
- **目的**：解决基点光圈拖动距离被限制在400（拖出窗口后WM_MOUSEMOVE停止）
- **结果**：基点可拖到超过400，但坐标转换可能有偏差（屏幕坐标vs客户区坐标）
- **关键词**：GetAsyncKeyState、全局拖动、WM_MOUSEMOVE、拖出窗口、基点、光圈

### v26 - SetCapture + 小地图LEFT_OFFSET=3（2026-08-28）
- **改动1**：蒙板WM_LBUTTONDOWN中调用SetCapture(hwnd)，鼠标拖出窗口后仍能收到WM_MOUSEMOVE，WM_LBUTTONUP中调用ReleaseCapture
- **改动2**：小地图三点识别中LEFT_OFFSET=3，把小地图显示范围向右扩3px
- **目的**：修复光圈拖动（SetCapture是标准做法）+ 小地图显示不全
- **结果**：光圈拖动修复，但小地图右边被切（LEFT_OFFSET方向搞反了，应该向左扩）
- **关键词**：SetCapture、ReleaseCapture、WM_LBUTTONDOWN、WM_MOUSEMOVE、小地图、LEFT_OFFSET、三点识别

### v27 - 引导文字屏幕坐标修复 + LEFT_OFFSET=0（2026-08-28）
- **改动1**：倍率校准引导文字在SetWindowOrgEx(屏幕坐标原点)下，文字坐标改用屏幕坐标（wr['left']+x, wr['top']+y），解决引导文字不显示
- **改动2**：LEFT_OFFSET=0，回滚v26的小地图偏移（因为方向搞反了）
- **目的**：修复引导文字不显示 + 回滚错误的小地图偏移
- **结果**：引导文字显示了，小地图回到原始状态
- **关键词**：引导文字、SetWindowOrgEx、屏幕坐标、客户区坐标、TextOutW、倍率校准

### v28 - LEFT_OFFSET=0纯净版（2026-08-28）
- **改动**：仅LEFT_OFFSET=0，无其他改动
- **目的**：纯净测试版，确认小地图原始状态
- **结果**：小地图左边仍被切（需要向左扩）
- **关键词**：小地图、LEFT_OFFSET、纯净版

### v29 - 小地图LEFT_OFFSET=-3（2026-08-28）
- **改动**：小地图三点识别中LEFT_OFFSET=-3，把小地图显示范围向左扩3px
- **目的**：修复小地图左边被切（用户反馈左边显示不全）
- **结果**：小地图左边稍微好了一点，但还需要再调
- **关键词**：小地图、LEFT_OFFSET、向左扩、三点识别、minimap、显示不全

### v50 - 蓝色框校准功能 + 光点锁定蓝框内归一化（2026-08-28）
- **改动1**：新增蓝色框校准功能（F4触发），在小地图上点击四个角粗定位，方向键微调，S保存
- **改动2**：lock_screen_from_dot() 修改为蓝色框内归一化（已校准时），未校准回退旧方案（整个小地图归一化）
- **改动3**：小地图上实时显示蓝色框（校准模式显示四个角点，正常模式以光点为中心画框，到边贴边）
- **改动4**：蓝色框配置保存到 data/blue_box_config.json，启动时自动加载
- **目的**：解决光点锁定到边后不准的问题（旧方案用整个小地图归一化，到边时光点在小地图边缘但人物在屏幕边缘，比例不对）
- **原理**：蓝色框=一屏范围在小地图上的对应大小，光点在蓝色框内的归一化位置=人物在屏幕内的归一化位置。到边时蓝色框贴边，光点在框内继续移动，始终有效
- **校准方法**：F4进入校准模式→依次点击小地图四个角（左上→右上→右下→左下）→点击角点选中→方向键微调→S保存
- **结果**：v50打包成功，待用户测试校准效果
- **关键词**：蓝色框、校准、F4、光点锁定、归一化、lock_screen_from_dot、到边不准、一屏范围、blue_box_config.json
- **关联提交**：631ae5f
- **关联文件**：maple_route_ui.py（_load_blue_box/_save_blue_box/_start_blue_box_calibration/_handle_blue_box_click/_handle_blue_box_key/_draw_blue_box/lock_screen_from_dot）

---

## 待修复问题清单（v31基线）

| 编号 | 问题 | 优先级 | 修复方向 |
|------|------|--------|----------|
| P1 | 检测框偏上（hp_marker/mp_marker） | 高 | 保留SetWindowOrgEx，把检测框坐标从截图坐标转屏幕坐标（加wr['left'],wr['top']） |
| P2 | 小地图左边显示不全 | 中 | LEFT_OFFSET调为负值（向左扩），需测试-3/-6/-9 |
| P3 | 加药检测框Y轴固定值 | 低 | FIXED_BAR_Y=782是否正确，需确认游戏窗口高度 |
| P4 | 人物基点位置偏上 | 高 | 同P1，坐标系统问题 |

---

## 【记录003】v73 打包灰屏(窗口未响应)排查与修复（2026-08-31）

### 现象
- v70/v72 打包后 OpenCV 主窗 PLAY AND HAPPY 灰屏、标题栏显示未响应；v69 正常；源码 python 直接运行正常。
- 用户判断不是每帧检测的问题，后证实正确。

### 排查过程（全部有实测证据）
1. 源码运行 / 模拟 frozen 环境：__init__ 0.42s 完成，复现不出灰屏。
2. 逐阶段计时：单帧各步 0~150ms，远低于 Windows 约5秒的未响应阈值，排除每帧检测导致卡。
3. git diff v69(e4052d5)到v72：仅4处安全改动（注释自动备份线程、绿框偏移改 getattr 等价、F3热键/面板不按不触发、tk事件泵加 None 判断），不按F3时运行逻辑与v69等价。
4. v69/v72 的 _internal 共1147个文件逐个比大小：0差异，排除缺文件、UPX压坏DLL。
5. 对照《打包黑屏问题排查》历史根因逐一核查：map_area_rect 在__init__开头(557行)初始化无后部重置、_ui_bg 用 load_png 且4通道转3通道、data 用 spec 绝对路径打包，均未踩老坑。
6. 查到 Windows Defender 实时保护=开启（系统自带，用户以为没开杀毒），冷启动会实时扫描首次加载的DLL。

### 根因（推测，置信度较高）
- 启动竞态：run() 在 namedWindow(8152) 建好窗口后，要跑完第一帧全部截图/检测/draw，到 waitKey(8404) 才第一次给窗口泵Windows消息。打包冷启动（PyInstaller解包 + Defender扫描几十个DLL）把这段拖过约5秒，窗口被系统判未响应置灰。
- 能解释全部现象：偶发、换名/重打有时就好(缓存热)、源码不复现(不经bootloader)、与检测频率无关、v70控制台正停在主循环第一帧。
- 附带真实缺陷：可写配置用 app_dir()=exe同级data 读取(REGION_FILE等245-259行)，但打包json在 _internal/data；新版首次运行exe同级data为空，读不到 minimap_region.json 等，走 _detect_minimap 自动检测，游戏窗口最小化(GetWindowRect=-32000)时检测成错误的 158x9。

### 解决方案（v73，3处代码/打包 + 1系统设置，均不碰业务逻辑）
1. run() 的 namedWindow 后、while 前，先 cv2.imshow(win,self._ui_bg)+cv2.waitKey(1)，让窗口创建即渲染并泵一次消息，之后再跑首帧检测。(_ui_bg 已确认恒为3通道)
2. spec 改 upx=False 关闭压缩（EXE和COLLECT两处）。
3. 打包后把 _internal/data 的10个 json 复制到 exe同级 data，首次运行直接读到已校准 region(128x116)，不依赖自动检测。
4. 系统：Add-MpPreference -ExclusionPath 把 maple_bot 目录加入 Defender 排除项（需管理员，已操作）。

### 验证
- py_compile 语法0通过；模拟frozen(mock sys.executable=v73.exe)首次直接读到 region 128x116 PASS(错误时为158x9)；真实GUI namedWindow+imshow(_ui_bg)+waitKey 渲染PASS无异常；zip 1170文件齐全(exe/data配置/_internal资源)。
- 状态：待用户真机验证是否还灰屏。

### 踩过的坑 / 教训
- 模拟frozen时 app_dir() 依赖 sys.executable：源码运行 sys.executable=python.exe，必须手动把 sys.executable 指到 v73.exe 才能正确模拟 exe同级data 路径，否则会误判配置读不到。
- 窗口最小化时 GetWindowRect 返回 left/top=-32000、尺寸异常(160x28)，是Windows特征，排查时注意区分。
- Defender 是系统自带，用户说没开杀毒时仍要查 Get-MpComputerStatus。
- Edit 工具对 UTF-8 BOM/CRLF 文件会 Native execution failed，改用Python按字节替换并写回BOM。

### 关联
- 改动前快照 4993c90，完成提交 6a79456；spec=MapleBot_v73.spec；产物 dist/MapleBot_v73.zip(79.3MB, onedir, console=True)。
- 关键词：灰屏、未响应、启动竞态、waitKey消息泵、namedWindow首帧、Defender实时扫描、UPX、exe同级data、_MEIPASS、app_dir、冷启动、-32000最小化


---

## 【记录004】v75 人物特征偏移失效修复 + 偏移点偏上待解决（2026-08-31）

### 现象1（已修复）
- UI上X偏移/Y偏移输入框(char_x_offset/char_y_offset)调了没反应，蒙板上人物绿色框不动；以前调了会有个点展示出来，现在没有了。
- 用户截图显示X偏移=20、Y偏移=20，但蒙板绿色框位置不随偏移变化。

### 排查过程
- 读_get_player_screen_pos(7437行)：优先用lock_screen_from_dot()光点锁定，成功后直接return pos，完全不加偏移；偏移只在回退到_match_character模板匹配时才加(原7452-7454行)。
- 现在光点锁定优先且总成功(小地图光点稳定)，所以偏移永远不生效，UI输入框调了白调。
- 蒙板上绿色框用的是另一套偏移_char_box_offset_x/y(F3面板,默认39/78)，在蒙板绘制时加(5806-5807行)，所以绿色框能动，但和UI的char_x_offset/y是两套独立偏移。

### 根因1
- 偏移应用路径不完整：char_x_offset/char_y_offset只覆盖了模板匹配回退路径，光点锁定优先路径被跳过。光点锁定总成功时偏移永远不生效。

### 解决方案1（v75，只改_get_player_screen_pos一个函数）
- 偏移读取提到函数开头：x_off/y_off = _field_values.get("char_x_offset"/"char_y_offset")
- 所有返回路径统一加偏移：光点锁定return (pos[0]+x_off, pos[1]+y_off)；模板匹配保持result=(mx+x_off, my+y_off)；宽限期return (last_pos[0]+x_off, last_pos[1]+y_off)
- _last_char_match_pos仍保存未加偏移的真实位置(供_match_character的ROI回退搜索用)，不影响匹配逻辑
- 别的一概不动

### 验证1
- 语法OK；打包v75成功(79.3MB, onedir, console=True, upx=False)；exe同级data 10个json配置齐全，region 128x116。
- 状态：偏移已生效，但用户测试发现偏移点偏上头(见下方现象2)。

### 现象2（待解决，用户要求先分析不动代码）
- v75后偏移生效了，但偏移后的人物位置点/绿色框跑到了人物头上方，没有在人物点位(身体/脚)上。
- 用户截图：绿色框框住人物但中心偏上，rx/ry比例调试文字显示在框右上角。

### 根因2（分析，未实锤）
- 【主要】两套偏移叠加：UI的char_x_offset/y(20/20) + 蒙板F3的_char_box_offset_x/y(39/78) = 蒙板绿色框总偏移59/98，偏移过大导致偏上头。
- 偏移基准不匹配：char_x_offset/y=20/20原本针对模板匹配中心点设置，v75后作用于光点锁定，两者坐标基准(头/中心/脚)不同，偏移值直接套用就偏。
- 光点锁定坐标基准不明确：光点小地图比例换算回屏幕坐标，对应人物哪个点位(头/中心/脚)未明确定义，可能本身偏上。

### 可选解决方案2（待用户确认后实施，当前不动代码）
- 方案A【最直接】：统一偏移，去掉一套(要么UI的char_x_offset/y，要么F3的_char_box_offset_x/y)，避免叠加
- 方案B【更准】：光点锁定给大致位置 + 模板匹配在附近ROI精确搜索，取模板匹配结果(融合方案)
- 方案C：多模板匹配(保存10张不同角度模板取最高分，抗换装/换姿势)
- 方案D：明确光点锁定坐标基准，偏移值针对该基准重新校准

### 踩过的坑 / 教训
- 修复一个功能时要检查是否有重复/叠加的同类机制：char_x_offset/y和_char_box_offset_x/y是两套独立偏移，修复前者生效后导致两者叠加，反而引入新问题。
- 偏移值是针对特定坐标基准的，换了基准(模板匹配→光点锁定)后偏移值不能直接套用，需要重新校准。

### 关联
- 代码提交：b8bb4c2（v75偏移修复）；spec提交：2286357（v75 spec）；回滚点：cf2fb98（v74）
- 产物：dist/MapleBot_v75.zip(79.3MB, onedir, console=True)
- 关键词：人物特征偏移、char_x_offset、char_y_offset、光点锁定、模板匹配、偏移失效、偏移叠加、_char_box_offset、_get_player_screen_pos、蒙板绿色框


---

## 【记录005】人物特征10张修复：切换方案覆盖为1张的bug（2026-09-01）

### 现象
- 用户反馈人物特征只能保存1张，要求改成保存10张。
- 代码中 CHAR_MAX_TEMPLATES=10、_capture_character_feature 已实现10张滚动保存、_load_char_templates 已实现扫描加载10张，但实际运行时切换方案后只剩1张。

### 根因
- 人物特征有两套存储机制互相冲突：
  1. **磁盘文件机制（10张，正确）**：_capture_character_feature 保存到 data/char_templates/char_<id>.png，新ID递增，超10删最旧；启动时 _load_char_templates 扫描加载最多10张。
  2. **方案配置base64机制（1张，旧机制）**：_save_calib 只取 _char_templates[-1] 最后1张转base64存到 route_XXX_calib.json 的 char_template_b64 字段；_switch_route 切换方案时从该字段读取1张，直接覆盖 self._char_templates = [单张]，导致10张变1张。
- 冲突链路：启动加载10张 → 保存特征追加到磁盘和内存 → 切换方案(F12) → 从方案配置读1张base64覆盖内存 → 只剩1张。

### 解决方案（v94）
1. **_switch_route（原1639-1649行）**：删除从方案配置加载人物特征并覆盖内存的逻辑。人物特征是全局的（识别自己角色用，和地图方案无关），切换方案时保持磁盘加载的10张不动。
2. **_save_calib（原3534-3540行）**：保留 char_template_b64 字段写入（兼容旧方案文件），但加注释说明仅作兼容备份，不再作为加载来源。
3. 权威存储统一为磁盘 data/char_templates/char_<id>.png（10张滚动），启动时由 _load_char_templates 加载。

### 验证
- 语法检查通过（py_compile）。
- 打包v94成功（onedir, console=True, upx=False）。
- dist/MapleBot_v94/_internal/data 完整（UI图片、json配置、templates）。
- exe同级data已创建并复制10个json配置，char_templates目录已创建。
- best.onnx模型已打包。
- 状态：待用户真机验证切换方案后人物特征保持10张。

### 踩过的坑 / 教训
- 新增功能时要检查是否有旧的并行机制：磁盘文件和方案配置base64两套人物特征存储，新机制做对了但旧机制的加载路径没删，导致切换时覆盖。
- 人物特征是全局数据（识别自己角色），不应该随方案切换而变化；方案相关的数据才需要随方案加载。
- 修复"只能存N张"类问题时，要同时检查保存路径和加载/覆盖路径，不能只看保存逻辑。

### 关联
- 改动前快照：03c32d1
- 完成提交：6f017a2
- spec：MapleBot_v94.spec
- 产物：dist/MapleBot_v94/（onedir, console=True）
- 关键词：人物特征、10张、CHAR_MAX_TEMPLATES、_switch_route、char_template_b64、方案切换、覆盖、全局数据、磁盘存储


---

## 011. 2026-09-01 方法论踩坑：改动前没看清来龙去脉，只盯当前功能，牵一发而动全身（最高优先教训）

### 关键词
改动前影响面分析、来龙去脉、牵一发而动全身、最小增量、列表vs字典、引用点、数据流向、后期项目、越修越远

### 现象
把人物特征从"保存最后1张"改成"保存最后10张"这么一个小需求，反复改了多轮仍"一张也存不了"，期间还一度把文件改出语法错误、把原本正常的链路改坏，用户多次批评"项目后期了还单独写东西、不看前面结构、脱节、越修越远"。

### 根因（方法论层面，比代码bug更重要）
1. **没先看清原代码完整链路就动手**：_char_templates 是一个被"框选保存 / 启动加载 / F4方案保存 / 切换方案 / UI下拉 / 模板匹配"多处共用的列表，只盯着"保存"这一个函数改，没Grep全部引用点。
2. **没搞清数据类型**：F4保存里 tpl=self._char_templates（列表）却用 tpl["img"]（字典索引），列表非空时必抛异常——这才是"存不了"的真正根因之一，却一直在改无关的磁盘逻辑。
3. **照搬后期版本(v76/v94)重写，而非在29号稳定基线上做最小增量**：引入新ID递增、_resave重排、自动滚动、改切换方案等一堆"看起来更完整"的改动，每多改一处就多一个出错点，连锁反应。
4. **改A坏B**：顺手改了切换方案、方案保存，破坏了原本正常工作的部分（倍率方案用户反复强调不能动）。

### 正确做法（已固化为 AGENTS.md 第一章"第0步 影响面分析"铁律）
改任何一处前，先回答清楚：
1. 这个函数/变量/数据结构被谁调用、谁读写（Grep全部引用点），数据从哪来、到哪去；
2. 它是什么数据类型/结构（列表还是字典、单对象还是集合），改后所有使用点是否仍成立；
3. 牵连哪些模块链路（保存/加载/切换/UI/匹配/打包），会不会牵一发而动全身；
4. 后期项目优先在原实现上做最小增量，能改1行不重写1个函数，不另起平行实现；
5. 拿历史正常版本(git show旧commit)逐行对比，只动需要变的那几行，其余一字不改。

### 踩过的坑 / 教训（强制记忆）
- **先搞懂"它现在是怎么完整跑通的、都连着谁"，再决定"最小改哪里"，严禁只看当前功能就下手。**
- 一个"存不了"的现象，根因可能在共用数据结构的另一个使用点，不要在表象位置反复改。
- 稳定基线之上做扩展 = 最小增量；推倒重写 = 后期项目最大风险。
- 用户没让动的正常链路（尤其倍率），一字不动。
- 命令行内联Python转义易出错，改代码优先用独立补丁脚本文件，且每次改完立即py_compile。

### 验证方法
- 改动前先输出"影响面清单"（引用点+数据类型+牵连链路+最小改动行）给用户确认，再动手。
- 纯逻辑（如10张滚动增删）脱离UI写独立小脚本模拟验证，再打包。

### 关联
- 规则固化：AGENTS.md "一、改动前必须做" 第0步、第2/3/4条强化
- 相关代码记录：本文件005号（人物特征两套存储机制冲突）
- 基线commit：1746d82（29号稳定版）

---

## 【记录012】2026-09-01 打包后启动未响应（UI渲染了但窗口标题显示未响应）

### 关键词
打包未响应、upx、Defender扫描、exe同级data、v73防灰屏、冷启动

### 现象
- 打包后exe启动，UI界面完整渲染出来了（不是纯灰屏），但窗口标题栏显示"PLAY AND HAPPY (未响应)"，鼠标点击/键盘操作无反应
- 之前的纯灰屏（UI没渲染）已由首帧泵消息修复（run() namedWindow后加imshow+waitKey），但这次UI渲染了仍未响应

### 根因（两个叠加）
1. **spec upx=True**：upx压缩的DLL在冷启动时需要解压，Windows Defender会扫描解压后的DLL内容，导致启动极慢（超过5秒被Windows判未响应）。v73(2026-08-31)已验证upx=False解决，但后续改spec时又改回了upx=True
2. **exe同级data目录缺失**：PyInstaller打包只把json放进_internal/data（只读资源目录），但app_dir()函数返回exe所在目录，程序从exe同级/data/加载可写配置（minimap_region.json、fight_potion_config.json等10个json）。exe同级没有data目录时，首运行读不到配置，走_detect_minimap()自动检测路径，增加启动时间和不确定性

### 解决方案
1. **MapleBot.spec改upx=False**（两处：EXE段和COLLECT段），和v73一致
2. **打包后手动复制**：把dist/MapleBot/_internal/data/*.json（10个）复制到dist/MapleBot/data/（exe同级），确保首运行直接读到已校准配置
3. 首帧泵消息修复保持不变（run() namedWindow后imshow+waitKey(1)）

### 踩过的坑（教训）
- **v73记录明确写了"打包后必须把json复制到exe同级data"，但后续多次打包都漏掉了这一步**——只看了代码改动，没看打包后的目录结构
- **spec改upx=True时没查v73记录**——以为upx压缩能减小体积，忽略了Defender扫描导致的冷启动未响应
- **UI渲染了≠启动成功**——首帧泵消息只保证第一帧渲染，后续帧如果DLL加载/配置加载被阻塞仍会未响应
- **打包规范必须固化到spec和流程里**，不能每次靠记忆

### 验证方法
- 启动exe后，窗口标题不显示"(未响应)"
- UI按钮可点击响应（切换标签页、点击运行等）
- 小地图直接显示已校准区域（不需要F9重新检测）

### 关联
- PROJECT_STATE.md → "2026-08-31 v73 防打包灰屏"区块
- MapleBot.spec（upx=False）
- 记录003（打包黑屏）、记录011（改动前影响面分析方法论）

---

## 【记录013】2026-09-01 人物特征保存10张全链路修复成功

### 关键词
人物特征、10张、shape解包、3通道、tuple比较、F4、切换方案、全链路

### 现象
- 29号基线人物特征固定保存1张，用户要求改为保存最新10张
- 多次尝试改10张时连锁改坏：越修越远、一张都存不了、切换方案覆盖为1张、F4存方案丢失人物特征
- 最终发现29号基线本身就有一批shape解包bug（3通道图时全部触发），1张能存是因为frame恰好是灰度2通道没触发

### 根因（全链路5处同源bug + 1处逻辑bug）
1. **_capture_character_feature的fh,fw=frame.shape**：3通道图shape=(h,w,3)解包到2变量报too many values to unpack，直接导致"截不了图，第一步就失效"
2. **_capture的ch,cw=captured.shape**：同上，保存模板时解包报错
3. **_match_character的roi.shape > 20**：tuple不能直接和int比较，Python3 TypeError，ROI回退路径报错
4. **_match_character的th > roi.shape**：同上，模板尺寸校验报错
5. **F4 _save_calib的tpl=self._char_templates**：把整个列表当字典用tpl["img"]，TypeError被外层except吞掉，char_b64永远None，F4存方案时人物特征丢失
6. **_switch_route从方案base64读1张覆盖内存**：人物特征是全局数据不随方案切，切换方案时被覆盖为1张

### 解决方案（最小增量，只动需要变的行）
1. 所有shape解包改shape[:2]（兼容3通道图）：frame.shape[:2]、captured.shape[:2]、timg.shape[:2]、thumb.shape[:2]
2. roi.shape比较改shape[0]/shape[1]：if roi.shape[0] > 20 and roi.shape[1] > 20、if th > roi.shape[0] or tw > roi.shape[1]
3. F4取最后一张：tpl = self._char_templates[-1]（取最新一张存base64，兼容旧方案文件）
4. _switch_route删除base64覆盖逻辑（人物特征全局不随方案切）
5. 10张实现：_load扫描char_*.png按ID取最新10张、_capture的new_id=max(existing_ids)+1递增不覆盖、超10删最旧、列表append

### 踩过的坑（教训）
- **不看全链路就下手**：只盯"保存"这一个函数改，没Grep全部引用点，_char_templates被F4/切换方案/UI/匹配多处共用，改一处牵一发而动全身
- **照搬后期版本重写**：不在29号稳定基线上做最小增量，而是照抄v76/v94的完整实现，引入新ID递增/_resave重排/自动滚动等一堆改动，每多改一处就多一个出错点
- **顺手改正常部分**：改保存时顺手改了切换方案、方案保存，破坏了原本正常工作的部分（倍率方案用户反复强调不能动）
- **Bash输出过滤[:2]**：工具输出会过滤掉[:2]和[0]等方括号索引，导致以为修复没生效反复重试，实际文件里是正确的。验证必须用Read工具直接读文件，不能只看Bash输出
- **Edit工具频繁Native execution failed**：可靠做法是Write临时.py脚本→Bash执行→删除，不用Edit工具做大段替换

### 验证方法
- 连存12次，内存和磁盘都留最新10张（ID2-11）
- F4存方案后重启，人物特征不丢失
- 切换方案后人物特征不被覆盖
- 打包后启动不灰屏/不未响应
- 蒙板黄点不运行时也显示

### 关联
- 记录003（人物特征保存后重启丢失）
- 记录005（切换方案覆盖为1张的bug）
- 记录011（改动前影响面分析方法论）
- 记录012（打包未响应：upx=True+exe同级data缺失）
- 总纲"五、人物特征保存方法和丢失原因"

---

## 【记录014】2026-09-01 输入框时灵时不灵修复成功

### 关键词
输入框、时灵时不灵、GetAsyncKeyState、0x8000、0x0001、漏键、轮询、_poll_num_input、_poll_key_capture

### 现象
- 打怪/药品页数字输入框和按键输入框，快速连续按键时偶尔有键输入不进去
- 时灵时不灵，没有固定规律
- 退格、回车、ESC等功能键也偶尔失灵

### 根因
\_poll_num_input()\ 和 \_poll_key_capture()\ 用 \GetAsyncKeyState(vk) & 0x8000\ 检测按键，只检查**最高位**（当前是否按下）。

主循环每帧调用一次轮询（约10-50ms间隔），如果用户按键速度很快（按下+释放在两次轮询之间完成），第一次轮询时键还没按下（最高位0），第二次轮询时键已经释放（最高位0），就会**漏捕获**这个键。

这就是"时灵时不灵"的根因——按键速度和轮询间隔的竞争。

### 解决方案
同时检查**最高位**和**最低位**：
\\\python
state = user32.GetAsyncKeyState(vk)
if state & 0x8000 or state & 0x0001:  # 当前按下 或 上次调用以来按下过（防漏键）
    current.add(vk)
\\\
- \ x8000\（最高位）：当前是否按下
- \ x0001\（最低位）：自从上次调用 \GetAsyncKeyState\ 以来，键是否被按下过

即使按键在两次轮询之间按下又释放，最低位也会记录"曾经按下过"，从而捕获到这个键。

### 涉及函数
- \_poll_num_input()\（行6418）：数字输入轮询，支持主键盘0-9、小键盘0-9、退格、回车、ESC、负号、小数点
- \_poll_key_capture()\（行6395）：按键录入轮询，支持F1-F12、Ctrl、Shift、Home、End等所有键

### 改动范围
只改了2处，每处3行：
1. \_poll_key_capture\ 里的 \GetAsyncKeyState\ 检测
2. \_poll_num_input\ 里的 \GetAsyncKeyState\ 检测

没有改动输入框聚焦、失焦、保存、UI绘制等其他逻辑。

### 踩过的坑
无。本次直接定位根因（GetAsyncKeyState只查最高位导致漏键），最小改动修复。用户验证OK。

### 验证方法
1. 打开打怪页，点击HP阈值输入框聚焦
2. 快速连续按数字键（如1234567890），确认每个数字都输入进去
3. 快速按退格键，确认每次都能删除一个字符
4. 切换到药品页，测试按键录入框，快速按字母键确认捕获
5. 测试小键盘数字键输入

### 关联
- 总纲"开发准则"第7.4条：输入与交互（杜绝乱触发、连点、误触发）
- 总纲"问题排查原则"第30条：排查未知问题必须先加日志定位根因
- 说明书：输入框时灵时不灵修复说明书.md
- git commit: 2026-9-1号 12点10分 输入框防漏键修复

---

## 【记录014】2026-09-01 输入框时灵时不灵修复成功（GetAsyncKeyState漏键）

### 关键词
输入框、时灵时不灵、GetAsyncKeyState、0x8000、0x0001、漏键、轮询、_poll_num_input、_poll_key_capture

### 现象
- 打怪/药品页数字输入框和按键输入框，快速按键时偶尔有键输入不进去
- 时灵时不灵，没有固定规律
- 退格、回车、ESC等功能键也偶尔失灵

### 根因
\_poll_num_input()\ 和 \_poll_key_capture()\ 用 \GetAsyncKeyState(vk) & 0x8000\ 检测按键，只检查**最高位**（当前是否按下）。

主循环每帧调用一次轮询，如果用户按键速度很快（按下+释放在两次轮询之间完成），第一次轮询时键还没按下（最高位0），第二次轮询时键已经释放（最高位0），就会**漏捕获**这个键。

这就是"时灵时不灵"的根因——按键速度和轮询间隔的竞争。

### 解决方案
同时检查**最高位**和**最低位**：
\\\python
state = user32.GetAsyncKeyState(vk)
if state & 0x8000 or state & 0x0001:  # 当前按下 或 上次调用以来按下过（防漏键）
    current.add(vk)
\\\
- \ x8000\（最高位）：当前是否按下
- \ x0001\（最低位）：自从上次调用 \GetAsyncKeyState\ 以来，键是否被按下过

即使按键在两次轮询之间按下又释放，最低位也会记录"曾经按下过"，从而捕获到这个键。

### 涉及函数
- \_poll_num_input()\：数字输入轮询（主键盘0-9、小键盘0-9、退格、回车、ESC、负号、小数点）
- \_poll_key_capture()\：按键录入轮询（F1-F12、Ctrl、Shift、Home、End等所有键）

### 改动范围
只改了2处，每处3行：
1. \_poll_key_capture\ 里的 \GetAsyncKeyState\ 检测
2. \_poll_num_input\ 里的 \GetAsyncKeyState\ 检测

没有改动输入框聚焦、失焦、保存、UI绘制等其他逻辑。

### 踩过的坑
无。本次直接定位根因（GetAsyncKeyState只查最高位导致漏键），最小改动修复。

### 验证方法
1. 打开打怪页，点击HP阈值输入框聚焦
2. 快速连续按数字键（如1234567890），确认每个数字都输入进去
3. 快速按退格键，确认每次都能删除一个字符
4. 切换到药品页，测试按键录入框，快速按字母键确认捕获
5. 测试小键盘数字键输入

### 关联
- 输入框时灵时不灵修复说明书.md
- 总纲"开发准则"第7.4条：输入与交互
- 总纲"问题排查原则"第30条：排查未知问题必须先加日志定位根因
- git commit: 2026-9-1号 12点10分 输入框防漏键修复


---

## 【记录015】2026-09-01 主循环卡死第0帧（冷启动日志一直刷第0帧）

### 关键词
冷启动、第0帧、frame_count不增长、_capture_map、map_area_rect、NoneType、TypeError、continue、异常静默吞掉、主循环卡死

### 现象
- 打包后启动程序，UI窗口正常显示（路线/打怪/药品等标签页都渲染了）
- 控制台窗口一直刷`[冷启动] 第0帧开始`（从6.75s到8.17s全是第0帧，frame_count不增长）
- 程序看似启动了，但主循环没有正常推进，小地图不更新、人物检测不工作
- 用户截图：控制台标题MapleBot.exe，日志全是第0帧

### 排查过程
1. 冷启动日志是之前为排查打包未响应/灰屏加的调试日志，在run开始→namedWindow→首帧泵消息→每帧开始→waitKey完成5个节点打印时间戳
2. 日志只刷到`第0帧开始`，说明卡在了第0帧的处理中，没有走到waitKey完成
3. 看主循环代码：`map_area = self._capture_map()`在try里，异常时`except Exception: time.sleep(0.05); continue`
4. continue会跳过后面的`self.frame_count += 1`，所以如果_capture_map()一直抛异常，frame_count永远是0
5. 看_capture_map()函数：`r = self.map_area_rect`然后直接访问`r["left"]`，没有对r为None做保护
6. 用户还没点"截取小地图"按钮时，map_area_rect为None，访问r["left"]抛TypeError: 'NoneType' object is not subscriptable
7. 29号版本有这个保护，后来合并功能时被改没了

### 根因
`_capture_map()`在`map_area_rect`为None时抛TypeError，主循环`except: continue`静默吞掉异常，`frame_count += 1`在continue之后永远执行不到，导致主循环卡死在第0帧。

连锁链路：
1. 启动程序 → map_area_rect为None（还没截取小地图）
2. 主循环第0帧 → _capture_map() → 访问None["left"] → TypeError
3. except捕获 → sleep(0.05) → continue → 跳过frame_count += 1
4. 下一帧还是第0帧 → 重复步骤2-3 → 永远卡死

### 解决方案
1. **_capture_map()加None保护**：在`r = self.map_area_rect`后加判断，如果r为None或width/height<=0，返回全黑图`np.zeros((MAP_H, FIXED_W, 3), dtype=np.uint8)`，不抛异常
2. **主循环异常打印**：把`except Exception:`改成`except Exception as _e: print("[主循环] _capture_map异常: %s" % _e)`，不再静默吞异常，方便以后排查
3. 改动范围：只改2处，每处3-5行，没有动其他逻辑

### 踩过的坑 / 教训
- **异常静默吞掉是万恶之源**：`except Exception: continue`会把真正的错误藏起来，导致程序看似在运行但实际卡死，排查时完全看不到错误信息。外部不可控操作（截图、窗口、文件IO）允许try，但必须打印或写日志+明确降级处理，不能静默。
- **改动时要保留原有保护**：29号版本_capture_map()有map_area_rect为None的保护，后来合并3功能（人物定位框/绿框钳制/镜头死区检测）时，从3e04150 commit提取代码，把原来的保护覆盖掉了。改动前必须看清原代码的来龙去脉，不能只看要加的功能。
- **frame_count增长位置要注意**：如果continue在frame_count += 1之前，异常会导致frame_count不增长，调试日志会一直显示第0帧。写循环时要注意关键计数器的位置，避免被continue跳过。

### 验证方法
1. 打包后启动程序，不点"截取小地图"按钮
2. 观察控制台日志：冷启动日志应该正常推进（第0帧→第1帧→第2帧→...），不会一直刷第0帧
3. 点"截取小地图"按钮，小地图正常显示，光点检测正常工作
4. 人物特征检测、怪物检测、蒙板显示等功能正常
5. 控制台不会出现`[主循环] _capture_map异常`的错误信息

### 关联
- 主循环卡死第0帧修复说明书.md
- 总纲"开发准则"第7.3条：空校验与生命周期（任何对象使用前必做空判断）
- 总纲"问题排查原则"第28条：有问题直接解决根因，不逃避、不折中、不用try-except绕过去
- 总纲"改动前必须做"第0步：先看清原代码的来龙去脉，再动手
- git commit: 2026-9-1号 修复主循环卡死第0帧


---

## 【记录016】2026-09-01 小地图光点锁定人物坐标完整实现结构（31号版本24d6025，以后照抄此版本，不要自己重写）

### 版本确认
- **28号末版79aa2c4**：只有小地图绿框基础实现（_draw_blue_box），**没有**人物定位框和镜头死区检测
- **31号版本24d6025**：完整实现，包含小地图绿框+人物定位框+镜头死区检测全套功能，以后照抄此版本

### 完整实现结构（31号版本24d6025）

#### 1. 常量定义（行251-259）
- BG_DETECT_REGIONS_FILE = data/bg_detect_regions.json
- BG_DETECT_DEFAULT_REGIONS = 三个检测点：左下(35,744,40,39) / 左上(170,43,46,23) / 右中(1320,409,43,28)
- BG_DIFF_THRESHOLD = 8.0
- BG_MOTION_MIN_REGIONS = 3
- BG_STILL_FRAMES_TO_DEADZONE = 3

#### 2. 初始化变量（行724-734）
- _bg_regions = [dict(r) for r in BG_DETECT_DEFAULT_REGIONS]
- _bg_last_frames = [None, None, None]
- _bg_diff_values = [0.0, 0.0, 0.0]
- _camera_state = "deadzone"  # 状态机：deadzone(镜头不动)/following(镜头移动)
- _camera_still_count = 0
- _blue_box_deadzone_pos = None  # 进入死区时冻结的绿框位置
- _last_dot_pos = None
- _bg_dragging = -1
- _bg_motion_count = 0
- _char_box_offset_x = 39  # 人物框X偏移（F3面板可调）
- _char_box_offset_y = 78  # 人物框Y偏移

#### 3. _detect_camera_motion(frame)（行3153-3208）
- 三区域帧差检测，内缩3像素排除蒙板框线干扰
- 状态机：deadzone→following（光点移动且3处背景都在动）；following→deadzone（光点不动或背景动<3处，持续3帧）
- 进入死区时冻结：_blue_box_deadzone_pos = _calc_blue_box_pos(当前光点)

#### 4. _calc_blue_box_pos(mx, my)（行7615-7634）
- **公共方法，小地图绿框与大屏幕人物框共用，保证两框位置永远一致**
- 以光点为中心+边缘钳制（左4/右7/上5/下5）
- 返回(box_x, box_y)，未校准或区域无效返回None

#### 5. _draw_blue_box(map_frame)（行7565-7612）
- 校准模式：画角点+半透明绿线
- 正常模式：following用_calc_blue_box_pos(px,py)，deadzone用冻结的_blue_box_deadzone_pos
- 颜色：绿色(0,255,0)

#### 6. lock_screen_from_dot()（行7636-）
- 人物屏幕坐标，同样用_calc_blue_box_pos
- following：绿框以光点为中心；deadzone：绿框冻结，光点在固定框内归一化
- 归一化后×整个窗口尺寸(_target_window_size，含标题栏边框，不是客户区)

#### 7. 蒙板人物框绘制（行6044-6067）
- **用lock_screen_from_dot()的坐标(char_pos)，不是人物特征匹配位置**
- 宽126高136，加_char_box_offset_x=39/_char_box_offset_y=78偏移（F3面板可调）
- 颜色：绿色(0x00FF00)，2px边框，空刷

#### 8. 主循环调用
- 先截当前帧→_detect_camera_motion(frame)更新镜头状态→再算人物位置，同帧一致
- _draw_blue_box(display)在小地图显示时调用

### 关键教训
1. 小地图绿框和人物框必须共用_calc_blue_box_pos，不能各自计算，否则两框不一致
2. 人物框必须用lock_screen_from_dot()的坐标，不能用char_pos（人物特征匹配位置）
3. 镜头状态机deadzone/following必须完整实现，不能只画固定绿框
4. 窗口尺寸用_target_window_size（整个窗口含标题栏），不是客户区
5. 以后照抄31号版本24d6025，不要自己重写

### 关联
- 31号版本commit: 24d6025
- 小地图光点锁定人物坐标方案.md
- git show 24d6025:maple_route_ui.py 可查看完整代码


## 017号记录：2026-09-01 完整备份（全部功能正常，除绿框位置未校准）

**关键词**：完整备份、本地备份、绿框已现、三点检测正常、人物特征正常、X倍率正常、平台录制正常、双蒙板正常

**备份位置**：
```
C:\Users\PC\Doubao\chats\2026-08-15\new-chat-4\maple_bot_完整备份_2026-9-1号_22点35分_清理后_全部功能正常除了绿框位置没校准
```

**备份内容**：完整项目目录，包括源码、config、data/json、所有dist打包版本、图片、exe、文档等全部文件

**当前状态**：
- 人物绿框已显示（屏幕中心兜底，位置待校准）
- 三点检测框正常（独立绘制，随脚本启动显示）
- 人物特征黄点正常（常驻显示）
- X倍率正常
- 平台录制正常
- 双蒙板正常
- 输入框正常
- 药品检测框正常

**下一步**：调校人物绿框位置，用小地图光点在绿框中的比例位置（lock_screen_from_dot算法）

**关联commit**：f4a6746（角色绿框已现等待调校位置）

---


## 018号记录：2026-09-01 绿框校准键盘修复+闪退修复+垃圾文件清理+默认配置

**关键词**：绿框校准、键盘时灵时不灵、set差集、闪退、NoneType、best.onnx、垃圾文件清理、默认配置、人物偏移0 0

### 一、垃圾文件清理
- **清理前**：9.2GB（31个旧dist目录+backup目录+旧备份+临时文件）
- **清理后**：613MB
- **删除内容**：dist旧版本（MapleBot_v68~v94）、backup目录、dist_lock3/dist_final旧版本、2026-08-21/26旧备份目录、根目录旧exe和zip、temp/__pycache__
- **保留内容**：源码、config、data、所有.md文档、dist_lock5（当前版本）、9-1号完整备份目录、_v31_full.py、MapleBot.spec、best.onnx
- **完整备份位置**：`C:\Users\PC\Doubao\chats\2026-08-15\new-chat-4\maple_bot_完整备份_2026-9-1号_22点35分_清理后_全部功能正常除了绿框位置没校准`

### 二、绿框校准键盘时灵时不灵修复
- **现象**：绿框校准模式下，方向键微调角点位置时灵时不灵，有时候按两下都没反应
- **根因**：键盘处理逻辑用字典记录按键状态，行8441-8442有bug：`if _vk not in getattr(self, '_bluebox_key_state', {}): self._bluebox_key_state = {}`，当某个键不在字典里时会重置整个字典，导致其他键状态丢失，反复重置引起时灵时不灵
- **修复**：改成和输入框一致的set差集边沿触发方式
  ```python
  if not hasattr(self, '_bluebox_prev_keys'):
      self._bluebox_prev_keys = set()
  _current = set()
  for _vk in _bluebox_vks:
      if user32.GetAsyncKeyState(_vk) & 0x8000:
          _current.add(_vk)
  _new_keys = _current - self._bluebox_prev_keys
  self._bluebox_prev_keys = _current
  for _vk in _new_keys:
      self._handle_blue_box_key(_vk)
  ```
- **踩坑**：Edit工具频繁Native execution failed，可靠做法是Write临时.py→Bash执行→删除

### 三、闪退修复（TypeError: 'NoneType' object is not subscriptable）
- **现象**：启动脚本后闪退，报错在`_detect_minimap`行1232
- **根因1**：`self.map_area_rect`为None时，行1232`old["left"]`直接下标访问导致TypeError
- **修复1**：加`if old is not None`判断，None时跳过防抖检查直接更新
- **根因2**：打包后`best.onnx`缺失，YOLO模型加载失败，导致小地图检测失败，map_area_rect为None
- **修复2**：打包后手动复制best.onnx到exe同级目录
- **踩坑**：清理旧dist目录时，_yolo_model_path可能指向旧路径，导致模型加载失败

### 四、绿框默认配置
- **配置文件**：`data/blue_box_config.json`
- **默认值**：width=84, height=52, bl_ox=1, bl_oy=20, tr_ox=85, tr_oy=-32
- **保存方式**：F4进入校准→点两个点（左下+右上）→方向键微调→再按F4保存
- **加载方式**：初始化时`_load_blue_box()`自动加载，重启脚本绿框还在
- **注意**：必须点两个点才能保存，点一个点或没点不会生成配置文件

### 五、人物特征X/Y偏移默认值改0 0
- **配置文件**：`data/fight_potion_config.json`
- **修改前**：char_x_offset="-1", char_y_offset="-38"
- **修改后**：char_x_offset="0", char_y_offset="0"
- **用途**：模板匹配人物位置时的偏移量，默认0表示不偏移

### 六、当前状态
- 人物绿框已显示（屏幕中心兜底，位置待校准）
- 三点检测框正常（独立绘制，随脚本启动显示）
- 人物特征黄点正常（常驻显示）
- 绿框校准键盘正常（set差集方式）
- 闪退已修复（None判断+best.onnx）
- 绿框默认配置已保存（84x52）
- 人物偏移默认0 0
- X倍率正常、平台录制正常、双蒙板正常、输入框正常、药品检测正常

### 七、下一步
- 对接小地图光点在绿框中的比例位置到游戏主窗口角色绿框（lock_screen_from_dot算法）
- 排查_target_window_size为什么是(0,0)，导致lock_screen_from_dot返回None
- 三点检测背景的死区/跟随状态机引用到小地图绿框位置（已实现，待验证）

### 关联commit
- 241a1f1（清理垃圾文件后快照）
- 最新commit（绿框键盘修复+闪退修复+默认配置）

---

## 019号记录：2026-09-01 诊断日志加错位置导致绑定功能损坏

**关键词**：诊断日志、_save_target_window_size、自动绑定损坏、手动绑闪退、安全诊断方式、主循环打印

### 一、现象
- 为排查 _target_window_size=(0,0) 问题，在 _save_target_window_size() 函数里加了诊断print
- 结果：自动绑定功能坏了，手动绑定直接闪退
- 回滚到上一版（1b91c09）后正常

### 二、根因分析
- 直接原因：修改了绑定流程中的关键函数 _save_target_window_size()，虽然只加了print，但可能影响了函数执行时序
- 深层原因：违反了"改动前必须看清来龙去脉"的规则——_save_target_window_size() 在绑定成功后被调用，是绑定流程的一部分
- 教训：诊断日志不要加在关键流程函数里，应该加在主循环的读取端，只读取状态不修改逻辑

### 三、正确的诊断方式（安全）
- 不在 _save_target_window_size() 里加任何代码
- 只在主循环 lock_screen_from_dot() 调用后，每30帧打印一次状态：
  if self.frame_count % 30 == 0:
      _tw = getattr(self, '_target_window_size', None)
      _wr = getattr(self, 'window_rect', None)
      print("[诊断] frame=%d target_size=%s hwnd=%s window_rect=%s lock_pos=%s" % (...))
- 这样只读取状态，不碰绑定逻辑，不会影响任何功能

### 四、踩坑总结
1. 诊断日志加在关键流程函数里 = 改关键流程，哪怕只加print也可能出问题
2. 安全诊断原则：只在读取端加日志，不在写入端/流程端加
3. 改动前必须确认：这个函数被谁调用、在什么流程里、改了会不会牵连其他功能
4. 回滚是最快的恢复方式：发现改坏了立即回滚，不要在坏的基础上修

### 五、当前状态
- 已回滚到正常版（1b91c09）
- 已用安全方式在主循环加诊断日志
- 待用户运行诊断版，看 [诊断] 日志确定 _target_window_size 为何为(0,0)

### 关联commit
- 1b91c09（诊断前快照，自动绑定正常）

---
