# 人物特征偏移与蒙板黄点显示说明书

## 功能说明
人物特征匹配后，支持X/Y方向偏移校准，并在游戏窗口蒙板上用黄色圆点显示人物位置（不运行时也显示）。

## 组成部分
1. **偏移输入框**：char_x_offset（X偏移）、char_y_offset（Y偏移），支持负数
2. **偏移应用**：`_get_player_screen_pos()` 返回人物位置时加上偏移量
3. **蒙板黄点显示**：主循环把 char_pos 更新到蒙板数据，蒙板窗口绘制黄色实心圆

## 实现细节

### 偏移输入
- 输入框类型：num（数字），允许负号（key 0xBD/0x6D）
- 聚焦后首次输入覆盖旧值（`_num_field_replace = True`），后续输入追加
- 输入时设置 `_offset_feedback_start` 和 `_offset_feedback_done = False`
- 3秒无输入自动失焦保存

### 偏移应用
`_get_player_screen_pos()` 匹配到人物模板后，返回坐标时加上：
```python
x + self._field_values.get("char_x_offset", 0)
y + self._field_values.get("char_y_offset", 0)
```

### 蒙板黄点显示（关键修复）
**问题**：原来 char_pos 只在 `if self._running:` 块里更新到蒙板数据，不运行时 char_pos 为 None，黄点不显示。

**修复**：把 char_pos 更新（含 `_monster_overlay_data` 空初始化）移到 `if self._running:` **外面**，monsters/monster_hp_bars 仍只在运行时更新。

这样不运行时蒙板上也能看到人物位置黄点。

### 蒙板绘制
- 从 `data.get("char_pos")` 取人物位置
- 画黄色实心圆（0x00FFFF）半径6，橙边框（0x0080FF）
- 偏移反馈闪烁：`blink_until > now` 时300ms交替显示/隐藏（用户后来决定保持一直显示，不改动）

## 踩过的坑

### 坑1：改偏移反馈时把人物特征搞坏
- **现象**：为了让黄点"不闪"，改动了偏移反馈逻辑，结果把人物特征匹配搞坏了（人物框消失、检测框乱跳）
- **原因**：没有看全整个功能链路的来龙去脉，只盯"闪烁"这一个点改，牵连了人物特征匹配
- **教训**：改动前必须做第0步影响面分析，看全链路再动手

### 坑2：蒙板黄点不运行时不显示
- **现象**：用户要求"特征的点位在主游戏窗口蒙板上直接展示出来"，但不运行时黄点不显示
- **根因**：char_pos 更新在 `if self._running:` 块里，不运行时不更新
- **解决**：把 char_pos 更新移到 if 块外面，最小改动1处

### 坑3：偏移反馈逻辑反复改
- **现象**：用户先后要求"失焦后3秒闪烁5秒"→"停止输入后1秒显示3秒消失"→"不改了，一直显示就行"
- **教训**：需求不确定时先确认再动手，不要反复改

## 涉及函数
- `_get_player_screen_pos()`：人物位置匹配+偏移应用
- `_poll_num_input()`：偏移数字输入
- `_show_offset_feedback()`：偏移反馈闪烁（当前保留，用户决定不改）
- 主循环 `run()`：char_pos 更新到蒙板数据
- 蒙板绘制函数：黄点绘制

## 验证方法
1. 绑定游戏窗口，不启动脚本，确认蒙板上有黄色人物位置点
2. 输入X偏移（如+50），确认黄点向右移动50像素
3. 输入Y偏移（如-30），确认黄点向上移动30像素
4. 输入负数偏移，确认支持
5. 启动脚本运行，确认黄点仍然显示且位置正确
6. 切换方案，确认偏移值保留

## 关联
- 总纲"五、人物特征保存方法"章节
- 工作记录011号（改动前影响面分析方法论）
- 工作记录013号（人物特征10张全链路修复）
- git commit: 蒙板黄点不运行时也显示
