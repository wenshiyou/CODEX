# 打包后黑屏问题排查与解决方案

> 最后更新：2026-08-27
> 关联提交：c379989 → daced7a → bb0ff13 → 0288347 → 2e8e1e3（首次修复）→ b3a1673（复发）→ 505f4c8（二次修复）→ d10787b（按键失灵修复）

## 一、问题描述

- 双击运行打包后的 `MapleBot.exe`，窗口能打开但**全黑**，UI 不渲染
- 不是闪退（进程还在），是窗口未响应 / 主循环卡死
- 源码直接 `python maple_route_ui.py` 运行正常，只有打包后黑屏

## 二、排查流程（按时间顺序）

### 第1步：怀疑 UPX 压缩损坏 DLL（c379989）
- 假设：PyInstaller 的 UPX 压缩可能损坏 OpenCV DLL，导致图片加载失败 → 黑屏
- 操作：spec 文件中 `upx=False` 关闭压缩
- 结果：**无效**，仍然黑屏

### 第2步：加启动日志 + console=True（daced7a）
- 假设：图片资源路径错误，加载失败导致黑屏
- 操作：`console=True` 显示控制台，启动时逐步骤打印日志
- 结果：发现图片都能正常加载，排除资源路径问题

### 第3步：imshow 处记录 frame 信息（bb0ff13）
- 假设：截图/帧处理异常，frame 为空导致 imshow 黑屏
- 操作：在 `cv2.imshow` 前记录 frame 的均值、形状、类型到 `debug_frame.log`
- 结果：frame 数据正常，排除截图问题

### 第4步：主循环逐步骤日志（0288347）
- 假设：主循环某一步抛出异常，导致卡死
- 操作：主循环每个关键步骤前后写日志到 `debug_loop.log`，同时关闭自动备份线程
- 结果：**定位到异常位置**——主循环在访问 `map_area_rect` 相关逻辑时卡住

### 第5步：找到根因（2e8e1e3）
- 根因：`map_area_rect` 初始化位置错误

## 三、根本原因

### 代码结构问题

```
__init__ 执行顺序：
  1. self.sct = mss.mss()
  2. 自动绑定窗口 → 调用 _detect_minimap() → 设置 self.map_area_rect = {检测结果}
  3. ...（中间大量初始化代码）...
  4. self.map_area_rect = None   ← 【BUG】把第2步的检测结果重置为None！
  5. if self.map_area_rect: ...  ← 永远为False
```

### 连锁反应

1. `_detect_minimap()` 在 `__init__` 前部成功检测到小地图，设置了 `map_area_rect`
2. `__init__` 后部的 `self.map_area_rect = None` 把检测结果**覆盖重置**
3. 主循环中 `if self.map_area_rect:` 判断为 False，小地图逻辑全部跳过
4. 部分代码路径直接访问 `map_area_rect["width"]` 等属性，抛出 `AttributeError`
5. 异常未被正确处理 → 主循环死循环 / 窗口消息泵阻塞 → **UI 无法渲染 → 黑屏**

## 四、解决方案

### 正确做法（2e8e1e3 / 505f4c8）

```python
class MinimapRouteRecorder:
    def __init__(self):
        # ✅ 必须在最开头初始化，在自动绑定/_detect_minimap之前
        self.map_area_rect = None
        self.sct = mss.mss()
        # ... 自动绑定调用 _detect_minimap()，可能设置 map_area_rect ...
        # ... 中间初始化 ...
        # ❌ 绝对不能在这里再写 self.map_area_rect = None，会覆盖检测结果
        # 改为注释说明：
        # map_area_rect已在__init__开头初始化，自动绑定可能已设置值，这里不再重置
        if self.map_area_rect:
            print("Map area:", ...)
```

### 核心原则

1. **被早期方法访问的属性，必须在 `__init__` 最开头初始化**
2. **`__init__` 中后部不要重置已经被前面代码设置过的属性**
3. 如果需要"防止未赋值时 AttributeError"，在开头初始化一次就够了，不要重复初始化

## 五、黑屏复发记录（重要教训）

### 复发过程

- `2e8e1e3` 已修复黑屏，`87ddedd` 正式版确认不黑屏
- `b3a1673`（对面线程改动）**撤销了修复**：
  - 删除了 `__init__` 开头的 `self.map_area_rect = None`
  - 在后部又加回了 `self.map_area_rect = None` 重置
- 打包 v18 → 黑屏复发
- 排查花费大量时间，最终通过 `git diff 339d56d b3a1673` 对比发现初始化被改回

### 二次修复（505f4c8）

恢复 `2e8e1e3` 的修复：
- `__init__` 开头加回 `self.map_area_rect = None`
- 删除后部的重置代码

## 六、黑屏修复后按键失灵（重要教训）

### 问题现象

- 黑屏修复后（v19b/v21/v22），UI 界面正常显示
- **所有按钮点不动**（刷新、平台、运行、窗口绑定等全部无反应）
- 准星拖拽绑定（全局鼠标检测）可以工作，但 UI 按钮全部失效
- 不是闪退，进程还在，窗口也在，就是点击没反应

### 排查过程

#### 第1步：怀疑 _on_mouse 异常导致回调崩溃
- 假设：_on_mouse 中某个按钮处理抛异常，导致整个鼠标回调失效
- 操作：给 _on_mouse 加 try-except 包装器（v21）
- 结果：**无效**，按钮还是点不动

#### 第2步：怀疑 _capture_map 返回 None 导致主循环异常
- 假设：_capture_map 在未绑定窗口时返回 None，后续代码访问属性抛异常
- 操作：_capture_map 加 None 检查（v22）
- 结果：**无效**，按钮还是点不动

#### 第3步：console=True 调试版看控制台输出（关键突破）
- 操作：打包 console=True 版本，运行后看黑色控制台窗口
- 发现：控制台持续报错 `draw error: could not broadcast input array from shape (X,Y,3) into shape (X,Y,4)`
- 这是**通道不匹配**错误：3 通道图片赋值给 4 通道区域

### 根本原因

b3a1673（对面线程改动）删除了 `_imread3` 函数：

```python
# 被删除的 _imread3（作用：4通道转3通道）
def _imread3(path):
    img = load_png(path)
    if img is not None and len(img.shape) == 3 and img.shape[2] == 4:
        img = cv2.cvtColor(img, cv2.COLOR_BGRA2BGR)
    return img
```

改用 `load_png`（保留 4 通道 BGRA）：
```python
def load_png(path):
    img = cv2.imdecode(np.fromfile(path, dtype=np.uint8), cv2.IMREAD_UNCHANGED)
    return img  # 返回4通道BGRA，不转3通道
```

**连锁反应**：
1. `load_png` 用 `IMREAD_UNCHANGED` 加载 PNG，保留 alpha 通道 → 返回 4 通道（BGRA）
2. UI 背景图 `_ui_bgs[tab]` 变成 4 通道
3. `frame = self._ui_bg.copy()` → frame 是 4 通道
4. draw 函数中某些操作把 3 通道图赋值给 4 通道 frame → `could not broadcast` 错误
5. draw error 被 try-except 捕获 → 显示 fallback 的静态 `_ui_bg`
6. 静态背景图 + 通道异常 → 窗口消息泵异常 → **鼠标回调不触发 → 所有按钮点不动**

### 解决方案（d10787b）

加载 UI 背景图后，统一转 3 通道：

```python
img = load_png(p)
if img is not None:
    if img.ndim == 3 and img.shape[2] == 4:
        img = cv2.cvtColor(img, cv2.COLOR_BGRA2BGR)  # 4通道转3通道
    self._ui_bgs[tab] = cv2.resize(img, (UI_W, UI_H))
```

### 踩过的坑

| 坑 | 说明 | 正确做法 |
|----|------|----------|
| 怀疑 _on_mouse 异常 | 加 try-except 包装器，实际不是根因 | 先看控制台报错，不要瞎猜 |
| 怀疑 _capture_map None | 加 None 检查，实际不是根因 | console=True 看真实错误 |
| 删 _imread3 没考虑通道 | load_png 返回4通道，draw 函数假设3通道 | 删函数前必须确认所有调用点的通道预期 |
| draw error 被吞 | try-except 只打印不修复，掩盖了根因 | console=True 才能看到被吞的错误 |
| 只看"UI显示正常" | 静态背景图也能显示，但按钮失效 | 打包后必须验证按钮可点击 |

### 关键教训

1. **console=True 是排查打包问题的第一手段**——很多错误被 try-except 吞了，只有控制台能看到
2. **删除工具函数前必须全局搜索调用点**，确认通道/返回值预期一致
3. **打包后验证不能只看"能启动+UI显示"**，必须点按钮测试交互
4. **通道不匹配（3 vs 4）是 OpenCV 常见坑**，`IMREAD_UNCHANGED` 会保留 alpha

## 七、踩过的坑（汇总）

| 坑 | 说明 | 正确做法 |
|----|------|----------|
| 怀疑 UPX 损坏 DLL | 花时间关 UPX，实际无关 | 先加日志定位，不要瞎猜 |
| 怀疑图片资源路径 | 加 console 验证，实际图片正常 | console=True + 启动日志快速排除 |
| 怀疑 frame 截图异常 | 记录 frame 均值/形状，实际正常 | 日志要打到具体变量值 |
| 回滚/合并丢失修复 | b3a1673 撤销了 2e8e1e3 的修复 | 合并代码后必须 diff 检查关键修复是否保留 |
| 用 try-except 吞异常 | 试图用 try-except 掩盖，用户明确禁止 | 找根因，修代码，不吞错误 |
| `__init__` 初始化顺序 | 属性在后部初始化，前部方法已访问 | 所有属性在 `__init__` 最开头统一初始化 |

## 七、黑屏快速排查清单

遇到打包后黑屏，按此顺序排查：

1. **console=True 重新打包**，看控制台有没有报错/异常
2. **检查 `__init__` 初始化顺序**：有没有属性在被早期方法访问后又被重置
3. **检查主循环异常**：加逐步骤日志，看卡在哪一步
4. **git diff 对比上一个正常版本**：看哪些初始化/关键代码被改了
5. **不要先怀疑 DLL/UPX/资源路径**，这些通常不是主因

## 八、关联文件

- 源码：`maple_route_ui.py`（`__init__` 中 `map_area_rect` 初始化位置）
- 打包配置：`MapleBot.spec`（`console=False` 正式版，调试时临时改 `True`）
- 总纲：`PROJECT_STATE.md` 文档索引

---

## 九、正确打包方式（详细步骤 + 坑 + 解决办法）

### 9.1 打包前必做（按顺序）

#### 第1步：关闭正在运行的 exe
```powershell
taskkill /F /IM MapleBot.exe
# 或 taskkill /F /IM MapleBot_v19.exe（对应版本号）
```
**坑**：不关闭就打包 → `PermissionError: [WinError 5] 拒绝访问`，打包失败。
**解决**：先 taskkill，确认 `Get-Process MapleBot*` 无输出再继续。

#### 第2步：语法检查
```powershell
python -m py_compile maple_route_ui.py
echo $LASTEXITCODE  # 0=通过
```
**坑**：语法错误直接打包 → 打包成功但运行闪退/黑屏，浪费时间。
**解决**：每次打包前必须语法检查，0 才继续。

#### 第3步：清理旧缓存
```powershell
Remove-Item -Recurse -Force build -ErrorAction SilentlyContinue
Remove-Item dist\MapleBot_vXX.exe -Force -ErrorAction SilentlyContinue
```
**坑1**：不删 build → PyInstaller 用缓存的 Analysis，新代码没打进去 → "打包后功能没变化"。
**坑2**：不删旧 exe → 可能运行的还是旧版本，以为修改无效。
**解决**：每次打包前必须删 build 目录和旧 exe。

### 9.2 spec 文件关键配置

```python
a = Analysis(
    ['maple_route_ui.py'],          # 入口必须是这个，不是 MapleBot.py
    datas=[('config', 'config'), ('data', 'data')],  # 必须打包这两个目录
    hiddenimports=['ultralytics', 'cv2', 'mss', 'sklearn'],
    ...
)
exe = EXE(
    ...
    name='MapleBot_v19',            # 版本号，改 name 时 spec 文件名也要对应
    upx=False,                      # 关闭UPX，避免损坏OpenCV DLL（虽然不是黑屏根因，但稳妥）
    console=False,                  # 正式版不显示控制台；调试黑屏时临时改 True
)
```

**坑1**：入口写错成 `MapleBot.py` → 打包的是旧入口，功能不对。
**坑2**：datas 漏了 `data` 目录 → 图片/模板缺失 → UI 空白或闪退。
**坑3**：`name` 和 spec 文件名不一致 → 输出 exe 名字不对，以为打包失败。
**坑4**：`upx=True` → 可能损坏 OpenCV DLL（虽然本次黑屏不是这个原因，但历史上踩过）。
**解决**：统一用 `upx=False`，入口固定 `maple_route_ui.py`。

### 9.3 执行打包

```powershell
# 复制 spec 并改 name（新版本号时）
Copy-Item MapleBot.spec MapleBot_v19.spec -Force
(Get-Content MapleBot_v19.spec) -replace "name='MapleBot'", "name='MapleBot_v19'" | Set-Content MapleBot_v19.spec -Encoding utf8

# 打包（必须 --clean，否则 base_library.zip 可能找不到）
pyinstaller --noconfirm --clean MapleBot_v19.spec
```

**坑1**：不加 `--clean` → `FileNotFoundError: base_library.zip`，PKG 阶段失败。
**原因**：build 目录残留的缓存和新 Analysis 不一致。
**解决**：每次加 `--clean`，或手动删 build 目录。

**坑2**：`hiddenimports` 报 `ultralytics not found` / `sklearn not found` → 这是正常的，这两个库没装，不影响打包，忽略即可。

**坑3**：打包时间约 30-60 秒，不要中途中断。

### 9.4 打包后验证

```powershell
# 确认文件存在且大小合理（约 70-80MB）
Get-Item dist\MapleBot_v19.exe | Select-Object Length, LastWriteTime
```

**运行验证清单**（必须逐项确认）：
1. 双击能启动，不闪退
2. UI 界面显示正常（背景图、按钮都在）
3. **按钮能点击**（刷新、平台、运行等都有反应）
4. 小地图区域有显示
5. 窗口绑定功能正常
6. 没有报错弹窗

**坑**：只看"能启动"就以为成功 → 实际按钮全失效/小地图黑屏。
**解决**：必须逐项验证，尤其是按钮点击和小地图显示。

### 9.5 调试打包问题的方法

#### 方法A：console=True 看错误
```powershell
# 临时改 spec
(Get-Content MapleBot_v19.spec) -replace "console=False", "console=True" | Set-Content MapleBot_v19.spec -Encoding utf8
pyinstaller --noconfirm --clean MapleBot_v19.spec
# 运行后看控制台输出的异常信息
```
**适用**：闪退、启动报错、不确定哪里异常。

#### 方法B：源码直接运行对比
```powershell
python maple_route_ui.py
```
**坑**：源码运行正常但打包后异常 → 99% 是路径问题（`resource_path` / `sys._MEIPASS`）或初始化顺序问题。
**解决**：检查所有 `resource_path()` 调用，确认打包后路径正确。

#### 方法C：git diff 对比上一个正常版本
```powershell
git diff <上一个正常版本的commit> HEAD -- maple_route_ui.py
```
**适用**：改了很多代码后出问题，不知道哪改坏了。
**重点看**：`__init__` 初始化顺序、属性是否被重复赋值/重置、路径相关改动。

### 9.6 打包常见问题速查表

| 问题 | 可能原因 | 解决办法 |
|------|----------|----------|
| `PermissionError` 拒绝访问 | exe 正在运行 | taskkill 后重试 |
| `base_library.zip` 找不到 | build 缓存损坏 | 加 `--clean` 或删 build 目录 |
| 打包后功能没变化 | build 缓存/源码没保存 | 删 build + 确认保存 + 重新打包 |
| 启动闪退 | 语法错误/资源缺失 | console=True 看错误 + 检查 datas |
| 窗口黑屏 | `__init__` 属性初始化顺序错误 | 检查 map_area_rect 等属性是否被后部重置 |
| 按钮点不动 | 主循环异常/鼠标事件被拦截 | console=True 看异常 + 检查事件循环 |
| 图片不显示 | 路径错误/通道错误 | 检查 resource_path + load_png 返回值 |
| exe 太小（<50MB） | 依赖没打进去 | 检查 hiddenimports + datas |
| `ultralytics not found` 警告 | 库没装 | 正常忽略，不影响打包 |

### 9.7 打包强制规则

1. **入口固定 `maple_route_ui.py`**，不要改
2. **`upx=False`**，不要开压缩
3. **每次打包前删 build 目录 + 旧 exe**
4. **打包命令必须加 `--clean`**
5. **正式版 `console=False`，调试时临时改 `True`**
6. **打包后必须验证按钮可点击 + 小地图显示**，不能只看能启动
7. **改 `__init__` 初始化顺序前，先看本文档第三章根因**
8. **新版本号要同时改 spec 文件名和 name 参数**
