# 【冒险岛世界】多锚点角色识别（人名 / 人脸 / 后脑）说明书

> 最后更新：2026-09-13　对应源码：`maple_route_ui.py`（搜 `_role_`、`ROLE_ANCHORS`、`_get_player_screen_pos`、`_role_match_in`）
> 本文件是该功能的**唯一详细文档**；`PROJECT_STATE.md` 总纲只保留一行索引，正文都在本文件。
> 定稿路线：内存读坐标已彻底证伪（IL2CPP、资源在服务器端）→ 光点比率/镜头惯性证伪 → 整角色大图/小块投票证伪 → **定稿纯视觉"小锚点多冗余 + 局部半径跟踪"，1:1 对齐心火辅助**。

---

## 0. 一句话总览

不再靠服装/光点/内存定位人物，改在人物身上找**三个换装扮也不变的小锚点**：**角色名(name)、朝右人脸(face_r)、后脑/头发(back)**，每帧都做模板匹配；**人名权重最高，人名丢了用脸/后脑分高者兜底，脸和后脑同一时刻只显示分高的一个**。匹配到的锚点坐标直接当人物坐标（单平台只看 X，不做到脚补偿）。

---

## 1. 三个锚点与各自匹配方式

| key | 含义 | 图类型 | 匹配方式 | 作用 |
|-----|------|--------|----------|------|
| `name` | 头顶角色名（如 sssr） | text 黑底白字 | V 通道 OTSU 二值化后灰度 `TM_CCOEFF_NORMED` | **主锚点，权重最高**，在就以它为准 |
| `face_r` | 朝右时的脸 | color 多边形 | 灰度 `TM_CCOEFF_NORMED` + 多边形外"软掩膜"；**水平镜像再匹配一次 = 朝左**，并据此判朝向 | 平地脸朝镜头时的主力 |
| `back` | 后脑/头发（爬梯子脸朝里时出现） | color 多边形（10 顶点） | 同 face_r，**不镜像** | 上梯子/脸朝里、人名又丢时的兜底 |
| `pet1/2/3` | 宠物名 | text | 同 name | 预留，未采不参与跟踪 |

**关键设计：不写任何"是否在梯子上"的状态机。** 三张图每帧都直接检测，出现哪张就是哪种姿态——平地脸朝镜头自然 face 分高，梯子上脸朝里自然 back 分高。曾尝试"脚本上梯状态切匹配集"，真机实锤：**用户手动挂绳时脚本内部上梯状态根本没置位，导致后脑永远不被匹配**，故彻底去掉状态判定。

---

## 2. 采集（角色识别管理窗）

入口：控制面板「人物特征」按钮 → 弹出「角色识别管理」窗（旧的人物特征采集已废弃，对齐心火）。

- **多边形描点**：鼠标逐点连线，**点满 4 个点自动闭合**；闭合后**点某条边=在该处新增一个顶点并可直接拖动**，循环"点边加点→拖点"修形；描的是**主体本身、不带背景**（范围略小于角色，宁少勿多）。
- 锚点类型：角色名 / 面部（只采朝右，向左靠镜像）/ 后脑 / 宠物名×3。
- text 锚点（角色名、宠物名）走 OTSU 抠笔画；color 锚点（脸、后脑）多边形外填黑、保存原尺寸与顶点。
- 每个锚点的 meta = `{kind, poly(相对外接框顶点), box, w, h, off_x=0, off_y=0}`。
- 管理窗每行实时显示该锚点当前命中率 %（**只读检测线程算好的现成分数，不自己抓帧**，原因见踩坑 4）。

---

## 3. 匹配算法（稳，不许再换）

- **统一用灰度 `TM_CCOEFF_NORMED`**：先各自减均值，抗整体明暗变化（人物被打会明暗闪烁），**背景假分低（实测 0.4~0.53）**，这是不被带飞的根本。
- **color 锚点的"软掩膜"提分**：采集时多边形外填了纯黑，黑底参与 CCOEFF 会把固定图的分拉低（后脑曾只有 0.55）。匹配前按 poly 建掩膜，把**外圈那圈黑填成"多边形主体内像素的平均灰度"**——减均值后外圈≈0、不贡献相似度，主体分上来（后脑提到 0.79~0.80），**算法仍是 CCOEFF、不出假分**。
- **全图只搜活动带** `y∈[DETECT_TOP_MARGIN=30, H-DETECT_BOTTOM_MARGIN=90]`，顶去标题栏、底去血蓝技能 UI，省算力也防 UI 误匹配。
- 同帧多个 text 锚点共用一次二值化结果（`_bin_cache`），不重复算。

---

## 4. 定位仲裁与局部跟踪（`_get_player_screen_pos`）

跟踪参数（`role_recognize.json` 的 params，默认 `ROLE_TRACK_DEFAULT`）：

| 参数 | 默认 | 含义 |
|------|------|------|
| thr | 0.62 | 命中阈值（真锚 0.72~0.83、背景假分 ≤0.53，真假已拉开） |
| rx / ry | 180 / 120 | 局部搜索窗半宽 / 半高（以上一帧为中心，白框可视化） |
| maxmove | 48 | 局部窗内允许的单帧最大跳变，超了当误匹配 |
| faststep | 2 | 连续 miss 几帧转全图重搜 |
| research | 1500ms | 距上次全图超过该值强制全图一次 |
| hold / fps | 90 / 24 | 保持 / 检测帧率 |

每帧流程：
1. 三锚点都在"局部窗 or 全图活动带"里匹配；
2. **定位候选**：name 过阈 → 只用 name；name 丢 → face_r/back 过阈者按分排序、分高者优先；
3. **跳变保护**：局部窗内候选离上一帧 > maxmove 时——**弱匹配(<0.75)当误匹配丢弃，强匹配(≥0.75)认定为真角色合法瞬移、直接采信**（背景假分到不了 0.75，不会带飞）；
4. 命中则更新 last/foot、miss 清零；全候选都丢则 miss+1，累计到 faststep 转全图。
5. 朝向只由 face_r 判（镜像命中=朝左），face 不过阈就保持上次朝向、不乱翻。

---

## 5. 蒙板上的显示框（颜色是大坑，见踩坑 1）

| 框 | 颜色(COLORREF) | 含义 |
|----|---------------|------|
| 人名/人脸/后脑锚点框 | **橙 0x00A5FF（三个统一，不要五颜六色）** | 用各自采集多边形、以命中中心为不动点；脸/后脑只画分高者一个 |
| 白框 0xFFFFFF | 局部搜索窗（rx/ry），全图重搜时不画 |
| 黄框 0x0000FFFF | 怪物识别(YOLO)范围 |
| 蓝框 0x00FF0000 | 人物技能(攻击射程)范围，以人物点几何直算、不依赖识别线程 |

锚点框**不带文字标签**，只留纯框。黄/蓝范围框在主循环 overlay 组装处用人物显示点 + 面板战斗配置几何直算，**不依赖 B 识别线程**（上梯高帧冻结 B、或没点运行时也照常显示）。

---

## 6. 踩坑清单（按踩的顺序，别再犯）

### ★坑1（本次最关键）：品红 0xFF00FF 是透明蒙板的"色键"，画这个颜色会被抠空、框永远看不见
- 透明覆盖蒙板用 `SetLayeredWindowAttributes(hwnd, COLOR_MAGENTA=0x00FF00FF, 0, LWA_COLORKEY)` 把**品红设为透明色键**：凡是画成品红色的像素都会被当成透明挖掉。
- 现象：后脑 back=0.79~0.80 明明过阈、数据和绘制方式都和人脸一模一样，框就是不显示；技能范围"紫框"也不出。根因是这两个框当时都选了品红 0xFF00FF，**一画上去就被色键抠空**，排查了很久才发现不是识别问题、也不是绘制问题，是颜色撞了色键。
- **结论：蒙板上严禁使用品红 0xFF00FF（以及任何接近的洋红）做任何绘制颜色。** 角色三锚点统一橙 `0x00A5FF`；需要"紫"的技能框改蓝 `0x00FF0000`；怪物框黄 `0x0000FFFF`。新增任何蒙板颜色前先对照本文件颜色表。

### ★坑2：带 mask 的彩色匹配会撞恒定假高分，禁用
- 用过 BGR 三通道带 mask 的 `TM_CCORR_NORMED`、`TM_SQDIFF_NORMED`，在亮岩壁/UI 上撞出**恒定 0.84 假分**（真 name 才 0.3），定位框被带到右上角、人物乱跑，已回滚。
- 只用灰度 `TM_CCOEFF_NORMED`；黑底拉分用坑"软掩膜"（外圈填主体均值）解决，**不许换匹配算法**。

### ★坑3：maxmove=48 会误杀合法瞬移
- 瞬移一下跳两三百 px，瞬移后真锚点在局部窗内也匹配到了，却因离上一帧 >48 被当误匹配丢弃 → miss → 全图重搜 → 定位框跑掉（用户看到"框刚好框住瞬移后的人却没在框内识别"）。
- 解法：强匹配 ≥0.75 不受 maxmove 限制直接采信，弱匹配大跳变才丢弃。若瞬移距离超过 rx/ry 出了局部窗，仍要靠全图找回，必要时调大 rx/ry。

### ★坑4：角色识别管理窗自己抓帧匹配 → 拖动/点 X 整程序闪退
- 管理窗定时回调 `_live_tick` 原本每 500ms 自己抓一次全屏、对三锚点各做一遍全图匹配；拖动窗口或点 X 关闭时，这份重活和 tk 窗口管理在同一线程交错，整个进程崩。
- 解法：检测线程每帧把分数存 `self._role_last_scores`，`_live_tick` **只读这份现成分数**刷新每行 %（不再抓帧、省一份 CPU）；加 `_role_live_on` 标志，`on_close` 先置 False 停掉 after 链再关窗，开头/续杯都查 `winfo_exists()`。

### 坑5：靠脚本状态判断"在不在梯子上"不可行
- 用户手动挂绳不会走脚本上梯流程，`_climb_state/_ladder_precise_mode` 不置位，用它决定配不配后脑会导致后脑永不匹配。**锚点检测不看任何状态，三图每帧都检、分高者胜。**

### 坑6：性能
- 曾出现绘制段 545ms、帧率掉到 4.9~15 后闪退：name 在阈值附近抖 → miss 频繁 → 三锚点全图重搜（face 还镜像=每轮 4 次全图匹配）。阈值拉开 + 强匹配瞬移采信后 miss 减少；GDI 对象每帧创建后在绘制 `finally` 统一 `DeleteObject(gdi_objs)`，确认无句柄泄漏。**整帧 connectedComponents 去碎点会掉帧闪退，禁用。**

---

## 7. 数据文件 / 函数索引

- 数据目录 `data/role_recognize/`：`role_recognize.json`（params + 每锚点 meta）+ `name.png / face_r.png / back.png`；**全局长期保存、换方案才替换，不随战斗方案切换**。
- `ROLE_REC_DIR / ROLE_REC_FILE / ROLE_ANCHORS / ROLE_TRACK_DEFAULT`：常量定义。
- `_role_anchor_path(key)`：锚点图路径。
- `_role_match_in(...)`：单锚点匹配（text 走 OTSU、color 走软掩膜 CCOEFF、face 镜像判朝向、限活动带）。
- `_role_text_binarize(...)`：V 通道 OTSU 统一二值化（采集 denoise、实时不 denoise）。
- `_get_player_screen_pos(...)`：三锚点匹配 + 显示集 + 定位仲裁 + 存 `_role_last_scores/_role_anchor_polys/_role_search_box`。
- `_open_role_recognize_window / _rebuild_anchors / _live_tick / on_close`：角色识别管理窗。
- 检测线程 A `_player_loop` 调本函数产人物坐标；蒙板 GDI 绘制搜 `_role_colors`、`yolo_crop/feat_crop`；色键 `COLOR_MAGENTA`(约 11694) 与 `SetLayeredWindowAttributes`(约 12192)。

---

## 8. 已证伪的死路（别再回头试）

读人物内存（grap-core64.aes、资源在服务器端）；破开内存数据包；光点比率/倍率绝对定位（镜头惯性、光点≠真人）；小块投票凑 N 块；整角色/服装大图、45 张连拍；整帧 connectedComponents；多锚点轮流产坐标（乱跳）；到脚补 75px（已去、off=0）；adaptiveThreshold 实时二值；带 mask 的 CCORR/SQDIFF；靠 `_climb_state` 切锚点；用品红做蒙板框色；管理窗自己抓帧全图匹配。
