# 【冒险岛世界】人物坐标实时性与同层误下跳踩坑说明书

> 对应问题：人物在第一层、人和怪明明同层一样高，却被持续判成"怪在正下方"，按住↓+跳空落层十几秒；打完怪有梯不上、发呆。
> 最终结论：人物坐标是一切距离/分层/选梯计算的根，**绝不能冻结或节流卡住，必须每帧实时**；省 CPU 只能去限频怪物 YOLO / 全图搜索。另记一条"管理员旧进程杀不掉、新代码不生效"的排障坑。

## 一、问题现象
- 真机日志连续十几秒反复刷 `[下坡] 怪在正下方Y差59,↓按住≥50ms+跳落层`，Y 差基本恒定 59（偶 54~60），人早落地、怪和人同层，却一直空按 ↓+跳。
- 打完怪旁边明明有怪/有梯子，人物却站着发呆不动；偶发梯子状态机抛 `TypeError`。

## 二、两条独立根因（必须分开看）

### 根因①：人物坐标被"节流+长保持"卡住，腾空 Y 回不到地面（主因）
屏幕坐标 Y 向下增大：人跳起来腾空时 Y 变小，落地才回到大 Y。人物识别线程 `_person_loop` 在两次重构里被加了多道"减速/保持"：

| 机制 | 引入提交/时间 | 当时用意 | 实际后果 |
|---|---|---|---|
| fps=24 节流（原 `if _in_precise or 时间到节拍才匹配`） | 9-14 `b3896f49` 拆三线程 | 多锚点模板匹配费 CPU，限每秒 24 次、没到节拍沿用上一人物点 | 坐标最快 41.6ms(1000/24) 才更新，不是每帧跟手 |
| hold=90 丢失保持（`ROLE_TRACK_DEFAULT "hold":90`，找不到时 90 拍内一直返回旧点） | 9-13 `d3fc15b7` 多锚点稳版 | 人名半透明/脸被特效挡会偶发丢帧，先顶上一点不让框闪断 | 90 拍 ÷ 24fps ≈ **最长 3.75 秒一直返回旧坐标** |
| 局部窗 180×120 + 全图限频 1500ms | 9-13 `d3fc15b7` | 平时只在上一帧附近小窗找，全图匹配贵、1.5 秒最多一次 | 瞬移/大跳冲出小窗后最长 1.5 秒才全图找回 |
| maxmove=48 跳变丢弃 | 9-14 `ff3a83c0` | 相邻帧位移 >48px 判误匹配丢弃 | 真快速移动 >48px 的真新坐标也被一起丢 |

腾空那一下的"小 Y"被沿用/保持最长数秒，下游 `_below2=(t_cy-py_layer)>下阈值` 拿这个偏小的人 Y 和怪脚比，就算成"怪在正下方"，于是空落层。**这不是刷新慢一两帧，是坐标几秒回不到最新。**

### 根因②：管理员旧进程杀不掉，改了代码一直跑旧进程（排障坑）
- 脚本以**管理员权限**启动后，普通权限(Medium)命令行 `Stop-Process/taskkill` 报 **Access denied**，旧进程杀不死；单实例 Mutex 又让新实例自动退出 → 表面"重启了"，实际一直在跑旧代码，改动完全没生效。
- debug.log 是追加写的，旧进程留下的 Traceback（例：`_ladder_realign_step ... 'int' object is not callable`，栈里调用行 6112 与当前文件 Grep 到的 5918 对不上）会误导排查，以为新代码在报错。
- **判据：报错栈行号和当前文件实际行号对不上 = 旧版本/旧进程的历史日志，不是当前代码的问题。**

## 三、定稿做法（本次实施）
1. **人物识别一帧一更新**：删掉 `_person_loop` 的 fps 节拍门，每个截图新帧都跑 `_get_player_screen_pos` 并原子发布 `_raw_char_pos`；上梯"高帧豁免"逻辑随之移除（全局都每帧，无需再豁免）。
2. **保留"找不到停原地"**：本帧没识别到才由 `_get_player_screen_pos` 内部停最后点（局部窗快跟 → 全图限频找回），这是防闪断、不是冻结——一旦识别到新点立刻覆盖。
3. **X 与显示框照常实时**，动作侧不做任何"落地基线/腾空冻结/锁定 Y"。
4. **省 CPU 与坐标新鲜度解耦**：人物局部窗匹配很轻、允许每帧；真正贵的怪物 YOLO、全图模板搜索继续限频，配合 CPU 三档。**不准靠卡人物坐标省 CPU。**
5. **备选补丁（本次未启用）**：若每帧实时后仍偶有腾空瞬态，再加"1 秒滚动窗取 Y 最大值=地面值，仅用于上下分层判定"，X 和显示仍用实时值。先靠每帧实时，不行才补。

## 四、重启脚本的正确姿势（管理员进程）
1. 普通权限杀不掉管理员 python，必须提权：`Start-Process powershell -Verb RunAs`，弹 UAC 点"是"，里面执行 `taskkill /F /IM python.exe /T`（/T 连子进程一起杀），等 2 秒，再 `Start-Process E:\conda\envs\yolo26\python.exe maple_route_ui.py`（工作目录 = maple_bot_v2）。
2. 重启后**核对新 PID 和启动时间**（`Get-Process python`），确认不是旧 PID。
3. 用"最新时间段日志、栈行号与当前文件一致、Traceback=0"确认跑的真是新代码，不被 debug.log 历史旧账误导。

## 五、验证
- 改后 `[人物耗时]`：1 秒 26 帧（跟截图帧率）、人物匹配约 359ms/秒（独立线程占单核约 1/3；8 核无压力，4 核走 CPU 三档）。
- 新进程时段日志 Traceback=0。
- 真机回归：同层不再空下跳、上梯正常、打怪跟手。

## 六、回滚与关联
- 改前快照 `a71fbf9`（人物改每帧刷新前快照）；本次仅改 `_person_loop`（去 fps 节流、docstring 同步），hold / 局部窗 / 全图限频 / maxmove 保留未动。
- 关联：打怪 Y 分层 `_below2/_above2`、人物识别 `_get_player_screen_pos`、线程 `_person_loop/_recognize_loop`；行号随编辑以 Grep 日志文案/变量名为准。

## 七、永久铁律（同步总纲开发准则 0.2）
人物坐标是全局基准，**绝不能冻结/长保持/节流卡住**；只允许"丢帧短暂停最后点、新点一来立刻覆盖"。任何让坐标"几秒回不到最新"的机制都禁止。
