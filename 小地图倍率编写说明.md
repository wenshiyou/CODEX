# 小地图倍率编写说明（完整版）

> **本文档是「小地图倍率（scale）」功能的完整编写/维护说明。**
> 记录实现方法、详细步骤、关键点，以及在开发过程中踩过的坑与解决办法。
> 后续新增/修改倍率相关功能，必须先看本文档；改完务必更新本文档，并在 PROJECT_STATE.md 的「文档索引」登记。
>
> 对应代码文件：`maple_route_ui.py`
> 核心函数：`_effective_scale` / `_update_scale_calibration` / `_finish_auto_calibration` / `_recalc_scale_from_region` / `find_player_dot` / `draw`（倍率显示段）

---

## 一、功能背景与目标

倍率（scale）用于「游戏画面坐标 ↔ 小地图坐标」的换算：
```
小地图X = 人物小地图X + (怪屏幕X - 人物屏幕X) * scale_x
小地图Y = 人物小地图Y + (怪屏幕Y - 人物屏幕Y) * scale_y
```
因为小地图 X/Y 压缩比率不同，**必须分开算 scale_x 和 scale_y**，不能默认相等。

**用户对倍率数值的核心需求（本次确立，必须遵守）：**
> 1. 倍率显示一个「总值」。
> 2. **总值 = 检测值 + 手动偏移值**。
>    - 没检测 → 检测值 = 0，则 `总值 = 0 + 偏移`（用户直接输偏移即得到总值）。
>    - 有检测 → 检测值 = 实际检测出的值，则 `总值 = 检测值 + 偏移`。
> 3. **总值一旦定下就锁定，绝不能再自动变。**（人物走动/自动校准不能改它）
> 4. **检测值可以被后续检测覆盖**（重测用新结果顶替旧结果）。
> 5. **偏移值只能手动调**，不能被检测覆盖、不能被自动校准改动。

---

## 二、关键变量（倍率相关）

| 变量名 | 含义 | 谁写它 |
|--------|------|--------|
| `_calibrated_scale_x` | 检测出的 X 倍率（检测值） | 三点检测 / 手动记录写入；可被再次检测覆盖 |
| `_calibrated_scale_y` | 检测出的 Y 倍率（检测值） | 同上 |
| `_field_values["scale_x_offset"]` | 手动 X 偏移值 | 只在倍率差弹窗手动输入 |
| `_field_values["scale_y_offset"]` | 手动 Y 偏移值 | 只在倍率差弹窗手动输入 |
| `_map_screen_scale` | 主 scale（旧代码兼容） | 跟随 `_calibrated_scale_x` |
| `_manual_calib_done` | 是否已完成手动/三点校准 | 三点校准 / 手动记录成功时设 True |

---

## 三、最终倍率（总值）计算：`_effective_scale()`

这是唯一出口，所有用到倍率的地方（打怪换算、显示）都走它。

```python
def _effective_scale(self):
    """返回最终倍率(总值) = 检测值 + 手动偏移值。
    检测值：三点检测/手动记录写入的 _calibrated_scale_x/y；未检测时为0。
    手动偏移：倍率差弹窗里用户输入的 scale_x_offset/scale_y_offset。
    总值一旦定下来就被锁定(见 _update_scale_calibration)，不再随人物走动变化。
    没检测时检测值=0，则 总值=0+偏移（用户直接输偏移即得到总值）。"""
    base_x = getattr(self, '_calibrated_scale_x', 0.0)   # 检测值，未检测=0
    base_y = getattr(self, '_calibrated_scale_y', 0.0)
    try:
        off_x = float(self._field_values.get("scale_x_offset", "0") or "0")   # 手动偏移
    except (ValueError, TypeError):
        off_x = 0.0
    try:
        off_y = float(self._field_values.get("scale_y_offset", "0") or "0")
    except (ValueError, TypeError):
        off_y = 0.0
    return (base_x + off_x, base_y + off_y)
```

**关键点：**
- `base_x`（检测值）默认取 **0.0**，不是 0.10。这样「没检测时总值 = 0 + 偏移」成立。
- 偏移值 `off_x/off_y` 用 `float()` 解析，异常时兜底为 0（防止手输非数字崩）。
- `_screen_to_map`、`_get_monster_map_pos_verified`、`_get_monster_platform`、倍率显示，全部调用 `_effective_scale()`。

---

## 四、锁定总值：`_update_scale_calibration()`

**这个函数曾经是罪魁祸首。** 它原本每帧用「滑动平均」自动改 `_calibrated_scale_x/y`，导致：人物一走动，检测值被改动 → 总值一直变。这正是用户报「总值不断变化」的根因。

**现改为直接 return（不再自动校准）：**

```python
def _update_scale_calibration(self):
    """
    调用时机：每帧人物位置更新后调用
    # ===== 用户需求：总值(检测值+偏移)定下后就固定，绝不自动变。 =====
    # 检测值 _calibrated_scale_x/y 只能由「三点检测 / 手动记录」写入，且可被下次检测覆盖；
    # 偏移值 scale_x_offset/y_offset 只能手动调。这里禁止任何自动校准改动检测值。
    # 否则人物一走动，检测值被滑动平均改动，总值就会一直漂移。
    return
```

**关键点：**
- 禁用「自动越跑越准」的滑动平均机制，**检测值只由检测/手动记录写入**。
- 如果你未来又想恢复「自动校准」，只能在检测值为 0 时启用，且绝不能覆盖用户已锁定的检测值。

---

## 五、三点校准写检测值：`_finish_auto_calibration()`

三点（X 点 / Y 点）校准成功后会写入 `_calibrated_scale_x/y`，并锁定。

**X 分支关键代码：**
```python
if axis == 'X':  # X倍率：用绿圈(右)
    ...
    dx_screen = gsx - base_sx   # 屏幕X位移（绿圈在基点右方为正）
    dx_map = gmx - base_mx      # 小地图X位移
    if dx_screen <= 0:
        self._calib_fail("X倍率：绿圈应在基点右方")
        return
    self._calibrated_scale_x = (dx_map / 2.0) / float(dx_screen)  # 除以2（记忆文件坑4）
    # 【护栏】倍率超出(0.01~100)判失败，避免算出 36555 这种垃圾值
    if not (0.01 <= self._calibrated_scale_x <= 100.0):
        self._calib_fail("X倍率异常(%.4f)，请重试" % self._calibrated_scale_x)
        self._calibrated_scale_x = 0.10  # 回退默认，避免残留垃圾值
        return
    self._map_screen_scale = self._calibrated_scale_x  # 主scale兼容旧代码
    self._add_log("X倍率=%.4f (小地图%dpx/屏幕%dpx)" % ...)
# ... Y 分支同理，写 _calibrated_scale_y
self._manual_calib_done = True   # 三点校准成功：锁定倍率
self._save_calib()               # 保存倍率到文件
```

**关键点：**
- 公式 `倍率 = (小地图位移 / 2.0) / 屏幕位移` —— **除以 2** 是关键坑（见第 8 节坑列表）。
- **护栏**：结果不在 0.01~100 就判失败回退默认，防止 `36555` 这种垃圾值污染打怪逻辑。
- 走到末尾才 `_manual_calib_done = True` 并保存——**只有真正算成功走到末尾才锁定**。
- 再次检测会走到这里重新赋值 → **覆盖旧检测值**（符合「可被再次检测覆盖」需求）。

---

## 六、区域初始值：`_recalc_scale_from_region()`

程序启动/切图时按小地图区域尺寸算一个**初始估值**（`scale_x = FIXED_W/width`）。**关键：只在该区域从未检测过时使用。**

```python
def _recalc_scale_from_region(self):
    """根据小地图区域尺寸计算X/Y scale初始值（仅当从未检测/手动记录过时使用）
    用户需求：检测值定下后固定(总值=检测值+偏移)，可被再次检测/手动记录覆盖，但区域初始化只在无检测值时生效"""
    r = getattr(self, 'map_area_rect', None)
    # 已有检测值(非0) → 保留用户检测/手动记录值，不让区域初始化覆盖（否则总值会变）
    if getattr(self, '_calibrated_scale_x', 0) or getattr(self, '_calibrated_scale_y', 0):
        return
    if r and r["width"] > 0 and r["height"] > 0:
        self._calibrated_scale_x = FIXED_W / r["width"]
        self._calibrated_scale_y = MAP_H / r["height"]
        self._map_screen_scale = self._calibrated_scale_x
        print("[scale] 初始值: X=%.4f Y=%.4f (区域%dx%d)" % ...)
```

**关键点：**
- **若检测值已存在（非0），直接 return**，绝不让区域初始化覆盖它（否则总值会变）。
- 只有这样，才能保证「用户检测后的检测值」不被程序重启/切图/刷新时覆盖。

---

## 七、倍率显示（小地图底部）

位置：`draw()` 里，小地图底部，灰底红字，**始终显示**（未校准时显示提示）。

```python
eff_sx, eff_sy = self._effective_scale()   # 最终总值
if eff_sx <= 0 and eff_sy <= 0:
    scale_text = "待设置中..."               # X、Y 都没检测值也没偏移
else:
    _parts = []
    _parts.append("X %.4f" % eff_sx if eff_sx > 0 else "X 未校准")
    _parts.append("Y %.4f" % eff_sy if eff_sy > 0 else "Y 未校准")
    scale_text = "  ".join(_parts)
# 灰底 + 红字，紧贴小地图左下角
```

**关键点：**
- 各轴**单独显示**：X 设好了先显示 X，Y 设好了再追加显示 Y，互不等待。
- 显示的是 **`_effective_scale()` 的总值**（检测值+偏移），不是裸检测值。
- 字体 2/3（`_fs=0.5`），去掉「倍率」二字，只留 `X ... Y ...`。
- 灰底条紧贴文字（左右各 1px），浅灰（RGB 210,210,210），底/左对齐。

---

## 八、踩过的坑与解决办法（必须记住）

### 坑 1：总值一直变（本次最核心的坑）
- **现象**：定完点后，`0.01+0.01=0.02` 这个总值还在往上/下漂移。
- **根因**：`_update_scale_calibration` 每帧用「滑动平均」改 `_calibrated_scale_x/y`，人物一走动检测值就变。
- **解决**：让 `_update_scale_calibration` 直接 return（禁用自动校准），检测值只由检测/手动记录写入；`_recalc_scale_from_region` 也只在该值为 0 时设初始估值。

### 坑 2：倍率算出 36555 这种垃圾值
- **现象**：倍率显示成几万，完全失真。
- **根因**：`dx_screen`（屏幕位移）很小、`dx_map` 正常，导致 `倍率 = (dx_map/2)/小dx_screen` 爆大；或人物没真走到光圈、光点检测跳飞。
- **解决**：加护栏，结果不在 0.01~100 就判失败、回退默认 0.10 并提示重试。

### 坑 3：倍率公式要「除以 2」
- 记忆文件坑 4：蒙板是小地图的 2 倍，所以 `倍率 = (小地图位移 / 2.0) / 屏幕位移`。漏了除以 2，倍率会偏大一倍。

### 坑 4：没检测时检测值不该是 0.10
- 早期 `_effective_scale` 里 `getattr(..., 0.10)` 导致没检测时总值 = 0.10+偏移，而不是「0+偏移」。
- **解决**：改成默认取 0.0，让「没检测时总值=偏移」。

### 坑 5：人物光点被"吃掉"/被固定黄色块抢走（影响检测）
- **现象**：光点检测卡在固定坐标（如 `(79,84)`），人物光点被忽略 → 录制采不到连续点 → 二点线/直线。
- **根因**：地图上怪物/地形高亮黄块和人物光点亮度相近；或 MORPH_OPEN 吃掉小光点。
- **解决**：把 `find_player_dot` 还原为「取平均亮度最高的黄色区域，再取该区域内最亮像素做中心」（去掉 MORPH_OPEN、收紧黄色阈值 H35/S100/V180）。

### 坑 6：黄色阈值过松导致候选过多
- 阈值 `H40/S80/V150` 过松会在地图上识别出 17 个黄块，光点检测不稳定。
- **解决**：收紧为 `H35/S100/V180`，减少干扰。

### 坑 7：平台录制画线"二点线"
- **现象**：平台录制只画出首尾两点连成的直线，不是跟随人物的弯线。
- **根因**：人物光点检测没跟住人物，只在起点/终点各采到一个可靠点；中间点被「同X覆盖」逻辑（`abs(last_x - new_x) < 3` 就覆盖）吃掉。
- **解决**：先修光点检测跟住人物（坑5），同时录制时确保人物光点位置连续变化。

---

## 九、以后新增/修改功能流程（必须遵守）

1. **改代码前先 git commit**（保存当前可运行状态，留回滚点）。
2. 按需求实现功能，代码加注释。
3. **每做完一个功能，写/更新一份 .md 说明文档**（步骤、关键点、坑），放项目根目录。
4. **在 `PROJECT_STATE.md` 的「文档索引」表格登记**：文件名、位置、说明。
5. 完整提交到 git（源码 + data + 配置 + MD 文档全部）。
6. 若本次是独立功能且有可运行 exe，可一并提交记录版本。

> 这样即使换了线程、记忆丢失，也能凭文档 + 总纲索引快速修复或重建。
