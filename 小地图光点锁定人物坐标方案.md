# 小地图光点锁定人物坐标方案（完整版 · 含操作流程）

> 版本：v4.0（2026-08-28）
> 目的：用可靠的小地图光点（黄点 ffff88），通过"小地图窗口(map_area_rect) + 游戏窗口(_target_window_size) + 归一化比例"锁定游戏主窗口里人物的真实坐标，并反用到怪物小地图显示。
> **核心：完全不用倍率（scale）。用几何比例（归一化 0~1），尺度不变，任何地图一套通用。**
> 对应主文件：`maple_route_ui.py`

---

## 一、方案原理（一句话）
**小地图光点在"小地图窗口(map_area_rect)"内的归一化相对位置(0~1) = 人物在游戏屏幕的归一化相对位置(0~1)。屏幕坐标 = 窗口尺寸 × 归一化位置。**
不用倍率，用几何比例（归一化 0~1），尺度不变，任何地图一套通用。

---

## 二、关键概念
1. **小地图窗口 `map_area_rect`** = 程序用"小地图文字(左)、大地图文字(右)、底部模板(底)"三个特征**自动定位**的小地图区域（`_detect_minimap()` 自动发现，不用手动）。
2. **游戏窗口 `_target_window_size`** = 窗口绑定后自动记录的 (窗口宽, 窗口高)，即 (1382, 807)。
3. **"四个锚点" = 游戏窗口四角 = `_target_window_size`/`window_rect`**，**程序自动有，不用手动点**。（示意图里的"四个蓝点"就是窗口四角。）

---

## 三、二次映射（1:1 计算 + 2倍显示）
- **第一次映射（1:1，准）**：小地图原始（1:1）+ 蒙板，做**归一化计算**——光点相对 `map_area_rect` **原始宽高**（1:1，不放大）归一化。**基准 1:1，准无误差。**
- **第二次映射（放大2倍，纯显示）**：把第一次的"小地图 + 蒙板框"**放大2倍**显示。**只影响显示，不影响计算**。

> **为什么不能"一次映射直接2倍"**：一次放大2倍后，小地图尺寸变2倍，归一化/坐标基准跟着变 → 不准。**必须 1:1 计算 + 2倍纯显示。**

---

## 四、具体操作流程
### 第1步：绑定游戏窗口
- 点窗口绑定（或自动绑）→ 程序记录 `_target_window_size`（窗口尺寸 = 四锚点）→ 小地图显示。

### 第2步：`_detect_minimap()`（自动）
- 用三特征（小地图文字左/大地图文字右/底部模板底）自动定位小地图窗口，产出 `map_area_rect = {left, top, width, height}`。
- 小地图窗口 = "小地图蒙板"，动态、自动、每张图都识别。

### 第3步：光点锁定（每帧自动）
- `find_player_dot()` 检测小地图人物黄点（ffff88）→ `_player_map_pos = (mx, my)`。
- `lock_screen_from_dot()`：
  ```
  rx = mx / map_area_rect["width"]
  ry = my / map_area_rect["height"]
  screen_x = 窗口宽 × rx
  screen_y = 窗口高 × ry
  ```
  → 人物在游戏屏幕真实坐标 `_player_screen_pos`。
- 接入 `_get_player_screen_pos()`：优先 `lock_screen_from_dot()`，光点不可用才回退模板匹配/宽限。

### 第4步：画怪（反用归一化）
- `monster_to_map(怪屏幕x, 怪屏幕y)`：
  ```
  rx = psx/win_w + (monster_sx-psx)/win_w
  ry = psy/win_h + (monster_sy-psy)/win_h
  怪小地图 = (map_area_rect["left"]+rx*win_w, map_area_rect["top"]+ry*win_h)
  ```

---

## 五、小地图显示（二次映射 + 蒙板框）
- **计算**用 1:1（`map_area_rect` 原始宽高）。
- **显示**：`MAP_SCALE = 2.0`（放大2倍显示的纯显示）。
- **蒙板框**：在小地图上画一个**绿框，以人物光点(`_player_map_pos`)为中心**，直观框住黄点（转成 map_display 坐标画方框），让你直观看到锁定范围。

```python
# 在小地图显示上画框住光点的绿框
if getattr(self, '_player_map_pos', None):
    _pmx, _pmy = self._player_map_pos[0], self._player_map_pos[1]
    _bx = int(_pmx * scale_x); _by = int(_pmy * scale_y)
    cv2.rectangle(map_display, (_bx-28, _by-24), (_bx+28, _by+24), (0,255,0), 2)
```

---

## 六、函数实现（已在 maple_route_ui.py）
### `lock_screen_from_dot()`
```python
def lock_screen_from_dot(self):
    r = getattr(self, 'map_area_rect', None)
    if not r or r.get("width",0)<=0 or r.get("height",0)<=0: return None
    mp = getattr(self, '_player_map_pos', None)
    if not mp: return None
    mx,my = mp[0], mp[1]
    rx = mx/float(r["width"]); ry = my/float(r["height"])
    win_w, win_h = getattr(self, '_target_window_size', None) or (0,0)
    if win_w<=0 or win_h<=0: return None
    return (int(win_w*rx), int(win_h*ry))
```
### `monster_to_map()`
```python
def monster_to_map(self, monster_sx, monster_sy):
    r = getattr(self, 'map_area_rect', None)
    if not r: return None
    pos = self._player_screen_pos
    if not pos: return None
    psx, psy = pos
    win_w, win_h = self._target_window_size or (0,0)
    rx = psx/win_w + (monster_sx-psx)/win_w
    ry = psy/win_h + (monster_sy-psy)/win_h
    return (int(r["left"]+rx*win_w), int(r["top"]+ry*win_h))
```
### `_get_player_screen_pos()`（接入）
```python
def _get_player_screen_pos(self, frame):
    pos = self.lock_screen_from_dot()
    if pos is not None:
        self._last_char_match_pos = pos; self._last_char_match_time = time.time()*1000
        return pos
    match = self._match_character(frame)
    if match: return (match[0]+off_x, match[1]+off_y)
    last = getattr(self,'_last_char_match_pos',None)
    if last and time.time()*1000 - self._last_char_match_time < 1500: return last
    return None
```

---

## 七、打包注意
- **用 spec 打包**，`datas=[('绝对路径\config','config'),('绝对路径\data','data')]`。
- **`upx=False`**。
- **不用** PowerShell 命令行 `--add-data "data;data"`（`;` 被吞 → data 没进 → 灰屏）。
- `cv2.imread` 读中文路径失败，用 `load_png`。
- `_on_mouse` 加 try-except（防点击全部失效）。

---

## 八、测试是否有效
| 看什么 | 有效 | 无效 |
|--------|------|------|
| 日志 `[光点锁定] 光点(x,y) 归一化(...) 屏幕(x,y)` | ✅ 在跑 | ❌ 没跑(回退模板匹配了) |
| 日志全是 `未匹配到角色(模板N套)` | ❌ 光点锁定没生效 | —— |
| 人物走动，屏幕定位点平滑跟随、不乱串 | ✅ 有效 | ❌ 需调 |
| 小地图绿框(框住黄点)跟随人物 | ✅ 可视化正常 | ❌ 光点/框不对 |
| 小地图怪物紫点位置对 | ✅ 画怪有效 | ❌ 需调 |

---

## 九、相关文件
- 主代码：`maple_route_ui.py`
- 排障：`工作记录_排障记录.md`（001点击失效/002打包灰屏）
- 黑屏：`打包黑屏问题排查与解决方案.md`

---

## 十、踩坑补充（后期维护 · 2026-09-01）

> 本节记录后期维护中光点锁定相关的踩坑经验，回滚29号后重新加功能时参考。

### 10.1 窗口尺寸必须用整个窗口，不是客户区
- **坑**：lock_screen_from_dot 误用客户区尺寸，导致 X/Y 倍率基点偏头顶
- **正确**：用 `_target_window_size` = 整个窗口尺寸（1382×807），包括标题栏和边框
- **公式**：`screen_x = win_w * rx`，`screen_y = win_h * ry`，win_w/win_h 是整个窗口

### 10.2 map_area_rect 初始化坑
- **坑**：`__init__` 后部又 `self.map_area_rect = None` 重置，覆盖了自动绑定的检测结果，导致灰屏
- **正确**：`__init__` 开头初始化 `self.map_area_rect = None`，自动绑定后 `_detect_minimap()` 设置，后部不能再重置
- **验证**：控制台显示 `Map area: 158 x 9`（而非"未检测到"）说明正常

### 10.3 光点检测稳定性
- 光点颜色：`ffff88`（纯黄色），像素数少时不稳
- 绿框钳制：左4右5上3下3，限制绿框整体移动范围，不重叠窗口边缘
- 光点在绿框内归一化 = 人物在屏幕内归一化（核心原理不变）

### 10.4 YOLO 模型打包
- **坑**：spec 没把 best.onnx 打进包，dist 目录模型缺失，每帧检测路径各 print 一次约 50Hz，任务栏疯狂跳
- **正确**：①spec 的 binaries 或 datas 加入 best.onnx ②模型缺失时只 print 一次 + 短路标志，不每帧重复检测
- **验证**：打包后检查 dist 目录下 best.onnx 存在

### 10.5 闪退诊断
- **坑**：主循环 `except Exception: pass` 静默吞异常，闪退无日志
- **正确**：①外部 IO 允许 try 但必须打印日志+降级 ②自己代码逻辑错误必须修根因 ③`__main__` 加 `faulthandler.enable()` 抓 native 崩溃栈
- **AGENTS 第8条**：禁止静默吞异常，所有异常必须可追溯

### 10.6 人物特征模板
- 特征存最后 10 张（CHAR_MAX_TEMPLATES=10），滚动保留
- 单独存储启动自加载
- 偏移反馈黄点：改偏移后等3秒触发，在人物位置闪烁5秒

### 10.7 回滚29号后重新加功能的顺序建议
1. 先验证29号基线所有功能正常（小地图、光点、校准、平台录制）
2. 逐个加回功能，每加一个打包测一次
3. 加功能前先通读同模块已有代码，沿用既有结构
4. 不另起一套、不自建平行实现
