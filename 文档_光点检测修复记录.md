# 小地图光点检测修复记录（回滚后可照此改回）

> 本文档记录针对「小地图人物光点检测、基点(红点)跑偏、怪物紫点误检」所做的改动。
> 因改动引入了"城内误检紫点"问题，已回滚到 `b69eb73`（我改动前正常的版本）。
> 若之后要恢复这些修复，**严格按本文档改回**，并优先定位/避免紫点误检副作用。

---

## 一、问题现象（当时遇到）

1. **基点/红点跑到右边**：人物没动，红点(基点)却跑到地图右边，没盖住人物黄点。
2. 人物黄点上**红点(基点)**正常后，又出现**紫点(怪物误检)**跟着人物/落在人物附近。
3. 城内(没怪)双击运行就出现紫点 —— YOLO 把人物/背景误检成怪物。

## 二、根本原因（排查结论）

1. **黄色 HSV 阈值过宽**：`YELLOW_H_LOW=20` 太宽，把地图上偏橙/偏红的点(如 H=18~24)误判成黄色候选。
   - 人物光点黄星：HSV H=27~30（纯黄）、S=119~255、V=214~255（很亮）。
   - 干扰点(偏红/梯子)：H=15~24（偏橙红），不该算黄色。
2. **光点取点方式**：`find_player_dot` 取"最亮轮廓",若地图有更亮干扰会选错。
3. **怪物换算**：`_effective_scale` 默认改 0 时，未校准 → scale=0 → `_get_monster_map_pos_verified` 怪物坐标全换算到人物，紫点聚错位置（偏下/一堆）。但此改动也需回退，避免紫点误检。

## 三、已做的改动（已回滚，文档记录改回方法）

### 改动1：黄色 H 下限收紧（已在回滚前改，现回到 20）
- `YELLOW_H_LOW = 20` → `26`（排除偏橙/偏红点被误判成黄色，只认纯黄人物光点）
- 位置：`maple_route_ui.py` 第 218 行附近
- 验证：H26-35 后黄色候选只剩人物光点 `(30,85)`，右边偏红点被排除。

### 改动2：`find_player_dot` 取"面积×亮度综合最大" + 外接矩形中心取圆心
- 原：`best = max(candidates, key=lambda p: p[3])`（取最亮），返回最亮像素。
- 改：`best = max(candidates, key=lambda p: p[2]*p[3])`（面积×亮度综合最大），用 `cv2.boundingRect(c)` 外接矩形中心作为光点正中心(实心圆形圆心)。
- 位置：`maple_route_ui.py` `find_player_dot`（约 2862 行）。
- 配套：候选结构改为 `(cx, cy, area, mean_val)`（cx=外接矩形中心X, cy=外接矩形中心Y）。

### 改动3：`_effective_scale` 默认值
- `base_x = getattr(self, '_calibrated_scale_x', 0.0)`（原本 0.0）。
- ⚠️ 若改回 0，未校准时 scale=0 → 怪物换算崩。需注意：换算用 scale 不能为 0。建议若 0 则用默认值兜底(如 0.10)，或只显示用检测值、换算用非0默认。

## 四、紫点误检问题（当前回滚要避开）

**出现原因（待进一步确认）**：改动后城内误检出紫点。可能在 `_detect_monsters` 或怪物坐标换算。
- 若回滚后紫点消失 → 是上述改动引入。
- 若回滚后仍出现 → 可能是 YOLO 本身在城区把人物/背景误检成怪，与非前述改动无关。

## 五、当前状态（回滚后）

- `maple_route_ui.py` 已回退到 `b69eb73`（我改动前正常版本）。
- 备份：`maple_route_ui_回滚前备份_20260827_112430.py`（改动前的当前版）。
- git 关键字：`bb9b4f9`(光点改H26+面积x亮度+外接矩形中心)、`2af9c71`(去人物紫点排除)、`1c5c937`(回退排除)。

## 六、以后恢复这些修复的建议

1. **先回退到干净基线**（`b69eb73` 或当前无紫点版），确认无紫点。
2. **逐一改回**：
   a. 先改 `YELLOW_H_LOW=26`，验证光点/基点是否准、**是否出紫点**。
   b. 再改 `find_player_dot` 取面积×亮度+外接矩形中心，验证红点盖人物、无紫点。
   c. 若有紫点，暂停该步，排查 `_detect_monsters`/`_get_monster_map_pos_verified`/`_effective_scale` 是否引入。
3. 改完一个小步就**打包测试**，不一次改太多。

> 核心要点：**人物光点是唯一纯黄极亮的圆形；紫色误检点要避免；未校准时换算 scale 不能为0。
