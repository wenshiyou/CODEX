# 主循环卡死第0帧修复说明书

## 问题描述
打包后启动程序，UI窗口正常显示（路线/打怪/药品等标签页都渲染了），但控制台窗口一直刷 `[冷启动] 第0帧开始`，frame_count 不增长，主循环没有正常推进，小地图不更新、人物检测不工作。

## 根因分析

### 直接原因
`_capture_map()` 在 `map_area_rect` 为 None 时抛 TypeError，主循环 `except: continue` 静默吞掉异常，`frame_count += 1` 在 continue 之后永远执行不到，导致主循环卡死在第 0 帧。

### 连锁链路
1. 启动程序 → `map_area_rect` 为 None（还没点"截取小地图"按钮）
2. 主循环第 0 帧 → `_capture_map()` → `r = self.map_area_rect`（r 为 None）→ 访问 `r["left"]` → TypeError: 'NoneType' object is not subscriptable
3. `except Exception:` 捕获 → `time.sleep(0.05)` → `continue` → 跳过 `self.frame_count += 1`
4. 下一帧还是第 0 帧 → 重复步骤 2-3 → 永远卡死

### 历史原因
29 号版本 `_capture_map()` 有 `map_area_rect` 为 None 的保护，后来合并 3 功能（人物定位框/绿框钳制/镜头死区检测）时，从 3e04150 commit 提取代码，把原来的保护覆盖掉了。

## 解决方案

### 改动 1：_capture_map() 加 None 保护
**位置**：`_capture_map()` 函数，`r = self.map_area_rect` 之后

**改前**：
```python
r = self.map_area_rect
reg = {
    "left": self.window_rect["left"] + r["left"],
    ...
}
```

**改后**：
```python
r = self.map_area_rect
# 未截取小地图时map_area_rect为None，返回全黑图，不抛异常（否则主循环卡死第0帧）
if not r or r.get("width", 0) <= 0 or r.get("height", 0) <= 0:
    return np.zeros((MAP_H, FIXED_W, 3), dtype=np.uint8)
reg = {
    "left": self.window_rect["left"] + r["left"],
    ...
}
```

### 改动 2：主循环异常打印，不再静默吞
**位置**：主循环 `map_area = self._capture_map()` 的 try-except

**改前**：
```python
try:
    map_area = self._capture_map()
except Exception:
    time.sleep(0.05)
    continue
```

**改后**：
```python
try:
    map_area = self._capture_map()
except Exception as _e:
    print("[主循环] _capture_map异常: %s" % _e)
    time.sleep(0.05)
    continue
```

## 踩过的坑 / 教训

### 1. 异常静默吞掉是万恶之源
`except Exception: continue` 会把真正的错误藏起来，导致程序看似在运行但实际卡死，排查时完全看不到错误信息。

**规则**：外部不可控操作（截图、窗口、文件 IO）允许 try，但必须打印或写日志 + 明确降级处理，不能静默。自己代码的逻辑/计算错误必须修根因，禁止用 try 包起来掩盖。

### 2. 改动时要保留原有保护
29 号版本 `_capture_map()` 有 `map_area_rect` 为 None 的保护，后来合并功能时从其他 commit 提取代码，把原来的保护覆盖掉了。

**规则**：改动前必须看清原代码的来龙去脉，不能只看要加的功能。从其他 commit 提取代码时，要对比原代码，确认没有覆盖掉原有的保护逻辑。

### 3. frame_count 增长位置要注意
如果 `continue` 在 `frame_count += 1` 之前，异常会导致 frame_count 不增长，调试日志会一直显示第 0 帧。

**规则**：写循环时要注意关键计数器的位置，避免被 continue 跳过。如果计数器用于调试日志，建议放在循环最开头，确保每次循环都增长。

## 验证方法
1. 打包后启动程序，不点"截取小地图"按钮
2. 观察控制台日志：冷启动日志应该正常推进（第 0 帧→第 1 帧→第 2 帧→...），不会一直刷第 0 帧
3. 点"截取小地图"按钮，小地图正常显示，光点检测正常工作
4. 人物特征检测、怪物检测、蒙板显示等功能正常
5. 控制台不会出现 `[主循环] _capture_map异常` 的错误信息

## 关联
- 开发工作记录 015 号
- 总纲"开发准则"第 7.3 条：空校验与生命周期（任何对象使用前必做空判断）
- 总纲"问题排查原则"第 28 条：有问题直接解决根因，不逃避、不折中、不用 try-except 绕过去
- 总纲"改动前必须做"第 0 步：先看清原代码的来龙去脉，再动手
