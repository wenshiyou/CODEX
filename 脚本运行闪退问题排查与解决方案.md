# 脚本运行闪退问题排查与解决方案

## 问题描述
- 双击运行测试.bat后，脚本闪退，黑屏提示"按任意键继续"
- 右键选择"以管理员身份运行"也不行
- PowerShell窗口是空的，没有运行Python脚本

## 根本原因
权限检测代码自动以管理员权限重启时，工作目录没有正确设置，导致重启后的进程闪退。

具体原因：
1. 脚本检测到非管理员权限，自动调用`ShellExecuteW`以管理员权限重启
2. 重启时没有正确设置工作目录（`ShellExecuteW`的第5个参数）
3. 重启后的进程工作目录不正确，导致相对路径的文件找不到，脚本崩溃闪退

## 临时解决方案（快速验证）
注释掉权限检测代码，直接运行脚本：
```python
if __name__ == "__main__":
    # === 管理员权限检查（临时跳过，用于排查环境问题） ===
    # import ctypes as _ctypes, sys as _sys, os as _os
    # def _is_admin():
    #     ...
    # if not _is_admin():
    #     ...
    #     _sys.exit()
    
    print("[权限] 已以管理员权限运行，模拟输入可正常发送到游戏")
    MinimapRouteRecorder().run()
```

这样脚本就不会自动重启，直接运行，验证脚本本身是否正常。

## 最终解决方案
修复权限检测代码，确保自动重启时工作目录正确：
```python
import ctypes as _ctypes, sys as _sys, os as _os
def _is_admin():
    try:
        return _ctypes.windll.shell32.IsUserAnAdmin()
    except:
        return False
if not _is_admin():
    print("[权限] 检测到非管理员权限，游戏以管理员运行时必须以管理员启动bot")
    print("[权限] 正在自动以管理员权限重启...")
    try:
        _params = " ".join(['"%s"' % a for a in _sys.argv[1:]]) if len(_sys.argv) > 1 else ""
        _work_dir = _os.path.dirname(_os.path.abspath(_sys.argv[0]))  # 脚本所在目录作为工作目录
        _ctypes.windll.shell32.ShellExecuteW(None, "runas", _sys.executable, _params, _work_dir, 1)
    except Exception as _e:
        print("[权限] 自动提升失败: %s" % _e)
        print("[权限] 请右键 MapleBot.exe 选择'以管理员身份运行'")
        try:
            input("按回车退出...")
        except:
            pass
    _sys.exit()
```

关键点：
1. 必须导入`os as _os`（之前漏掉了，导致`NameError`崩溃闪退）
2. `ShellExecuteW`的第5个参数必须设置为脚本所在目录，确保工作目录正确
3. 或者，用户右键点击批处理文件，选择"以管理员身份运行"，这样就不会自动重启了

## 排查步骤
1. **先确认脚本本身是否正常**：注释掉权限检测代码，直接运行脚本，如果能正常运行，说明脚本本身没有问题
2. **检查Python环境**：`python --version`，`pip list`，确认依赖库都安装了
3. **检查权限检测代码**：确认`os`是否导入，工作目录是否正确设置
4. **直接运行脚本查看错误信息**：`python maple_route_ui.py > output.log 2>&1`，然后查看output.log
5. **右键选择"以管理员身份运行"**：避免自动重启，直接以管理员权限运行

## 环境信息
- Python 3.13.13
- numpy 2.5.2
- opencv-python 5.0.0.93
- opencv-python-headless 5.0.0.93
- pillow 12.3.0
- pywin32-ctypes 0.2.3（脚本用的是标准库ctypes，不需要pywin32）

## 注意事项
- 即使账号是管理员账号，程序默认还是以普通权限运行，除非右键选择"以管理员身份运行"
- 这是Windows UAC（用户账户控制）的安全机制，普通权限的程序不能向管理员权限的程序发送模拟输入
- 所以脚本必须以管理员权限运行，否则按键/加药全部无效
