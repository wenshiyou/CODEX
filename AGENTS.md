# MapleBot 项目执行规则（每次对话自动加载，必须严格遵守）

## 零、对话启动（每次新对话必须做）
0. 新对话启动后，第一件事：读取 PROJECT_STATE.md（总纲），了解项目当前状态、文档索引、已知问题、打包记录，再开始任何操作。
0.1 **记忆机制与防遗忘（必须理解）**：
   - **对话记忆会消失**：当前对话的上下文随对话变长会被压缩，早期细节（参数值、踩坑经历）容易丢失；新对话启动后之前的对话记忆完全没有。
   - **文件记忆持久化**：AGENTS.md（规则）、PROJECT_STATE.md（总纲）、开发工作记录.md（问题记录）、git提交历史（改动记录）是持久化的，不会丢失。
   - **关键规则必须写进文件**：不能只在对话里说，必须写入 AGENTS.md 或 PROJECT_STATE.md，否则新对话就忘。
   - **每次动手前主动对照规则检查**：不依赖自觉，必须列出"对照规则"检查项逐项确认（见第4条）。
   - **上下文太长时先查文件**：不确定的参数、之前的解决方案，先 Grep 工作记录和代码，不凭记忆猜。
   - **关键节点必须重读记忆文件**（不需要定时重读，在这些节点重读即可）：
     ① 开始一个新功能/新任务前（如从修小地图切换到修加药）
     ② 打包前（对照打包规范逐条检查）
     ③ 排查疑难问题前（对照排查框架）
     ④ 对话明显变长时（感觉开始遗漏规则时）
     ⑤ 用户提醒"看记忆文件"时
   - **查找历史版本时必须说实话（2026-09-01用户强制要求）**：
     ① 找历史版本时必须确认版本号和功能对应关系，用git log --oneline确认commit日期和功能描述
     ② 找到了就是找到了，没找到就是没找到，必须第一时间直说"XX版本没找到XX功能"，不允许混淆版本（如把28号的基础实现当成31号的完整功能）
     ③ 不允许假装找到了、不允许用印象代替事实、不允许用"应该是"代替确认
     ④ 任何事情都要有反馈结果，查找过程中及时向用户汇报进展（找到/没找到/找到的是什么）
   - **每次动手前必须先看PROJECT_STATE.md总纲（2026-09-01用户强制要求，最高优先）**：
     ① 每次遇到问题、或动手写/改/加任何代码前，第一件事：打开PROJECT_STATE.md总纲文件
     ② 在总纲的"文档索引"里找到本次要用到的对应功能的说明书.md，完整读取，深入理解该功能的实现原理、参数、踩过的坑
     ③ 弄清来龙去脉（这个功能怎么完整跑通的、都连着谁、数据从哪来到哪去），再动手
     ④ 不允许不看总纲和说明书就凭记忆/印象直接改代码
     ⑤ 总纲里没有对应说明书的功能，先查开发工作记录.md和git历史，确认后再动手
     ⑥ **必须按最新日期的MD文档来（2026-09-01用户强制要求）**：同一个功能有多个版本说明书时，必须用最新日期的版本；旧版本只作历史参考。如果最新实现没有对应说明书，先查开发工作记录.md和git历史里最新的commit，确认后再动手。每次功能更新后必须同步更新说明书到最新版本。

## 一、改动前必须做（铁律）
**★第0步 · 改动前影响面分析（最高优先，强制，项目后期尤其重要，2026-09-01血的教训）**
- **先看清原代码的来龙去脉，再动手**：改任何一处前，必须先把它放进整个项目链路里看清楚，不能只盯着眼前这个功能/这几行。
- **必做的影响面排查（逐项 Grep 确认，不许凭印象）**：
  1. 这个函数/变量/数据结构**被谁调用、谁在读写**（Grep 全部引用点），数据从哪来、流到哪去；
  2. 它的**数据类型/结构**是什么（是列表还是字典？是单对象还是列表？），改动后所有使用点是否仍成立；
  3. 改这一处会**牵连哪些模块/链路**（保存、加载、切换方案、UI显示、匹配、打包），会不会"牵一发而动全身"；
  4. 项目已到后期，**优先在原有实现上做最小增量改动**，能改 1 处绝不重写 1 个函数，更不许另起一套平行实现；
  5. 拿"历史上正常工作的版本"（git show 旧commit）逐行对比，只动需要变的那几行，其余一字不改。
- **反例（本次真实踩坑）**：把"保存最后1张"改成"保存10张"时，没先看清 `_char_templates` 是列表、没看清它还被 F4保存/切换方案/UI下拉/模板匹配多处使用，就照搬后期版本重写，还顺手改了保存和切换逻辑，结果连锁改坏、越修越远、一张都存不了。
- **一句话**：先搞懂"它现在是怎么完整跑通的、都连着谁"，再决定"最小改哪里"，严禁只看当前功能就下手。

1. 先 git commit 提交当前快照（源码+config+data/json，排除图片/exe/build/dist）
2. 新增功能或改动前，先通读同模块已有代码，**并按上方"第0步"做完整影响面分析（Grep全部引用点、搞清数据类型与数据流向、列出牵连链路）**；严格沿用其格式与原理、保持整体统一，优先复用现有方案、在原实现上做最小增量，不另起一套、不自建平行实现（坐标体系、截图方式、状态机全局只留一套）；凡涉及共用逻辑/坐标/数据，一律以原有实现为准、不擅自改动，确实走不通时先说明并经用户确认后再改
3. 先说清楚：①我的理解 ②改哪里、改成什么（精确到行/最小改动范围）③为什么改 ④影响面：牵连哪些函数/链路、会不会动到正常工作的部分，用户确认后再动手
4. **每次动手前必须主动列出"对照规则"检查项**：①已提交快照 ②已做第0步影响面分析（看清来龙去脉/引用点/数据类型/牵连链路）③已说清最小改动说明 ④用户已确认。四项全过才能动手，形成肌肉记忆，不靠自觉。
5. 有记忆文件/备份/行业标准的，先查再动手，不凭感觉猜
6. 调用任何函数前，先 Grep 看函数定义和参数，不凭记忆猜

## 二、改动中必须遵守
7. 只做需求内的事，不脑补、不瞎加、不造假
8. 禁止用 try-except 吞错误、掩盖问题。严禁 `except: pass` / `except Exception: pass` 这种静默吞异常的烂写法——异常被吞会导致闪退无日志、bug 烂在里面查不到。外部不可控操作（截图、窗口、文件 IO）允许 try，但必须打印或写日志 + 明确降级处理，不能静默；自己代码的逻辑/计算错误必须修根因，禁止用 try 包起来掩盖。所有异常必须可追溯。
9. 所有数值限界、所有对象做空判断、所有 If 有兜底 Else
10. 魔法数字抽离常量，变量命名语义清晰
11. 关键步骤写注释（解释逻辑意义，不复读代码）
12. 用户没说要换的东西（背景图、配置、参数），一直用原来的，绝对不自己换
13. 有参考图的，一比一复现，量好尺寸和相对距离，不猜

## 三、改动后必须做
14. 再 git commit 一次（改好确认没问题后）
15. 每完成一个功能，写一份 .md 说明文档（步骤/关键点/坑），并在 PROJECT_STATE.md 文档索引登记
16. 打包测试验证，不一次改太多
17. 每次备份 push 到 Git 远程仓库
18. **★改好后必须全链路验证，发现影响别的功能立马回滚（最高优先，2026-09-01血的教训）**：
   - 改完任何一处后，必须验证**所有牵连功能**（不只是改的那个功能），确认没把正常工作的部分搞坏；
   - 验证清单：改的功能本身 + 第0步影响面分析里列出的所有牵连链路（保存/加载/切换方案/UI显示/匹配/打包/蒙板/倍率等）；
   - **一旦发现影响到别的功能（哪怕改的功能本身是好的），立马 `git checkout` 回滚到改动前快照，不许"顺便修一下"越修越远**；
   - 回滚后重新做影响面分析，找出牵连点，换更小的改动范围或换方案，再说清楚给用户确认；
   - 反例：改人物特征10张时，没验证切换方案和F4存方案，结果切换方案把10张覆盖成1张、F4存方案丢失人物特征，还试图"顺便修"，越修越远，最后回滚到29号基线重做。

## 四、绝对禁止
18. 禁止私自新增功能（需求没写的特效/判断/逻辑/弹窗/动画一律不写）
19. 禁止假代码、占位代码、空函数、TODO
20. 禁止残留无效逻辑（废弃/重复/冗余更新一律删）
21. 禁止说谎，做了就是做了，没做就是没做，做不了直接说+给可选方案

## 五、打包规范
22. 用 cmd /c 包裹打包命令（PowerShell 会吞分号导致 data 没进去）
23. 用 onedir 模式（不用 onefile，路径不稳定）
24. 打包前关闭正在运行的 exe
25. 打包后验证 dist/data 目录文件完整（特别是 minimap_region.json、ui_*.png）
26. 项目没有 assets 目录，打包时不要加 --add-data assets;assets

## 六、问题排查原则
27. 三次还没改成功的，直接找网上资料参考，不卡在那里乱改
28. 有问题直接解决根因，不逃避、不折中、不用 try-except 绕过去
29. 上下文太长时，先查记忆文件再操作
30. **★排查未知问题必须先加日志定位根因，禁止猜（2026-09-01用户强制要求）**：遇到未响应/卡顿/偶发bug等未知问题，第一步不是改代码，而是在关键节点加时间戳日志（print带elapsed时间），打包让用户运行，看日志停在哪一步，定位根因后再修。日志只在前N帧打印，不影响正常性能。禁止"应该是XX问题"就直接改代码。

## 七、代码生成核心规则（所有代码100%遵守，违规直接重写）
### 7.1 绝对铁律
- 严禁私自新增功能：需求没写的特效/判断/逻辑/弹窗/动画/机制，一律不写
- 严禁假代码/占位/空函数：禁止 TODO、空回调、假接口、无逻辑空方法，所有代码必须可直接运行
- 严禁逻辑漏洞：所有分支有兜底，所有数值有限界，所有对象有空校验
- 严禁残留无效逻辑：废弃/重复/冗余更新/无效监听全部删除
- 严禁随机发挥样式/结构：代码结构、变量规范、逻辑层级全程统一

### 7.2 代码规范
- 变量、函数命名语义清晰，禁止 a/b/c/temp/num 无意义命名
- 魔法数字必须抽离常量，禁止零散硬编码
- 代码分层清晰：参数定义→初始化→核心逻辑→触发判断→兜底逻辑
- 关键步骤写注释，只解释逻辑意义，不复读代码
- 无用 Print/Log 默认关闭

### 7.3 空校验与生命周期（杜绝幽灵BUG）
- 任何对象使用前必做空判断，杜绝空引用
- 对象销毁/禁用后：立刻停止一切逻辑、监听、计时、更新，禁止销毁后继续执行
- 所有定时器、回调、帧事件：对象销毁必须强制关闭/清除，杜绝内存泄漏、幽灵执行
- 循环内禁止频繁 new 对象、重复高耗运算

### 7.4 输入与交互（杜绝乱触发、连点、误触发）
- 所有按键、鼠标、点击逻辑必须防抖、防重复触发，一帧不准多次执行同一动作
- 禁用状态 = 视觉置灰 + 关闭监听 + 完全无响应，缺一不可
- 被遮挡、未激活、已销毁对象，绝对不响应点击/按键
- 区分：按下、按住、抬起、点击，逻辑不混用

### 7.5 UI组件标准（彻底解决乱跑、可编辑、点击无效）
- 所有UI位置、尺寸固定，不允许自适应乱飘、窗口缩放错位、重叠乱跑
- 禁用输入框：只读+禁用双锁定，不能输入、不能粘贴、光标无法进入
- 按钮禁用必须同步：视觉置灰 + 移除点击事件，禁止"看着不能点实际能点"
- 无需求不允许加动画、渐变、闪烁、悬浮特效

### 7.6 数值与逻辑安全（杜绝越界、负数、卡死）
- 所有数值自动限界：血量、数量、坐标、计数禁止负数、禁止越界
- 所有 If 判断必须有兜底 Else，禁止逻辑断层
- 禁止死循环、高频递归、无终止循环
- 状态逻辑统一管理，同一时间只允许一个状态，禁止状态抖动、来回切换

### 7.7 最终自查清单（每次生成代码必须自检）
- [ ] 是否私自加了多余功能
- [ ] 是否存在假代码、空函数、占位逻辑
- [ ] 是否有未处理的边界情况
- [ ] 是否有未清理的定时器/监听
- [ ] 是否有空引用风险
- [ ] UI是否固定不乱跑、禁用是否彻底生效
- [ ] 数值是否限界、If是否有兜底Else

## 八、问题诊断与解决框架（每个问题必须按此响应）
### 8.1 问题诊断（先想清楚再动手，简要展示推理）
- 环境验证：先确认屏幕分辨率/DPI/游戏版本/窗口模式/系统语言/杀毒，不一开始就怀疑代码
- 可复现性：必现还是偶发？偶发优先怀疑时序竞争/资源未释放/外部弹窗
- 隔离变量：明确区分"现象"和"猜测"，哪些确定哪些推测
- 最小复现：要求最简复现步骤，不要整份脚本

### 8.2 解决方案输出（成熟工程师标准）
- 原因判断：一句话说清根本原因，不绕弯子
- 备选方案：至少2个，标注【稳妥型】（稳定性高）和【激进型】（性能优），说明推荐及理由
- 代码实现：可用代码，含异常捕获+重试+关键日志+import说明
- 验证方法：怎么测、观察什么指标
- 风险提醒：副作用（CPU/帧率/反作弊等）

### 8.3 行为准则（不许违反）
- 禁止猜测API：不确定的库明确说"我需要确认XXX"，不编造（与第6条Grep函数定义互补）
- 禁止过度设计：5行能修的不给50行框架（与第7条"只做需求内的事"互补）
- 禁止跳过根因：加sleep必须说明真正时序问题+sleep为何生效+隐患（与第28条"解决根因"互补）
- 必须先怀疑自己：说"游戏改了/系统问题"前先查代码逻辑和边界
- 长时间运行敏感：任何建议必须考虑12小时+稳定性，指出内存/句柄/CPU累积问题

### 8.4 报错处理（给报错时必须做）
- 真正位置：不只看最后一行堆栈，向上追溯根源
- 区分触发条件和根本原因：如"找不到图片"是触发，根因可能是截图时机/窗口遮挡
- 给出至少一种绕过方案+一种根治方案

### 8.5 未知BUG排查模板（通用万能，绝大多数未知bug优先用）
**核心原则（科学排查法+最小信息熵原则）：** 先排查后修复，禁止上来就改代码；全程区分【客观事实】和【推测】，推测必须标注。
- **三个严禁**：严禁直接给出修复代码、严禁默认某一假说成立、严禁跳过验证步骤直接下结论，所有推导必须有对应证据支撑。
- **固定框架**：六步顺序不得增减、不得打乱（事实锚定→分层假说→单假说验证→排查路径→分阶段修复→兜底发散）。
- **假说标准**：每个假说必须可证伪、有明确触发边界、不模糊笼统，且分属不同系统层级（业务逻辑层/运行时层/依赖服务层/环境基础设施层），禁止同层级重复。

**第一步：客观事实锚定（信息清洗）**
- 拆分【已确认客观现象】和【待验证主观推论】，严格区分，禁止混淆
- 列出排查必需但缺失的核心信息（无则标注"无"）
- 信息不足时停在这一步，禁止脑补信息

**第二步：分层故障假说（4种，分属不同系统层级，按概率从高到低排序）**
- 假说必须满足：可证伪、有明确触发边界、不模糊笼统
- 必须分属不同层级：业务逻辑层/运行时层/依赖服务层/环境基础设施层，禁止同层级重复
- 每个假说给初始置信度(0-100%)+依据

**第三步：单假说验证体系（每个假说独立输出）**
- 证实证据：出现哪些客观现象，判定该假说成立（置信度≥90%）
- 证伪证据：出现哪些客观现象，直接排除该假说
- 取证操作：按执行成本从低到高排序，明确每一步能拿到什么证据

**第四步：最优排查路径规划**
- 按「低成本验证优先、高排除效力优先」原则排序操作步骤
- 每步完成后更新各假说置信度，标注可提前排除的假说
- 标注关键决策点（拿到某证据后，可直接收敛到某一类根因）

**第五步：分阶段修复方案（仅基于假说，绑定验证结果，禁止通用方案）**
- 临时止血方案：最快恢复业务，附操作成本/业务影响/回滚方式
- 根因修复方案：彻底解决，附改动范围/技术风险/依赖条件
- 防御性加固方案：避免同类复发，附落地成本/收益范围
- 所有方案必须标注【前置验证条件】（必须拿到什么证据才能执行）

**第六步：兜底发散排查（所有假说被证伪时）**
- 给出3个非常规方向：边缘边界条件/底层内核硬件/时序竞态/第三方依赖隐性变更
- 每个说明具体排查切入点

**强制约束：**
1. 全程区分【客观事实】和【推测结论】，推测必须标注【推测】前缀
2. 禁止"应该是""大概率是"等模糊表述替代证据，概率判断必须给依据
3. 修复方案必须后置，且绑定对应假说的验证结果，禁止上来就给代码修改
4. 信息不足时先完成第一步事实锚定，再推进后续，禁止脑补

**精简快速版：**
现象：____，我不知道bug根源。不要直接大改代码。先做事实锚定（区分现象vs推测），再分层构建假说+逐个验证，最后绑定假说给分阶段修复方案。不确定点标注【推测】，禁止脑补。

## 九、高阶工程思维（游戏脚本专用，优先级高）
### 9.1 高阶思维总纲
- 收到需求禁止直接写代码：先拆解子模块，梳理目标/风险/失败场景，输出方案思路后再编码（与第3条"先说清改动说明"互补，本条强调模块拆解）
- 遇到BUG不死磕同一参数/实现：分层排查（截图层/识别层/坐标层/执行层/业务层）
- 异常时优先验证手段：打印关键中间数据缩小范围定位根因，不盲目乱改碰运气
- 站在真实游戏环境思考：卡顿/噪点/闪烁/窗口移位/最小化/遮挡/帧率波动/抗锯齿/分辨率缩放，全部纳入考虑

### 9.2 图像定位模块
- **识别目标必须是原游戏图层，绝对不能识别蒙板上的标志（2026-09-01用户强制要求）**：蒙板上有大量绘制的标志（人物黄点、怪物框、水平线、倍率标记等），如果图像识别去匹配蒙板上的标志，会导致识别混乱、误匹配、定位不准。所有模板匹配/图像识别必须针对原始游戏截图（未叠加蒙板的干净画面），蒙板只用于显示，不用于识别。
- 目标固定优先ROI局部检测，仅初始化/连续失败后全图扫描更新ROI
- ROI扩展做图像边界保护，杜绝坐标越界
- 识别失败用计数器状态管理，不每轮全图匹配
- 多匹配候选点增加过滤逻辑，不直接拿第一个结果
- 阈值/偏移/重试/超时全部提取顶部配置区，禁止业务逻辑硬编码（与第10条魔法数字互补）

### 9.3 流程与控制逻辑
- 禁止无保护裸while True，必须有停止开关/最大超时/退出条件（与7.6禁止死循环互补）
- 优先状态机架构，业务拆分为独立状态，避免大段逻辑堆砌
- 少用固定sleep硬延时，优先等待式逻辑（条件达成再继续），必要时可控随机延时模拟真人（与8.3禁止跳过根因互补）
- 每个步骤考虑失败分支，不只写理想流程

### 9.4 代码结构与调试
- 功能拆分为细粒度单一职责函数（截图/查找/校验/执行/判断），模块解耦
- 强制埋入调试日志：截图状态/匹配分数/原始坐标/裁剪区域/当前状态
- 预留调试开关，一键开启详细输出，正式运行关闭冗余日志（与7.2"无用Log默认关闭"互补）

### 9.5 异常容错与排障
- 截图失败/窗口丢失/目标找不到/坐标越界/噪声干扰，不允许崩溃，要有降级/重试/状态回退/告警
- 疑难BUG分层排查5步：①输入源（截图画面是否正确）→②识别输出（分数/坐标是否符合预期）→③坐标计算/ROI校验→④动作模块（点击/发送是否生效）→⑤业务状态流转
- 当前方案持续问题，主动提出备选实现，不在错误路线反复修改

### 9.6 键鼠窗口动作
- 场景区分前台模拟输入/后台窗口消息发送，按需选用
- 动作增加可控随机偏移/间隔，规避机械化行为特征

### 9.7 输出规范（每次交付必须包含）
1. 问题拆解与方案设计（子模块/检索策略/状态机/容错/风险点）
2. 完整可运行代码（关键位置注释设计思想）
3. 参数说明（每个配置参数作用、调大调小影响）
4. 调试指引与已知风险（排查BUG操作步骤）
5. BUG修改优先局部精准修复，保留原有架构；底层缺陷时明确指出根因+备选实现

### 9.8 严格禁止
- ❌拿到需求直接写代码，跳过方案拆解
- ❌无脑循环全图模板匹配，暴力消耗CPU
- ❌到处散落硬编码数字
- ❌无任何保护的无限死循环
- ❌遇到问题只反复调参数碰运气，不定位根因
- ❌只处理理想成功流程，忽略异常场景
- ❌拿到识别结果不校验直接使用，忽略边界/多目标干扰

## 十、Git备份与恢复规范（标准操作流程）
### 10.1 日常提交（每次改完代码/做完功能）
- 提交范围：源码 + config + data（json），**排除图片和 exe**
- **提交信息命名规则（强制，2026-09-01用户要求）**：时间 + 备注 + 全部正常功能的名字，用"+"连接。格式：`YYYY-M-D号 HH点mm分 功能1正常+功能2正常+功能3正常...`
  - 例子：`2026-9-1号 12点04分 人物特征保存十张正常+特征偏移正常+平台录制正常+X倍率正常+蒙板正常+小地图光点正常+打包不灰屏正常`
  - **目的**：下次要找某个功能正常的版本时，看提交信息就知道，不用一个个试
  - **每次提交都要把当前已验证正常的功能全部写上**，不要只写改了什么
  - 修复好一个功能后，立即加到提交名字上
- 提交后 push 到远程仓库

### 10.2 安全恢复/回退（想退回以前版本）
- 恢复到某个提交时，**先看差异/出方案，禁止直接覆盖当前文件**
- 必须恢复三样：源码 + config + data（json），缺一不可（只恢复源码会导致配置/数据丢失，功能异常）
- 恢复后验证功能是否正常

### 10.3 查看历史节点
- git log 查看最近提交历史，列出时间节点和每次改了什么

### 10.4 排查"恢复后不一样"
- 检查 .gitignore，确认关键文件（data/、config/、平台json、日志）是否被 git 管到
- 被 .gitignore 排除的文件会导致恢复不完整，需单独备份

### 10.5 一键自动备份（防git排除的运行时文件丢失）
- 把 源码 + data（config的json）+ 配置 打包成带时间戳的 zip
- 每次新名字，**绝不覆盖前面的备份**
- 存到指定备份目录
- 适用于 git 排除的运行时文件（如 png 图片、exe、日志）的完整快照

## 十一、文档与工作记录更新规范
### 11.1 文件分工
| 文件 | 用途 | 更新时机 |
|------|------|----------|
| AGENTS.md | 项目执行规则（本文件） | 规则变更时 |
| PROJECT_STATE.md | 项目总纲（状态/文档索引/已知问题/打包记录/固定值） | 每次完成功能/解决问题/打包后 |
| 开发工作记录.md | 综合工作记录（版本历史+问题记录+踩坑记录） | 每次解决问题/打包版本后立即记录 |
| 工作记录_排障记录.md | 专项排障记录（点击失效/打包灰屏等） | 每次排障完成后 |
| 单独功能 .md | 每个功能的详细说明文档 | 每个功能完成后 |

### 11.2 工作记录更新时间（强制）
**以下时间点必须写工作记录，不得事后补写：**
1. **每次解决一个问题后** — 立即记录（现象→根因→解决→验证）
2. **每次打包一个版本后** — 记录版本历史（版本号/commit/日期/改动/状态）
3. **每次踩坑后** — 记录坑和解决方案（避免下次重复）
4. **每次回滚/恢复后** — 记录回滚原因和恢复结果
5. **用户要求"工作记录一下"时** — 立即记录当前操作
6. **★用户说"XX功能好了"时（强制，2026-09-01用户要求）** — 立即把此次功能的说明书更新一下：①功能实现说明 ②踩过的坑 ③解决方案写清楚 ④登记在总纲PROJECT_STATE.md文档索引中。不能只在对话里说，必须写成.md文件并登记到总纲。

### 11.3 工作记录内容规范（每条记录必须包含）
1. **编号**：递增（001、002...）
2. **日期**：精确到天（2026-08-28）
3. **关键词**：5-10个（方便后续搜索，如"蒙板、SetWindowOrgEx、坐标偏上"）
4. **现象**：用户描述+截图说明（必现还是偶发）
5. **排查过程**：试过哪些方法，哪些无效（关键突破点标注）
6. **根因**：精确到代码行/逻辑（不只看最后一行堆栈）
7. **解决方案**：具体改了什么，附关键代码
8. **踩过的坑**：避免下次重复的教训
9. **验证方法**：怎么确认修复成功（观察什么指标）
10. **关联提交/文档**：git commit id、关联md文件路径

### 11.4 版本历史记录格式（表格）
| 版本 | git/commit | 日期 | 改动内容 | 状态 |
|------|-----------|------|----------|------|
| v49 | xxx | 2026-08-28 | 光点锁定+输入框修复 | 测试中 |

### 11.5 PROJECT_STATE.md 更新规范
- **文档索引**：每次新增 .md 文件后，在文档索引表格登记（文件名、位置、说明）
- **已知问题**：解决后更新状态，新增问题及时添加
- **打包记录**：每次打包后更新版本和状态
- **固定值**：新增常量时添加，修改时更新
- **格式**：`#` 一级标题、`##` 二级、`###` 三级，表格用 `|` 分隔，列表用 `-`

### 11.6 功能说明文档规范
- 每个功能完成后，单独写一个 `.md` 文件
- 内容：实现步骤、关键点、踩过的坑、验证方法
- 文件名：`功能名说明书.md` 或 `功能名方案.md`
- 完成后在 PROJECT_STATE.md 文档索引登记

---
**核心原则**：高效、成熟、稳定。不多做、不漏做、不瞎做、不报错、不BUG、逻辑干净、完全贴合需求。
