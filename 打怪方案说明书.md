# 打怪方案说明书（战斗逻辑）

> 2026-09-04/05 用户逐条定稿。记录本次会话选定的打怪方案、实现要点、关键变量。排查打怪问题先看这里。

## 一、核心目标

打怪要**有效率 + 准**。关键：**人物位置和怪位置必须同一瞬间采样（同帧）**，才能算准 X/Y 差、正确判断"能不能打"。所以检测放同帧。

## 二、打怪判定：X/Y 分开算（治"上平台怪从下面空打错位"）

- **X差(水平) ≤ 350**（技能射程，面板 `atk1_distance`）**且 Y差(垂直) ≤ 80**（`ATTACK_Y_BAND`）才打。
- **不用欧氏距离**（那条连怪绿线的长度），因为正上方/上平台的怪 X差小、Y差大，欧氏距离很小，会被误判在射程内 → 从下面打够不着 = 空打错位。
- 实现：`combat_logic.select_combat_target` 里用 `x_gap = abs(cx-px)`、`y_gap = abs(cy-py)`；`cast` 只认 `tier0`（同平台 + Y差≤80 + X差≤350）。X>350 或 Y>80 → 改为"靠近/跨层"（走过去/爬过去）。

## 三、打怪方向（治"反向打"）

- **不识别人物朝向**（识别不准）。**打怪前先朝怪方向按一次方向键**再打（怪在右按→，怪在左按←）。
- 用 `_combat_last_face_dir`（上次让角色朝怪的方向）判断要不要重转：方向变了才按一次，**第一次必按**；方向一致不重复按（不打断攻击节奏）。

## 四、空怪两层机制（治"打一下无血条无伤害还一直打"）

- **锁怪前**：投票式空怪过滤 `_filter_static_monsters` —— 同一位置被连续识别 **3 次**仍不动 = 假怪(建筑/背景误检)→ **临时排除 3 秒**，时间到恢复正常，若还不动再排除。替代旧的"1秒计时"。
- **锁怪后**：`打一下后无血条也无伤害数字 = 空怪` → drop；并记下该位置进 `_combat_dropped_phantoms`，**5 秒内不再重锁它**（防"drop后又锁同一只"死循环空打）。drop 后重置 `_combat_target_attacked`。

## 五、后台检测线程（方案B，治"主线程太厚重、几秒才刷一次"）

- **人物匹配 + 怪匹配 + YOLO + 血条**放到**同一个后台检测线程**，用**同一帧**算（同帧同步 → 距离准）。
- 主线程只读后台结果 + 过滤假怪，做战斗/小地图/蒙板（不再做重活 → 不卡、按时刷）。
- 线程安全：后台线程**用自己的 mss 实例**（不共用主线程 `self.sct`），怪列表用原子替换；2 秒宽限防漏检闪没。
- **线程数：3 个常驻** = 主线程 + 蒙板线程(已有) + 后台检测线程(新增 1 个)。

## 六、战斗接管 zhi"打几下就不动"

- `_combat_active`：怪进技能范围(cast/pursue)时置 **True** → **暂停巡路**（`_random_step` 开头判断，则释放移动键并 return）。cross/switch/idle 恢复 False。
- 否则巡路一直走，人物被巡路带着移动，攻击被"移动中不发技能"拦住 → 一直 cast 但不出手。

## 七、400×400 人物锁定绿框（默认隐藏）

- 绿框数据保留不删，`_show_char_roi` 默认 **False**（不画）。要显示就置 True。绿框只在蒙板循环里按开关绘制。

## 八、用 .py 直接测试（免打包）

- 根目录双击 `跑测试.bat`（自动检测管理员权限 → UAC 提权）。**已修 .py 自动提权重启漏脚本名**的问题（否则重启会开成 Python 交互窗口 `>>>`）。
- 我改完代码，你**重启脚本**即生效，不用每次打包 EXE。控制台能看到 `[打怪] 主攻`、`[检测线程] 已启动`、异常等。

## 九、关键常量 / 文件 / 变量

| 项 | 说明 |
|---|---|
| `ATTACK_Y_UP = 80` / `ATTACK_Y_DOWN = 30` | 技能Y范围·方向性：怪比人高≤80px、怪比人低≤30px（2026-09-05 下30由50改），超出够不着→走近 |
| `skill_range = atk1_distance` | 技能水平射程(面板，默认350) |
| `COMBAT_FAR_RANGE = 500` | 同平台探测带 |
| `LAYER_Y_GAP = 150` | 同平台判定(怪脚Y-人物脚Y≤150) |
| `combat_logic.py` | 决策核心：`select_combat_target`/`combat_step`/`lock_status`/`decide_attack` |
| `maple_route_ui.py` | `_combat_tick`/`_filter_static_monsters`/`_filter_dropped_phantoms`/`_detection_loop`/`_set_combat_move`/`_combat_active` |
| `test_combat_sim.py` | 决策核心合成场景测试（34/34 通过） |
| `跑测试.bat` | 双击用 .py 跑(自动提权) |

## 十、排查打怪问题顺序

1. 看控制台 `[打怪] 主攻` 是否出现（真出手）。
2. `[打怪受阻] 移动中不发技能` —— 若刷，看 move_dir/held 是否被巡路/移动拦住。
3. `[距离]` —— 看 X 差是否 ≤350、人物是否站得稳。
4. `[新决策] state/cast/pursue` —— 看决策是否正确。
5. `[静态过滤]` / 空怪 drop —— 看有没有被误剔/空打。
