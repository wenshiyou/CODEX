# 【冒险岛世界】强绑窗口尺寸 + 小地图三模板定位 说明书

> 适用：MapleStory Worlds（冒险岛世界，进程 msw，窗口标题含 "MapleStory"）。旧"冒险岛怀旧服"仅作历史参考，本文以冒险岛世界为准。
> 定稿日期：2026-09-06　对应提交：5a7f7d9（强绑窗口尺寸+重新录制小地图三模块）
> 主源码：`maple_route_ui.py`　配置：`data/minimap_region.json`　模板：`data/templates/`

---

## 一、强绑游戏窗口尺寸（全窗口 1276×749，不区分客户区）

### 1. 定稿值
- `GAME_W = 1276`、`GAME_H = 749`（`maple_route_ui.py` 约 289–290 行）。
- 口径：**GetWindowRect 全窗口尺寸（含标题栏/边框），统一按"一个窗口"算，不区分客户区，以后也不区分。**
- 历史：游戏更新前写死 1290×756；2026-09-06 游戏更新后实际全窗口变为 1276×749，已改。**再变尺寸只改这两个常量，全程序自动跟随。**

### 2. 三个关键函数
| 函数 | 位置(约) | 作用 |
|---|---|---|
| `_update_window_rect` | 1128 | `GetWindowRect` 取全窗口 left/top/width/height 存 `self.window_rect` |
| `_save_target_window_size` | 1215 | 绑定后调用：①写死 `_target_window_size=(GAME_W,GAME_H)`；②移除 `WS_THICKFRAME`（**禁止鼠标拖拽改大小**，保留标题栏仍可移动位置）；③立即调一次 `_ensure_window_size` 不等主循环 |
| `_ensure_window_size` | 1228 | 当前全窗口尺寸与目标差 >2px 就 `SetWindowPos` 拉回（`SWP_NOMOVE|SWP_NOZORDER|SWP_NOACTIVATE`：只改大小、不挪位置、不抢焦点） |

### 3. 触发时机（"绑定即强制、之后一直纠偏"）
- **自动绑定**：`__init__` 找到窗口 → `_save_target_window_size()`。
- **手动绑定**：准星拖拽 `_bind_window` → 同一个 `_save_target_window_size()`，自动/手动同步。
- **主循环**：每 30 帧 `_ensure_window_size()` 巡检，被改就拉回。
- 结论：**只要绑定，窗口立刻变 1276×749，且永远不能被手动改大小（只能移动位置）。**

### 4. 改尺寸步骤（以后再变照做）
1. 改 289–290 的 `GAME_W/GAME_H`；
2. 全局搜旧尺寸数字（如 1290/756）确认没有别处写死；
3. 重启脚本绑定，看游戏窗口是否被拉到新尺寸、`data/minimap_region.json` 是否正常。

---

## 二、小地图三模板自动定位（唯一自动方案）

### 1. 三张模板（2026-09-06 游戏更新后重录，位于 `data/templates/`）
| 文件 | 尺寸(宽×高) | 是什么 | 定位作用 |
|---|---|---|---|
| `minimap_title.png` | 55×14 | 左上角 "MINIMAP" 标题 | 定**左边界、上边界**（mini_x, mini_y） |
| `bigmap_title.png` | 16×20 | 右上角关闭(X)按钮 | 定**右边界**（big_x） |
| `minimap_bottom.png` | 123×11 | 小地图底边长条 | 定**下边界**（bottom） |

- 匹配方法：`cv2.matchTemplate(TM_CCOEFF_NORMED)`，阈值 **0.55**。
- 游戏更新会重绘 UI（字体/图标/边框），模板是"死认像素"，**UI 一变相似度就掉、定位失败 → 必须用最新游戏画面重新截这三张模板**（本次实测旧模板大写标题/灰色按钮/旧底边全部失配，换新后离线匹配 val=1.00 / 0.95 / 1.00）。

### 2. 定位流程（`_detect_minimap`，约 1264 行）
1. 截全窗口 frame（`_capture_window`，mss 按 window_rect 抓屏）。
2. **标题**：在 `frame[0:120, 0:300]` 内匹配 `minimap_title`，得 `mini_x, mini_y`；`val<0.55` → **本帧直接 return（不定位、不覆盖旧区域）**。
3. **右按钮**：在 `frame[0:120, 100:400]` 内匹配 `bigmap_title`，`big_x = 100 + loc.x`。
4. 用偏移量算 left/right/top（见下）。
5. **底边**：在 `frame[top : top+350, left-20 : right+20]` 内匹配 `minimap_bottom`，`val≥0.55` 得 `bottom`；找不到 → return。
6. 生成两个矩形并 `_save_region()` 写入 `data/minimap_region.json`：
   - `minimap_rect = {left, top:mini_y, width:right-left, height:bottom-mini_y}`（外框，含标题）
   - `map_area_rect = {left, top:top+TITLE_PAD, width:right-left, height:bottom-top-TITLE_PAD}`（**真正裁来识别光点/画平台的地图区**）

### 3. 偏移量定稿（2026-09-06，在 `_detect_minimap` 内）
| 常量 | 值 | 含义 |
|---|---|---|
| `LEFT_OFFSET` | -6 | left = mini_x - 6 |
| `TOP_OFFSET` | **24** | top = mini_y + 模板高 + 24（本次由27→24，即地图上边向上多含3px） |
| `BOTTOM_OFFSET` | **-8** | bottom = 底边匹配中心 - 8（本次由-9→-8，即下边向下多含1px） |
| 右边界补正 | **+3** | right = big_x + 按钮宽 + 3（本次由+1→+3，即右边向右多含2px） |
| `TITLE_PAD` | 45 | 地图区相对外框顶部下移45px |

- **微调方法**：用户看效果后只改这几个偏移；改完必须**重启脚本**才生效（运行中的进程加载的是旧值）。
- 1276×749 下最终区域：`minimap={left:14,top:40,200×233}`，`map={left:14,top:123,200×150}`。

### 4. 刷新时机
- **打开脚本**：绑定后自动检测 1 次（`debug=True`，会写 `debug_detect.png` 调试图、打印各步 val）。
- **运行中**：`_auto_refresh=True` 时主循环**每 30 帧（约 0.3 秒）**轻量检测 1 次（`debug=False`）；新旧区域差 ≤1px 不更新、不写文件（防抖）。
- **手动框选小地图后**：`_auto_refresh=False`，以手动框为准（手动框选功能正常，本次未动）。

### 5. 已删除：描线（扫描线）法
- 早期失败方案 `_detect_minimap_scanline`（靠灰度扫边框硬算区域）**已于 2026-09-06 整体删除**，三模板匹配失败时也**不再回退**到它。
- 原因：游戏没在前台/画面不对时，描线法会对着错误画面硬算出畸形区域（曾写出宽159高10的扁条、map 高度 -20），把好区域覆盖掉导致小地图"不显示"。
- **现行原则：三模板是唯一自动定位方案，匹配不上就本帧跳过、保留上一次合法区域，绝不用别的方法硬算。**

---

## 三、已知坑与排查（务必看）

1. **游戏被遮挡 = 定位会错**。`_capture_window` 用 mss 按屏幕坐标抓屏（不是后台截图）：游戏窗口被控制台/浏览器/豆包等挡住时，抓到的是遮挡窗口，底边模板可能误匹配到上方，写出错误区域（出现过 map 高度 -20）。**定位/运行时让游戏窗口在前台、不要被遮挡。** PrintWindow 后台截图对本游戏会花屏（硬件加速），不可用。
   - 待办（尚未做）：给 `_save_region` 加"区域合法性校验"（宽/高过小、负高度、越界一律丢弃不保存），作为最后一道兜底。
2. **小地图不显示的排查顺序**：①游戏是否前台无遮挡；②控制台打印三张模板 val 是否都 ≥0.55（哪张低就重截哪张）；③`data/minimap_region.json` 的 map 宽高是否为正、是否约 200×150；④看 `debug_detect.png` 三个框位置对不对。
3. **改偏移/模板后必须重启脚本**；改 `GAME_W/GAME_H` 同理。
4. 模板要用**游戏原始像素**截取，不要用聊天软件压缩过的图（matchTemplate 对缩放/压缩敏感）。

## 四、本次相关提交
- `5692c13` A1怪物蒙板特征点元组修复 + A2群攻计数修复（同会话早前）
- `5a7f7d9` 强绑窗口尺寸 1276×749 + 重录三模板 + 删描线法 + 偏移定稿（本说明书对应）
