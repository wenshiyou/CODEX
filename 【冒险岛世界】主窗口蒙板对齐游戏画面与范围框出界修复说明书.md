# 【冒险岛世界】主窗口蒙板对齐游戏画面与范围框出界修复说明书

> 对应问题：游戏窗口强制 1280×800 后，寻怪黄框、技能蓝框、角色锚点框等所有框延伸到游戏画面外面（压标题栏/边框）。
> 最终方案：蒙板仍与整个外窗四点重合，只把"能画的区域"和"能识别的区域"收进真正的游戏画面（客户区）子矩形。

## 一、问题现象
游戏窗口强制 1280×800 后，寻怪黄框、技能蓝框、角色锚点框等**所有框会延伸到游戏画面外面**，压到窗口标题栏和边框上；人物越靠近画面边缘越明显。截图工具反而截不到这些框（蒙板对截图隐身）。

## 二、先分清四个东西
- **外窗**：`GetWindowRect`，含标题栏+边框，强制 1280×800。
- **客户区（=真正游戏画面）**：`GetClientRect`，实测只有 **1264×761**；它相对外窗：左 8、**上 31（标题栏）**、右 8、下 8。
- **主蒙板 `_overlay_hwnd`**：win32 分层透明窗（WS_POPUP 无标题栏），品红色键透明、`EXCLUDEFROMCAPTURE` 对截图隐身，所有框/点/线都 GDI 画在它上面。它对齐的是**外窗**，所以也是 1280×800。
- **抓帧**：`mss.grab(window_rect)` 抓**外窗**，帧坐标原点 = 外窗左上角。蒙板、抓帧、人物/怪坐标三者都以外窗左上为 (0,0)，内部自洽。

## 三、根因（实测数据）
| 对象 | 左上角 | 尺寸 |
|---|---|---|
| 外窗 | (58,49) | 1280×800 |
| 游戏画面(客户区) | (66,80) | 1264×761 |
| 蒙板 | (58,49) | 1280×800 |

蒙板比游戏画面**左上偏 (-8,-31)、宽多 16、高多 39**。旧代码框坐标允许铺满整个 0~1280 / 0~800，于是人物走到画面边缘时，以他为中心的框就画进了 y0~31 标题栏、x0~8 边框——那一圈没有游戏内容，肉眼看就是"全部框伸出游戏画面"。**不是某个框没夹紧，是载体（蒙板）盖的是外窗、而画面只是外窗里的子矩形。**

## 四、为什么前两次改法无效
1. 只把框坐标 `max/min` 夹到帧宽：帧宽本身=外窗 1280，照样允许进标题栏。
2. 蒙板几何只在"游戏窗口变化时"重设、且只跟自己上次设的值比，**从不读蒙板自身实际尺寸**——蒙板一旦被改大/错位而游戏没动，永远拉不回。
3. 想用 BitBlt/PrintWindow 截图验证：蒙板 `EXCLUDEFROMCAPTURE` 对这些捕获方式隐身，永远截不到框，验证错了对象。

## 五、最终方案（做法2：蒙板仍和整个外窗四点重合，只把"有效区"收进画面）
用户要求蒙板仍与整个窗口重合，故不改窗口基准，改"哪里能画、哪里能识别"：

1. **动态算画面子矩形 `client_subrect`**：`_update_window_rect` 每帧用 `GetClientRect + ClientToScreen(0,0)` 算出客户区在外窗坐标系里的子矩形，当前=(8,31,1272,792)。**不写死 8/31**，随主题/DPI/窗口样式自动重算；取不到时兜底为整个外窗，绝不崩。
2. **显示层一处 GDI 裁剪**：蒙板 WM_PAINT 里，先 `FillRect` 把品红铺满整个外窗（保证标题栏那圈也透明），**再** `CreateRectRgn(client_subrect) + SelectClipRgn`，此后所有实战框/点/线被系统自动裁在画面内，一处生效、不会漏。
3. **识别层同步收窄**：怪物识别循环的 YOLO 寻怪区 `_yolo_crop`、特征匹配区 `_feat_crop`，以及主循环显示框，边界一律与 `client_subrect` 取交集，标题栏/边框不扫描（少误判、略快）。
4. **载体几何自检锁死**：蒙板 50ms 循环改为读**蒙板自身实际 `GetWindowRect`** 与游戏外窗比，差 1px 立即 `SetWindowPos` 拉回，任何原因变大/错位都能自恢复。
5. 范围框边界再夹固定 `GAME_W/GAME_H`，防抓帧尺寸瞬时漂移。

## 六、改动代码定位（按函数，行号随编辑以 Grep 为准）
| 位置 | 改动 |
|---|---|
| `_update_window_rect` | 新增动态 `self.client_subrect`（外窗坐标系下的画面子矩形） |
| `__init__` 帧槽附近 | `self.client_subrect=None` 初始化 |
| gdi32 原型声明区 | 补 `CreateRectRgn / SelectClipRgn` 的 restype/argtypes（防64位句柄截断） |
| 蒙板 `WM_PAINT` | FillRect 后、实战绘制前 `SelectClipRgn` 画面裁剪；`finally` 先 `SelectClipRgn(hdc,None)` 再 `DeleteObject` |
| 蒙板 50ms 几何循环 | 读蒙板自身几何对比游戏、不一致即拉回 |
| `_recognize_loop` 怪物识别 | yolo/feat ROI 与 client_subrect 取交集（含退化兜底分支） |
| 主循环黄/蓝框计算 | 显示框边界与 client_subrect 取交集 |

## 七、为什么不影响打怪（战斗判定零改动）
攻击射程、人怪距离、按键、移动、上梯用的**原始坐标**（`_raw_char_pos`、怪坐标、`window_rect` 外窗基准）一律没动；人和怪在同一外窗坐标系，8/31 偏移在"人减怪"差值里自动抵消。本次只动 **GDI 显示** 和 **识别搜索区**，画面边缘的怪照样打，不会变近变远。

## 八、后遗症排查清单（本次已逐项确认）
- 新变量 `_px1.._py2` 只在"非上梯高帧"块内定义使用，该块无 else、高帧整块跳过，不会 NameError。
- ROI 两个退化兜底分支已统一到画面边界，无残留 0/帧宽。
- 裁剪 region 每次重绘新建，`finally` 先解除再删除，**不泄漏 GDI 句柄**；创建失败返回 0 时不选不删，安全。
- 校准引导、基点、红绿蓝校准点在"设置裁剪之前"绘制，不受裁剪影响。
- F4 关调试（data 为空）时不建裁剪区，行为同前。
- `client_subrect` 是**相对外窗**的偏移、不含屏幕绝对坐标，拖动游戏窗口不影响其正确性。
- 识别上沿 `max(30,31)=31` 仅比原来低 1px，下沿 710 不变，不漏怪。

## 九、坑（后续维护必看）
1. **8/31 绝不能写死**：标题栏高度随系统主题、显示缩放、窗口样式变，必须运行时 `ClientToScreen` 现算。
2. 蒙板是 POPUP（客户区=外窗），游戏是带标题栏窗口（客户区<外窗），两个"客户区"不是一回事，别混。
3. `SelectClipRgn` 选入的区域必须先 `SelectClipRgn(hdc,None)` 解除才能 `DeleteObject`，否则每次重绘漏一个 GDI region，久跑句柄泄漏。
4. 裁剪区必须在 `FillRect` 铺品红**之后**设，否则标题栏那圈没铺到品红会变成不透明色块。
5. 蒙板对截图隐身，**不能靠截图判断框位置**，只能读几何数据 + 肉眼。
6. 战斗距离判定**不要**也夹到 client_subrect，否则边缘怪会不打；只夹显示与识别搜索区。

## 十、验证方法
- 外部脚本量 `GetWindowRect/GetClientRect/ClientToScreen`：蒙板=外窗、client_subrect=客户区相对偏移。
- 真机人物走到画面四边、拖动游戏窗口：所有框收在画面内、不压标题栏边框。
- debug.log 无"绘制异常/wnd_proc异常"，帧率不降；任务管理器 GDI 对象数长时间稳定不涨。

## 十一、回滚点
本次改动前快照 `e5121c7`；其上 `5a1b3d5`（窗口1280×800+PID排除绑自己）。
