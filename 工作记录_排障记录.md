# 工作记录 · 排障记录（每次解决一个问题就记录，方便重复）

> **用途**：把"解决每个问题/功能"的工作过程全记录下来，登记在总纲。
> 以后要重复做同样的事，直接查本记录，看上次怎么解决的。
> **规则**：每解决一个问题，就追加一条（现象→根因→解决→验证）。
> 对应总纲：PROJECT_STATE.md 「文档索引」登记本文件。

---

## 【记录001】点击全部失效（所有按钮点不动）

- **日期**：2026-08-27
- **现象**：界面显示正常（不灰屏），但点所有按钮都没反应（点击全部失效），按钮按压特效也没有。
- **根因**：`_on_mouse`（OpenCV imshow 鼠标回调）里**某处抛异常**，导致**整个鼠标回调崩掉**，之后**所有按钮点不动**（因为回调一旦异常，OpenCV 后续不再正常分发鼠标事件）。
- **解决**：给 `_on_mouse` 加 **try-except 包装**，把原主体抽到 `_on_mouse_inner`，`_on_mouse` 只负责 `try: _on_mouse_inner(...) except: 记录日志`。这样**单个异常不会让整个回调失效**，其他按钮仍能点。
- **修复脚本**：`_fix_mouse.py`（对 `_on_mouse` 的特定开头做 replace，加 try-except 包装成 `_on_mouse_inner`）。
- **提交**：`2ef8fa4`
- **关键代码**：
  ```python
  def _on_mouse(self, event, x, y, flags, param):
      try:
          self._on_mouse_inner(event, x, y, flags, param)
      except Exception as e:
          _debug_log("[鼠标] _on_mouse异常: %s" % e)
  def _on_mouse_inner(self, event, x, y, flags, param):
      # 原 _on_mouse 全部逻辑...
  ```
- **验证**：点按钮有反应（点击恢复）。

---

## 【记录002】打包灰屏（data 没进 exe / 界面灰色空白）

- **日期**：2026-08-27
- **现象**：打包后界面灰色空白（不是黑屏，是 np.ones*200 灰底）。
- **根因**：
  1. 用 **PowerShell 命令行** `--add-data "data;data"` 打包时，**`;` 被 PowerShell 当命令分隔符吞掉**，导致 `data` 没作为 `--add-data` 正确传入 → **data 没进 exe** → `load_png(_MEIPASS/data/ui_bg_blank.png)` 找不到 → 灰色填充。
  2. 或 `load_png` 用 `np.fromfile` 读**中文路径**失败。
- **解决**：
  1. **用 spec 打包**（`datas=[('C:\\...\\data','data')]` 绝对路径，不经命令行 `;`）→ data 进 exe。
  2. `cv2.imread` 改用 `load_png`（np.fromfile + imdecode）。
- **关键**：打包**一定要用 spec**（datas 绝对路径），**不要用 PowerShell 命令行 + `--add-data "data;data"`**（`;` 被吞）。
- **参考文档**：`打包黑屏问题排查与解决方案.md`（对面写的，含根因/排查流程/踩坑清单）。

---

## 【补充】常用打包规范（记住）
- **用 spec 打包**（`pyinstaller --clean --noconfirm <spec>`），spec 里 `datas=[('绝对路径\config','config'),('绝对路径\data','data')]`。
- **不要**用 PowerShell 命令行 `--add-data "data;data"`（`;` 被吞，data 进不了）。
- `upx=False`（防压缩损坏 OpenCV DLL 黑屏）。
- `cv2.imread` 对中文路径读不到，统一用 `load_png`（np.fromfile+imdecode）。
- `MapleBot.spec` 的 `datas` 用**绝对路径**，避免相对路径打包时找不到。

---

## 【记录003】跨层上梯 _transit_step 自调用无限递归爆栈（RecursionError）→ 战斗帧空转、人物呆住不上梯

- **日期**：2026-09-15
- **现象**：人物呆住、不上梯，帧率掉到 15 左右；debug.log 被成百上千行一模一样的栈帧刷屏——`File ".../maple_route_ui.py", line xxx, in _transit_step / self._transit_step()` 层层重复，最终 `RecursionError: Maximum recursion depth exceeded`。
- **根因**：旧版跨层状态机 `_transit_step` 在内部某分支又调用 `self._transit_step()`，而该分支状态没向前推进、结束条件没命中 → 自己套自己无限递归，约 1000 层触顶抛 RecursionError，被主循环 try 吞掉，这一帧上梯/打怪全废；下一帧又进来又爆，外面看就是卡住、不上梯、掉帧。
- **解决（当前代码状态）**：`_transit_step`（当前在 16241）改成"外部主循环每帧调一次、内部只把状态向前推进一步后 return，下一帧再进"；全文件 Grep `self._transit_step()` 只剩 combat_tick 内 7 处**外部**调用，`_transit_step / _climb_state_machine / _move_to` 内部零自调用、无 A→B→A 间接环。
- **规避（已写入 AGENTS.md 第32条、总纲开发准则0.3）**：状态机/tick/集合/每帧推进类函数，严禁内部直接或间接自调用成环；新增这类函数或加分支后，必须 Grep 自调用、顺调用链确认不成环。
- **甄别坑（旧进程日志）**：那片递归栈是 16:59 旧进程(PID22480)历史。判断是否仍在发生：看时间戳是否晚于**当前进程**启动时间、栈行号是否对得上当前文件（当前 `_transit_step` 在 16241、内部无自调用）。管理员脚本必须 UAC 提权 `taskkill /F /IM python.exe /T` 后重启，否则改了代码仍跑旧进程。
- **配套甄别（别误判）**：检测线程（截图/人物/YOLO）是"开脚本就常跑"，与打怪开关 `_running`（F10起/F12停）相互独立。**没点运行时 `_running=False`，combat_tick 第一行就 return，主循环分段里 `6combat` 为空、无任何战斗诊断日志，但 YOLO/角色跟踪照刷——这是正常的，不是卡住**。判"真卡住"必须先确认当时 `_running=True`（在运行打怪）。
- **状态（不混淆）**：递归本身在当前代码已消除；真机"不上梯"需在 `_running=True` 实跑时另查，不能算到递归头上。

---

## 【记录004】debug.log 按“当日秒数”裁剪，跨天追加时误删今天/误留昨晚 → 运行段切不出、发呆现场被清

- **日期**：2026-09-22
- **现象**：日志“只留最近几分钟”异常——今天上午的运行段被删、昨晚 22/23 点段反被保留；按行首 HH:MM:SS 切“最后运行段”会切到昨晚，真机发呆现场被错误清掉，无法复盘。
- **根因**：`_maint_trim_debug_log`（maple_route_ui.py）拿行首 `[HH:MM:SS]` 的**当日秒数**直接和 cutoff 比较，行首没有日期；日志跨午夜持续追加时，今天上午秒数(~43000) 小于昨晚 22-23 点秒数(~80000+)，今天较早行被当“旧”删、昨晚被当“新”留。
- **解决**：重写为按**物理行序**裁剪——以末尾有戳行时间为基准，从后往前还原每行跨天绝对秒数（从后往前当日秒数反而明显变大、超过 5 分钟容差 = 往前跨过一个午夜，基准 −86400），再从末尾保留 N 分钟做一次物理截断；无时间戳的堆栈/续行随切片自然保留；`.tmp + os.replace` 原子换；`except` 改为 `print` 不再静默吞。保留时长 5 → **60 分钟**（函数默认值与 `_maint_run` 实参都改为 60）。
- **验证**：ast 抽取真实方法 + 假 self 跑 4 组断言全过——A 跨天(昨晚 600 行全删、今天 1001 行全留，首尾时间正确)、B 同天 60 分钟窗口边界(首 11:16:40、3601 行)、C 无戳续行随切片保留、D 真实 debug.log 副本 248245→12603 行且昨晚段清零；真机对真实日志裁剪 **252940→17298 行、[22:/[23: 清零**；提权重启后新进程(PID19700)持续写新文件、无 .tmp 残留；py_compile 通过、AST 方法名/self 属性遮蔽交集为空。
- **触发时机**：维护在战斗线程 `run()` 的 `while True` 内，`_last_maint_ts=0` 首圈即跑、之后每 600s 一次；本次重启后已用同一函数对真实日志等价首裁一次（幂等，程序自裁结果相同）。
- **回滚**：裁剪重写前、已含“保留1小时”改动的提交 = `8138f8b`；本次修复提交 = `2d2c2e7`。
- **注意**：保留 1 小时后日志量约 ×12（满负荷约数万行/十几 MB 每小时），文件到 20MB 仍走原有轮转 `debug.log.bak`；看日志仍守“先最后 3 分钟、只看运行态、按物理行最后 F10/[初始化] 切段、MP 横带<0.85 段忽略”的铁律。
