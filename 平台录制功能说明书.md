# 平台录制功能说明书

## 一、功能概述

按F5开始/结束平台录制：人物在游戏中走过平台，脚本通过检测小地图上人物黄色光点的移动轨迹，实时画红色折线，结束后抽稀保存为平台数据。一条录制=一个平台段（折线），可多次录制拼接多个平台。

## 二、完整流程（按执行顺序）

### 步骤1：截取小地图画面
- 方法：`_capture_map()`（约1991行）
- 原理：用mss截取屏幕，区域=窗口左上角+map_area_rect偏移（存在data/minimap_region.json的"map"字段）
- 返回：BGR格式numpy数组
- 关键点：map_area_rect必须准确框住小地图区域，截偏了光点检测不到

### 步骤2：检测人物黄色光点
- 方法：`find_player_dot(map_area)`（约2005行）
- 原理：
  1. BGR转HSV颜色空间
  2. HSV范围过滤黄色：H=20~35, S≥100, V≥180（收紧阈值，排除噪声）
  3. cv2.findContours找所有黄色轮廓
  4. 对每个轮廓用cv2.moments计算质心(cx,cy)
  5. 计算每个轮廓区域的平均V通道亮度
  6. 取平均亮度最高的轮廓的质心作为人物位置
- 返回：(x, y)小地图坐标；找不到返回None
- 关键点：
  - 不用面积过滤（人物光点可能只有2~4px，面积过滤会漏掉）
  - 用亮度取最亮轮廓（人物光点比其他黄色物体亮）
  - 不用MORPH_OPEN（会吃掉小光点）
  - 必须用cv2.moments质心，不能用轮廓内最亮像素（会跳动）

### 步骤3：主循环每帧获取光点位置
- 位置：约6762行
- 代码：`player_pos = self.find_player_dot(map_area)`
- 关键点：**每帧都检测**，不能隔帧用缓存（frame_count%2），否则轨迹点密度不够

### 步骤4：F5开始录制
- 方法：`_handle_hotkey(VK_F5)`（约2874行）
- 开始时：recording_platform=True, platform_points=[]
- 热键检测：GetAsyncKeyState轮询，按下瞬间触发一次（_key_state防重复）

### 步骤5：录制中收集轨迹点
- 位置：约6832行
- 逻辑：
  1. player_pos有效时才收集
  2. 如果上一个点和当前点X差值<1px：覆盖上一个点（人站着不动时不产生新点）
  3. 否则：追加新点到platform_points
- 关键点：
  - 覆盖阈值是**X差值<1px**（不是欧氏距离，不是3px）
  - 阈值太大会丢失中间轨迹点，太小会产生冗余点
  - 1px是最佳平衡：移动1px就记录，站着不动就覆盖

### 步骤6：实时画红色折线
- 位置：约3456行（_render_minimap方法中）
- 代码：`cv2.polylines(display, [np.array(self.platform_points, np.int32).reshape(-1,1,2)], False, COLOR_RECORDING, 1)`
- COLOR_RECORDING = (0, 0, 255) 红色BGR
- 线宽=1px
- 只在len(platform_points)>1时画

### 步骤7：F5结束录制，抽稀保存
- 方法：`extract_platform(points)`（约2834行）
- 抽稀逻辑：
  1. 起点必保留
  2. 遍历后续点，与上一个保留点的欧氏距离≥2px才保留
  3. 终点必保留
- 输出格式：`[{"id": len(self.platforms), "points": [[x,y],...]}]`
- 关键点：
  - 抽稀间距是**2px**（不是5px，5px会把曲线抽成直线）
  - 首尾点必须保留，否则平台端点丢失
  - 抽稀后加入self.platforms列表

### 步骤8：保存到文件
- 方法：`_save_to_route(route_id)`（约1338行）
- 文件：data/route_XXX_platforms.json
- 格式：`{"platforms": [...], "count": N}`

## 三、采过的坑（重要！）

| 坑 | 现象 | 原因 | 解决 |
|----|------|------|------|
| 光点检测用"轮廓内最亮像素" | 轨迹跳动、不稳定 | 轮廓内最亮像素可能在边缘，帧间跳动 | 改回cv2.moments质心，稳定平滑 |
| MORPH_OPEN开运算 | 光点检测不到 | 2~4px的小光点被腐蚀掉 | 注释掉MORPH_OPEN |
| 隔帧检测(frame_count%2) | 轨迹点稀疏、直线 | 每2帧才检测一次，漏掉中间位置 | 改成每帧都检测 |
| 覆盖阈值3px | 中间点不收集、只有起点终点 | 人物慢走时X变化<3px被覆盖 | 改成1px |
| 抽稀间距5px | 曲线变直线、二点线 | 间距太大把折点都抽掉了 | 改成2px |
| HSV阈值太松(S=80,V=150) | 检测到噪声黄色物体 | 淡黄色/暗色物体被误判 | S=100, V=180收紧 |
| 用面积过滤轮廓 | 有时检测不到光点 | 人物光点面积可能很小 | 用亮度取最亮，不靠面积 |
| 用欧氏距离判断覆盖 | Y方向微动也产生新点 | 人物站着不动时Y有1px抖动 | 只看X差值（平台主要水平移动） |

## 四、关键参数速查

| 参数 | 值 | 位置 | 作用 |
|------|-----|------|------|
| YELLOW_H_LOW | 20 | 218行 | 黄色H下限 |
| YELLOW_H_HIGH | 35 | 219行 | 黄色H上限 |
| YELLOW_S_LOW | 100 | 220行 | 饱和度下限 |
| YELLOW_V_LOW | 180 | 221行 | 亮度下限 |
| COLOR_RECORDING | (0,0,255) | 407行 | 录制红线颜色(BGR红) |
| 覆盖阈值 | X差值<1px | 6834行 | 同位置覆盖不追加 |
| 抽稀间距 | ≥2px | 2843行 | 保存时点间距 |
| 检测频率 | 每帧 | 6762行 | 不能隔帧 |

## 五、调试方法

- 日志标签：
  - `[录制A]`：每帧输出player_pos和点数（确认光点是否检测到）
  - `[录制B]`：结束时extract结果（确认抽稀是否非空）
  - `[录制C]`：每次新增点时输出位置和总点数（确认点密度）
  - `[光点检测]`：每10帧输出候选轮廓数和详情
- 日志文件：dist/debug.log
- 第一帧截图：debug_map_area.png（确认小地图区域是否正确）

## 六、相关文件

- 主程序：maple_route_ui.py
- 小地图区域配置：data/minimap_region.json
- 平台数据：data/route_XXX_platforms.json
- 梯子数据：data/route_XXX_ladders.json
- 校准数据：data/route_XXX_calib.json
