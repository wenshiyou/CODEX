# MapleBot 项目状态备忘

## 重要文档索引（第一项 · 所有MD文件清单）

| 序号 | 文件名 | 用途 | 优先级 |
|------|--------|------|--------|
| 1 | AGENTS.md | 项目执行规则（每次对话自动加载，最高优先级） | ★★★ |
| 2 | PROJECT_STATE.md | 项目总纲（本文件：状态/文档索引/已知问题/打包记录/固定值） | ★★★ |
| 3 | 开发工作记录.md | 综合工作记录（版本历史+问题记录+踩坑记录+工作记录规范） | ★★★ |
| 4 | 工作记录_排障记录.md | 专项排障记录（点击失效/打包灰屏等具体问题） | ★★☆ |
| 5 | 功能实现指南与排错手册.md | 功能实现总纲（热键/蒙板/小地图/倍率/打包/调试） | ★★☆ |
| 6 | 打包黑屏问题排查与解决方案.md | 打包黑屏专项（根因/排查流程/踩坑清单） | ★★☆ |
| 7 | 脚本运行闪退问题排查与解决方案.md | 闪退专项（闪退/权限检测/自动重启） | ★★☆ |
| 8 | 小地图光点锁定人物坐标方案.md | 光点锁定方案（不用倍率/三特征定位/归一化/双蒙板） | ★★☆ |
| 9 | 小地图倍率编写说明.md | 倍率编写说明（总值=检测值+偏移并锁定，最完整） | ★★☆ |
| 10 | 小地图倍率的成功方案说明书.md | 倍率旧版方案（光点检测改进/映射比率修复/三点校准） | ★☆☆ |
| 11 | 平台录制功能说明书.md | 平台录制完整流程（截图→光点检测→收集点→画红线→抽稀保存） | ★☆☆ |
| 12 | 弹窗组件实现规范与做法.md | 所有弹窗组件统一规范（灰底白字/右上角X/可拖拽/最上层） | ★☆☆ |
| 13 | 代码编写总结与操作流程.md | 代码编写总结与操作流程 | ★☆☆ |
| 14 | 文档_光点检测修复记录.md | 光点检测修复记录 | ★☆☆ |
| 15 | BUILD_GUIDE.md | 打包指南（完整流程/文件路径/常见问题/一键打包脚本） | ★★☆ |
| 16 | CODING_STANDARDS.md | 代码规范（注释要求/三点判定边界法） | ★☆☆ |
| 17 | BACKUP_LOG.md | 备份日志（每次备份记录） | ★☆☆ |
| 18 | README.md | 项目说明 | ☆☆☆ |
| 19 | 打怪方案说明书.md | 打怪方案（方向修复/空怪两层/后台检测线程/战斗接管/XY距离分开算/绿框隐藏/用py测试） | ★★★ |
| 20 | 【冒险岛世界】截图特征键盘微调版+放大镜（见本文件两段） | 截图框选自绘+蓝框细一半+键盘方向键移动(步长0.1)+节流慢速+int取整防闪退+放大镜(蓝框+10px放大5倍) | ★★☆ |
| 21 | 【冒险岛世界】强绑窗口尺寸与小地图三模板定位说明书.md | 全窗口固定1276×749(改尺寸只动GAME_W/H)+三模板(mini map标题/右上X/底边)唯一定位+偏移定稿+删除描线法+遮挡坑排查 | ★★★ |
| 22 | 【冒险岛世界】打怪Y范围面板与瞬移设置改版说明书.md | 打怪页Y距离弹窗(脚底基点上负下正,默认上-60/下+30,UI有向决策abs)；瞬移取消原两框改"瞬移设置"弹窗设X/Y瞬移距离(默认空,X或Y>0才瞬移,水平追怪850ms节流/垂直改读Y)+动作录制占位按钮；两135×39按钮并排盖满原框 | ★★★ |
| 23 | 【冒险岛世界】寻怪范围面板与打怪流畅性锁平台MP界面说明书.md | 寻怪X/Y可自定义(默认X1300/Y150,寻怪弹窗,替换动作录制按钮,技能Y换图,三135×39按钮一排)；群攻独立4条Y默认-60/+30；空打容差根因修复(打一下250ms换)；打怪站定不带方向(删随机小走位)；解卡窗口450ms→5s；锁单平台绿线硬边界(到边停/禁落层)；自动吃药恢复MP界面门控(底部60横带X0-1300,阈值0.85,前台+MP可见才补) | ★★★ |

> **使用规则**：遇到问题先查索引→找到对应文档→读取解决。工作记录写完后必须在本索引登记。

## 开发准则（永久记住，最高优先级）
0. **【重中之重】每次改动代码前，必须先git commit提交当前快照**：哪怕只改一行，动手前先 `git add -A && git commit -m "改动前快照：xxx"`，确保改坏了随时能 `git checkout` 或 `git reset` 退回来。改好确认没问题后再提交一次。这是铁律，违反就是浪费用户时间。
   - **提交范围**：除用户生成的临时图片(data/char_templates/*.png、debug_*.png、*.jpg/*.jpeg/*.gif等)、日志(*.log)、EXE(*.exe)、大模型(*.onnx)、build/dist中间产物排除外，其他所有文件（源码.py、data下的.json配置、config、.md文档、.spec打包配置、脚本、**程序必需的模板图片data/templates/*.png和UI图片data/ui_*.png**）全部 `git add -A` 完整提交，不遗漏。**注意：程序必需的模板和UI图片必须提交，否则恢复后打包会黑屏/缺资源。**
   - **commit vs push区别**：`git commit` 是本地提交（秒级，快），每次改动前后都要做；`git push` 是推送到GitHub远程（受网络影响），每完成一个功能或有进展时做。两者都要做，commit是回滚的保命符，push是远程备份。
   - **网络不通时**：git push连不上github.com时，用 `python push_to_github.py` 脚本通过GitHub API推送（api.github.com可连通）。
0.1 **【铁律】改之前搞清来龙去脉，改之后全链路验证，影响别的功能立马回滚（2026-09-01血的教训）**：
   - **改之前**：必须先看清原代码的来龙去脉——这个函数/变量被谁调用、谁在读写（Grep全部引用点），数据类型是什么（列表还是字典），牵连哪些模块/链路（保存/加载/切换方案/UI/匹配/打包/蒙板/倍率），会不会"牵一发而动全身"。涉及别的功能的，必须把那个功能的代码也看清楚再改，不许只盯当前功能下手。
   - **每次改动前必须提交快照**：`git add -A && git commit -m "改动前快照：xxx"`，确保改坏了随时能回滚。
   - **改之后必须全链路验证**：不只是验证改的那个功能，必须验证第0.1条里列出的所有牵连功能，确认没把正常工作的部分搞坏。
   - **一旦发现影响到别的功能（哪怕改的功能本身是好的），立马 `git checkout` 回滚到改动前快照**，不许"顺便修一下"越修越远。回滚后重新做影响面分析，换更小的改动范围或换方案，再说清楚给用户确认。
   - **反例**：改人物特征10张时，没看清_char_templates被F4保存/切换方案/UI下拉/模板匹配多处使用，就照搬后期版本重写，还顺手改了保存和切换逻辑，结果连锁改坏、越修越远、一张都存不了，最后回滚到29号基线重做。
1. **做任何东西之前，都先参考行业标准**：正规软件的弹窗、按钮、输入框是什么样的，先参考再动手，不自己瞎设计
2. **符合用户习惯和方便**：做的东西要符合大多数人的使用习惯，方便操作，不搞"创新"的交互
3. **先出方案再动手**：先给用户看设计方案，确认后再写代码
4. **代码后面都要写注释**：每一行代码后面都要添加一行注释，说明设计思想
5. **禁止用try-except吞错误**：永远不要用try-except方法吞错误
6. **每弄好一步都备份到GIT**：哪怕一小步，都要完整备份到GIT远程仓库
7. **备份必须完整**：包括代码、data目录、配置文件、MD文档等所有文件
8. **每做一件事之前找标准，有依据地去执行**：不要凭感觉瞎做，先找行业标准、参考实现、最佳实践，有依据地去执行
9. **仔细考证再动手**：不要动不动就说"找到了方法"，实际还是不行，要先考证、确定好、验证过，再动手修改
10. **提交快照命名规则（强制，2026-09-01）**：时间 + 备注 + 全部正常功能的名字，用"+"连接。格式：`YYYY-M-D号 HH点mm分 功能1正常+功能2正常+...`。每次提交都要把当前已验证正常的功能全部写上，修复好一个功能立即加到名字上。目的：下次找某个功能正常的版本时看提交信息就知道，不用一个个试。
11. **图像识别必须识别原游戏图层，绝对不能识别蒙板上的标志（强制，2026-09-01）**：蒙板上有大量绘制的标志（人物黄点、怪物框、水平线、倍率标记等），识别蒙板会导致混乱、误匹配、定位不准。所有模板匹配/图像识别必须针对原始游戏截图（未叠加蒙板的干净画面），蒙板只用于显示，不用于识别。
12. **用户说"XX功能好了"时必须更新说明书并登记总纲（强制，2026-09-01）**：用户验证功能OK后，立即把此次功能的实现说明、踩过的坑、解决方案写成.md说明书，并登记到PROJECT_STATE.md文档索引中。不能只在对话里说，必须持久化到文件。
13. **排查未知问题必须先加日志定位根因，禁止猜（强制，2026-09-01）**：遇到未响应/卡顿/偶发bug，第一步是在关键节点加时间戳日志（print带elapsed），打包让用户运行看日志停在哪一步，定位根因后再修。禁止"应该是XX"就直接改代码。
10. **不要毛毛躁躁**：做事要稳，仔细思考，确认无误再动手，不要急躁
11. **每做一个功能都要设好日志**：日志存在文件中，自己可以随时查看，一旦有问题就知道出在哪里了
12. **代码后写上注释**：后面再回看就知道关键点在哪里，每一行代码后面都要添加注释说明设计思想
13. **每个功能都要符合流畅性、操作方便性、带人性化**：不要做反人类的交互
14. **每个功能都要做出来标准，不是乱做，争取一次做好**：不要反反复复返工
15. **只做需求内的事，不脑补、不瞎加、不造假、不遗漏边界、不产生隐性BUG**：核心原则
16. **有据可查的，按据行事**：有记忆文件、备份文件、行业标准、参考实现的，严格按照来做，不要凭感觉瞎改
17. **没有据可查的，先测试验证后再动手**：不确定的东西，先写测试代码验证，确认可行后再修改正式代码
18. **每改个东西，日志先行**：把日志保存在文件中，自己可以直接查看，一旦有问题就知道出在哪里了
19. **三次还没改成功的，直接找网上资料参考**：不要在乱改，不要卡在那里，主动搜索网上的成熟方案和最佳实践
20. **先整理步骤和数据，沟通无误后再行动**：把理解到的需求整理成详细的步骤和数据，经过用户确认无误后再开始动手修改，不要凭感觉瞎改
21. **每写一个功能，先把规范好的行为发用户审核**：以后每写一个功能，都先把详细的步骤和方案发给用户审核，用户确认后再开始动手写代码，避免乱改代码把其他功能搞坏
22. **不要说谎，做了就是做了，没做就是没做**：不能做也直接说，这样可以选别的方案。追求高效、准确。如果有功能实现不了或者自己测试不了、或者安装不了，就直说，然后发几个可选方案过来，用户重新选择
23. **每做完一个功能，必须写一份详细 .md 说明文档（步骤/关键点/坑），并在 PROJECT_STATE.md 的「文档索引」登记文件名+路径**：防止换线程或记忆丢失后无法修复/重现。这是铁律，每个功能都要这样做。
24. **每次动手改代码/配置前，必须先说清楚：①我的理解是什么 ②具体要改哪里、改成什么 ③为什么这么改 ④可能影响什么。用户确认无误后再动手改，禁止边猜边改、先斩后奏**：这是防止乱改搞坏其他功能的核心规则，所有改动（哪怕一行代码、一个配置值）都必须遵守。
25. **沟通规范（双向确认制）**：对于用户发出的指令，先把自己理解的内容反馈给用户，收到用户确认后再动手修改；如果有更好的建议和补充，或者认为用户的想法有问题，也要主动告诉用户，双方一起确认后再动手改代码。禁止不沟通直接改、禁止理解有歧义时凭猜测改。
26. **规则迭代与持续改进**：对于现有规则和MD文档，如有更优方案（更规范、更准确、更有效率），可以主动提出建议，双方讨论确认后更新规则；但在新规则确认生效之前，必须严格遵守现有规则，不得以"规则不合理"为由跳过或违反。规则更新后同步更新 AGENTS.md 和 PROJECT_STATE.md，确保两处一致。
27. **提交快照命名规范（强制）**：每次 git commit 提交信息必须包含三要素：①日期时间（YYYY-MM-DD）②当前任务名称 ③进度完成情况（如"初版/完成/修复/优化中"）。格式示例：`2026-08-29 光点锁定映射功能-初版完成` 或 `2026-08-29 打包黑屏修复-已解决待验证`。禁止无意义提交信息（如"update""fix""测试"）。
28. **★性能负载优先（2026-09-07用户强制要求，详见AGENTS.md §9.9）**：功能不能只做到"效果实现"，必须同时压低运行负载，做到效果一致前提下的最低占用率。①每帧/高频代码先分段计时量化、按耗时大头排序优化，不凭感觉；②内容不变的产物（固定文字/静态面板/日志）禁止每帧重绘，预渲染一次或缓存、数据变化才重建，缓存键覆盖全部输入及所有变化路径；③盯紧PIL描边文字、BGR↔RGB转换、大数组copy、astype(float)、cv2.resize、磁盘IO等每帧高耗操作；④优化后给优化前后ms/秒对比数据并验证效果一致，优化手段本身也要算成本；⑤装饰性高耗功能优化成本高于价值时，经用户确认直接删除也是合格优化（2026-09-07跑马灯87~172ms/秒整段删除、日志区缓存44.5→6.6ms/秒）；⑥按12小时长稳审查，禁止每帧new对象、缓存只增不清。
29. **★战斗锁定/靠近/防卡/爬梯/边缘定位（2026-09-07定稿，详见AGENTS.md §9.10）**：①识别≠锁定，YOLO全图+怪物特征仅技能范围内两套识别保留；②锁定门=面板寻怪范围(X≤far_range_x且|Y|≤far_range_y,按地图手设),框外不锁、不做跨层；③框内只分cast(射程内+Y达标)/pursue(其余先靠近)；Y够不着先追到X≤300再跑跳,用人物脚Y前后对比二次验证(800ms内升≥6px=真坡继续,没动=平地放弃并压制该侧3秒改锁另侧)；④"卡没卡"只认小地图光点(1秒窗口位移<2小地图单位=卡→重按方向+跳,连续2次放弃该侧),不看屏幕角色特征点；⑤人物特征丢失：贴左右边(脚X距画面边≤45)主动向中间走随机300~500px找回(期间暂停巡路/战斗),中间丢则原地等全图重扫；⑥爬梯点位：>30跑、到30跑跳、20~30走进20、<20直跳、<4对齐开爬,冲过头(退回>30)重置重试；⑦光点基点=小地图ffff88质心正中心(无偏移)；录制绿线平台前视45小地图单位,Y波动>6判坡:低向高(上坡)跑+跳、高向低(下坡)只走不跳、≤6当平地(巡路_move_to用小地图折线,战斗high_slope用屏幕脚Y,两套不混用)；⑧锁定稳定性:候选=寻怪框全部怪,tier0(射程内+Y达标)优先同档取最近,打怪中不换锁;范围外(tier1)锁定只能被范围内(tier0)怪替换、不能换另一只范围外(漏检帧沿最后坐标追,治左右横跳),原范围内怪死则立刻重选且drop瞬间清零YOLO/血条节流强制立即全图扫(打完秒锁);检测分档:YOLO锁范围内怪2Hz省电/无锁寻敌4Hz快发现,血条0.30s对齐3Hz。

## 永久强制规则（所有代码生成必须100%遵守，违规直接判定生成失败）
### 一、绝对铁律（违反任意一条直接重写）
1. 严禁私自新增功能：需求没写的特效、判断、逻辑、弹窗、动画、机制，一律不写
2. 严禁假代码、占位代码、空函数：禁止TODO、空回调、假接口、无逻辑空方法
3. 严禁逻辑漏洞：所有分支必须有兜底，所有数值必须限界，所有对象必须做空校验
4. 严禁残留无效逻辑：废弃逻辑、重复逻辑、冗余更新、无效监听全部禁止
5. 严禁随机发挥样式/结构：代码结构、变量规范、逻辑层级全程统一

### 二、代码规范标准
1. 变量、函数命名语义清晰，禁止无意义命名
2. 魔法数字必须抽离常量，禁止零散硬编码数值
3. 代码分层清晰：参数定义、初始化、核心逻辑、触发判断、兜底逻辑
4. 关键步骤写注释，只解释逻辑意义，不复读代码
5. 不输出调试垃圾代码，无用Print/Log默认关闭

### 三、对象&生命周期强制标准
1. 任何物体、组件、UI对象，使用前必做空判断
2. 对象销毁、禁用后：立刻停止一切逻辑、监听、计时、帧更新
3. 所有定时器、协程、帧事件、回调：对象销毁必须强制关闭/清除
4. Update/帧循环禁止频繁new对象、频繁重复高耗运算

### 四、输入&交互强制标准
1. 所有按键、鼠标、点击逻辑必须防抖、防重复触发
2. 禁用状态=视觉置灰+关闭监听+完全无响应
3. 被遮挡、未激活、已销毁对象，绝对不响应点击、按键
4. 区分：按下、按住、抬起、点击，逻辑不混用

### 五、UI组件强制标准
1. 所有UI位置、尺寸固定，不允许自适应乱飘
2. 禁用输入框：只读+禁用双锁定
3. 按钮禁用必须同步：视觉置灰+移除点击事件
4. 无需求不允许加动画、渐变、闪烁、悬浮特效

### 六、数值&逻辑安全标准
1. 所有数值自动限界：禁止负数、禁止越界
2. 所有If判断必须有兜底Else，禁止逻辑断层
3. 禁止死循环、高频递归、无终止循环
4. 状态逻辑统一管理，同一时间只允许一个状态

### 七、最终自查强制要求
每次脚本生成完成，必须自动对照以上全部规则自查：
- 是否私自加了多余功能
- 是否存在假代码、空函数、占位逻辑
- 是否有未处理的边界情况
- 是否有未清理的定时器/监听
- 是否有空引用风险
- UI是否固定不乱跑、禁用是否彻底生效

最终准则：不多做、不漏做、不瞎做、不报错、不BUG、逻辑干净、完全贴合需求。

## 基本信息
- 项目根目录：C:\Users\PC\Doubao\chats\2026-08-15\new-chat-4\maple_bot
- 主源码：maple_route_ui.py（约6100+行）
- 可执行文件：dist\MapleBot.exe
- 打包配置：MapleBot.spec（PyInstaller）
- 远程Git：https://github.com/wenshiyou/CODEX.git（main分支）
- 游戏进程：Maplestory_Classic（管理员权限运行）
- 游戏窗口标题：冒险岛怀旧服
- Windows环境，PowerShell语法，UTF-8编码

## 功能实现指南总纲（重要！）
**详细的功能实现原理、操作流程、注意事项、常见问题及解决方案，全部记录在 `功能实现指南与排错手册.md` 中。**

下次功能损坏时，先查看该文档，可快速修复或重新实现，少走弯路。

### 重点功能索引
| 功能 | 详细文档章节 | 关键代码位置 |
|------|-------------|-------------|
| 热键跑马灯 | 功能实现指南与排错手册.md → 一、热键跑马灯功能 | `_render_minimap` 方法中（约3501行） |
| 蒙板三点校准（F11） | 功能实现指南与排错手册.md → 二、游戏蒙板三点校准功能 | `_start_auto_calibration` 方法（约2207行） |
| 小地图显示 | 功能实现指南与排错手册.md → 三、小地图显示功能 | `_render_minimap` 方法 |
| 倍率显示 | 功能实现指南与排错手册.md → 四、倍率显示功能 | `_render_minimap` 方法中（约3403行） |
| 打包流程 | 功能实现指南与排错手册.md → 五、打包流程 | `MapleBot.spec` |
| 调试运行 | 功能实现指南与排错手册.md → 六、调试运行方法 | `运行调试.bat` |
| 小地图倍率成功方案 | 小地图倍率的成功方案说明书.md（完整记录实现方法、关键部分、采过的坑与解决办法） | `find_player_dot` 方法 + `_finish_auto_calibration` 方法 |

### 走过的弯路（已解决，记录在总纲中）
1. **模板尺寸55导致截图失败**：`_calib_template_size=55`，但实际截取54x54，改成54解决
2. **绿蓝不能同时匹配**：每帧同时匹配互相干扰，改成奇偶帧交替匹配
3. **第三步第四步圈不消失**：蒙板绘制逻辑stage>=2都显示，改成stage=3绿圈消失、stage=4都消失
4. **切换方案倍率不显示**：加载校准数据后没重新计算倍率，添加重新计算逻辑
5. **调试日志不显示**：用了`_add_log`不渲染UI，改用`_rlog`
6. **小地图灰边**：背景图本身带灰边，替换背景图解决
7. **大地图标志X识别不到**：搜索范围太小，从mini_x+200加大到mini_x+400
8. **跑马灯卡顿（2026-09-07最终处理：整段删除）**：每帧textbbox+PIL白描边双段渲染+颜色转换耗87~172ms/秒，是draw最大头；文字每帧移动缓存无效、预渲染长条又有抗锯齿边缘对齐问题，该提示仅装饰无实际功能，经用户确认整段删除（初始化字段/draw绘制块/常量全清，F5-F12热键功能保留）。同日日志区改缓存：内容/滚动不变整块贴缓存，44.5→6.6ms/秒
9. **怪物特征弹窗点击任意按钮闪退（2026-09-03，浪费2小时）**：
   - **症状**：怪物特征弹窗能打开，但点击弹窗内任意按钮（添加向左/向右特征、全部删除、保存并关闭、删除单个特征、X关闭）都会导致程序闪退。人物特征弹窗在相同环境下正常工作。
   - **根因**：主循环中有一个tkinter独立窗口事件泵（第9306行），只在有窗口打开时调用`self._tk_root.update()`，避免与OpenCV冲突。但事件泵的窗口列表中只有`_save_window`、`_plan_window`、`_clear_window`、`_char_feature_window`，**没有包含`_monster_feature_window`**。导致怪物特征弹窗打开时事件泵不调用，弹窗无法响应鼠标事件，点击就闪退。
   - **修复**：在事件泵的窗口列表中添加`_monster_feature_window`，一行代码解决。
   - **下次如何避免（新增任何全局组件必做）**：
     1. **对照法检查清单**：找到一个已有的、正常工作的同类组件（比如人物特征弹窗），Grep它的所有引用点，列出它在哪些地方被检查/调用，然后对照检查新组件是否在所有这些地方都被同步更新了。
     2. **具体检查命令**：
        - `Grep "_tk_root.update"` → 找到事件泵的位置
        - `Grep "_char_feature_window"` → 找到已有弹窗的所有引用点
        - `Grep "_monster_feature_window"` → 对比新弹窗的引用点是否齐全
     3. **测试顺序（不要跳步）**：先测弹窗能否打开 → 再测弹窗内每个按钮能否点击（这一步就能发现事件泵问题）→ 最后测弹窗关闭后是否影响其他功能。
     4. **核心原则**：新增任何全局组件（弹窗、线程、定时器、事件泵、蒙板、检测循环）时，不要只写组件本身的代码，必须问自己："这个组件打开后，谁来处理它的事件/数据/生命周期？"然后沿着这个问题追踪到主循环，确认所有相关的调度器、事件泵、全局列表都已同步更新。

## 代码规范（强制遵守）
0. **统一用微软雅黑字体**（2026-09-06用户约定）：所有文字显示统一用微软雅黑（PIL simhei/微软雅黑），中文+英文统一字体、统一大小、统一加粗，不混用cv2原字体
1. **所有代码后面都要写注释**，每一行代码后面都要添加一行注释，说明这行代码的作用
2. 方法/函数开头要有文档字符串（docstring），说明功能、参数、返回值
3. 复杂逻辑处要有注释，说明设计思路
4. 修改代码时，要更新相关注释
5. 详细规范见 `CODING_STANDARDS.md`

## 固定值（永久常量，不要改）
- 血条位置：**按窗口比例换算**（2026-09-04用户确认；HP左=510/1382*w、MP左=619/1382*w、宽=107/1382*w、Y=h-25）；旧的"Y=782/510/619/107"固定值是1382x807窗口专属写法，仅作参考
- 游戏窗口尺寸：1366×768（2026-08-30确认，以这个为准；窗口可变时按上方比例换算）
- 小地图内部渲染尺寸：FIXED_W=340, MAP_H=250
- 小地图颜色：黄点=自己(0,255,255)、蓝线=梯子(255,100,0)、深绿线=平台(0,255,0)、紫点=怪物(255,0,255)、红线=录制中(0,0,255)、白十字=端点
- 紫色点：map_display上实心圆半径6，坐标经_get_monster_map_pos_verified校准
- 平台编号：红色(0,0,255)字体0.6，白色描边thickness=3，LINE_AA，绿线上方8PX
- 梯子线：map_display上thickness=2
- 绿线：map_display上thickness=2，始终最上层（人物光点除外）
- 人物光点：游戏自带黄色光点，不自己画，定位用cv2.moments取光点正中心
- 怪物Y基准：YOLO检测框底部y2（脚位置），X用中心(x1+x2)//2
- 人物屏幕位置：模板匹配中心+偏移量(_match_character返回best_loc+模板宽高//2)
- 人物匹配搜索（2026-09-04定稿）：找到时400x400区域、丢失时立即全图；只认≥0.70；无锚定/确认闸
- 怪物特征匹配阈值=0.70（2026-09-04用户最终定回；中间试过0.75紫点太少）；只认打怪范围700x300内
- YOLO置信度=0.4、NMS=0.45（2026-09-04用户最终定回0.4）；怪物匹配半分辨率0.5x提速
- _key_state字典包含F4-F12全键（F11已修复，2026-09-04）
- COMBAT_NEAR_RANGE=350（打怪近距探测：技能范围法师350px一边，有怪直接打不用移动）
- COMBAT_FAR_RANGE=500（同平台搜索：每边500px，一次找一边，左右顺序随机）

## 绘制层级（从底到顶，map_display上）
1. 端点十字(白色,display上)
2. 录制中黄线(display上)
3. 缩放display到map_display
4. 紫色怪物点(半径6,实心)
5. 平台编号(红字白描边thickness=3)
6. 梯子蓝线(线宽2)
7. 平台绿线(线宽2,始终最上层)
8. 游戏自带人物黄点（在截图底层，但绿线可能盖住）

## 坐标转换方案（当前：人物锚点+相对偏移）
- _screen_to_map：怪物小地图位置 = 人物小地图位置 + (怪物屏幕位置 - 人物屏幕位置) * scale
- scale_x靠左右端点校准（最准），scale_y=scale_x（小地图等比例渲染）
- 人物上下移动时Y独立微调，但校准值和scale_x差距>30%视为噪声忽略
- _get_monster_map_pos_verified：先线性转换，再只对和人物Y相差<30px的同平台怪找X最接近且Y偏差<15px的绿线点校准Y
- 高处/低处平台的怪（Y差>30px）不强制拉到绿线上，保留线性转换Y
- 端点数据格式：(屏幕X, 小地图X, 小地图Y)，保存到data/route_%d_calib.json
- 端点自动记录：每3秒检测一次，脚本启动就工作（不依赖运行状态），手动校准后关闭自动记录
- 端点按钮在面板上，文字"左端点""右端点"，有按下阴影特效
- ⚠️ **重大教训（2026-08-27）：换算用的 scale 绝对不能为 0**。`_effective_scale` 若在未校准时把检测值默认设为 0，则 `map=人物坐标+偏移×0=人物坐标`，**所有怪物坐标全被换算到人物位置，紫点/怪物标记全聚在人物上/错乱**。未校准时 scale 必须用合理默认值（如 0.10）兜底，绝不能用 0。（根因：`_effective_scale` 默认 0.0 → 改成 0.10 解决）
- ⚠️ **重大原理（2026-09-09）：镜头滚动会让"检测相对坐标变化"的逻辑瞬时失效，详见后文「镜头滚动与相对坐标检测」小节**。游戏镜头跟随人物滚动（垂直爬梯快到顶、人物走到地图正中时最明显），镜头一动，屏幕坐标与小地图光点在短时间内会抖动甚至反向回弹，相邻帧逐帧比 Y/X 会把"其实在上升"误判成"没动/没抓住"。动作成败一律只比"动作前稳态基准"与"动作后结果"，过程中持续执行、不被中间帧打断，到终点再核对一次绝对重合。

## 遮挡检测（2026-09-07 更新，以冒险岛世界为准）
- 判定1：GetForegroundWindow()判断游戏窗口是否在前台，不在前台（被挡/最小化）跳过加血加蓝
- 判定2（恢复MP界面门控）：底部横带模板匹配 data/templates/mp_label.png(29×18)，搜索区 X 0~1300、Y 只取底部往上60px，TM_CCOEFF_NORMED 阈值 MP_LABEL_THRESH=0.85；匹配不到=小退/选角/活动弹窗等无血条界面，即便窗口在前台也不加药
- 两条必须同时满足(in_game)才吃药；真机分数：正常游戏0.97、活动/小退底部带0.46
- 教训：旧版全窗口搜+阈值0.35太松，活动弹窗上半屏误匹配0.58；且templates目录曾为空导致模板没加载、门控被架空

## 自动吃药
- 启动后3秒内不检测吃药（避免窗口刚加载截图不准误加蓝）
- 每500ms检测一次
- HP/MP条用固定比例槽位(基准1296宽：HP左299宽139、MP左438宽148，Y=h-28)，低饱和灰像素占比(BLANK_GRAY_*)判空槽
- 吃药前置门控：窗口前台 且 MP标志匹配≥0.85（见"遮挡检测"）

## 录制规则
- 平台绿线录制时，同一X位置(差值<3px)新点覆盖旧点（以后画的为准）
- 红线（录制中）也是同样规则
- 保存时绿线定型
- 平台编号按录制顺序，用户要求底下=编号1（尚未实现按Y排序）
- 左右端点、梯子蓝线、平台绿线全部保存在同一套方案中（data/route_%d_*.json）
- **梯子覆盖规则（2026-09-04用户要求）**：同一个梯子=小地图X差≤5 且 Y范围有交叠(≤6px)，后面的录制**盖掉**前面的旧记录，不叠加（F6每录一次=1条，同梯子重录会替换，数据里出现12条重复=该规则此前未实现）

## 台子选择功能
- 面板小地图左上方"台子选择"按钮
- 点击弹出平台编号圆形按钮，可多选/单选，再点取消
- 选中=黄底黑字圆形，未选=白底黑字圆形
- 空选=全部平台
- 多个台子选好后，打完一个去最近的另一个
- **锁平台硬边界（2026-09-07 冒险岛世界）**：勾选编号后，选怪只打归属到选中绿线的怪(ENABLE_CROSS=False不跨台)；移动硬边界 `_locked_platform_x_range()` 直接取勾选绿线小地图X范围(不依赖脚下离哪条线近,半空/斜坡也稳)，`_combat_at_locked_edge()` 在追怪/上坡/下坡到绿线端点即停；勾选时禁用正下方"按下+跳落层"，只勾1个台子=只在该绿线内左右打、不掉下去

## 日志
- 右侧滚动条支持鼠标拖拽和滚轮（每次3行）
- 日志区域：UI_LOG_X/Y/W/H

## 运行状态显示
- 运行时显示"运行中..."，未运行时为空

## 不运行时怪物检测
- 每10帧一次YOLO检测（约100ms），让紫色点在脚本启动后就显示
- 运行时在_combat_tick中每50ms一次YOLO+血条合并

## 打怪规则（用户定义，部分已实现部分待实现）
1. 以人物为基点优先找±500码以内的怪
2. 打怪时停止巡路，打死当前怪再重新开启
3. 技能距离内有怪时优先全部打完
4. 怪物存活检测：绿色血条或头上伤害数字（红→黄渐变），其一存在=怪没死
5. 500码内没怪时计算最近的怪（包括上下平台和500外同平台）
6. 平台判定参考小地图绿线和人物光点关系
7. 上下梯子顺序：跳→瞬移→梯子；上梯子前检查300PX内有怪先刷完
8. 绿线边缘检测，禁止人物掉下去
9. 上坡怪压着方向键跳着打，下坡直接走；巡路每3秒检测X变化，卡住就向回跳
10. 识别到的怪1秒内没有X值变化就放弃锁定

### 斜坡打怪（当前关闭，2026-09-04 用户定稿）
- **状态：默认关闭**。`fight_cfg["slope_attack"]` = "1" 才启用，不配置默认关。
- **为什么关**：旧实现用 `y_diff>25`（人物/怪屏幕Y差）猜测"在斜坡上"，人物参考点(手)与怪脚底板天然差约40px，导致**同平台怪被误判成斜坡**，走进"走+跳+站定"循环、不落地放技能 → 锁定了也不打。
- **重新启用要求（用户明确）**：斜坡打怪规则**必须在"确认斜坡位置"时才用**——用平台几何/绿线波动真实数据判断斜坡确实存在，不能靠屏幕Y差猜测；不知道斜坡在哪就一律不用（平地打）。

### 战斗方案（2026-09-04/05 会话逐一定稿，均已在代码实现）
**A. 打怪方向（治好反向打）**
- 不识别"人物朝左还是朝右"（识别不准）。**打怪前先朝怪方向按一次方向键**再打（怪在右按→、怪在左按←）。
- 用 `_combat_last_face_dir`（上次让角色朝怪的方向）判断要不要重转：方向变了才按一次，第一次必按。方向一致不重复按（不打断攻击节奏）。

**B. 空怪两层机制（治"打一下无血条无伤害还一直打"）**
- **锁怪前**：投票式空怪过滤 `_filter_static_monsters`——同一位置被连续识别3次仍不动=假怪(建筑/背景误检)→临时排除3秒，时间到恢复，还不动再排除。
- **锁怪后**：`打一下后无血条也无伤害数字=空怪` → drop；并记下该位置进 `_combat_dropped_phantoms`，**5秒内不再重锁它**（防"drop后又锁同一只"死循环空打）。drop 后重置 `_combat_target_attacked`。

**C. 后台检测线程（方案B，治"主线程太厚重、几秒才刷一次"）**
- 人物匹配+怪匹配+YOLO+血条 **同一帧**放一个后台线程算（同帧同步→距离准），主线程只读结果+过滤假怪，做战斗/小地图/蒙板。
- 后台线程用自己的 mss 实例（不共用主线程 `self.sct`），怪列表用原子替换，2秒宽限防漏检闪没。
- 线程总数 3：主线程 + 蒙板线程(已有) + 后台检测线程(新增1个)。

**D. 战斗接管（治好"打几下就不动"）**
- `_combat_active`：怪进技能范围(cast/pursue)时置 True **暂停巡路**（`_random_step` 开头判 `_combat_active` 则释放移动键 return），cross/switch/idle 恢复 False。
- 否则巡路一直走、人物被巡路带着移动，攻击被"移动中不发技能"拦住 → cast 但不出手。

**E. 打怪距离 X/Y 分开算 + 方向性Y范围（治好"上平台怪从下面空打错位"）**
- 不用欧氏距离（那条绿线长度）。**X差(水平)≤技能射程 且 Y差按方向取**：以人物脚底为基点(上负下正)，Y范围已**面板化**——打怪页「Y距离」弹窗设上方/下方打怪范围，默认上`-60`/下`+30`(字段attack_y_up/down，UI有向、决策前abs成正容差，常量ATTACK_Y_UP=60/ATTACK_Y_DOWN=30仅作缺省)，才打怪；超出→不打，改为靠近/跨层。详见第22号说明书。
- 决策核心 `combat_logic`：`cast` 只认 `tier0`（同平台 + 方向性Y范围 + X差≤技能射程）。
- **瞬移（2026-09-07改版，详见第22号说明书）**：原瞬移键/距离两录入框取消，改「瞬移设置」弹窗设 X瞬移距离(水平追怪)/Y瞬移距离(垂直)，默认空，**X或Y任一>0且瞬移技能键(沿用配置z)存在才瞬移，都空=不瞬移**；追怪远距且基本同层(Y差≤40)每850ms水平瞬移贴近，垂直攀爬(`_move_to`)只认Y距离、空则走跳→梯子；「动作录制」按钮仅占位(功能后做)。

**F. 400×400 人物锁定绿框默认隐藏**（不删，`_show_char_roi` 默认 False，要显示置 True）。

**G. 用 .py 直接测（不用打包）**：根目录双击 `跑测试.bat`（自动管理员重启，已修 .py 重启漏脚本路径→不会开成 python 交互窗口）。改完代码重启脚本即生效。

### 关键约定（用户反复强调，务必记住）
- **打怪要有"效率+准"**：人物和怪的位置必须**同一瞬间采样**（同帧），才能算准 X/Y 差。
- **Y值方向性**：怪比人高≤80px、怪比人低≤30px 内才打（`ATTACK_Y_UP=80`/`ATTACK_Y_DOWN=30`，2026-09-05 下30从上50改）；X 在技能射程(350)内。
- **绿框等显示要素"好了别乱动"，要改只做增量**，不能破坏现有功能。

### 打怪功能当前状态（2026-09-04/05 实测，好与坏）
**✅ 好的（已实现/有改善）：**
1. 会**走向/追赶**远处的同平台怪（不分左右取最近）。
2. **转身能转对了**（【冒险岛世界】发/松方向键统一用 `keybd_event` 扫描码；冒险岛世界吃 keybd_event，与怀旧服 DirectInput 相反，详见下方"键盘发送"）。
3. **打完能停手**（点按每下 keydown+keyup 都能松开，无"松不开"问题）。
4. **空怪"打一下没血条没伤害就停"**（血条贴近判定收紧35/45px，伤害数字判定收紧±30/50px，不会误以为"还活着"打三下）。
5. 距离 **X/Y 分开算**；后台线程同帧算人物+怪+YOLO+血条。

**⚠️ 坏的（还没弄好/问题）：**
1. **空打仍偶发**：检测偶发误判（背景/附近怪的血条/伤害数字被当成目标，或误检成怪）。
2. **X/Y 坐标差不稳**：人物/怪物位置不稳 → 距离判断有时偏。
3. **攻击有时打不到**：人物朝向/位置没完全对准怪。
4. **跨平台（爬梯子/上平台打）还没弄**：怪在别的平台够不着就不追（ENABLE_CROSS=False）。
5. **人物特征匹配还不稳定**：偶尔丢人物位置（全图+ROI都失败），需重录人物特征到0.9+才稳。

### 关键输入机制教训（务必记住，以冒险岛世界为准）
- **【冒险岛世界】键盘吃 `keybd_event`（扫描码 + 方向键 ext=0x0001），`SendInput` 扫描码单独发反而不动（2026-09-06 实测）**；发键/松键必须同一机制（都用 keybd_event 的 down+up），不能一个 SendInput 一个 keybd_event。
- （仅参考·怀旧服相反）冒险岛怀旧服是 DirectInput，只认 SendInput 扫描码、keybd_event 的 keyup 收不到会松不开/转不动。两套机制相反、别混用；当前主线只做冒险岛世界，怀旧服结论仅作背景参考。

## 用户永久原则
- 固定值优先（颜色/图片/坐标用常量）
- 新代码加中文注释，新手也看得懂
- 纯增量不改已有代码，涉及旧功能要多想，不行立马恢复
- 每次备份push到Git远程仓库
- 人物光点以正中心为基准
- 绿线始终最上层
- 有问题直接解决，不能逃避或采取折中方案
- 做东西要想清楚最终目的，不要搞复杂
- 备份名写上时间
- **以后备份都要提交到Git远程仓库（git add + git commit + git push origin main），本地备份不够安全**
- **调用任何函数/方法前，先Grep看函数定义和参数，不凭记忆猜，避免参数个数/类型错误**
- **用户没有明确说要换的东西（背景图片、配置、参数等），一直用原来的，绝对不能自己随便换；不知道就查以前的记录或Git历史**
- **有发效果图/参考图的，第一时间下载查看，必须一分不差地一比一复现：量好每个元素的大小尺寸、和周围元素的相对距离/间隙、对齐方式，绝对不能自己猜尺寸和位置，绝对不能乱排**
- **以后每个功能做完，都更新一下这个MD文件，包括方法、流程、关键重点的东西和文件路径，和出现过的问题和解决方案**
- **每个动作步骤后都要有结果反馈检测**：向右移动后检测X值是否变化，跳/上梯子/下平台后检测Y值是否对应变化，截图保存后检测文件是否生成，按键发送后检测游戏是否响应等。但必须优化好检测方案，不能阻塞主循环导致卡顿。详见下方"动作结果反馈检测方案"章节
- **★本游戏输入特性（以冒险岛世界为准；2026-09-06 实测覆盖 2026-09-05 旧结论）**：冒险岛世界吃 **`keybd_event`（扫描码，方向键 ext=0x0001）**，`SendInput` 扫描码单独发不动；发键/松键必须同一机制（都用 keybd_event），不能一个 SendInput 一个 keybd_event。函数：点按 `_press_game_key`、按住/松开 `_send_win_key`、持续按住 `_hold_combat_key`。（仅参考·怀旧服相反：怀旧服是 DirectInput 只认 SendInput、keybd_event 的 keyup 收不到会导致攻击键松不开一直空打——那是怀旧服的根因，不是冒险岛世界，别套错。）
- **★不同游戏特性不同（写代码前先摸清）**：坐标体系、键鼠触发机制、伤害/血条表现、平台/技能距离这些都可能是某游戏特有的。写代码要**适配这款游戏的实际机制**，不是"本应如此"就行——很多通用/想当然的写法到了这游戏就不生效。遇到"本应生效却不生效"，先怀疑"是否不符合这款游戏的机制"。

## 数据文件
- data/route_%d_platforms.json — 平台绿线
- data/route_%d_ladders.json — 梯子蓝线
- data/route_%d_calib.json — 左右端点
- data/route_config.json — 当前方案配置
- data/templates/mp_label.png — MP标签模板（已弃用，遮挡改用API）
- data/templates/gray_bar.png — 血条空白灰色模板
- data/ui_calib_left.png / ui_calib_right.png — 端点按钮图片

## 关键代码位置（行号可能随修改变化，用函数名定位）
- _screen_to_map：屏幕坐标→小地图坐标（人物锚点+相对偏移）
- _get_monster_map_pos_verified：怪物坐标转换+绿线Y校准
- _recalc_scale_from_region：从小地图区域尺寸计算初始scale
- _recalc_scale_from_edges：从端点校准scale_x
- _update_scale_calibration：人物移动时滑动平均微调scale
- _auto_calibrate_edges：自动记录左右端点（每3秒）
- find_player_dot：检测小地图上人物黄色光点（cv2.moments取质心）
- _check_auto_potion：自动吃药检测（含3秒启动延迟+前台遮挡判断）
- _is_bar_blank_at：竖框灰色模板匹配检测血条空白
- _detect_hp_mp_bars：检测HP/MP条位置（固定Y=782）
- _on_mouse：鼠标事件回调（含日志滚动条拖拽+滚轮）
- draw函数中：紫色点、编号、梯子、绿线绘制
- 主循环：不运行时每10帧检测怪物
- 录制点添加：同一X位置覆盖旧点

## 关键技术方案和经验教训（重要！）

### 键盘发送（踩过坑，最终方案——以冒险岛世界为准）
- **问题**：PostMessage/SendMessage 后台消息发键，游戏收不到（两版皆然）
- **【冒险岛世界】最终方案（2026-09-06 实测）**：用 `keybd_event` 扫描码发键/松键（方向键加 ext=0x0001）；**SendInput 扫描码单独发不动，不要用**。点按 `_press_game_key`、按住/松开 `_send_win_key`、战斗持续按住 `_hold_combat_key`/`_release_combat_key`
- （仅参考·怀旧服相反）怀旧服最终方案是 SendInput 扫描码模式(KEYEVENTF_SCANCODE=0x0008) + keybd_event 双发兜底
- **前台切换（两版通用，保留）**：AttachThreadInput绑定线程 + 先按Alt键绕过Windows SetForegroundWindow限制 + BringWindowToTop
- **方向键（两版通用）**：必须加扩展键标志(ext=0x0001)，否则游戏不认
- **修饰键清理（两版通用）**：发键前检查Alt/Ctrl/Shift是否卡住，卡住就先发keyup
- **发键后恢复（两版通用）**：恢复原前台窗口，分离线程
- **随机延时（两版通用）**：按键保持80-180ms随机，防检测

> **【冒险岛世界】键盘输入方式（2026-09-06 实测，这是冒险岛世界的，与旧游戏不同）**：
> - 这是**冒险岛世界（MapleStory Worlds）**游戏实测：**单独发键 `keybd_event` 生效**（发右方向键角色动了）；**`SendInput` 扫描码单独发没生效**（角色没动）。
> - **和旧游戏（冒险岛怀旧服"DirectInput 只认 SendInput扫描码"）不同**——冒险岛世界吃 `keybd_event`，SendInput 单独发不动。
> - 所以**冒险岛世界**键盘输入用 `keybd_event`（发键/松键 keybd_event down+up；方向键需扩展键标志 ext=0x0001）。
> - 备注：仅测了"发右方向键角色动"，其他键/松键(keyup)是否完整生效待进一步验证。

### 热键监听
- **现役机制（2026-09-04更正，记忆文件此条过时）**：`_check_hotkeys` GetAsyncKeyState 轮询（主循环每帧调一次）
- **边沿检测用最低位(0x0001=距上次调用后有过按下)**：主循环6-7fps帧间140-160ms，快速点按(按下又松开)落在两帧之间，只看最高位(0x8000当前按住)必漏；最低位能抓到必触发（2026-09-04修复）
- _key_state字典**含F4/F11**（缺F11一按KeyError→全局异常=整个机器人退出，2026-09-04已补全）
- **废弃**：GlobalHotkeyListener/GlobalMouseListener 低级键盘钩子类已无实例（死代码），勿再参考旧"钩子+F11不可用"的说法
- F5=录制平台，F6=录制梯子，F7=清空，F8=保存，F9=手动框选，F10=运行，F11=倍率校准，F12=停止

### 加血加蓝（最终方案）
- **血条位置：按窗口尺寸比例换算（2026-09-04用户确认保留）**：HP左=510/1382*w、MP左=619/1382*w、宽=107/1382*w、条Y=h-25（原固定值只在窗口1382x807生效，窗口不同时红框偏移"太远"）
- **检测方式**：竖框内灰色模板匹配(gray_bar.png)，阈值0.70，匹配到灰色=空白=加药
- **不用数字OCR**：数字识别不准，灰色模板匹配更可靠
- **遮挡检测**：GetForegroundWindow()判断前台窗口，不在前台不加药（MP标签模板匹配辅助判断）
- **启动延迟**：启动后3秒不检测（窗口刚加载截图不准会误加蓝）
- **检测频率**：每500ms一次；**冷却**：HP/MP各自独立冷却500-1000ms随机

### 录制平台和梯子
- **平台绿线**：检测小地图深绿色(0,255,0)，F5开始/停止录制
- **梯子蓝线**：检测小地图蓝色(255,100,0)，F6开始/停止录制
- **录制原理**：人物移动时，每帧把人物光点位置append到platform_points/ladder_points
- **覆盖规则**：同一X位置(差值<3px)新点覆盖旧点，以后画的为准
- **保存格式**：platforms是点列表[(x,y),...]，ladders是{x,y_top,y_bottom}
- **保存文件**：data/route_%d_platforms.json / route_%d_ladders.json
- **录制中显示红线**(0,0,255)，保存后显示绿线/蓝线

### 端点校准（scale_x校准）
- **作用**：校准屏幕X→小地图X的转换比例
- **自动记录**：每3秒检测，人物站最左3秒记左端点，站最右3秒记右端点
- **手动记录**：面板上"左端点""右端点"按钮，手动点一下记录当前位置
- **手动优先**：手动记录后关闭自动记录
- **数据格式**：(屏幕X, 小地图X, 小地图Y)
- **scale_x计算**：(右小地图X - 左小地图X) / (右屏幕X - 左屏幕X)
- **持久化**：保存到data/route_%d_calib.json，重开脚本不丢失
- **显示**：小地图上白色十字标记

### 自动校准（同屏三点校准 / 倍率按钮）
- **创立原因**：旧的左右上端点校准，人物从地图最左走到右端点时大地图很长会跨画面（地图滚动多次），屏幕X差≠实际世界距离，Y轴同理（人物向上移动时画面也向上滚动）
- **方案**：同屏三点校准，人物站左下角不动（画面不滚动，屏幕距离=实际距离）
- **三点定义**：
  - 基点（红）：人物脚的位置
  - 绿点：基点向右800px
  - 蓝点：基点向上400px（原500，因超出边界改为400）
- **蒙板显示**：三点+连线+距离文字（X:800, Y:400），仅校准中显示
- **小地图显示**：三个实心圆点（基点红、绿点绿、蓝点蓝），半径4
- **倍率公式**：
  - scale_x = (绿点小地图X - 基点小地图X) / 800
  - scale_y = (基点小地图Y - 蓝点小地图Y) / 400
  - X/Y独立scale，不能默认相等（X/Y比率差约4倍）
- **校准完成后保存到** data/route_%d_calib.json
- **操作流程**：
  1. 人物站左下角 → 点"倍率"按钮开始校准
  2. 蒙板显示红/绿/蓝三点，小地图显示三个圆点
  3. 在小地图上点一下绿点对应位置（右800px）
  4. 再点一下蓝点对应位置（上400px）
  5. 自动计算倍率，三点显示自动关闭
  6. 小地图底部状态栏显示"倍率 X:0.XXX Y:0.XXX"
- **按钮开关**："倍率"按钮（41x25，位置213,104，图片data/ui_calib_auto.png），点一下开始校准，再点一下关闭（清空数据，三点消失），按下特效：阴影3帧
- **拖动调节（校准完成后）**：
  - 基点固定不动
  - 绿点只能左右拖动（调节X方向距离）
  - 蓝点只能上下拖动（调节Y方向距离）
  - 拖动时实时重新计算倍率（_recalc_auto_calib_scale）
- **状态变量**：
  - _auto_calib_stage：0=空闲，1=待标记绿点，2=待标记蓝点，3=完成
  - _auto_calib_base：基点(屏幕X, 屏幕Y, 小地图X, 小地图Y)
  - _auto_calib_green_map：绿点小地图坐标
  - _auto_calib_blue_map：蓝点小地图坐标
  - _auto_calib_dragging：拖动状态 None/'green'/'blue'
  - _calibrated_scale_x / _calibrated_scale_y：校准后的倍率
- **当前问题（待修复）**：小地图上的红色基点位置不对，应该和人物黄色光点重合，但脱离了
  - 原因：_auto_calib_base记录的小地图坐标可能和小地图绘制的坐标系不一致
  - 影响：绿点位置也错（因为绿点是相对于基点计算的）
  - 修复方向：确保小地图上的基点用人物黄色光点的真实位置（find_player_dot返回的坐标），坐标系和小地图绘制一致

### 紫色怪物小圆点（关键注意事项）
- **创立原因**：小地图上怪物红点和其他玩家红点冲突，不能直接检测红点
- **方案**：YOLO检测屏幕上怪物位置 → 坐标转换到小地图 → 画紫色圆点
- **画在哪里**：必须画在map_display上（缩放后的340x250画布），不能画在display上（原始146x112，缩放后看不见）
- **半径**：6像素实心圆，COLOR_MONSTER_MAP=(255,0,255)
- **坐标转换**：人物锚点+相对偏移（怪物小地图位置 = 人物小地图位置 + 屏幕偏移 × scale）
- **不运行时也显示**：主循环每10帧做一次YOLO检测（脚本启动就显示，不用先点运行）
- **Y校准**：只校准和人物Y相差<30px的同平台怪（绿线X最接近且Y偏差<15px），高处怪不强制拉到绿线
- **scale_y=scale_x**：小地图等比例渲染，Y跟随X，人物上下移动时微调（偏差>30%视为噪声忽略）

### 面板崩溃/空白（重大教训）
- **原因1**：用try-except包裹draw函数各部分，错误被吞掉导致紫色点不显示/面板空白
- **原因2**：修改紫色点位置时误删了梯子蓝线和平台绿线的绘制代码
- **教训**：
  - 绝对不能用try-except吞掉绘制错误来"解决"问题，这是逃避
  - 有问题直接解决，找到根因修复
  - 修改绘制代码时要检查是否误删了其他元素
  - 每次改动后打包测试，不要一次改太多
- **恢复方法**：用git checkout回退到正常版本

### 截图方式
- 用mss库（底层BitBlt从屏幕DC截取）
- **不能用于遮挡检测**：DirectX窗口被挡住时仍能截到游戏画面（GPU后台缓冲）
- 窗口最小化时截到黑屏

### 小地图检测
- find_player_dot：HSV颜色过滤找黄色光点(0,255,255)，cv2.moments取质心（正中心）
- 光点面积过滤：1-30像素（过滤噪声）
- **重要教训（2026-08-23）**：千万**不能**用 `cv2.morphologyEx(mask, cv2.MORPH_OPEN, np.ones((2,2)))` 腐蚀——它会把 **2~4px 的小型黄色光点整个"腐蚀掉"**，导致人物小地图光点检测不到 → 倍率校准（三点记录）失败。已**去掉该 OPEN 腐蚀**，小光点即可正常检测。（改动位置：`find_player_dot`，约1886行删掉 `mask = cv2.morphologyEx(...)` 这行）
- 局部搜索优化：在上次位置附近25x25范围搜索，失败再全图搜索
- 小地图区域：用户手动框选或自动检测，map_area_rect记录位置

### 镜头滚动与相对坐标检测（2026-09-09 重大原理，所有"比坐标变化"的逻辑都要遵守）
- **现象/根因**：游戏镜头始终跟随人物滚动，垂直爬梯快翻上平台、人物走到地图正中时镜头移动最明显。镜头一动，人物屏幕坐标、小地图光点坐标在短时间内会**抖动、停滞甚至反向回弹**（真机案例：爬梯光点 Y 74→71→86，明明上升过，中间帧又掉回原值）。因此"相邻两帧/短窗口内逐帧比 Y、X 有没有朝目标方向变"在镜头滚动期是不可信的。
- **会被污染的逻辑（排查优先看这些）**：①起跳抓梯是否成功（`_ladder_post_jump_process` 的"500ms Y 没上升=没抓住"）；②climbing 段"1 秒 Y 没动=卡住"；③`_check_move_blocked` 按住方向却没位移的防卡判定；④到顶/到位判定；⑤跨层瞬间的怪小地图换算 `_screen_to_map`、旧锁定缓存（所以到顶要 `_reset_lock_after_arrival` 全图重扫）；⑥任何用"上一帧坐标→这一帧坐标"推朝向/推移动方向的地方。
- **正确做法（铁律）**：
  1. 只在**动作开始前的稳态**（人还站地、没起跳）记录一次基准坐标，动作中途不刷新这个"成败基准"；
  2. 动作过程中**持续执行按键**（如一直按住↑），**绝不能因为某一中间帧 Y 回弹/停滞就判失败、松手或重置**；
  3. 给一个判定窗口，窗口内**只要出现过一次**相对基准朝目标方向变化达标，就**锁存为成功且不可逆**，之后不再回看中间帧；
  4. **只在动作终点再核对一次绝对重合**（光点中心 vs 梯子端/绿线平台，容差±1~2px），重合后再多按 1 秒、然后显式松开出梯；
  5. "卡住/没动"类检测要么用"动作前基准"做更长窗口比较、主动避开镜头滚动窗口，要么在爬梯/跨层/镜头移动期间直接冻结，别在滚动期下失败结论。
- 一句话：**动作成败只比"没动之前"和"到位之后"两个稳态，中间只执行不判错，终点做一次绝对重合校验。**

### 人物检测（模板匹配）——2026-09-04 用户定稿版
- _match_character：多模板cv2.matchTemplate(TM_CCOEFF_NORMED)，阈值0.70
- **搜索规则（用户最终确定，勿再改）**：找得到→400x400区域内找（快）；点丢失→**立马全图找（不需要等）**；两种搜索都只认≥0.70高分
- **融合=主簇加权平均**：两两≤60px聚簇，取点数最多/总分最高簇加权（2点在我这边=以我为准，撞脸点离群不参与）；单点≥0.70直接采纳
- **已删除**（它们是"找不到就卡住/跑偏"根源，还原用户原始方法）：锚点簇优先/高置信簇/连续性确认闸
- 找不到→返回None（宽限显示上次位置1.5秒），下帧继续全图搜=不卡死
- 绿框=以当前采纳的人物位置为中心400x400（不以上次搜索位置为中心，否则停在老地方框不住人）
- 函数：_get_player_screen_pos

### YOLO怪物检测
- _detect_monsters：YOLO模型检测怪物，返回[(x1,y1,x2,y2,score),...]
- 怪物坐标基准：X用检测框中心(x1+x2)//2，Y用底部y2（脚位置）
- 模型路径显示在面板"怪物数据"按钮右侧
- 不运行时每10帧检测一次（约100ms），运行时每50ms检测+血条合并
- 血条合并：检测绿色血条和YOLO框关联，确认怪物存活

### 【冒险岛世界】窗口绑定
- _find_game_window：按窗口标题查找（当前新游戏 MapleStory Worlds，匹配 "MapleStory"；旧游戏"冒险岛怀旧服"）。找不到时打印"未找到游戏窗口(MapleStory)"
- 启动时自动绑定，也可手动用准星拖拽绑定；自动/手动绑定都调用 _save_target_window_size
- window_rect记录窗口位置(left,top,width,height)
- _update_window_rect：用GetWindowRect更新窗口位置
- 绑定后自动检测小地图区域(_detect_minimap)：**只用三模板法**（mini map标题+右上X按钮+底边，模板在 data/templates/），阈值0.55；早期失败的"描线/扫描线法"已于2026-09-06删除，匹配不上本帧跳过、不硬算不覆盖。详见《【冒险岛世界】强绑窗口尺寸与小地图三模板定位说明书.md》

### 【冒险岛世界】窗口固定尺寸（定稿 1276×749；2026-09-06 MapleStory Worlds 版本，改窗口尺寸看这里，完整说明见《【冒险岛世界】强绑窗口尺寸与小地图三模板定位说明书.md》）
**一、改了几处：**
1. **`GAME_W/GAME_H` 常量（约289-290行）**：`GAME_W=1276, GAME_H=749`（**全窗口尺寸，不区分客户区**；2026-09-06游戏更新后由1290×756改为1276×749，再变只改这两个常量）
2. **`_save_target_window_size`（1214行，绑定后调用）**：写死 `_target_window_size=(GAME_W,GAME_H)` + 移除窗口 `WS_THICKFRAME`（禁改大小，保留标题栏可拖动）+ 末尾调 `_ensure_window_size()`（绑定后立即拉回）
3. **`_ensure_window_size`（约1228行）**：检测 `window_rect` 当前全窗口尺寸 != 目标(1276×749) 就 SetWindowPos 拉回（用 SWP_NOMOVE 不锁位置，可拖动）

**二、为什么不生效（这次踩的坑）：**
- **根因1：`_find_game_window` 只认"冒险岛"标题**。新游戏窗口标题是 `MapleStory Worlds-克罗诺斯托里`（进程 msw），不含"冒险岛"→ 找不到窗口 → 自动绑定走 `else`（"未找到游戏窗口"）→ 没调 `_save_target_window_size` → `_target_window_size` 还是 None → `_ensure_window_size` 里 `if _target_window_size is None: return`（不拉回）。
- **根因2：`_save_target_window_size` 读"当前窗口尺寸"**（`window_rect`），不是写死固定值；用户要"写死固定"→ 读当前窗口固定的是绑定瞬间尺寸，不是指定值。
- **根因3：只改了尺寸数字，没注意"绑定句柄/标题"也换了**（新游戏 msw 句柄、标题都不同），拉回对象/找窗口对象要对应。

**三、解决办法（已改好）：**
- `_find_game_window` 匹配关键词改成 `"MapleStory"`（139行），找不到打印"未找到游戏窗口(MapleStory)"。
- `_save_target_window_size` 写死 `(GAME_W,GAME_H)`（不用读当前窗口）+ 移除 `WS_THICKFRAME` + 调 `_ensure_window_size()` 立即拉回。
- 改尺寸只需改 `GAME_W/GAME_H` 两个常量，`_save_target_window_size` 自动跟随（自动/手动绑定都调它，同步生效）。

**四、手动 / 自动绑定（同步）：**
- **自动绑定**（645行 `self.hwnd=_find_game_window()` → `_save_target_window_size()` 649）
- **手动绑定**（10546 `_bind_window` → `_save_target_window_size()` 10553）
- 两者都调 `_save_target_window_size`（同一个写死+禁改+拉回函数），**手动/自动同步生效**。
- 绑定后 `_ensure_window_size`（主循环 10641 每30帧 + 绑定立即 1225）检测拉回。

**五、窗口与蒙板对齐：**
- 蒙板(Overlay `_overlay_hwnd`)按 `window_rect` 定位置顶（`SetWindowPos(self._overlay_hwnd,-1, wr['left'],wr['top'],wr['width'],wr['height'])`），尺寸=固定尺寸(1276×749)，与游戏窗口完全重合。
- **血条坐标**按窗口比例换算（HP左=510/1382*w、MP左=619/1382*w、宽=107/1382*w、Y=h-25），窗口改尺寸后按 1382 基准比例仍生效。

### 【冒险岛世界】截图特征键盘微调版（2026-09-06 成功）
**功能**：人物/怪物特征截图框选（`_interactive_box_select` 自绘框选，替代 cv2.selectROI）：
- **蓝框线条细一半**：线宽 `line_w=1`（原 selectROI 粗框）。
- **键盘方向键移动框**：框好后用方向键 ↑↓←→ 移动整个框位置（`GetAsyncKeyState` 检测虚拟码 37/38/39/40，比 waitKey 方向键值可靠）。
- **缩放步长 0.1px**（精细微调）+ **节流 0.05s**（按住方向键每 0.05s 移一次，避免狂移太快）+ **坐标画/返回 int 取整**（防浮点闪退）。
- 回车确认 / ESC 取消 / 右键取消。

**踩过的坑（务必记住）**：
1. **cv2.waitKey 方向键值随 OpenCV 版本不同**（2424832 等不可靠），改用 `GetAsyncKeyState(37/38/39/40)` 才可靠。
2. **浮点步长（0.2/0.5）会让 state 坐标变 float** → `cv2.rectangle`/返回时收到 float 报错闪退。**必须画框/返回时 int 取整**（`int(x0), int(y0)`）。
3. **方向键检测循环里加 sleep 会破坏检测时机**（方向键失效不动），节流改用"记录上次移动时间、间隔够才移"（不用 sleep 阻塞在检测循环）。

**关键代码**：`_interactive_box_select(caption, frame, line_w=1)`（maple_route_ui.py ~6260 行）：`_st=0.1` 步长、`_last_arrow_t` 节流、`int()` 画框返回。

### 【冒险岛世界】截图放大镜版（2026-09-06 成功）
放大镜加在 `_interactive_box_select`（人物/怪物特征共用）：
- **放大镜 = 截图蓝框+周边10px 范围内全部内容（含你画的蓝框线）放大5倍**，画在框右边8px（超画面自动放框左/上边）。
- **只放大一个范围，不做额外处理**：不另画线（取含蓝框的画面 `disp` 的框+周边区域一起放大，不重新画蓝框线）。
- `_pad=10`（框周边10px）、`_zscale=5`（放大5倍）、`_roi = disp[_gy0:_gy1, _gx0:_gx1]`（含你画的蓝框）。
- 踩坑：① 独立放大镜窗口弹窗会灰屏/闪退 → 改画在主窗口 `disp` 框边；② 放大倍数太大（10倍）超画面不显示 → 按需（5倍）并画在框边、超画面自动换边；③ 不要重新画蓝框线（画了和主画面不同步），取含蓝框的 `disp` 一起放大。

### 透明窗口/Overlay
- 用OpenCV imshow显示控制面板（不是独立透明窗口）
- 控制面板和游戏窗口分开两个窗口
- 控制面板用PNG素材做UI背景（data/ui_*.png）
- 小地图显示在控制面板的"路线"标签页中
- 之前曾是同一个窗口，后来分开了

### 方案保存/加载
- 每套方案包含：平台绿线、梯子蓝线、左右端点
- 文件：data/route_%d_platforms.json / route_%d_ladders.json / route_%d_calib.json
- F7保存，F8加载，F12切换方案，F10清空
- route_config.json记录当前方案和运行模式
- 最多3套方案

### 打包
- **详细打包操作说明见 `BUILD_GUIDE.md`**（包含完整流程、所有文件路径、常见问题解决方案、一键打包脚本）
- PyInstaller打包：python -m PyInstaller MapleBot.spec --noconfirm
- 打包前必须关闭MapleBot.exe（否则PermissionError）
- 打包前先python -c "import py_compile; py_compile.compile('maple_route_ui.py', doraise=True)"检查语法
- exe输出到dist/MapleBot.exe
- data/目录下的模板和配置需要打包进去（spec文件配置）

## 待实现/待优化
- 紫色点Y值不准（scale_y需要更准的校准方式，当前进行中）
- 平台编号按Y值排序（底下=编号1）
- 怪物存活检测加伤害数字识别（红→黄渐变）
- 右键测坐标功能未生效
- 梯子上下逻辑（跳→瞬移→梯子）
- 巡路卡住检测（每3秒X无变化就向回跳）
- 斜坡打怪（上坡跳打，下坡直接走）
- 绿线边缘防掉落
- Git远程push偶尔网络失败，需重试

## 备份位置与清理规则（冒险岛世界，2026-09-07 定稿）
- **唯一当前工作版**：`C:\Users\wenwen\Desktop\MXD\maple_bot`（留在 C 盘，活跃开发，不挪走）
- **所有备份统一放 E 盘**：`E:\冒险岛备份`（不再放 C 盘项目内），分 4 个子目录：
  - `历史整盘备份\`：整目录拷贝的历史快照（如 maple_bot_backup_20260905_214842）
  - `旧版本源码\`：仿熊猫识图版、三维识别版、冒险岛世界初版等旧分支源码
  - `压缩包备份\`：早期散落的整包 zip
  - `源码快照zip\`：每次功能验收后的源码完整备份 zip（以后新备份直接生成到这里）
- **备份大小规则（用户铁律）**：备份保持 1G 多、和现在瘦身後一样；**单个/累计达到 2G 就清理一次**，优先删可再生成内容：`build_*`/`dist_*` 打包产物、`__pycache__`、`*.pyc`、虚拟环境 `.venv`；源码/data 录制/文档/配置/.git 永不删
- **备份只装成果**：源码(.py)、data（录制路线/模板/模型/配置）、MD 文档、必要资源；排除 build/dist 打包产物、__pycache__、*.pyc、*.log
- 2026-09-07 清理记录：删除大备份内 149 个 build_*/dist_* 历史打包产物共 12.32GB（exe 可由 git 源码随时重新打包，且我们只用 .py 直测）；全部备份从 C 盘剪切到 E 盘；C 盘 MXD 从 16.16GB 降到约 0.72GB，C 盘剩余 53.7G→68.8G
- 远程 GitHub（origin=CODEX 仓库）需开代理(127.0.0.1:7890)才能推；网络不通时先保证本地 git 快照 + E 盘备份双保险，事后再 push

## Git提交记录
- 5447b90 2026-09-07【冒险岛世界】目前全部正常功能：寻怪范围面板+群攻独立Y+空打修复+打怪站定不带方向+解卡5s+锁单平台硬边界+MP界面门控0.85
- e9b8ff5 2026-09-07【冒险岛世界】选目标重构+位移防卡死+检测自适应+下坡贴近+群攻空怪判定
- 8ffc823 备份_2026-08-22_0325: 遮挡检测改用GetForegroundWindow
- 76485c1 备份_2026-08-22_0308: X/Y独立scale+日志拖拽滚轮+录制覆盖+启动3秒不加蓝+默认光点+紫色点10帧刷新+端点持久化
- 6098583 紫色怪显示正常版_2026-08-22
- 73e837b 自动吃药成功版本_Y782
- 747f899 完整备份: 自动吃药成功版本

---

# 打包操作流程（2026-08-23 更新，必须严格遵守）

## 一、正确打包命令（用cmd，不要用PowerShell）

**推荐：onedir模式（和原备份MapleBot_snap一致，稳定性最高）**
```
cmd /c "pyinstaller --noconfirm --clean --onedir --windowed --name MapleBot --add-data config;config --add-data data;data maple_route_ui.py"
```

**调试用：加控制台看报错**
```
cmd /c "pyinstaller --noconfirm --clean --onedir --console --name MapleBot_debug --add-data config;config --add-data data;data maple_route_ui.py"
```

或用spec文件：
```
pyinstaller MapleBot.spec
```

**为什么用cmd不用PowerShell**：PowerShell会把 `--add-data "data;data"` 中的分号 `;` 解析成命令分隔符，导致data文件夹没正确打包进去。

**为什么用onedir不用onefile**：onefile模式资源解压到临时目录_MEIPASS，路径不稳定易导致UI图片加载失败；onedir模式资源在_internal文件夹中，路径稳定，和原备份MapleBot_snap一致。

**入口文件必须是maple_route_ui.py**：不是main.py（main.py是另一个PyQt5程序，界面和功能完全不同）。

## 二、打包前检查清单

0. **【强制·打包前第1步】备份 exe 同级 data 整目录**（2026-09-04 二次血的教训）：`Copy-Item dist_vXX\MapleBot\data backups\vXX_data_时间戳 -Recurse`。用户录制（route_*.json / plans.json / best.onnx / 人物特征 png）全部在 **exe 同级 data**（DATA_DIR=app_dir()/data），PyInstaller 重新打包会**删除并重建整个 dist_vXX\MapleBot 应用目录**（含 exe 同级 data，一夜清零），不备份=用户数据丢失
1. **关闭正在运行的MapleBot.exe**（否则PermissionError拒绝访问）
   - 命令：`Get-Process MapleBot -ErrorAction SilentlyContinue | Stop-Process -Force`
   - ⚠️ 注意：MapleBot.exe 非管理员启动会走"权限重启"（ShellExecuteW runas + _sys.exit()，退出码0≠崩溃），真正实例是提升后的子进程，**必须全部关干净再打包**
2. **确认data文件夹包含所有UI图片**（特别是ui_tab_potion.png、ui_bg_blank.png）
3. **确认config文件夹存在**
4. **语法检查**（可选）：`python -c "import py_compile; py_compile.compile('maple_route_ui.py', doraise=True)"`
5. **检查已知BUG**（恢复完整备份后必须验证）：
   - UI背景图加载必须用`load_png()`，不能用`cv2.imread()`（打包后路径异常会导致灰屏）
   - `__init__`中不能在`_detect_minimap()`之后重置`self.map_area_rect = None`（会覆盖检测结果导致灰屏）
   - `map_area_rect`必须在`__init__`最开头初始化，自动绑定后不重置
6. **【强制】spec必须upx=False**（v73验证，upx=True压缩DLL被Defender解压扫描导致冷启动极慢未响应）。检查MapleBot.spec的EXE段和COLLECT段都是upx=False
7. **【强制·打包后第1步】恢复 exe 同级 data**（打包只把 seed 放进 _internal/data，app_dir() 读 exe 同级 data，缺失会读不到配置；打包后 exe 同级 data 不存在=只加不覆盖）：
   - `Copy-Item dist_vXX\MapleBot\_internal\data\* dist_vXX\MapleBot\data\ -Recurse`（json/UI图/模板 seed）
   - `Copy-Item data\models\best.onnx dist_vXX\MapleBot\data\best.onnx`（模型放 exe 同级 data 根，yolo_config.json 的 model_path="best.onnx" 按 DATA_DIR 解析）
   - ⚠️ **严禁对 route_*.json / plans.json / 用户运行时数据使用 -Force 覆盖**（2026-09-04 血泪教训）

## 三、文件路径

- 项目根目录：`C:\Users\PC\Doubao\chats\2026-08-15\new-chat-4\maple_bot`
- 主源码：`maple_route_ui.py`
- 打包输出：`dist\MapleBot.exe`
- UI图片/模板：`data\` 目录
- 配置文件：`config\` 目录
- 手动备份：`backup\` 目录
- 根目录备份：`maple_route_ui_backup_*.py`（多个历史版本）
- 打包脚本：`build.bat`（旧版入口main.py，参考用）
- spec文件：`MapleBot.spec`

## 四、资源路径机制（重要！）

代码中有两个路径函数，用途不同：

1. **`resource_path(relative_path)`** — 加载打包进exe的只读资源
   - 打包后：返回 `sys._MEIPASS + relative_path`（临时解压目录）
   - 开发时：返回脚本目录 + relative_path
   - 用于：UI图片、模板图片等只读资源

2. **`app_dir()`** — 可写数据目录
   - 打包后：返回exe所在目录
   - 开发时：返回脚本目录
   - 用于：配置文件、用户生成的数据（minimap_region.json等）

**关键规则**：所有UI图片加载必须写 `resource_path(os.path.join("data", "xxx.png"))`，**不能漏掉 `data/` 前缀**。

## 五、各功能实现方法

| 功能 | 实现方法 |
|------|----------|
| 热键监听 | `GetAsyncKeyState` 轮询（F5-F12），不是keyboard库 |
| 截图 | `mss` 库（BitBlt从屏幕DC截取） |
| 窗口绑定 | 按窗口标题查找，`GetWindowRect` 获取位置 |
| 人物光点检测 | HSV颜色过滤找黄色，`cv2.moments` 取质心（正中心） |
| 怪物检测 | YOLO模型（best.onnx） |
| 人物检测 | 模板匹配 `cv2.matchTemplate(TM_CCOEFF_NORMED)` |
| UI绘制 | OpenCV `imshow` + PNG素材透明通道混合 |
| 打包 | PyInstaller `--onefile --windowed` |
| F9手动框选 | OpenCV独立窗口1:1显示截图，拖拽框选 |

## 六、出现过的问题及解决方案

### 问题1：五大板块背景全部不显示
- **现象**：打包后运行，UI背景全是灰色，按钮图片正常
- **根因**：背景图片加载路径缺少 `data/` 前缀
  - 代码写的是 `resource_path(fname)` → 找 `sys._MEIPASS/ui_bg_blank.png`
  - 实际图片在 `sys._MEIPASS/data/ui_bg_blank.png`
- **解决**：改成 `resource_path(os.path.join("data", fname))`
- **代码位置**：`manual_select_region` 方法中背景加载循环（约第800行）

### 问题2：吃药页背景不显示
- **现象**：其他4个标签页背景正常，吃药页灰色
- **根因**：`ui_tab_potion.png` 在项目根目录，但不在 `data/` 文件夹中
- **解决**：从根目录复制到 `data/ui_tab_potion.png`

### 问题3：F9窗口被游戏挡住，出现在游戏背后
- **现象**：按F9后，截图选择窗口在游戏窗口后面，看不见
- **根因**：OpenCV窗口没有置顶
- **解决**：创建窗口后加 `cv2.setWindowProperty(sel_win, cv2.WND_PROP_TOPMOST, 1)`

### 问题4：F9窗口没有对齐游戏窗口
- **现象**：F9窗口位置和游戏窗口不对齐
- **当前代码**：`cv2.moveWindow(sel_win, self.window_rect["left"], self.window_rect["top"])`
- **注意**：`window_rect` 是 `GetWindowRect` 返回的整个窗口坐标（含标题栏边框），截图也是整个窗口，理论上应对齐。如仍有偏差，需考虑OpenCV窗口标题栏高度。

### 问题5：打包失败 PermissionError
- **现象**：`os.remove` 拒绝访问 `dist\MapleBot.exe`
- **根因**：MapleBot.exe正在运行
- **解决**：先 `Stop-Process MapleBot`，等2秒再打包

### 问题6：PowerShell打包后data文件夹没进去
- **现象**：打包成功，但运行后背景/资源找不到
- **根因**：PowerShell把 `--add-data "data;data"` 中的分号解析成命令分隔符
- **解决**：用 `cmd /c "..."` 包裹打包命令，或直接用spec文件

### 问题7：启动崩溃（map_area_rect为None）
- **现象**：打包后启动闪退，traceback显示 `map_area_rect` 为None
- **根因**：未校准小地图时，`map_area_rect` 为None，代码直接访问导致崩溃
- **解决**：访问前加None判断，未校准时用默认值或跳过

## 七、重要规则（必须遵守）

1. **以后有问题，先查看本记忆文件**，找正确方法，不要自己猜
2. **以后用备份恢复覆盖新文件时，必须先备份当前新文件**！（今天的教训：直接用8月17日备份覆盖，导致今天上午修改的小地图光点版本丢失）
3. **打包用cmd运行**，不要直接在PowerShell中运行带分号的 `--add-data`
4. **打包前关闭正在运行的exe**
5. **UI图片加载路径必须包含 `data/` 前缀**
6. **不要随意换打包方式**，严格按照上面的命令或spec文件
7. **上下文太长时，先查记忆文件再操作**，不要凭记忆

## 八、分发给别人

- **只发exe**：可以运行，但别人要自己按F9校准小地图、配置按键
- **exe + data文件夹 + config文件夹一起发**：别人打开就能用，你的配置都在
- 建议打包成zip一起发

## 九、今天的总结（2026-08-23）

**做了什么**：
1. 修复了启动崩溃（map_area_rect为None判断）
2. 修复了五大板块背景不显示（背景加载路径加data前缀）
3. 修复了吃药页背景缺失（ui_tab_potion.png复制到data）
4. 修复了F9窗口被游戏挡住（加WND_PROP_TOPMOST置顶）
5. 确认了正确的打包流程（cmd运行，含data/config和hidden-import）

**犯的错误**：
1. 直接用8月17日备份覆盖当前文件，导致今天上午修改的小地图光点处理版本丢失——**以后覆盖前必须先备份**
2. 一开始忘记了以前的打包方式（build.bat），自己裸打包，导致data/config没进去
3. 在PowerShell中运行带分号的--add-data，导致解析错误
4. 没有第一时间查记忆文件，凭记忆操作

**经验教训**：
- 打包是高频操作，必须严格按照固定流程，不能随意改
- 资源路径有两套机制（resource_path只读 / app_dir可写），改代码时要注意
- 上下文长了一定会忘，先查记忆文件再动手
- 覆盖文件前先备份，这是铁律

---

# 主窗口蒙板功能（2026-08-23 整理）

## 一、蒙板是什么
- 一个置顶透明窗口，覆盖在游戏窗口上，显示人物位置、怪物框、连线、血条标记等
- 用Win32原生API创建分层透明窗口（WS_EX_LAYERED | WS_EX_TOPMOST），不依赖tkinter
- 透明色：品红 COLOR_MAGENTA=0x00FF00FF，SetLayeredWindowAttributes用LWA_COLORKEY把品红变透明
- 窗口位置和大小 = 游戏窗口整个窗口（含标题栏），和_capture_window坐标系一致
- 每100ms刷新一次（SetTimer），主循环每50ms PeekMessage处理消息

## 二、蒙板显示的内容（统一蒙板）
| 元素 | 颜色 | 说明 |
|------|------|------|
| 人物偏移黄点 | 黄色实心圆(0x00FFFF)，半径6，橙边框(0x0080FF) | 人物模板匹配中心+偏移量，偏移反馈时闪烁5秒 |
| 怪物框 | 绿色空心矩形(0x00FF00)，线宽2 | YOLO检测到的怪物框 |
| 人物→怪物连线 | 绿色线(0x00FF00)，线宽2 | 从黄点连到怪物框中心 |
| 距离数字 | 绿色文字 | 连线中点显示像素距离 |
| 怪物血条标记 | 绿色矩形 | 近战挡住怪时凭血条定位 |
| HP条标记 | 白色小矩形 | 血条位置标记 |
| MP条标记 | 白色小矩形 | 蓝条位置标记 |
| 自动校准三点 | 红/绿/蓝圆点+连线+文字 | 倍率校准时显示 |

## 三、蒙板数据结构
- self._monster_overlay_data 字典，主线程更新，蒙板线程读取
  - char_pos: (cx, cy) 人物屏幕坐标（模板匹配中心+偏移）
  - monsters: [(x1,y1,x2,y2,score),...] YOLO怪物框
  - monster_hp_bars: [(bx,by,bw,bh),...] 怪物血条
  - hp_marker: (hx,hy) HP条标记
  - mp_marker: (mx,my) MP条标记
  - link_until: 时间戳，黄点闪烁截止时间

## 四、蒙板启动/停止
- _start_monster_overlay()：启动后台线程，创建Win32透明窗口
- _stop_monster_overlay()：停止线程，强制DestroyWindow，join线程（最多1秒）
- 线程函数：_monster_overlay_loop → 优先 _win32_overlay_loop，失败回退 _tkinter_overlay_loop

## 五、关键技术点
1. **64位函数签名必须设置**：CreateWindowExW、SetWindowPos等的restype和argtypes必须显式声明，否则句柄被截断成32位导致窗口创建失败
2. **WNDPROC回调对象必须保留引用**：self._overlay_wndprocs.append(wnd_proc_ref)，防止被GC后旧窗口残余消息调用已回收内存
3. **GDI对象必须删除**：每次WM_PAINT创建的Pen/Brush用gdi_objs列表收集，EndPaint前DeleteObject，否则GDI泄漏
4. **空刷用GetStockObject(5)**：画空心矩形时选入NULL_BRUSH，否则矩形会被填充
5. **SetWindowPos标志0x0050**：SWP_NOACTIVATE(0x0010) | SWP_SHOWWINDOW(0x0040)，不激活窗口但显示
6. **F9结束后必须重新定位置顶蒙板**：OpenCV的F9窗口会影响蒙板的z-order和位置，F9结束后调用SetWindowPos(hwnd, -1, ...)重新置顶

## 六、出现过的问题
- **问题**：蒙板窗口创建失败或句柄无效
  - **原因**：64位下没有设置函数签名，句柄被截断
  - **解决**：显式设置所有user32/gdi32函数的restype和argtypes
- **问题**：蒙板运行一段时间后崩溃
  - **原因**：WNDPROC回调对象被GC回收
  - **解决**：用列表保留所有历史回调引用
- **问题**：F9手动框选后蒙板位置偏移或被挡住
  - **原因**：OpenCV窗口影响了蒙板的z-order
  - **解决**：F9结束后强制SetWindowPos重新定位置顶

---

# 小地图光点映射方案（2026-08-23 整理）

## 一、整体方案：人物锚点 + 相对偏移
- 不直接用固定scale把屏幕坐标映射到小地图
- 以人物光点为参考点，怪物相对于人物的屏幕偏移 × scale = 小地图偏移
- 公式：
  - 怪小地图X = 人物小地图X + (怪屏幕X - 人物屏幕X) × scale_x
  - 怪小地图Y = 人物小地图Y + (怪屏幕Y - 人物屏幕Y) × scale_y
- X和Y用各自的scale（小地图X/Y压缩比率不同，必须分开算，不能默认相等）

## 二、人物位置获取
- **小地图坐标**：ind_player_dot() — HSV颜色过滤找黄色光点，cv2.moments取质心（正中心）
  - 光点面积过滤：1-30像素
  - 局部搜索优化：在上次位置附近25x25范围搜索，失败再全图搜索
- **屏幕坐标**：_get_player_screen_pos() — 多模板匹配cv2.matchTemplate(TM_CCOEFF_NORMED)，阈值0.70
  - 模板由用户截取人物特征图，保存在data/char_templates/
  - 匹配失败时在上次位置附近160x160 ROI搜索，阈值降到0.55
  - 再失败有1.5秒宽限期用上次位置，超过才返回None
  - 返回模板中心点 + char_x_offset/char_y_offset微调

## 三、怪物坐标转换
- _screen_to_map(screen_x, screen_y)：基础线性转换（人物锚点+相对偏移）
- _get_monster_map_pos_verified(screen_x, screen_y)：带绿线Y校准的转换
  - 先用_screen_to_map线性转换
  - 只对和人物Y相差<30px的同平台怪，找X最接近且Y偏差<15px的绿线点校准Y
  - 高处/低处平台的怪（Y差>30px）不强制拉到绿线上，保留线性转换Y
- 怪物屏幕坐标基准：X用YOLO检测框中心(x1+x2)//2，Y用底部y2（脚位置）

## 四、小地图绘制
- 怪物紫色点画在map_display上（缩放后的340x250画布），半径6实心圆
- 不能画在display上（原始146x112，缩放后看不见）
- 绘制层级（从底到顶）：端点十字→录制黄线→缩放display→紫色怪物点→平台编号→梯子蓝线→平台绿线→人物黄点

## 五、出现过的问题
- **问题**：怪物点位置不准，偏上或偏下
  - **原因**：scale_y不准，或Y没有独立校准
  - **解决**：X/Y独立scale，上端点校准Y，人物上下移动时滑动平均微调
- **问题**：高处怪物被拉到低层绿线上
  - **原因**：绿线Y校准没有限制范围
  - **解决**：只校准和人物Y相差<30px的同平台怪
- **问题**：人物光点检测不到时怪物点全消失
  - **原因**：_screen_to_map依赖人物位置，人物位置为None时返回None
  - **解决**：人物检测有1.5秒宽限期用上次位置

---

# 倍率（scale）校准方案（2026-08-23 整理）

## 一、为什么需要校准
- 屏幕坐标→小地图坐标需要scale（小地图像素/屏幕像素）
- 小地图X和Y的压缩比率不同（X/Y比率差约4倍），必须分开算
- 固定scale=0.10不准，需要根据实际游戏画面校准

## 二、三种校准方式（优先级从高到低）

### 方式1：同屏三点校准（倍率按钮，当前主推）
- **创立原因**：旧的左右上端点校准，人物从地图最左走到右端点时大地图很长会跨画面（地图滚动多次），屏幕X差≠实际世界距离，Y轴同理
- **方案**：人物站左下角不动（画面不滚动，屏幕距离=实际距离），同屏标记三个点
- **三点定义**：
  - 基点（红）：人物脚的位置
  - 绿点：基点向右400px（原800，后改400）
  - 蓝点：基点向上400px（原500，因超出边界改400）
- **倍率公式**：
  - scale_x = (绿点小地图X - 基点小地图X) / 绿点屏幕X偏移
  - scale_y = (基点小地图Y - 蓝点小地图Y) / 蓝点屏幕Y偏移
- **操作流程（5步）**：
  1. 点"倍率"按钮 → 蒙板显示绿/蓝圆，拖动到特色背景位置
  2. 再点"倍率" → 人物走到绿色橙色圆上站好
  3. 再点"倍率" → 人物走到蓝色橙色圆上站好
  4. 再点"倍率" → 截图特色背景做模板，模板匹配跟踪位置
  5. 再点"倍率" → 计算倍率并关闭显示
- **模板匹配跟踪**：第二次点倍率后截图绿/蓝点位置的55x55背景模板，用cv2.matchTemplate跟踪，阈值0.80，画橙色空心圆
- **状态变量**：
  - _auto_calib_stage：0=空闲,1=拖动定特色位置,2=已记绿点待记蓝点,3=完成
  - _auto_calib_base：基点(屏幕X,屏幕Y,小地图X,小地图Y)
  - _auto_calib_green_map / _blue_map：绿/蓝点小地图坐标
  - _auto_calib_green_screen / _blue_screen：绿/蓝点屏幕坐标
  - _auto_calib_green_offset / _blue_offset：相对基点偏移(拖动时调整)
  - _auto_calib_dragging：None/'green'/'blue'
  - _calib_green_template / _blue_template：背景模板图(numpy)
  - _calib_green_match_pos / _blue_match_pos：模板匹配到的屏幕位置
- **保存**：校准完成后保存到data/route_%d_calib.json

### 方式2：左右上端点校准（旧方案，仍保留）
- **左/右端点**：校准scale_x
  - scale_x = (右小地图X - 左小地图X) / (右屏幕X - 左屏幕X)
  - 自动记录：每3秒检测，人物站最左3秒记左端点，站最右3秒记右端点
  - 手动记录：面板上"左端点""右端点"按钮，手动点一下记录
  - 手动优先：手动记录后关闭自动记录
- **上端点**：校准scale_y
  - scale_y = (左端点小地图Y - 上端点小地图Y) / (左端点屏幕Y - 上端点屏幕Y)
- **数据格式**：(屏幕X, 屏幕Y, 小地图X, 小地图Y)，兼容旧格式(屏幕X,小地图X,小地图Y)
- **手动记录直接覆盖（100%权重）**

### 方式3：自动滑动平均校准（运行时微调）
- _update_scale_calibration()：每帧人物位置更新后调用
- 原理：记录上一帧人物屏幕/小地图坐标，当前帧计算变化量Δ屏幕和Δ小地图
- 实际scale = Δ小地图 / Δ屏幕（变化量足够大时才更新，避免噪声）
  - X轴：屏幕X变化>20px且小地图X变化>1px时才校准
  - Y轴：屏幕Y变化>15px且小地图Y变化>1px时才独立校准
- 滑动平均：新值占20%，旧值占80%，防止突变
- 人物上下移动时Y独立微调，但校准值和scale_x差距>30%视为噪声忽略

### 方式4：初始值（兜底）
- _recalc_scale_from_region()：根据小地图区域尺寸计算初始值
  - scale_x = FIXED_W(340) / 小地图宽度
  - scale_y = MAP_H(250) / 小地图高度
- 未校准时默认scale=0.10

## 三、关键变量
- _calibrated_scale_x / _calibrated_scale_y：校准后的X/Y倍率
- _map_screen_scale：主scale（=scale_x，兼容旧代码）
- _player_map_pos：人物小地图坐标
- _player_screen_pos：人物屏幕坐标

## 四、出现过的问题
- **问题**：scale_y不准，怪物Y坐标偏差大
  - **原因**：默认scale_y=scale_x，但小地图X/Y压缩比率不同
  - **解决**：X/Y独立scale，上端点或同屏三点校准Y
- **问题**：人物走到地图右端点时scale_x不准
  - **原因**：地图滚动多次，屏幕X差≠实际世界距离
  - **解决**：改用同屏三点校准（人物站左下角不动，画面不滚动）
- **问题**：校准值突然跳变
  - **原因**：静止时噪声导致误校准
  - **解决**：变化量阈值过滤（X>20px，Y>15px）+ 滑动平均（新值20%）
- **问题**：小地图上的红色基点位置不对，应该和人物黄色光点重合
  - **原因**：_auto_calib_base记录的小地图坐标和小地图绘制坐标系不一致
  - **状态**：待修复，确保基点用人物黄色光点的真实位置（find_player_dot返回的坐标）

---

# 动作结果反馈检测方案（2026-08-23 设计，待实现）

## 一、为什么需要结果反馈
- 之前的问题：发了按键但游戏没响应（窗口未激活、UIPI拦截、游戏卡顿），脚本继续执行下一步导致逻辑混乱
- 截图保存后不知道是否成功，模板匹配时才发现模板为空
- 移动/跳跃/爬梯后不知道是否成功，卡住了也继续走

## 二、需要结果反馈的动作（建议保留）

| 动作 | 检测方式 | 成功标准 | 超时 |
|------|----------|----------|------|
| 向右/左移动 | 检测人物屏幕X坐标变化 | X变化>10像素 | 500ms |
| 跳跃 | 检测人物屏幕Y坐标先升后降 | Y先减小>20再增大 | 800ms |
| 上梯子 | 检测人物屏幕Y坐标持续减小 | Y减小>30像素 | 1500ms |
| 下平台/下落 | 检测人物屏幕Y坐标增大 | Y增大>30像素 | 1000ms |
| 截图保存模板 | 检测文件是否生成+尺寸正确 | 文件存在且55x55 | 200ms |
| 窗口绑定 | 检测hwnd是否有效 | hwnd不为0且IsWindow | 500ms |
| 小地图光点检测 | 检测find_player_dot返回非None | 返回(x,y) | 300ms |
| 按键发送到游戏 | 检测游戏窗口是否在前台+按键状态 | 前台+GetAsyncKeyState | 100ms |
| YOLO怪物检测 | 检测返回列表非空 | len>0 | 500ms |

## 三、不需要结果反馈的动作（建议去掉，用户可思考）

| 动作 | 为什么不需要 |
|------|-------------|
| UI绘制（每帧） | 本身就在循环，反馈无意义，且会严重卡顿 |
| 日志输出 | 本身就是反馈 |
| 配置加载（启动时一次） | 失败会直接报错，不需要额外检测 |
| 蒙板窗口重绘 | 每帧都在做，检测无意义 |
| 倍率显示更新 | 纯UI，不影响逻辑 |
| 平台/梯子绘制 | 纯UI，数据已存在 |

## 四、优化方案（必须遵守，避免卡顿和崩溃）

### 4.1 异步非阻塞检测
- **绝对不能在主循环中用while等待检测结果**（之前崩溃就是因为阻塞等待）
- 用"发起动作→记录预期→下一帧或几帧后检查"的模式
- 用状态机：_action_state = {action: 'move_right', expected: 'x_increase', start_time: ..., retries: 0}
- 主循环每帧检查一次状态，超时或成功就清除状态，不阻塞

### 4.2 检测频率控制
- 不是每帧都检测，动作发起后等N帧再检测（给游戏响应时间）

## 文档索引（所有记忆文件和说明文档）

| 文件名 | 位置 | 说明 |
|--------|------|------|
| 脚本运行闪退问题排查与解决方案.md | 项目根目录 | 记录脚本运行闪退、黑屏、权限检测自动重启等问题的排查步骤和解决方案 |
| 小地图倍率编写说明.md | 项目根目录 | 【当前最完整】小地图倍率(总值=检测值+偏移并锁定)的编写说明：详细步骤、关键点、踩过的坑。修改倍率功能先看它 |
| 小地图光点锁定人物坐标方案.md | 项目根目录 | 【新方案·不用倍率·超详细】小地图光点(ffff88)+三特征定位(map_area_rect)+归一化比例(0~1)锁定人物真实屏幕坐标, 并反用画怪。双蒙板(1:1计算/2倍纯显示)、动态蒙板、尺度不变、任何地图一套。含原理/公式/代码/踩坑/验证 |
| 小地图倍率的成功方案说明书.md | 项目根目录 | 小地图倍率旧版方案：人物光点检测改进、映射比率修复、三点校准 |
| 功能实现指南与排错手册.md | 项目根目录 | 热键跑马灯、蒙板三点校准、小地图显示、倍率显示、打包流程、调试运行等功能指南 |
| 弹窗组件实现规范与做法.md | 项目根目录 | 所有弹窗组件的统一实现规范：灰底白字、右上角X关闭、可拖拽、最上层、输入框黑底白字、自动丢失焦点5秒等 |
| 平台录制功能说明书.md | 项目根目录 | 平台录制完整流程（截图→光点检测→收集点→画红线→抽稀保存）、关键参数、踩过的坑（质心/每帧检测/1px覆盖/2px抽稀） |
| 输入框时灵时不灵修复说明书.md | 项目根目录 | 输入框GetAsyncKeyState只查最高位导致漏键的根因分析与修复方案（同时查0x8000+0x0001），涉及_poll_num_input和_poll_key_capture |
| 人物特征偏移与蒙板黄点显示说明书.md | 项目根目录 | char_x_offset/char_y_offset偏移校准+蒙板黄点不运行时也显示的实现，含踩坑（改偏移反馈搞坏人物特征、char_pos在if running块里导致不显示） |
| 人物绿框与三点检测框说明书.md | 项目根目录 | 人物定位绿框(150x190)+三点镜头检测框的独立绘制实现，含大坑（self.app vs self引用错误导致AttributeError被静默吞掉绿框不显示）、lock_screen_from_dot返回None临时用屏幕中心兜底 |
| 主循环卡死第0帧修复说明书.md | 项目根目录 | _capture_map()在map_area_rect为None时抛TypeError+主循环except continue静默吞异常导致frame_count不增长的根因分析与修复方案，含踩坑（异常静默吞掉是万恶之源、改动时保留原有保护、frame_count增长位置） |
| 【冒险岛世界】强绑窗口尺寸与小地图三模板定位说明书.md | 项目根目录 | 【冒险岛世界·当前版】全窗口固定1276×749(不区分客户区,改尺寸只动GAME_W/H,绑定即拉回+禁改大小+每30帧纠偏)；小地图三模板(mini map标题55×14/右上X按钮16×20/底边123×11)唯一定位,阈值0.55,偏移定稿LEFT-6/TOP24/BOTTOM-8/右+3,最终map区200×150@(14,123)；已删描线法(匹配不上本帧跳过不硬算)；遮挡抓屏会误定位的坑与排查 |
| PROJECT_STATE.md | 项目根目录 | 项目总纲，开发准则、永久强制规则、文档索引 |
- 移动：等3帧（约50ms）后检测X变化
- 跳跃：等5帧后检测Y变化
- 爬梯：等10帧后检测Y变化
- 截图：等1帧后检测文件

### 4.3 超时和重试机制
- 每个动作有最大超时时间（见上表）
- 超时后重试N次（建议2次），重试仍失败则记录日志并跳过
- 重试次数用完后不卡死，继续下一步逻辑（标记该动作失败）

### 4.4 检测数据来源优化
- 人物位置用模板匹配的缓存值（_player_screen_pos），不要每次检测都重新匹配
- 截图保存检测用文件存在性检查（os.path.exists），不要重新读取文件
- 窗口状态用缓存的hwnd，不要每次都重新枚举窗口

### 4.5 失败处理
- 检测失败只记录日志（_add_log），不抛出异常
- 连续失败N次后暂停脚本，提示用户检查
- 绝对不能因为检测失败就崩溃（try-except包裹所有检测逻辑）

## 五、可能的不好影响（必须注意）

### 5.1 卡顿风险
- **风险**：每帧都做检测会增加主循环负担，导致帧率下降
- **缓解**：异步检测+频率控制+缓存数据，关键路径不做重计算
- **监控**：加帧率统计，低于30fps时自动降低检测频率

### 5.2 误判风险
- **风险**：X/Y变化不一定是动作导致的（怪物碰撞、其他玩家、地图自动滚动）
- **缓解**：检测变化方向（向右移动只检测X增大，不检测减小），变化量阈值（>10像素才算）
- **兜底**：误判后重试机制，连续误判才标记失败

### 5.3 崩溃风险（之前发生过）
- **风险**：检测逻辑访问None对象、数组越界、文件IO异常
- **缓解**：所有检测逻辑用try-except包裹，访问前检查None，数组访问检查边界
- **历史教训**：之前map_area_rect为None时直接访问导致崩溃，必须加None判断

### 5.4 延迟增加
- **风险**：每个动作都等反馈会增加动作间延迟，脚本整体变慢
- **缓解**：异步检测不等待，动作发起后继续执行，检测结果用于修正后续动作
- **权衡**：关键动作（爬梯、跳跃）可以等反馈，普通移动不等

### 5.5 代码复杂度增加
- **风险**：状态机+重试+超时逻辑增加代码量，维护难度增加
- **缓解**：封装成通用的ActionVerifier类，统一处理发起、检测、超时、重试
- **文档**：每个检测点加注释说明检测什么、成功标准、超时时间

## 六、实现优先级建议
1. **第一优先级**：截图保存结果反馈（最简单，风险最低，当前就有问题）
2. **第二优先级**：移动X变化检测（核心功能，误判风险低）
3. **第三优先级**：跳跃/爬梯Y变化检测（复杂，误判风险高，建议先做移动再做这个）
4. **第四优先级**：窗口绑定/光点检测反馈（已有部分逻辑，完善即可）
5. **暂不实现**：YOLO检测反馈（本身就有失败处理，额外检测意义不大）

## 七、用户决策点（请思考）
1. 跳跃检测：跳跃时Y先升后降，检测"升"还是"降"还是"都要"？只检测升可能误判（被怪打飞），只检测降可能漏判（跳上去没下来）
2. 爬梯检测：爬梯时Y持续减小，但人物可能在梯子上停留，超时时间设多少合适？
3. 连续失败处理：连续失败N次后是暂停脚本还是跳过该动作继续？暂停更安全但影响挂机，跳过可能导致角色卡死
4. 帧率监控：低于多少fps时降低检测频率？降低到什么程度？

### 攻击结果反馈（已有部分实现，需完善）
- **已有**：_detect_monster_hp_bars 检测怪物血条，_detect_damage_number 检测伤害数字，_combat_target_alive = target_has_hp or target_has_dmg
- **缺失**：攻击后如果连续N帧没有血条也没有伤害数字，说明怪不在当前位置（死了/跑了/误检），应该放弃锁定重新检测
- **建议实现**：攻击后启动计数器，连续5帧（约250ms）无血条无伤害数字 → 清除锁定 _combat_locked_target = None，下一帧重新选目标
- **注意**：不能太敏感（攻击后血条可能短暂消失），建议连续5帧无反馈才放弃
- **代码位置**：_combat_tick 第6201行后加逻辑

## 常用UI素材清单（data目录下，绝对不能随便换）

### 标签页背景
- ui_bg_blank.png - 路线标签页背景（123380字节）
- ui_tab_fight.png - 战斗标签页背景（128568字节）
- ui_tab_potion.png - 吃药标签页背景（140597字节）
- ui_tab_chat.png - 聊天标签页背景（156402字节）
- ui_tab_lie.png - 测谎标签页背景（144079字节）

### 按钮图标
- ui_platform.png - 平台录制按钮
- ui_ladder.png - 梯子录制按钮
- ui_save.png - 保存按钮
- ui_plan.png - 方案按钮
- ui_platform_clear.png - 清除平台按钮
- ui_ladder_clear.png - 清除梯子按钮
- ui_mode.png - 模式切换按钮
- ui_plan_clear.png - 清除方案按钮
- ui_run.png - 运行按钮
- ui_stop.png - 停止按钮
- ui_refresh.png - 刷新按钮
- ui_manual.png - 手动按钮
- ui_char_btn.png - 人物特征按钮

### 校准相关
- ui_calib_auto.png - 自动校准
- ui_calib_left.png - 左端点
- ui_calib_right.png - 右端点
- ui_calib_top.png - 上端点

### 其他
- ui_log_bg.png - 日志背景
- ui_winbind_bg.png - 窗口绑定背景
- ui_bound_dropdown.png - 绑定下拉菜单
- ui_crosshair.png - 十字准星
- ui_monster_data.png - 怪物数据
- ui_offset_label.png - 偏移标签
- ui_plan_toolbar.png - 方案工具栏

### 注意
- ui_fight_latest.png 和 ui_fight_new.png 是备用版本，当前未使用
- 所有素材路径：data/文件名
- 加载方式：resource_path(os.path.join("data", 文件名))
- 用户没说要换的素材，绝对不能随便换！

## 核心组件实现规范

### 一、MD文件编写规范
- 标题层级：`#` 一级、`##` 二级、`###` 三级，不跳级
- 表格：`| 列1 | 列2 |`，表头下加 `|---|---|`
- 列表：`-` 无序、`1.` 有序
- 代码块：```python ... ```
- 每个功能文档必须包含：功能说明、实现步骤、关键参数、踩过的坑、验证方法
- 写完后在 PROJECT_STATE.md 文档索引登记（文件名+位置+说明）

### 二、输入框完整作法（解决时灵时不灵）
- **字段定义**：`(x, y, w, h, type, id)`，type=`"key"`按键录入 / `"num"`数字录入
- **key型**：点击后捕获下一个按键，支持所有虚拟键码（VK_TO_NAME映射）
- **num型**：支持 `0-9`、小数点`.`、负号`-`、退格、回车确认
  - 负数：首字符可为`-`，后接数字和小数点
  - 小数点：只能有一个，小数点后可接数字
- **聚焦机制**：`_focused_field`记录当前聚焦框，聚焦时橙色边框（INPUT_FOCUS_COLOR）
- **失焦保存**：点击别处/切标签页时自动调用`_save_input_config()`
- **配置文件**：`fight_potion_config.json`，保存到`app_dir()/data/`
- **时灵时不灵根因**：①点击区域太小（需扩大）②失焦时没调用保存 ③`cv2.waitKey`返回值处理不对（需`& 0xFF`）

### 三、窗口制作方法和注意事项
- **UI控制面板**：`cv2.namedWindow` + `cv2.imshow` + `cv2.setMouseCallback(_on_mouse)`
  - 固定尺寸：UI_W=461, UI_H=900
  - 鼠标回调：`_on_mouse(event, x, y, flags, param)`处理LBUTTONDOWN/LBUTTONUP/MOUSEMOVE
  - 键盘：`cv2.waitKey(1) & 0xFF`处理按键和数字录入
- **pygame准星窗口**：`pygame.display.set_mode(NOFRAME)` + `SetWindowPos`置顶 + `SetLayeredWindowAttributes`白色透明
- **注意事项**：
  - cv2窗口不能在子线程创建（必须主线程）
  - pygame准星销毁时只`ShowWindow(SW_HIDE)`隐藏，不调用`pygame.display.quit()`（会导致后续调用报错）
  - 窗口置顶会影响其他窗口操作

### 四、背景UI加载方法和失效原因
- **load_png函数**：`cv2.imdecode(np.fromfile(path, dtype=np.uint8), cv2.IMREAD_UNCHANGED)`
- **为什么不用cv2.imread**：不支持中文路径和打包后的`_MEIPASS`特殊路径
- **4通道处理**：load_png返回4通道BGRA，绘制UI背景时必须`cv2.cvtColor(img, cv2.COLOR_BGRA2BGR)`转3通道，否则broadcast错误
- **失效原因**：①cv2.imread中文路径返回None ②4通道图赋值给3通道frame导致broadcast错误被try-except吞掉 ③打包后data目录没进去
- **失败兜底**：`np.ones((UI_H, UI_W, 3), dtype=np.uint8) * 200`（灰色填充）

### 五、人物特征保存方法（10张全链路，2026-09-01修复）

- **保存路径**：`CHAR_TEMPLATE_DIR = app_dir()/data/char_templates/`，文件名`char_<id>.png`
- **最多10张**：CHAR_MAX_TEMPLATES=10，保存最新10张，超10删最旧
- **ID分配**：new_id = max(existing_ids)+1递增，不覆盖旧文件
- **加载方法**：`_load_char_templates()`扫描char_*.png按ID排序取最新10张，优先从app_dir（用户保存的）加载，为空才从_MEIPASS（打包内置默认）加载
- **中文路径兼容**：所有模板读写用np.fromfile+cv2.imdecode（读）和cv2.imencode+tofile（写），不用cv2.imread/imwrite

**全链路shape解包bug修复（29号基线就存在，3通道图时全部触发）：**
- 所有shape解包必须用`shape[:2]`（兼容3通道图shape=(h,w,3)）：frame.shape[:2]、captured.shape[:2]、timg.shape[:2]、thumb.shape[:2]
- tuple不能直接和int比较，必须用`shape[0]/shape[1]`：if roi.shape[0] > 20 and roi.shape[1] > 20
- F4存方案取最后一张：`tpl = self._char_templates[-1]`存base64（兼容旧方案文件），不能把整个列表当字典用
- 切换方案不覆盖人物特征：人物特征是全局数据不随方案切，`_switch_route`里不能从方案base64读1张覆盖内存

**蒙板黄点显示：**
- char_pos不运行时也更新到蒙板数据（从if self._running块移到外面），确保偏移反馈闪烁和人物位置点不运行时也可见
- 偏移输入完成3秒后，`_show_offset_feedback()`设置blink_until=now+5000，蒙板绘制时黄点闪烁5秒
- 蒙板绘制从`data.get("char_pos")`取人物位置，画黄色实心圆（0x00FFFF）半径6，橙边框（0x0080FF）

**历史丢失根因（已解决）：**打包后保存到app_dir/data/char_templates/，但旧代码从sys._MEIPASS/data/char_templates/（只读临时目录）加载，保存的模板下次启动加载不到。解决：加载时优先app_dir，_MEIPASS只作兜底。

### 六、路径规范（resource_path vs app_dir）
- **resource_path(relative_path)**：打包内置**只读**资源（UI图片、模板图片），打包后返回`sys._MEIPASS + relative_path`
- **app_dir()**：用户**可写**数据目录（配置、保存的特征、日志、json），打包后返回exe所在目录
- **os.chdir(app_dir())**：程序启动时切换到app_dir，确保相对路径正确
- **中文路径统一**：所有文件读写用`np.fromfile+cv2.imdecode`（读）和`cv2.imencode+tofile`（写），不用`cv2.imread/imwrite`

### 七、截图/窗口捕获方法
- **捕获方式**：`_capture_window()`用mss或win32 BitBlt截取游戏窗口客户区
- **窗口丢失处理**：截图前检查`self.hwnd`是否有效，无效则跳过本帧不崩溃
- **最小化/遮挡**：BitBlt方式窗口被遮挡可能截到黑屏，需用PrintWindow或mss的窗口捕获
- **截图频率**：不要每帧都全图截图，小地图区域用ROI局部截取减少开销
- **失败兜底**：截图失败返回None，调用方必须判断None再使用，禁止直接访问shape

### 八、全局钩子（热键+鼠标）使用规范
- **GlobalHotkeyListener**：低级键盘钩子，必须在**主线程**安装（`install()`），每帧调用`pump()`处理消息
- **GlobalMouseListener**：低级鼠标钩子，同样主线程安装+每帧pump
- **钩子回调**：回调里只做事件入队（`self.events.put()`），不做重计算，避免钩子超时被系统卸载
- **卸载时机**：程序退出时调用`uninstall()`，避免钩子残留
- **常见坑**：子线程安装钩子→收不到事件；忘记每帧pump→事件堆积不响应；回调里做重操作→系统自动卸载钩子

### 九、模板匹配使用规范
- **函数**：`cv2.matchTemplate(img, tpl, cv2.TM_CCOEFF_NORMED)`，返回值范围-1~1，越接近1越匹配
- **阈值**：一般0.55~0.8，太低误检多，太高漏检；小地图文字用0.55，人物特征用0.70
- **ROI优化**：目标位置固定时先裁ROI再匹配，减少计算量；连续失败时才全图扫描更新ROI
- **多匹配过滤**：`cv2.minMaxLoc`只取最佳匹配；如需多目标，用阈值过滤+非极大值抑制（NMS），不能直接拿所有结果
- **模板加载**：用`load_png`或`cv2.imread`，加载后检查None，模板尺寸必须小于搜索图
- **边界保护**：匹配坐标+模板尺寸不能超出图像范围，ROI扩展时做边界clamp

### 十、配置文件读写规范
- **目录创建**：`os.makedirs(dir_path, exist_ok=True)`，exist_ok=True避免已存在时报错
- **编码**：所有json读写用`encoding="utf-8"`，避免中文乱码
- **写入**：`json.dump(data, f, indent=2)`，indent=2方便人读
- **读取**：用try-except包裹，文件不存在或格式错误时返回默认值，不崩溃
- **可写路径**：所有配置文件放`app_dir()/data/`下，**不能**放`resource_path()`（只读）
- **原子写入**：重要配置先写临时文件再rename，避免写一半崩溃导致文件损坏
- **常见坑**：打包后写`_MEIPASS`目录（只读临时目录）→下次启动丢失；中文路径用cv2.imwrite失败→用imencode+tofile


---

# 2026-08-23 晚间进展（人物定位方案 + Y轴漂移）

## 已完成
- **小地图光点检测修复**：去掉 `MORPH_OPEN(2x2)` 腐蚀（会吞掉 2~4px 小光点），`find_player_dot` 改为"取黄色区域最亮像素质心"作中心点。光点稳定，三点校准三个点能正常上点。
- **小地图加载优化**：`__init__` 先 `_load_region()` 加载已保存区域，失败才 `_detect_minimap()`。
- **倍率（三点校准）已走通**：scale_x/scale_y 已算出可用；怪物紫点直接用它映射。

## 发现的问题
1. **三层地图第三层怪 Y 漂**：人物站底层，第三层怪小地图 Y 漂在空中（第二层基本准）。根因：scale_y 不准（误差×高度偏移，越高层越漂）。
2. **人物换装 → 特征匹配/YOLO 都难稳定定位人物**：唯一稳的是小地图光点（认黄点，不看外观）。
3. 无法破解游戏拿世界尺寸：Unity IL2CPP + Addressables + 反作弊，不做逆向。

## 方案（已讨论，待实施）
人物定位分两层：
- **主定位**：小地图光点 `_player_map_pos`（人物大地图位置、哪层、走位）→ 稳，不受换装影响。
- **怪映射锚点**：`_player_screen_pos`（特征匹配）→ 辅助，失效时降级（紫点暂缺/用旧值，不崩、不影响走位打怪）。

## 明天待办（按顺序）
1. **怪映射锚点降级**：`_screen_to_map` / `_get_monster_map_pos_verified` 开头加 `if not _player_screen_pos or not _player_map_pos: return None`（紫点暂缺不崩）。
2. **打怪决策去依赖屏幕位置**：`_combat_tick` 里 has_target 等，用 `_player_map_pos` 兜底。
3. 人物位置判断确认以 `_player_map_pos`（光点）为准。

## 备用
- `test_scroll.bat` / `temp/test_scroll.py`：水平滚动量检测原型（phaseCorrelate 逐帧累计 dx；用于测世界宽度/倍率，可选）。
- 手动调 scale_y：改 `data/route_1_calib.json` 的 `calibrated_scale_y`（该文件当前不存在，需新建；方向=调小让高层怪往下）。

---

# 2026-08-31 v73 防打包灰屏（窗口未响应）

## 当前版本
- 最新打包：v73（dist/MapleBot_v73.zip，onedir，console=True，79.3MB），状态=待用户真机验证不灰屏。
- git：改动前快照 4993c90，完成提交 6a79456；spec=MapleBot_v73.spec（upx=False）。
- 详细排查见《开发工作记录.md》【记录003】。

## 本次灰屏根因（推测，置信度较高）与修复
- 根因=启动竞态：run() namedWindow 建窗后，要跑完第一帧全部检测才到第一次 waitKey 泵消息；打包冷启动(PyInstaller解包+Defender扫描DLL)拖过约5秒，窗口被Windows判未响应置灰。源码运行不复现。
- 修复1（代码4行，不碰业务）：run() namedWindow 后、while 前，先 cv2.imshow(win,self._ui_bg)+cv2.waitKey(1)，窗口先渲染泵消息。
- 修复2：spec upx=False。
- 修复3：打包后把 _internal/data 的10个json复制到 exe同级 data，首次运行直接读到已校准region(128x116)，不走_detect_minimap自动检测。
- 修复4（系统，已做）：maple_bot 目录加入 Windows Defender 排除项（Add-MpPreference -ExclusionPath，需管理员）。

## 关键机制（永久记住，排查打包问题必读）
- 两套data路径：只读资源走 resource_path()=_internal/data（UI图/模板）；可写配置走 app_dir()=exe同级data（minimap_region/plans/fight_potion/yolo/blue_box 等json，见源码245-259行）。**打包只把json放进_internal/data，必须额外复制一份到exe同级data，否则新版首运行读不到配置、走自动检测，窗口最小化(GetWindowRect=-32000)时会检测成错误的158x9。**
- 防灰屏首帧：OpenCV窗口 namedWindow 后必须尽快 imshow+waitKey 泵一次消息，中间不要夹耗时初始化。
- 历史灰屏老根因（本次已核查未复发，仍要每次打包前查）：map_area_rect 必须在__init__最开头初始化、后部不得重置为None；UI图用load_png且4通道必须转3通道(_ui_bg恒3通道)。
- 模拟frozen环境时 app_dir() 取 sys.executable 的目录，源码运行需手动把 sys.executable 指到目标exe路径才能正确模拟exe同级data。
- Defender系统自带、默认开启，用户说没开杀毒也要 Get-MpComputerStatus 核实；冷启动扫描DLL是打包版偶发卡顿/未响应的环境因素。

## 待办
- 等用户验证 v73 真机不再灰屏、小地图直接正常(无需F9)；验证通过后把本区块状态改为已解决。
- F3绿色人物框偏移面板（v72已做，X/Y偏移默认39/78，属性_char_box_offset_x/y）随v73一起待测。


---

# 2026-08-31 v75 人物特征偏移修复 + 偏移偏上待解决

## 当前版本
- 最新打包：v75（dist/MapleBot_v75.zip，onedir，console=True，79.3MB）
- git：代码 b8bb4c2，spec 2286357；回滚点 cf2fb98（v74）
- 详细记录见《开发工作记录.md》【记录004】。

## v75 已修复
- 人物特征偏移(char_x_offset/char_y_offset)失效：原只在模板匹配回退路径生效，光点锁定优先时被跳过；改为函数开头统一读取偏移，所有返回路径(光点锁定/模板匹配/宽限期)统一加偏移。
- 只改了_get_player_screen_pos一个函数，_last_char_match_pos仍存真实位置供ROI回退。

## 待解决（用户要求先分析不动代码）
- 偏移点偏上头：v75后偏移生效，但蒙板绿色框/人物位置点跑到人物头上方，不在点位上。
- 根因分析（未实锤）：①两套偏移叠加——UI的char_x_offset/y(20/20)+蒙板F3的_char_box_offset_x/y(39/78)=总偏移59/98，过大；②偏移基准不匹配——20/20原针对模板匹配中心点，现作用于光点锁定，基准不同；③光点锁定坐标基准不明确。
- 可选方案：A统一偏移去一套；B光点锁定+模板匹配融合；C多模板匹配；D明确基准重新校准。待用户确认后实施。

## 关键机制（永久记住）
- 两套独立偏移：UI的char_x_offset/y(_field_values，X偏移/Y偏移输入框)作用于_get_player_screen_pos返回值；蒙板F3的_char_box_offset_x/y作用于蒙板绿色框绘制。两者叠加，修改偏移相关功能时必须同时检查两套。
- _last_char_match_pos存未加偏移的真实位置，供_match_character的ROI回退搜索用；偏移只在返回给调用者时加。

---

# 2026-09-04 v56 打怪分层搜索+跨层决策（待验证）

## 【冒险岛世界】当前版本（唯一主线 · 2026-09-06，旧版/怀旧服说法与此冲突的以此为准）
- **对象**：MapleStory Worlds（冒险岛世界，进程 msw，窗口标题含 "MapleStory"）；怀旧服仅作历史参考，不再新增其记录
- **运行**：直接跑 .py（`E:\conda\envs\yolo26\python.exe maple_route_ui.py`，工作目录=maple_bot，会 UAC 提权），本阶段不打包
- **全窗口固定 1276×749**（GAME_W/H，不区分客户区，绑定即强制、禁手动改大小、每30帧纠偏）
- **小地图三模板唯一定位**（阈值0.55，描线法已删），1276×749 下 map 区 200×150@(14,123)，详见《【冒险岛世界】强绑窗口尺寸与小地图三模板定位说明书.md》
- **最新 git**：`5a7f7d9`（强绑窗口+重录三模板+删描线法+偏移定稿）；前序 `5692c13`（A1怪物蒙板特征点元组、A2群攻计数修复，战斗单测36/36）
- **怀旧服时期 MD 全量备份**：`backups/冒险岛怀旧服MD文件备份_20260906_225843.zip`（24个MD，可回滚）
- 打包备查：yolo26（Python3.9.25+PyInstaller6.22.2）；源码备份 backups/maplebot_源码配置备份_2026-09-03_2327.zip

## 本次内容（详见开发工作记录.md【记录020】）
1. 人物ROI中心bug已由用户修复（99c7c94，特征点位置做中心）→ 人物匹配 600→125ms/s
2. 怪物运行时ROI 700×300（用户，88108af）→ 15ms/次；不运行时全图每2帧1次
3. P2运行时去重：_combat_tick复用主循环截图+人物结果（省~50ms/帧）
4. 打怪分层：350近距随机序 → 500同平台一边随机 → 跨层（最近有怪选中平台/全图最近怪，复用_move_to攀爬+Y验证，爬梯失败2秒冷却清怪再上）
5. 删除废弃 _is_monster_in_manual_platform

## 已知待验证/风险
- 跨层决策是全新增行为：无怪时不再"原地等"，会主动去最近有怪平台（选台模式）/全图哪里怪打哪里（全图模式）
- 爬梯失败2秒冷却期间战斗自动清边上怪；行进中遇近怪自动取消跨层优先打怪
- 若出现平台间来回空跑：加"平台冷却/驻留时间"（后续优化）

## 打包检查清单（下次打包必看）
- 用 yolo26 环境打包（cmd /c "E:\conda\envs\yolo26\python.exe -m PyInstaller MapleBot.spec --noconfirm --clean --distpath dist_vXX --workpath build_vXX"）
- ⚠️【血泪教训2026-09-04】**打包后处理严禁整体覆盖 exe 同级 data**：曾用旧版本dist data -Force 覆盖，把用户刚录的平台/梯子方案全部冲掉。正确做法：只补缺失的 json/onnx（逐文件检查存在性），**route_*.json / 用户运行时数据永不覆盖**。推荐流程：用户录完方案后，先备份 data 目录到 backups/ 再打包
- 打包后：复制 _internal/data 的11个json + models/best.onnx 到 exe同级data（仅缺失时）；best.onnx 放 exe根目录

## 2026-09-04 v58 当前状态（截至本次会话 22点）
- **dist_v58**（dist_v58/MapleBot/MapleBot.exe）为最新，data 已从 backup 恢复
- **当前 git HEAD = 17e021a 基础（方案导出/导入重构完整 + 打怪=原始稳定版 a3b00aa）** + 今天下午补的几处方案小修复：
  - `1c8971b` 导出勾选列表不出来修复（_export_plan_dialog 占位Frame吃空间→删除）
  - `d4c8d99` 导入流程：点"导入方案"→弹"选择导入方式"小窗→点"按文件夹/单文件"→小窗立即隐藏+弹系统选择框（不再挡文件）；脚本主窗口导入全程可见（已撤销误隐藏）
  - `6e435e5` 导入方案自动按占用编号命名（占5号→名"方案5"）
- **打怪稳定版**：= a3b00aa 基线（打怪分层跨层/走台子/爬梯/拟人 都在）。**今天下午堆叠的那批打怪改动（人物定位防跑偏/锚点/连续性/本台子/找梯子Y匹配/单群攻/斜坡门槛 等）已全部回滚**（改动过多互相影响导致乱跑/空打/不动，回滚到稳定基线，后续按"一次只加一条、逐一验证"）
- 【重要·数据实证】今天反复出现的"空打/不动/乱跑/套不准"，日志显示 **人物位置置信度 0.00**（人物特征没匹配上→bot 不知道人物在哪→一切基于人物位置的判定失效）。它可能是根因（用户有异议，但日志[人物匹配]连续0.00是事实）。**打怪的地基 = 人物特征点要录准（[人物匹配]置信度0.9+）**，否则改打怪逻辑无效
- 打包环境：Doubao python（C:\Users\wenwen\AppData\Local\Doubao\...\python.exe，PyInstaller 6.22.2）；cmd /c 包裹；打包前备份 exe 同级 data
- 数据教训：route文件=用户录制，**打包/导入严禁覆盖**；**打包前必须备份 dist_v58\MapleBot\data 到 backups\**（PyInstaller --clean 重建会整目录删除）；打包后从备份恢复

## 【冒险岛世界】下一步（2026-09-06 起，一次只改一条、真机验证再下一条）
1. 按《打怪逻缉.txt》单平台 9 条规范逐条核实现装 combat 逻辑（就近打怪 / 技能范围X(面板设)与Y / 正打不切换 / 550px外追怪与换台 / 平台Y差150 / 斜坡边走边跳 / 空怪假怪拉黑3秒与10帧静止放弃 / 方向150ms / 绿线波动跳跃后置）。**【2026-09-07已完成】Y范围面板化(上-60/下+30)、追怪远距瞬移(X/Y距离弹窗,默认空,X或Y>0才瞬移)**，详见第22号说明书；待真机验证瞬移手感(850ms节流/同层Y差≤40阈值)。
2. 「动作录制」按钮目前仅占位(170,288,135×39)，功能后续再做。
3. 排查**自动加血失效**（_check_auto_potion / _detect_hp_mp_bars / gray_bar.png / fight_potion_config.json；先分清是按键没发出，还是血条位置在1276×749下错位）
4. 遮挡抓屏误定位兜底：给 _save_region 加区域合法性校验（负高度/过小/越界一律丢弃不保存）
5. 蒙板在 1276×749 下的等比例坐标复核（血条等比例锚点）


---

## 【冒险岛世界】文档记录方针（2026-09-06 起执行）
- 从本日起，所有记忆文件 / PROJECT_STATE / 功能说明书**只记录冒险岛世界（MapleStory Worlds，进程 msw）**内容；冒险岛怀旧服仅作历史参考，不再新增其记录。
- 旧文档里与冒险岛世界现状不一致的内容**就地替换更新**，不并列保留互相冲突的旧说法。
- 改动前的全部 MD 已全量备份到 `backups/冒险岛怀旧服MD文件备份_20260906_225843.zip`（同名文件夹另存一份），需要怀旧服资料从这里回滚/查阅。
- 每做完一个功能仍按铁律写专项说明书、并在本文件两处文档索引登记，文档标题统一带【冒险岛世界】。
