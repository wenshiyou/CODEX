# 外部成熟方案学习笔记：小地图导航 / 爬梯 / 对位控制

> 用途：重做"小地图光点 + 录制梯"上梯/下梯链路前的理论底稿。只学思路与工程范式，**不照抄代码**（游戏不同：MapleStory Worlds vs 老版 MapleStory，分辨率/机制有差异）。
> 采集时间：2026-09-21。所有结论区分【源码/资料所见】与【对本项目的建议·待拍板】。

---

## 0. 一句话结论

成熟横版游戏 bot 的共同范式高度一致（auto-maple、WAND、Surfacer 三源互证）：
**视觉低频"看位置" → 在静态"平台/绳梯图"上一次性规划路线 → 执行层用带死区的接近 + 固定时长原子动作去走，动作中不逐帧纠偏，只在动作终点判一次成败。**
我们当前失败的根因是把"贴脸起跳对位"也放到 202px 缩略图上、用 ±1px 死区且每帧重算反向，违背了上面每一条。

---

## 1. 资料来源与相关度

| 来源 | 是什么 | 相关度 | 链接 |
|---|---|---|---|
| tanjeffreyz/auto-maple | 707★ 冒险岛 bot，OpenCV+小地图+模拟按键，Python | ★★★★★ 最直接对标 | https://github.com/tanjeffreyz/auto-maple |
| auto-maple command_books（adele/kanna/shadower） | 职业"命令书"，真实走/跳/抓绳原子动作 | ★★★★★ 执行层铁证 | resources 仓库 command_books/*.py |
| auto-maple-plus（DeepWiki 架构文档） | auto-maple 增强版架构说明 | ★★★★ | https://deepwiki.com/kyranops/auto-maple-plus |
| WAND / WAND_EXT | 读内存派，平台图+Dijkstra+移动原语+轨迹投影 | ★★★★ 架构/移动分类 | https://spikemogo.github.io/WAND_EXT/usage/ |
| Surfacer（Godot 平台 AI） | 预解析"平台图"，边=可行移动轨迹，运行时 A* | ★★★★ 平台图理论 | github SnoringCatGames/surfacer |
| arXiv 1804.09153 | 关卡→图，节点=平台，边=按角色物理算出的可行跳跃 | ★★★★ 跳跃可达性 | https://arxiv.org/pdf/1804.09153 |
| Levi devlog《building platformer AI》 | 起跳/落点选择、跳跃轨迹=垂直步+水平步 | ★★★ 起跳运动学 | https://devlog.levi.dev/2021/09/building-platformer-ai-part-3.html |
| SilentByte《跳一跳机器人》 | 视觉算距离→t=d·f+c 换算按压时长，开环执行 | ★★★★★ 低帧率开环对位 | https://silentbyte.com/i-built-a-robot-to-beat-a-friend-at-a-mobile-minigame |
| 视觉伺服低帧率/延迟（UPC、Stanford、MindVision、arifkh） | 低采样率必须低增益；总延迟<16ms 才能紧闭环；轨迹生成与视觉解耦 | ★★★★ 解释我们为何抖 | 见正文引用 |
| 死区/迟滞/PID（toptechboy、SunFounder、AI-Ming、team31000、colorbot） | deadband、switch hysteresis、Bang-Bang vs PID、距离+速度双判 | ★★★★★ 治来回晃 | 见正文引用 |
| gameidea《Climb Ropes and Walls》 | 抓绳=身体碰撞激活 active rope，body_exited 释放（布尔状态信号） | ★★★ 抓握确认信号 | https://gameidea.org/2024/10/15/making-the-player-climb-ropes-and-walls/ |
| Latenode《AI jump gaps》 | 跳跃是"计划动作"不是恐慌反应：PrepareJump 判 gap 宽/高，不可达不跳 | ★★★★ 治乱跳/呆住 | https://community.latenode.com/t/how-to-make-ai-enemies-jump-gaps-in-2d-platformer-game/38236 |

---

## 2. 先把概念钉死：小地图 / 光点 / 两套坐标（本项目真实数值）

- **游戏窗口系（主画面 1280×800）**：YOLO 怪、人名/脸/后脑勺、按键作用的人物。像素量级几百。
- **小地图系（缩略俯视图，本项目裁剪块 202×152，独立 mss、约 50fps）**：
  - **光点** = find_player_dot 在小图上找亮黄箭头、取最大团中心，输出 `_player_map_pos`，范围 0~202 / 0~152，代表"人在整张地图缩略图上的位置"。
  - **录制梯 self.ladders**（route_004_ladders.json，9 条）：x=23~156、y_top=62~87、y_bottom=72~106，同属小地图块像素坐标。
  - 202px 宽对应游戏世界**很多个屏幕**，不是一个屏幕；1 小地图像素 ≈ 世界约 10 个游戏像素（代码 scale=0.10 是兜底猜测，未实测标定）。
- **auto-maple 的坐标尺度【源码所见】**：内部用归一化 0~1 小地图相对坐标，绘制时再换算像素。其对位死区 move_tolerance=**0.1（小地图宽 10%）**、微调 adjust_tolerance=**0.01（1%）**。
- **关键推论**：缩略图适合回答"去哪个梯、到没到层"，**不适合指挥"在哪个像素起跳"**。auto-maple 真正的走路/起跳对位发生在主画面动作层，小地图只判"是否进入容差带"。

---

## 3. 行业标准架构（三源互证）

### 3.1 三层分离，一个动作只有一个"司机"
1. **感知层（低频视觉）**：截图/检测玩家点、怪、绳梯、平台，只产出坐标与目标。
2. **规划层（地图图 + 图搜索）**：
   - 地图预录成图：**节点 = 平台立足点(foothold) / 绳梯上的点；边 = 两点间可行的移动**。
   - 边按**类型**区分：Walk 沿平台走 / Jump 平台间抛物线 / Fall 下落 / **Rope·Ladder 垂直爬** / Teleport / Portal（WAND、Surfacer 完全一致）。
   - 边是否存在，按角色速度上限、跳跃物理参数**实时计算可行性**（arXiv、Levi、Surfacer）。
   - 运行时 A*/Dijkstra **一次性**算出节点路径，执行途中**不重新选目标**。
3. **执行层（高频/开环原子动作）**：沿规划好的节点，逐段走 + 跳 + 爬；每段是阻塞式动作原语。

【源码所见 auto-maple】
- `Layout.shortest_path`：四叉树存历史立足点，A* 给路径点序列。
- `Move.main`：先 `shortest_path(player,target)` 拿 path，再逐点走；同时算 `local_error`(到当前点) 与 `global_error`(到终点)，两者都 > move_tolerance 才继续；水平/垂直用 toggle 交替；节点间 sleep（水平 0.15s / 垂直 0.05s）；离开容差带才松键。
- `_new_direction`：按新方向键前，若上一个方向不同**先 key_up 旧键**（一次只按一个方向，杜绝左右互搏）。

### 3.2 跳跃/爬梯是"有可行性判定的计划动作"，不是恐慌反应
- Latenode：进 `PrepareJump` 状态，先量 gap 宽 / 平台高，对照角色移动参数判可不可行；**不可达就不跳、换路线**，既不"永远尝试不可能的跳"，也"不在边缘呆住"。
- 对应我们：cross 门（Y 超跳高上限且 X<300 才上梯/下跳）本质是粗糙可达性判断；成熟做法是按跳跃物理算"这一跳够不够得到"，而不是拍固定像素。

---

## 4. 对位控制理论对比（核心：怎么不左右晃、能停准）

### 4.1 控制律：Bang-Bang → 比例/减速接近 → PID
| 控制律 | 做法 | 结果 |
|---|---|---|
| Bang-Bang（全速→到点停） | 按住方向直到看见到点才松 | 低帧率/有延迟时必然**冲过头→回拉→振荡**（我们现状） |
| 比例/减速接近 | 误差大走得快、误差小走得短（点动） | 平滑、能停准，工程简单（auto-maple Adjust 思路） |
| PID | P 比例 + I 消静差 + D 制动防 overshoot | 最稳但需调参；游戏按键是离散"按/松"，一般用"比例档+死区"即可 |

- 舵机跟踪实测建议（SunFounder/toptechboy）：**抖动(jerky)=死区太小，把 DEADZONE 加到 60~80；overshoot=步长太大，把步长 1→0.5；低帧率=降分辨率**。三条正好对应我们"死区 ±1px 太小、单次移动步长太大、人物识别约 3fps"。

### 4.2 死区 deadband（到了就停，不回摆）
- 目标中心设安全区，误差在区内**完全不动**，漂出才触发控制（toptechboy 40px 安全区）。
- **死区要同时看距离和速度**：距离<死区 且 目标速度低才停（colorbot），避免还在滑行就提前停 / 停了又被噪声带偏。
- auto-maple 数值：粗走死区 10%、微调死区 1%，且用 `move_tolerance/√2`（≈45°分量阈值）过滤微小误差，分量不够不按键。

### 4.3 迟滞 hysteresis / sticky（防止在两个目标间横跳）
- 瞄准辅助工程：**目标切换迟滞 + 稳定身份(stick to same target)**，避免在两个检测间反复跳（fgilde AI-Ming）。
- 对应我们：梯子/怪一旦锁定，整条动作期间不允许每帧重选（治"红白双框、红黄框乱跳、每帧换梯"）。

### 4.4 低帧率/高延迟下：闭环走不通，必须"低频视觉 + 开环走固定时长 + 再检测"
- 视觉伺服结论：视觉采样率比运动控制低一个数量级时，**必须用低增益，否则振荡**，且闭环收敛慢、抗扰差（UPC）。
- 紧闭环要求总延迟（采集→传输→处理→控制）<16ms，响应型也要<50ms；每 1ms 传感延迟都直接加到响应延迟（MindVision）。我们人物识别几百 ms、约 3fps，**物理上不能做逐帧紧闭环对位**。
- Stanford 在线轨迹生成：图像处理不必实时，**底层按预定轨迹/固定时长执行，视觉低频给目标点，传感器失败时有确定性安全动作**。
- **开环范式（SilentByte 跳一跳，最适合我们）**：
  - 视觉低频算出误差距离 d；
  - 按压/蓄力时长 `t = d·f + c`，f=每像素毫秒（实测标定），c=链路恒定延迟补偿（实测传输延迟近似恒定，arifkh 测得约 33.7ms 与频率无关）；
  - 一次性发固定时长动作，**动作过程中不纠偏**，做完再看一帧。
  - 三线程：抓帧线程 / 检测主线程 / **独立的动作执行定时线程**。
  - 教训：f、c、offset 强依赖标定，设备/分辨率一变要重标 → **这些系数必须放面板可调**（正合用户要的起跳偏移/提前停面板值）。
- 这正是用户说的"提前停/惯性补偿"的理论根据：松键后人物会继续滑行，提前量 ≈ 当前速度 × 停止延迟(c)。

### 4.5 信号质量：平滑 / 瞬丢保持 / 噪声地板 / 尺度漂移自检
- **平滑**：EMA（便宜）或 1€ 滤波（静止时重、快动时轻，只平滑位置不做超前预测）。
- **瞬丢滑行 coast**：目标被挡/瞬丢时按最后已知位置保持约 0.5s 再判丢，避免 stop-start 抖动（Arduino 人脸跟踪）。对应我们人名瞬丢用黑框/光点兜底，不立刻切坐标。
- **噪声地板 noise floor**：最大轮廓面积小于阈值就当噪声整帧忽略，防止跟踪框跳到边缘杂点（toptechboy）。
- **尺度漂移自检**：跟踪窗尺寸超过初始约 2 倍 = 锁到背景，重置/换算法（CamShift）。对应我们锁到背景假梯/假怪要能自检丢弃。

---

## 5. 执行层原子动作（auto-maple shadower 命令书实测）

【源码所见，文件名 command_books/shadower.py】
- **职责严格分离**：`Move` 主循环负责按住/松开方向键走容差带；`step()` 注释明确"**不应按任何方向键，方向键由 Auto Maple 管**"，step 只负责跳/位移技能那一下。
- **step(direction,target)**：水平闪跳 num_presses=2、上下=1；down 时 press(JUMP,3)，up 时 press(JUMP,1)，再接位移技能。
- **Adjust（宽死区后的微调）**：水平 `key_down(方向)→sleep(0.05)→key_up`，即 **50ms 极短点动后重新看误差**；垂直用小跳。**微调只在粗走进入宽死区后才做，不是一上来就点动。**
- **FlashJump（带速跑跳）**：`key_down(方向)→sleep(0.1)→press(跳)→补跳→key_up(方向)→sleep(0.5)`。即**先按方向 100ms 带出水平速度再跳，跳完固定等 0.5s 落地/动作物理完成**，中途不判。
- **通用按键时序**：按下 down_time≈0.1s、松开 up_time≈0.05s、动作前后各 sleep 0.05s；拟人随机延迟 0.1~0.3s。
- **Fall（下跳，components.py）**：记起点 → 按住 down 0.05s → 循环 press(space) 最多 6 次 → 直到玩家点位移超过 distance 判成功 → 松 down。**用"动作前基准 vs 动作后位移"判成败，有限重试，不是一次失败就换方案。**
- **抓握确认**：游戏引擎侧抓绳是身体碰撞激活 active rope、离开 body_exited 释放的布尔信号（gameidea）。我们的**后脑勺连续帧检测就是视觉版 active rope**——抓没抓到、到没到顶用这个布尔信号，不用坐标差猜。

**原子动作模板（提炼）**：
`进入状态(锁死目标) → 松干净所有无关键 → 按住方向带速 → 跳/抓(固定时序) → 动作中持续按键不判 → 终点用姿态/位移判一次 → 成功进下一状态 / 失败有限重试 N 次 → 仍失败放弃并换路线（不发呆）`。

---

## 6. 工程范式清单
- 检测 / 决策 / 执行**分线程**；视觉事件驱动，不每帧重决策（pixeler Detect→React 事件总线范式）。
- matchTemplate/检测前先**裁剪 ROI 限制搜索区**提速；边缘检测前 medianBlur 去噪 + Canny；涂黑边缘做"图像死区"、涂掉玩家自身框避免干扰（SilentByte）。
- 每个移动原语有**明确返回码**（WAND：Found/NotFound/Float 空中/NoTarget/Error），调用方按码分支，不靠模糊状态。
- 所有阈值/时序/标定系数（死区、点动时长、f/c 提前量、跑跳距离、按键 down/up 时间、重试次数/超时）**集中常量化、面板可调**。
- 拟人随机抖动只用于防检测，不影响逻辑判定。

---

## 7. 理论对比与对本项目的取舍建议（建议·待用户拍板，未改代码）

| 议题 | 方案 A | 方案 B | 对我们的倾向 |
|---|---|---|---|
| 路线/选梯 | 每帧贪心重选 | 静态平台/绳梯图一次规划、动作中锁死 | **B**（治每帧换梯/红白双框）；我们已有录制梯+平台绿线，可先做轻量"目标梯一次锁定"，完整 A* 可后补 |
| 水平对位尺度 | 在 202px 缩略图追 ±1px | 小地图判层/选梯，**最后对位回游戏窗口尺度**（人名 X + 录制时标定的屏幕起跳 X） | **B**（真机已证 A 在 X差=1 仍抓不到） |
| 接近控制 | Bang-Bang 按住走到点 | 宽死区粗走 → 50ms 点动微调，距离+速度双判，进带即停不回摆 | **B**；死区/点动时长面板可调 |
| 起跳方式 | 绝对对准后原地竖直跳 | **带水平速度跑跳为主**（按向~100ms 再跳），直跳仅失败后有限微调 | **B**（横版抓绳靠蹭，不靠绝对竖直） |
| 低帧率对位 | 逐帧闭环按/松 | 视觉低频算误差→t=d·f+c 开环走固定时长→再检测 | **B**（人物识别约 3fps，紧闭环必振荡） |
| 抓梯/到顶判定 | 坐标差猜 | 后脑勺布尔信号（连续帧抓、消失判顶）+ 位移/光点兜底 | **B**（已具备后脑勺，继续用） |
| 成败判定 | 单帧失败就换方案 | 动作前基准 vs 动作后结果，一次校验，有限重试（下跳最多 N 次） | **B** |
| 目标瞬丢 | 立即切坐标/重选 | coast 保持短窗（人名丢→黑框/光点兜底） | **B** |
| 假目标 | 检测到就锁 | 噪声地板（面积阈值）+ 尺度漂移自检（框异常放大丢弃） | **B** |
| 线程主权 | 多处都能按方向/跳 | 导航管方向键、动作原语管跳/上键，一次一键、换向先松旧、动作后固定后摇 | **B**（治抢键/拉扯） |
| 不可达 | 硬跳/呆住 | PrepareJump 可行性判定，不可达换路线，不发呆 | **B**（cross 门按跳跃物理/面板跳高值判，不写死像素） |

### 与已定规则的衔接（不冲突项）
- 锁怪唯一规则（Y 近优先再 X 近；血条/伤害判活）、B 线程锁怪主线执行、cross 门按面板跳高值、下跳两套时序、后脑勺判抓/到顶、日志"先看最后三分钟"、改前快照/先汇报后改——这些保持不变，新理论只用于重构**上梯/下梯的对位与动作状态机**。

### 仍需在动手前用真机/数据确认的点
1. 小地图↔游戏窗口的换算系数（f、c、scale）实测标定，而非沿用 0.10 猜测；录制梯时能否顺带记录"该梯起跳点对应的人名屏幕 X"。
2. 人物识别实际帧率/端到端延迟分布（决定开环步长档位与 c）。
3. 跑跳带速按方向时长、起跳后按↑窗口、点动时长，在 MapleStory Worlds 的真机数值（参考 auto-maple 100ms/50ms/0.5s 起步，真机微调）。
4. 同一梯在小地图的光点/蓝线中心定义是否一致（固定 offset 标定）。

---

## 8. 本次确认的当前版病根（证据，供重做时逐条消除）
1. 在 202px 缩略图用 ±1px（≈0.5%，严于成熟方案微调死区 1% 的一半）做起跳对位，尺度错配；真机 X差=1 原地跳后脑仍 0.36。
2. mm_goto 每帧重选梯、重算方向、d 变号即反按左右，违背"动作前定基准/动作中不判"镜头滚动铁律 → 原地左右晃。
3. 废掉了真机验证过的带速跑跳/伺服直跳，改成原地竖直跳，抓梯物理方式错误。
4. 直跳日志误印"跑跳"（_ladder_run_jumped 直跳未清）等状态标志管理问题。
5. 多状态/线程对方向键、跳键主权不清，存在抢键/松键不干净（需按第 5、6 节统一）。
